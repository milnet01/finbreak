# FIBR-0307 (FP02) — journal

## 2026-08-25 — close attempted, BLOCKED

All thirteen review findings were fixed, pushed and gate-green, and the
decided D5 note was written into `keywrap.py`. The close then ran
`check-code` and `review-code` over the fix-pass's own changes
(`2689463..HEAD`, ten source files).

`check-code` came back clean: `ruff`, `bandit`, `mypy`, `vulture`,
`semgrep`, `gitleaks` and `typos` produced no finding attributable to
FP02. The shell, workflow and YAML tools analysed zero files — no such
file was in the changed set.

`review-code` ran four lanes and did not. **Nine of its findings are
defects FP02 itself introduced**, folded with twelve pre-existing ones
into **FP03 (FIBR-0310)**. The phase stays 🚧, and FIBR-0019 stays 🚧
behind it.

**The one worth carrying forward.** FP02's clipboard fix does not work:
the auto-clear guard is parented to the dialog, so its timer dies when the
dialog is torn down, which both callers do the moment the user answers.
Measured on 2026-08-25 rather than argued. **Its test passed because the
test called `clear_if_ours()` directly instead of letting the timer
fire** — a vacuous leg. That is the second vacuous test this pass
produced, after finding 8's stated repro turned out not to hold, and the
pattern is the same both times: the test exercised the mechanism the fix
names rather than the outcome the user gets.

**One lane finding was weakened on inspection and is filed as
unverified.** The CRITICAL — that `_fsync` on an `O_RDONLY` descriptor
aborts every migration on Windows — cited `backup.py`'s portability note
as corroboration. That note is about a **directory** fsync, which is a
different case, so the evidence does not support the claim. The Windows
test box has no Python installed, so it could not be measured there.
FP03's first task is to measure it.

**False positives confirmed:** none. Nothing was added to
`docs/audit-allowlist.md` this close.

**Deferred to existing items rather than re-filed:** FIBR-0309 already
covers the `.tmp` `O_EXCL` gap, the missing directory `fsync` after
`os.replace`, and `validate_params`' unbounded cost parameters — all three
re-found independently by this sweep. FIBR-0308 already covers INV-11's
wording, of which "a separator other than a hyphen defeats the scan" is a
second instance.

**No CHANGELOG entry, deliberately.** `/close-phase` step 5a-2 asks for
one; this project's standing decision is that a fix-pass which has not
happened yet claims a fix the user does not have. The same reasoning that
kept FP02 out of `[Unreleased]` applies to FP03.
