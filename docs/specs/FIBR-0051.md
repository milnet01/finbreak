# FIBR-0051 — P07.5: app-shell UX redesign (window chrome + first-run wizard)

> **Status:** ✍️ **DRAFT — `/cold-eyes` pending** (global rule § 14: this spec is
> run through `/cold-eyes` to zero verified findings **before** any code). Not yet
> cleared for code.
> **Phase:** P07.5 — App shell & first-run wizard. **Kind:** implement.
> **Source:** user-request-2026-07-05 (roadmap FIBR-0051; the app-shell / dashboard
> UX redesign the user queued after FIBR-0050, brainstormed + design-approved
> 2026-07-05).
> **Security: LOW-but-real.** This phase writes **no** new persisted data, adds
> **no** schema change, and adds **no** pip dependency — but it **reshapes the
> unlock/lock routing**, so it must preserve the FIBR-0004 security spine intact:
> the derived key is still wiped on quit / lock / auto-lock (FIBR-0004 INV-3), and
> **no transaction data is ever shown while the vault is locked** (this spec's
> INV-3). It also adds a **Donate** menu that opens external URLs in the OS
> browser — a **user-initiated hand-off**, not an app network call; the app itself
> still opens **no socket** (security-model **INV-8** no-network holds; this spec's
> INV-8).
> **Roadmap:** [ROADMAP.md → FIBR-0051](../../ROADMAP.md).
> **Governed by:** `docs/standards/{coding,naming,testing,commits}.md`
> (`naming.md` governs the new public widget names — `HomeView`,
> `ManualEntryDialog`, `FirstRunDialog`, `UnlockDialog`; `coding.md § 5.2` governs
> the `tr()` / layout-manager / `QLocale` i18n obligations every new widget
> inherits); [ADR-0002](../decisions/0002-pyside6-over-pyqt6.md) (PySide6 over
> PyQt6 — the LGPL Qt-for-Python desktop stack; the chrome this phase adds is the
> Qt-idiomatic `QMainWindow`).
> Builds directly on [FIBR-0004](FIBR-0004.md) (the security spine + routing this
> phase reshapes) and reuses, **unchanged**, the FIBR-0005/0006/0007/0008/0009
> content screens (`AccountsWidget`, `CategoriesWidget`, `ImportWizardWidget`).

**Theme:** every phase to date grew the app **inside a bare full-screen
panel-swapper** (`AppShell` = a `QStackedWidget` that replaces the whole window
with whichever screen is current — first-run form, unlock box, main screen,
accounts, categories, import). There is **no window chrome**: no menubar, no
toolbar, no status bar, and the first thing a new user meets is a tiny
password box. This phase turns that into a **real desktop app window** — a
`QMainWindow` with a menubar, an icon toolbar of shortcuts, a status bar that
narrates activity, and a central content area — and moves first-run and unlock
into **popup dialogs shown over that window**, so the app *looks like an app*
from the first launch. It is the **frame** the remaining feature phases (P08
rules, P09 transfers, P10 dashboard) drop into: this ships **first**, on purpose,
so those features are built against the finished shell with no rewiring
(build-order decision, 2026-07-05). **No new persisted data, no schema change, no
new pip dependency** — this is a UI reshape that reuses every existing screen and
service.

**Contents:** At a glance (invariants) · Context · Design decisions (D1–D10) ·
Invariants (INV-1..INV-10) · Deliverables · Data model (none) · Out of scope ·
Exit criteria · Dependencies · Verified API basis · Cold-eyes loop log.

## At a glance — invariants

| INV | Guarantee | Upholds |
|-----|-----------|---------|
| INV-1 | The top-level window is a **`QMainWindow`** (replacing the `AppShell` `QStackedWidget`) with **four chrome parts**: a **menubar** (`File` · `View` · `Help` · `Donate`), a **toolbar** (`Manual entry` · `Import` · `Accounts` · `Categories` · `Lock`, each an icon-above-label `QAction`, `Qt.ToolButtonStyle.ToolButtonTextUnderIcon`), a **central content stack** (`QStackedWidget` set as `setCentralWidget`), and a **status bar** (`QStatusBar`). A `qtbot` test asserts all four exist and the menu/toolbar action sets are present | ADR-0002 (Qt desktop); design.md UI layer; coding.md § 5.2 |
| INV-2 | **Startup routing follows FIBR-0004 INV-5's `presence_state()`**, which returns the string **`"first_run"`** (neither vault file present) or **`"unlock"`** (both present — the locked/returning state), or raises `VaultStateError` (mixed pair). **Two states drive the chrome**, both *window-first-then-popup*: **`"first_run"`** → window shows, chrome **disabled**, a *Welcome* placeholder in the content area, and a **`FirstRunDialog`** opens over it (create password + currency); on success the vault is created, chrome **enables**, content → **Home**. **`"unlock"`** → window shows, chrome **disabled**, content → a **🔒 Locked** placeholder, an **`UnlockDialog`** opens; on success chrome enables, content → Home. A **mixed pair** (`VaultStateError`) instead aborts in `run()` with the existing `QMessageBox.critical` + non-zero exit **before the window exists** — it drives no chrome | FIBR-0004 INV-5 (presence routing); vault.py `presence_state` |
| INV-3 | **(Security.)** **No transaction data is shown while the vault is locked.** The `HomeView` transaction table is **not populated** (and the data-bearing content pages are not shown) in the first_run or locked states — only the Welcome / 🔒 Locked placeholder is. `HomeView.refresh()` (which reads the vault) runs **only after** an unlock/first-run success. A `qtbot` test asserts the content area holds the placeholder (not a populated table) while locked | FIBR-0004 (locked = nothing visible); security-model A5/T4 (no plaintext exposure) |
| INV-4 | **Lock returns to the locked shell, window intact.** Manual **Lock** (`File → Lock` / toolbar `Lock`) **and** idle **auto-lock** (`AuthService.on_auto_lock`, still wired by the shell) both: close the vault + wipe the key (via `AuthService.lock()`, unchanged), swap the content back to the **🔒 Locked** placeholder, **disable** the chrome, and re-open the `UnlockDialog` — **without** destroying the window (unlike today's whole-window swap to a bare `UnlockWidget`). Re-unlocking returns to Home | FIBR-0004 INV-3 (auto-lock wipes key); this spec's INV-2 |
| INV-5 | **The FIBR-0004 key lifetime is untouched.** `app.aboutToQuit` is still connected to `AuthService.on_about_to_quit` (key wiped on exit, FIBR-0004 INV-3); `AuthService` (`state` / `complete_first_run` / `complete_unlock` / `lock` / `on_auto_lock` / `on_about_to_quit` / `vault`) is called **unchanged** — this phase adds **no** service or crypto change. A `VaultStateError` at startup still exits cleanly | FIBR-0004 INV-3; services/auth.py |
| INV-6 | **Toolbar/menu actions route to content, `done` returns Home.** `Accounts` / `Categories` / `Import` show the **existing** `AccountsWidget` / `CategoriesWidget` / `ImportWizardWidget` as pages in the content stack (reused **unchanged**), and each widget's existing **`done`** signal returns the content stack to **Home** (re-`refresh`ing it). `Manual entry` opens the modal `ManualEntryDialog`; `View → Home` shows Home. An auto-lock while any content page is showing swaps it away to the Locked placeholder (INV-4) | FIBR-0005/0006/0007 (`done`-signal content widgets); design.md |
| INV-7 | **The status bar narrates activity.** Transient `QStatusBar.showMessage(msg, timeout)` messages report actions (*"Importing statement…"* / *"Added transaction"* / *"Vault locked"* / *"Unlocked"*), auto-clearing back to a resting *"Ready"*; a **permanent** right-side widget (`addPermanentWidget`) shows the transaction count (*"142 transactions"*), refreshed whenever Home refreshes. All strings via `tr()` | coding.md § 5.2 (i18n); user request 2026-07-05 |
| INV-8 | **The Donate menu hands the funding pages off to the OS browser via `QDesktopServices.openUrl` — the app fetches nothing itself.** A **`Donate`** menu (after `Help`) with three items, each calling `QDesktopServices.openUrl(QUrl(u))` for the **exact `.github/FUNDING.yml` URL** `u` (module-level constants): **GitHub Sponsors** `https://github.com/sponsors/milnet01`, **Patreon** `https://www.patreon.com/AntsProjectsHub`, **PayBru** `https://paybru.co.za/tip/ants-projects-hub`. **Testable guarantee:** each item calls `openUrl` **once** with the right `QUrl` (monkeypatched — no real browser launches) and performs no in-app fetch. This is the app's only outbound hand-off — it has no networking code, so security-model INV-8 (no-network) holds: `openUrl` delegates to the OS browser (user-initiated), it is not an app connection | security-model INV-8 (no-network); `.github/FUNDING.yml` |
| INV-9 | **Manual-entry round-trip (`qtbot`).** Opening `ManualEntryDialog`, picking an account + entering a valid amount/description + **Add** inserts exactly one transaction via the **existing** `TransactionService.add_transaction` (unchanged) and, on the dialog's success, Home's table **and** the status-bar count refresh; **Cancel** inserts nothing. An empty-picker / invalid-amount attempt shows the in-dialog error and inserts nothing (the FIBR-0004 `parse_transaction` contract, reused) | FIBR-0004 INV-4 (add one transaction); services/transactions.py |
| INV-10 | **Every new widget is translation- and RTL-ready.** **Testable:** every new user-facing string (menus, toolbar labels, status messages, dialog labels, Home placeholder) is passed as a **string literal to `tr()`** (greppable — no bare display literal), every new widget sits in a **Qt layout manager** (no fixed x/y — greppable), and amounts still render via `QLocale` (`_format_amount`, moved to `HomeView`, unchanged). **Design-intent (inherited, not re-tested here):** because layouts mirror from `layoutDirection`, the shell renders correctly RTL with no per-widget rework; the bundled locales + a runtime language switcher are FIBR-0017 | coding.md § 5.2 (tr / layout-managers / QLocale / RTL); FIBR-0004 (same posture) |

## Context

The app is layered **UI → Services → Repositories → SQLCipher** (design.md). Today
the **entire UI** is `app.py`'s **`AppShell(QStackedWidget)`**: it holds one
full-screen `QWidget` at a time and swaps it (`_swap` removes + deletes the old,
adds + shows the new). Its routing:

- **`AppShell.__init__`** reads `service.state()` — `"first_run"` → `FirstRunWidget`,
  else → `UnlockWidget` — and wires `service.on_auto_lock = self._show_unlock`.
- **`FirstRunWidget`** (`completed` signal) and **`UnlockWidget`** (`unlocked` /
  `unlock_failed` signals) are **full-screen `QWidget`s**; on success the shell
  swaps in **`MainWindow`**.
- **`MainWindow(QWidget)`** *is* the main screen: a manual-entry `QFormLayout`
  (Account / Date / Amount / Description) + a row of **plain `QPushButton`s**
  (`Add`, `Manage accounts…`, `Manage categories…`, `Import transactions…`,
  `Lock`) + an error `QLabel` + the transaction `QTableWidget`. It emits `locked`
  / `manage_accounts` / `manage_categories` / `import_transactions`; the shell
  swaps in `AccountsWidget` / `CategoriesWidget` / `ImportWizardWidget` (each a
  `QWidget` with a **`done`** signal that returns to a fresh `MainWindow`).
- **`run()`** builds the `QApplication`, wires `app.aboutToQuit →
  service.on_about_to_quit`, constructs `AppShell` (catching `VaultStateError` →
  `QMessageBox.critical` + `return 1`), and `shell.show()`.

This phase **reuses, unchanged**, the machinery already shipped:

- **`AuthService`** — `state()` (`→ presence_state()`: returns `"first_run"` /
  `"unlock"` — the token for the both-files-present locked state — or raises
  `VaultStateError`), `complete_first_run` / `complete_unlock` (called by
  the dialogs' existing worker flow), `lock()`, `on_auto_lock`,
  `on_about_to_quit`, `vault`. **No change.**
- **`FirstRunWidget` / `UnlockWidget` internals** — the two-field password form +
  currency picker (first-run), the masked field (unlock), the background
  **`DeriveWorker`** KDF flow, the `validate_first_run` / `complete_unlock`
  wiring, the error messages. This phase **re-homes** these into `QDialog`
  subclasses (D2) but keeps their derivation logic verbatim.
- **`AccountsWidget` / `CategoriesWidget` / `ImportWizardWidget`** — the content
  screens, each with a **`done`** signal. Reused as content-stack pages **with no
  change to the widgets themselves** (D4).
- **`TransactionService`** (`add_transaction` / `list_transactions` /
  `base_currency`) and **`_format_amount`** — the manual-entry + table logic
  lifted out of `MainWindow` into `ManualEntryDialog` + `HomeView` (D3), calling
  the same service.
- **`PasswordDialog`** (`ui/password_dialog.py`) — the **template** for the new
  dialogs' shape (`QDialog` + `QDialogButtonBox` + accessor methods + `exec()` →
  `Accepted`/`Rejected`).

So the genuinely new code is: the `QMainWindow` shell + its state machine
(`ui/main_window.py`, rewritten), a `HomeView` (`ui/home.py`), a
`ManualEntryDialog` (`ui/manual_entry.py`), the `QWidget → QDialog` re-home of
first-run/unlock, a small bundled SVG icon set + loader, and the Donate wiring.

## Design decisions baked into this spec

These are the non-obvious calls; `/cold-eyes` gates them before code. **D5
carries the load-bearing security decision** (locked-shell data hygiene); the
rest are UI-structure/idiom calls.

- **D1 — `QMainWindow` is the top-level; the content stack is its central widget.**
  Replace `AppShell(QStackedWidget)` with a `QMainWindow` subclass whose
  `setCentralWidget` is a `QStackedWidget` content area. `QMainWindow` is Qt's
  purpose-built app-window (native menubar/toolbar/status-bar docking regions);
  bolting chrome onto a bare `QStackedWidget` would reinvent it (rejected — more
  code, less idiomatic; coding.md § 1.1). `run()` builds the `QMainWindow`
  directly and keeps the `aboutToQuit` + `VaultStateError` handling verbatim
  (INV-5). The window title stays `"finbreak"`.
- **D2 — First-run & unlock become application-modal `QDialog`s shown
  *non-blocking* over the window.** The approved UX is *window first, popup over
  it* — for **both** first-run and the returning-user unlock. So
  `FirstRunWidget(QWidget)` → **`FirstRunDialog(QDialog)`** and
  `UnlockWidget(QWidget)` → **`UnlockDialog(QDialog)`**. The shell shows each with
  **`setModal(True)` + `show()`** — application-modal *appearance* (input to the
  main window is blocked) **without** a nested event loop — **not** `exec()`. This
  is deliberate: the internal **`DeriveWorker`** (a background `QThread`) keeps
  running on the **normal** event loop, so its queued `done`/`failed` signals are
  delivered exactly as today and there is **no** nested-loop / worker-teardown race
  (an `exec()` nested loop plus a transient dialog dropped right after `accept()`
  risks "QThread destroyed while thread is still running"; `show()` avoids it). The
  dialogs therefore **keep their existing signals** — `FirstRunDialog.completed`,
  `UnlockDialog.unlocked` / `unlock_failed` — and the shell connects to them
  (`completed` / `unlocked` → `_enter_unlocked`: closes the dialog, enables chrome,
  shows Home; `unlock_failed` → the error stays in the still-open dialog for a
  retry, as `UnlockWidget` does today). **The shell holds a reference to the live
  dialog** until success or dismissal, then closes + `deleteLater()`s it — so the
  worker always outlives its dialog. **Test seam (preserved):** because the
  dialogs stay signal-driven and non-blocking, tests drive them with
  **`qtbot.waitSignal(dialog.completed / .unlocked)`** exactly as the FIBR-0004
  vault tests drive the current `UnlockWidget` (`test_vault.py:479-486`) — no
  monkeypatch of a blocking `exec()` is needed. **Dismissal:** the dialog's
  `QDialogButtonBox` Cancel / window-close fires `rejected` — for `FirstRunDialog`
  the shell **quits** the app (no vault can be created); for `UnlockDialog` the
  shell leaves the **locked shell** on screen (chrome disabled, 🔒 Locked
  placeholder + an **Unlock** button, INV-2) so a stray Escape neither kills the
  app nor unlocks it. Rejected alt: `exec()` modal (the nested-loop +
  worker-teardown + test-seam costs above); stacked full-screen panels
  (contradicts the window-then-popup UX). The `setModal(True)` + `show()` +
  `completed`/`unlocked` wiring is the **entry trigger** too — the shell creates
  and `show()`s the dialog **after** `window.show()` (a non-blocking call, so no
  empty window flashes behind a blocking `exec()`).
- **D3 — `MainWindow` splits into the shell + `HomeView` + `ManualEntryDialog`.**
  Today's `MainWindow(QWidget)` conflates the *frame* with the *content* (the
  manual-entry form and the table). Split it: the new `MainWindow(QMainWindow)` is
  the **frame + state machine**; **`HomeView(QWidget)`** (`ui/home.py`) holds the
  getting-started panel (shown when there are zero accounts **or** zero
  transactions) **and** the transaction `QTableWidget` (shown once there is data),
  with the `_format_amount` `QLocale` helper moved verbatim beside it;
  **`ManualEntryDialog(QDialog)`** (`ui/manual_entry.py`) holds the
  Account/Date/Amount/Description form, opened by the `Manual entry` action.
  `HomeView.refresh()` re-reads via `TransactionService` (unchanged) and reports
  the row count for the status bar (INV-7). This keeps each file focused (the
  brainstorming isolation principle) and is the split the "add-transaction becomes
  a popup, content area becomes Home/dashboard" UX requires. The old
  `manage_accounts` / `manage_categories` / `import_transactions` / `locked`
  signals are replaced by direct `QAction` handlers on the shell.
- **D4 — Content screens reused unchanged as content-stack pages.**
  `AccountsWidget` / `CategoriesWidget` / `ImportWizardWidget` are already
  self-contained `QWidget`s with a `done` signal. They are added to the central
  `QStackedWidget` on demand and removed on `done` (returning to Home) — the same
  build-then-discard pattern the current `AppShell._show_accounts` etc. use, so
  auto-lock still swaps a content page away (INV-4). **No change to the three
  widgets.** (Their internal modality/behaviour — e.g. the import wizard's own
  dialogs — is out of scope for this phase.)
- **D5 — Locked-shell data hygiene: the content widget is *destroyed* on lock, not
  hidden.** (Security, INV-3.) In the `"first_run"` and `"unlock"` states the
  content area shows only a **placeholder** (`Welcome` / `🔒 Locked`); no
  `HomeView` (or other content page) exists yet, so nothing reads the vault.
  `HomeView.refresh()` (the only method that reads transactions) runs **only** on
  an unlock/first-run success and after a data-changing action. On **lock /
  auto-lock** the current content widget is **removed from the stack and
  `deleteLater()`d** (the build-then-discard pattern the old `AppShell._swap`
  already uses) and the content reverts to the 🔒 Locked placeholder — so a
  `HomeView` holding decrypted `QTableWidgetItem`s **cannot survive a lock**; the
  next unlock builds a **fresh** `HomeView`. Rejected alt: keep one persistent
  `HomeView` and merely `hide()` it while locked (or add a `clear()`) — risks a
  data widget retaining decrypted rows across a lock; destroying it is simpler and
  leak-free by construction. This preserves FIBR-0004's "nothing visible until
  unlocked" posture through the reshaped routing. (INV-4a asserts the **same
  `MainWindow`/shell instance** persists across the lock — it is the *content
  widget*, not the window, that is rebuilt.)
- **D6 — Donate = `.github/FUNDING.yml` links via `QDesktopServices`.** A `Donate`
  menu (last on the menubar) with three items → `QDesktopServices.openUrl(QUrl(u))`
  for `u` in module-level constants derived from `.github/FUNDING.yml`
  (`github: milnet01` → `https://github.com/sponsors/milnet01`;
  `patreon: AntsProjectsHub` → `https://www.patreon.com/AntsProjectsHub`;
  `custom` → `https://paybru.co.za/tip/ants-projects-hub`, verbatim). `openUrl`
  hands the URL to the OS browser — a **user-initiated** action, **not** an app
  network call (the app opens no socket; security-model INV-8 holds, INV-8). The
  URLs are **constants in the source**, not read from `FUNDING.yml` at runtime
  (no file read on the happy path; the two are kept in sync by a comment
  cross-referencing `FUNDING.yml`). Tested by monkeypatching `openUrl` and
  asserting the `QUrl` argument (no real browser launches in the test).
- **D7 — Icons: hand-authored minimal monochrome SVG glyphs, bundled as package
  data.** The toolbar actions carry small **hand-drawn SVG glyphs** (a `＋` for
  Manual entry, a down-arrow for Import, etc.) shipped in **`src/finbreak/ui/icons/`**
  and loaded via `QIcon(str(path))`, with `ToolButtonTextUnderIcon` so a **text
  label always shows** under each icon (unambiguous + translatable, INV-10 — the
  **label**, not the glyph, carries the meaning, so an imperfectly-tinted icon is
  never ambiguous). Hand-authored (not a third-party icon set) so there is **zero
  icon-library licensing** to track and no new dependency. The app currently ships
  **no custom theme** (verified — no `setStyleSheet` / `QPalette` / `setStyle` in
  `src/finbreak`; it inherits the OS palette, light **or** dark), so the glyphs use
  a **neutral mid-tone** that reads acceptably on both; **palette-adaptive icon
  re-tinting** is deferred to FIBR-0014 (P12 — the ROADMAP bullet's "dark-theme
  polish"). Rejected alts: `QIcon.fromTheme` (unreliable in a frozen cross-platform
  bundle — no icon theme on Windows/macOS); Qt `SP_*` standard pixmaps (a limited,
  mismatched set). **Bundle obligation:** the `icons/` SVGs are **package data**
  and MUST travel into the FIBR-0003 bundle (Deliverable 9); this is proven by the
  `--self-test` leg loading one bundled icon to a **non-null** `QIcon` **inside the
  frozen clean-room** (DoD #2), plus a source-tree unit test that every named icon
  loads non-null. A **branded app/window icon** is deferred (§ Out of scope —
  FIBR-0037).
- **D8 — Status bar: transient `showMessage` + one permanent count widget.** A
  helper on the shell (`_status(msg)`) calls `statusBar().showMessage(self.tr(msg),
  timeout)` for action feedback (auto-clears to `"Ready"`); a permanent
  `QLabel` added via `addPermanentWidget` holds the transaction count, updated from
  `HomeView.refresh()`'s row count (INV-7). At rest the transient area reads
  `"Ready"`. Simple + idiomatic; no activity-queue over-engineering (coding.md
  § 1.1).
- **D9 — No schema change, no new pip dependency, no new native lib.** This phase
  persists nothing new (`LATEST_SCHEMA_VERSION` **unchanged** — no migration) and
  adds no runtime dependency (the SVGs are static assets, not a package). So there
  is **no** `== N` schema-version test ripple (unlike FIBR-0007/0009) and **no new
  native-bundle surface** — the FIBR-0003 build smoke's role here is only to prove
  the **icon package-data** travels (Deliverable 9 / DoD #2), not a native stack.
  Stated explicitly so `/cold-eyes` doesn't hunt for a migration that isn't there.
- **D10 — The routing/`MainWindow` test ripple is named, not discovered late.**
  Converting `AppShell(QStackedWidget)` → `MainWindow(QMainWindow)` and
  first-run/unlock → dialogs **breaks** the FIBR-0004 routing tests that assert
  `AppShell(...).currentWidget()` `isinstance` `MainWindow` / `UnlockWidget`
  (`tests/features/vault/test_vault.py:539-551`) and the accounts/vault tests that
  construct `MainWindow(service)` as a plain `QWidget`
  (`test_vault.py:509-529`, `test_accounts.py:373-396`). These are **edited**
  (not created): the routing test now asserts the **`QMainWindow` + which
  dialog/placeholder is shown** per state; the `MainWindow(service)` constructions
  that drove the old manual-entry form are re-pointed at `ManualEntryDialog` /
  `HomeView` as appropriate. The edits are enumerated in Deliverable 10 so the
  "all existing suites green" DoD (#4) is concrete.

## Invariants (testable)

The table under *At a glance* is the canonical list (INV-1..INV-10). Sub-invariants:

- **INV-2a** (first-run happy path, `qtbot`) — from a `presence_state() ==
  "first_run"` vault dir, the shell shows with chrome disabled + Welcome
  placeholder and a `FirstRunDialog`; completing it (monkeypatched/driven
  derivation, as the FIBR-0004 vault tests already do) creates the vault, enables
  the chrome, and shows a populated-or-getting-started Home.
- **INV-2b** (unlock happy path, `qtbot`) — from an `"unlock"` (both-files-present)
  vault dir, the shell shows chrome-disabled + 🔒 Locked placeholder + an
  `UnlockDialog`; a correct
  password enables chrome + shows Home; a wrong password keeps the dialog (error
  shown), chrome still disabled (reuses the FIBR-0004 wrong-password path).
- **INV-2c** (corrupt install) — a mixed vault/sidecar pair still yields the
  `QMessageBox.critical` + non-zero exit at startup (`VaultStateError`, unchanged).
- **INV-4a** (auto-lock) — invoking the wired `on_auto_lock` callback (as the
  FIBR-0004 tests invoke the timeout handler directly) closes the vault, **removes
  + `deleteLater()`s the current content widget** (so no `HomeView` with decrypted
  rows survives — D5), disables chrome, and shows the 🔒 Locked placeholder +
  `UnlockDialog` — on **the same `MainWindow` instance** (the *window* is not
  destroyed; only its content widget is).
- **INV-6a** (content routing) — triggering the `Accounts` action shows
  `AccountsWidget` in the content stack; emitting its `done` returns to Home and
  re-refreshes; likewise Categories/Import.
- **INV-8a** (donate) — each of the three Donate actions calls a monkeypatched
  `QDesktopServices.openUrl` exactly once with a `QUrl` equal to the pinned
  constant; **no** other action triggers `openUrl`.
- **INV-9a** (Home getting-started ↔ table toggle, `qtbot`) — with **zero accounts
  or zero transactions**, `HomeView`'s **getting-started** panel is the current
  widget (not the table); after data exists (≥1 account **and** ≥1 transaction),
  the **transaction table** is the current widget. The toggle is observable via the
  view's current child, so the D3 branch is tested, not merely implied (closes the
  gap where INV-2a accepts "populated-or-getting-started" either way).

The security invariants INV-3 (no data while locked), INV-4 (lock returns to
locked shell + key wiped), INV-5 (key lifetime untouched), and INV-8 (no app
network call) each carry a dedicated `qtbot`/monkeypatch test.

## Deliverables

1. **`src/finbreak/app.py`** — `run()` builds the **`MainWindow(QMainWindow)`**
   directly (no more `AppShell`), keeps `app.aboutToQuit →
   service.on_about_to_quit` and the `VaultStateError → QMessageBox.critical →
   return 1` guard (INV-5/INV-2c), and `window.show()`. `AppShell` is **removed**
   (its routing moves into the shell).
2. **`src/finbreak/ui/main_window.py`** — rewritten as **`MainWindow(QMainWindow)`**:
   builds the menubar (`File`: Manual entry… · Import statement… · —— · Lock ·
   Quit; `View`: Home · Accounts · Categories; `Help`: About finbreak; `Donate`:
   the three funding items, D6), the toolbar (D7 icons + `ToolButtonTextUnderIcon`),
   the central `QStackedWidget`, and the status bar (D8). Owns the **state machine**
   (`_show_first_run` / `_show_unlock` / `_enter_unlocked` / `_lock` / `_show_home`
   / `_show_content(widget)`), sets `service.on_auto_lock` (INV-4), enables/disables
   chrome per state (INV-2/INV-3), and holds the `_status()` helper +
   transaction-count permanent widget (INV-7). `About` = a small `QMessageBox.about`.
3. **`src/finbreak/ui/home.py`** — **`HomeView(QWidget)`**: a getting-started panel
   (buttons: *Add an account* / *Import a statement* / *Add a transaction* — each
   emitting a signal the shell routes, or invoking the shell's actions) shown when
   there are **zero accounts or zero transactions**, and the transaction
   `QTableWidget` shown once there is data; `refresh()` reads `TransactionService`
   (unchanged) and returns/*exposes* the row count for the status bar (INV-3/INV-7).
   `_format_amount` moves here verbatim.
4. **`src/finbreak/ui/manual_entry.py`** — **`ManualEntryDialog(QDialog)`**: the
   Account/Date/Amount/Description form (lifted from `MainWindow`) + an error
   `QLabel` + OK/Cancel (`QDialogButtonBox`); on **Add** calls
   `TransactionService.add_transaction` (unchanged) and, on the reused
   `parse_transaction` `ValueError`, shows the in-dialog error and stays open
   (INV-9). Populates the account picker from `AccountService`.
5. **`src/finbreak/ui/first_run.py`** — `FirstRunWidget(QWidget)` →
   **`FirstRunDialog(QDialog)`**: same form + `DeriveWorker` flow, but success →
   `self.accept()` (the `completed` signal removed); the "no password recovery"
   warning + validation messages unchanged (D2).
6. **`src/finbreak/ui/unlock.py`** — `UnlockWidget(QWidget)` →
   **`UnlockDialog(QDialog)`**: same masked field + `DeriveWorker` flow, but
   success → `self.accept()`; a wrong password / newer-vault `SchemaVersionError`
   keeps the dialog open with the existing distinct messages (the `unlocked` /
   `unlock_failed` signals removed, D2).
7. **`src/finbreak/ui/icons/`** — hand-authored monochrome SVG glyphs
   (`manual_entry.svg`, `import.svg`, `accounts.svg`, `categories.svg`,
   `lock.svg`), plus a tiny loader (`ui/icons.py`: `icon(name) -> QIcon` resolving
   `Path(__file__).parent / "icons" / f"{name}.svg"`), so the toolbar and future
   phases share one icon source (D7).
8. **Donate URL constants** — module-level constants (in `ui/main_window.py` or a
   small `ui/donate.py`) for the three `.github/FUNDING.yml` URLs, with a comment
   cross-referencing `FUNDING.yml` (D6/INV-8).
9. **`pyproject.toml` + build scripts** — the `ui/icons/*.svg` ship as **package
   data**: add them to the wheel's package data (setuptools
   `[tool.setuptools.package-data]` / `include`) **and** to the PyInstaller freeze
   (`build-smoke.sh` / `_build-smoke-in-container.sh` gain
   `--add-data "src/finbreak/ui/icons:finbreak/ui/icons"` or `--collect-data
   finbreak`), so the icons travel into the FIBR-0003 bundle (D7/DoD #2). Loading
   must resolve relative to the package (works both from source and frozen).
   **`src/finbreak/_selftest.py`** additionally gains a leg that loads one bundled
   icon through `ui/icons.py` and asserts a **non-null `QIcon`** — so the
   Python-free clean-room run (`--self-test`) actually *proves* the icons travelled
   (DoD #2), mirroring how FIBR-0008/0009 extended the self-test to prove their
   native stacks travel.
10. **`tests/features/app_shell/{spec.md, test_app_shell.py}`** — INV-1..INV-10
    (+ INV-2a/2b/2c, INV-4a, INV-6a, INV-8a, INV-9a) via `qtbot` + monkeypatch, reusing the
    FIBR-0004 vault-fixture + driven-derivation helpers. **Plus the named test
    ripple (D10), edited not created:**
    - `tests/features/vault/test_vault.py:539-551` (the `AppShell(...)
      .currentWidget() isinstance MainWindow / UnlockWidget` routing test) → assert
      the `MainWindow(QMainWindow)` shows the right **dialog/placeholder** per
      state (`"first_run"` → `FirstRunDialog` + Welcome; `"unlock"` →
      `UnlockDialog` + 🔒 Locked; after unlock → Home).
    - `tests/features/vault/test_vault.py:465-529` — the `UnlockWidget(service)` /
      `MainWindow(service)` constructions re-pointed at `UnlockDialog` /
      `MainWindow` + `HomeView` / `ManualEntryDialog` as the assertion requires
      (the manual-entry-form assertions move to `ManualEntryDialog`).
    - `tests/features/accounts/test_accounts.py:373-396` — the `MainWindow(service)`
      constructions that exercised the account picker re-pointed at
      `ManualEntryDialog` (which now owns the picker) or `HomeView`.
    The exact edit list is finalised during TDD; every change is a **mechanical
    re-home** of an existing assertion to the split widget, no coverage lost.

## Data model

**No change.** `LATEST_SCHEMA_VERSION` is **unchanged**; no migration is added; no
table is touched (D9). This is called out explicitly because every prior feature
phase (P03–P07) shipped a migration + a schema-version test ripple — **this one
does not**.

## Out of scope (deferred to named phases)

- **The dashboard content** (summary tiles, pie/donut, spending trends, filterable
  analysis) — **FIBR-0012 (P10)**, the fourth piece of this body of work. This
  phase builds the **content area** Home lands in and leaves Home as a
  getting-started panel + the transaction table; P10 replaces Home's body with the
  charts. The content slot is designed for that swap.
- **The rules / auto-categorisation screen** — **FIBR-0010 (P08)**, the second
  piece. This phase adds no `Rules` toolbar/menu action (it joins `View` in P08).
- **Transfer-detection prompts** — **FIBR-0011 (P09)**, the third piece.
- **A branded application / window icon** — **FIBR-0037 (P13)**. This phase ships
  toolbar glyphs only; `setWindowIcon` with a branded icon is deferred.
- **A custom theme + theme-adaptive icon re-tinting** — **FIBR-0014 (P12** settings
  + theme polish**)**. The app ships no custom theme today (it inherits the OS
  palette); the glyphs use a neutral mid-tone (D7), and palette-adaptive re-tinting
  lands with the theme work.
- **True activity-reset idle detection + configurable auto-lock timeout** —
  **FIBR-0014 (P12)** (unchanged from FIBR-0004: the 10-minute timer is measured
  from unlock, not reset on activity). This phase only re-routes the existing
  auto-lock to the locked shell (INV-4).
- **Live language switching (`retranslateUi` on `QEvent.LanguageChange`)** —
  **FIBR-0017 (P12)**. New strings are `tr()`-wrapped (INV-10) but a running-app
  language switch is not added here. (`coding.md § 5.2` says build live-switching
  "from P02"; the **project-wide stance** — set at FIBR-0004, which deferred it —
  is that the switcher lands whole in FIBR-0017, so every phase ships `tr()`-ready
  strings without the `changeEvent` plumbing. This spec follows that stance, not a
  literal reading of § 5.2.)

## Exit criteria (Definition of Done)

1. `./scripts/ci-local.sh` green — ruff + format-check + bandit + pip-audit +
   gitleaks + `pytest -m "not perf"`; **mypy 0**. Any bandit hit **root-caused**,
   not suppressed (global rule § 1).
2. **FIBR-0003 build smoke re-run GREEN** (`./scripts/ci-local.sh --build` /
   `build-smoke.sh`): both the onefile and AppImage print `FINBREAK_SELFTEST_OK`
   in the Python-free clean-room. The **`--self-test` leg is extended to load one
   bundled icon** via the `ui/icons.py` loader and assert a **non-null `QIcon`**
   (Deliverable 9), so a green clean-room run genuinely **proves the `ui/icons/`
   package data travelled** — a bundle that dropped the SVGs would fail the
   sentinel (the previous "unchanged `--self-test`" wording would have proven
   nothing about icon-travel, since the base sentinel never loads an icon). No new
   native stack is added (D9); the source-tree unit test in DoD #3 additionally
   proves the icons load from the working tree.
3. Every INV (INV-1..INV-10 + INV-2a/2b/2c, INV-4a, INV-6a, INV-8a, INV-9a) has a
   passing test; `tests/features/app_shell/` added; each bundled icon loads
   non-null.
4. **All existing suites stay green** after the named routing/`MainWindow` test
   ripple (D10 / Deliverable 10) — every edit a mechanical re-home of an existing
   assertion, no coverage lost.
5. Security: INV-3 (no transaction data shown while locked), INV-4 (lock/auto-lock
   wipes the key + returns to the locked shell), and INV-8 (no app network call —
   `openUrl` is the only external hand-off, user-initiated) each have a passing
   test; `docs/audit-allowlist.md` is consulted before `/audit` + `/indie-review`.
6. ROADMAP FIBR-0051 → ✅ with a resolution note; CHANGELOG `[Unreleased]` updated;
   `docs/journal/FIBR-0051.md` written; tag `FIBR-0051-complete`.

## Dependencies

- **Roadmap:** depends on **FIBR-0004** (✅ — the security spine + routing this
  phase reshapes) and reuses the **FIBR-0005 / 0006 / 0007 / 0008 / 0009** content
  screens (all ✅). It is the **first** of the four-piece body of work
  (shell → P08 rules → P09 transfers → P10 dashboard); the later three depend on
  **it** (they build into this shell), not the reverse.
- **New runtime dependency:** **none** (D9). The SVG glyphs are static package
  assets; `QtSvg`/`QIcon` SVG rendering ships with the already-pinned
  `PySide6==6.11.1`.
- **No schema change** (D9): `LATEST_SCHEMA_VERSION` unchanged.

## Verified API basis

Empirically verified before drafting (global rule § 13):

- **PySide6 `6.11.1`** (the pinned runtime). Verified in the project venv:
  - **`QAction` is imported from `PySide6.QtGui`** (the Qt6 idiom — it moved out of
    `QtWidgets`; CLAUDE.md rule 5). `QIcon`, `QDesktopServices` also in `QtGui`;
    `QUrl` in `QtCore`.
  - `QMainWindow` has `menuBar`, `addToolBar`, `setStatusBar`, `statusBar`,
    `setCentralWidget`. `QStatusBar` has `showMessage`, `addPermanentWidget`,
    `clearMessage`. `Qt.ToolButtonStyle.ToolButtonTextUnderIcon` exists.
    `QDesktopServices.openUrl` is callable.
- **Current interfaces** (read from source, this session):
  - `AuthService.state()` → `vault.presence_state()` returns the string
    `"first_run"` (neither file) or **`"unlock"`** (both present — the
    locked/returning state), or **raises `VaultStateError`** on a mixed pair
    (`vault.py:39-49`). `on_auto_lock` (settable callback), `on_about_to_quit`,
    `lock()`, `complete_unlock(raw) -> bool`, `complete_first_run(raw, params,
    currency)`, `vault` — all as used today (`services/auth.py`).
  - `FirstRunWidget` emits **`completed`**; `UnlockWidget` emits **`unlocked`** /
    **`unlock_failed`**; both drive the derivation on a background `DeriveWorker`
    (`ui/first_run.py`, `ui/unlock.py`). Success paths call
    `complete_first_run` / `complete_unlock`.
  - `AccountsWidget` / `CategoriesWidget` / `ImportWizardWidget` each expose a
    **`done`** signal (`app.py:58-76`).
  - `MainWindow(QWidget)` today owns the manual-entry form + table + the
    `TransactionService`/`AccountService` calls + `_format_amount`
    (`ui/main_window.py`).
  - `PasswordDialog(QDialog)` is the dialog template (`exec()` → Accepted/Rejected,
    accessor methods; `ui/password_dialog.py`).
- **`.github/FUNDING.yml`** (read this session): `github: [milnet01]`,
  `patreon: AntsProjectsHub`, `custom: ["https://paybru.co.za/tip/ants-projects-hub"]`
  → the three Donate URLs (INV-8/D6).
- **Test ripple sites** (grepped this session): `tests/features/vault/test_vault.py`
  (`UnlockWidget` / `MainWindow` / `AppShell` at :465-551) and
  `tests/features/accounts/test_accounts.py` (`MainWindow(service)` at :373-396).

## Cold-eyes loop log

_(To be filled as the `/cold-eyes` loops run — global rule § 14. Loop 2+ runs
cold, no briefing on prior findings. Cleared for code only when a pass returns
zero verified findings across all lanes.)_

| Loop | Date | Lanes | CRIT | HIGH | MED | LOW | Notes |
|------|------|-------|------|------|-----|-----|-------|
| 1 | 2026-07-05 | 3 — accuracy vs live PySide6/reused code · implementability/testability · cross-doc | 0 | 4 | 4 | 6 | See notes ↓ |

**Loop 1 notes.** Zero CRITICAL; the design held, the fixes were accuracy +
testability hardening. **4 HIGH:** (1) `presence_state()` returns the token
**`"unlock"`**, not `"locked"` — a wrong-recall error inside the "Verified API
basis" section (fixed across INV-2, Context, Verified API basis, INV-2b); (2) the
first-run/unlock **modal `exec()`** had no test seam and raced the async
`DeriveWorker` on teardown → redesigned D2 to a **non-blocking `setModal(True)` +
`show()`** dialog that **keeps** the `completed`/`unlocked` signals (preserves the
`qtbot.waitSignal` seam, kills the teardown race); (3) **DoD #2 over-claimed** the
build smoke proves icon-travel (the base `--self-test` never loads an icon) → the
`--self-test` leg now **loads a bundled icon** in the clean-room; (4) **dead
ADR-0002 link** (`0002-pyside6-desktop-gui.md` → `0002-pyside6-over-pyqt6.md`).
**4 MED:** locked-shell hygiene had no mechanism (D5 now **destroys** the content
widget on lock, not hides); INV-8's "no socket" was unfalsifiable → reworded to the
`openUrl`-hand-off it actually tests; the Home getting-started↔table toggle had no
INV → added **INV-9a**; ADR-0002 is a **PySide6-vs-PyQt6 licensing** ADR (and the
app ships **no custom theme** — verified: no `setStyleSheet`/`QPalette` in `src`),
so D7's "dark theme (ADR-0002)" justification was rewritten to neutral-mid-tone
glyphs. **6 LOW:** `vault.py` line-range 39-48→39-49; dialog entry-trigger named
(folded into D2); INV-10 RTL scoped to the testable `tr()`/layout part;
"corrupt"-is-not-a-chrome-state reworded (INV-2); the vague "FIBR-0004 INV
(tr/RTL/QLocale)" citation → `coding.md § 5.2`; a note reconciling the FIBR-0017
live-switch deferral with `coding.md § 5.2`'s "from P02".
