# FIBR-0033 — Backup restore-verification ("does my backup work?")

- **Roadmap:** FIBR-0033 (security-account-recovery).
- **Kind:** feature.
- **Depends on:** FIBR-0014 (the `.fbk` encrypted backup/restore — shipped).

**Contents:** [Context](#context) · [The mechanism](#the-mechanism) ·
[Design decisions](#design-decisions) · [Invariants](#at-a-glance--invariants)
· [Deliverables](#deliverables) · [Out of scope](#out-of-scope) ·
[Exit criteria](#exit-criteria) · [Verified API basis](#verified-api-basis) ·
[Cold-eyes loop log](#cold-eyes-loop-log).

## Context

FIBR-0014 ships an encrypted `.fbk` backup + a destructive restore. But a
backup a user has *never test-restored* is a guess, not a safety net — a
corrupt file, a mistyped backup password, or a truncated copy is only
discovered at the worst possible moment (an actual restore, after the live
vault is already lost). FIBR-0033 adds a **one-click, read-only "does this
backup actually open?" check**: point at a `.fbk`, enter its backup password,
and confirm it decrypts and its schema + row counts are intact — **without
touching the live vault** and without installing anything.

The verify path is *already* the first three steps of `restore_backup`
(`services/backup.py:126`): read + guard the zip → validate KDF params → open
the backup's `vault.db` into a temp with the backup key. Restore then rekeys
and installs; **verify simply stops after the open** and reports what it found.

## The mechanism

`BackupService.verify_backup(src: Path, backup_password: str) -> VerifyResult`:

1. `_read_fbk(src)` + `_guard_manifest(manifest)` — the FIBR-0014 pre-login zip
   guards (fixed entry names, per-entry size caps checked *before* inflating,
   traversal rejection, `format_version`/`sqlcipher_compat`/`schema_version`
   gates). INV-11/INV-12/INV-13/INV-6a.
2. Materialise `params.json` to a `0o600` temp; `load_and_validate_params` (KDF
   floor, INV-11); `derive_key(backup_password, salt, params)`.
3. `Vault(tmp_db, tmp_sidecar).open(backup_key, in_memory_temp=True,
   cipher_compat=SQLCIPHER_COMPAT)` — the page-1 guard read forces a
   decrypt+HMAC, so a wrong password or a tampered file raises `DatabaseError`
   here; `run_migrations` inside `open` raises `SchemaVersionError` if the
   backup is newer than this build supports.
4. On a clean open: read `schema_version` and a **per-table row count** by
   dynamic enumeration (`SELECT name FROM sqlite_master WHERE type='table' AND
   name NOT LIKE 'sqlite_%'`, then `SELECT count(*)` per table — the same shape
   as the FIBR-0014 tests' `_snapshot_tables`). Close the connection, discard
   the temp dir.

**Everything runs in a system `tempfile.TemporaryDirectory()`** (not under the
vault dir — verify never renames anything into place), and the only file
written is the backup's own **ciphertext** `vault.db` plus its params temp;
`in_memory_temp=True` keeps SQLCipher's scratch in RAM. The temp dir is removed
on **every** path — success, wrong password, corrupt file, or an unexpected
error (a `finally`/context manager).

`VerifyResult` (frozen dataclass): `ok: bool`; on success
`schema_version: int` + `table_counts: dict[str, int]`; on failure
`reason: str` — a friendly, already-translated message (not a raw exception).
`verify_backup` **catches the known "bad backup" exceptions** — `BackupError`
(guard failures), `DatabaseError` (wrong password / tamper),
`KdfPolicyError` (KDF below floor), `SchemaVersionError` (too new) — and maps
each to a `VerifyResult(ok=False, reason=…)`. Truly unexpected exceptions
propagate (a bug, not a "bad backup" answer).

## Design decisions

- **D1 — Extract, don't duplicate (rule 3b).** Factor the shared read → guard →
  materialise-params → derive → open sequence out of `restore_backup` into a
  private `BackupService._open_backup_vault(src, backup_password) ->
  tuple[Vault, dict]` (returns the opened backup vault + its manifest).
  `restore_backup` calls it and continues to rekey + install; `verify_backup`
  calls it, reads schema/counts, and closes. Restore's externally-observable
  behaviour is unchanged — the existing FIBR-0014 restore tests are the
  regression guard, and must stay green untouched.
- **D2 — Verify derives only the backup key.** No `new_master_password`, no
  rekey, no sidecar write. Verify answers "can this open?", nothing more.
- **D3 — Read-only w.r.t. the live vault.** Verify never writes `vault_path` or
  its sidecar, never calls `_install`, never unlocks the live vault. The temp
  dir is a fresh system tempdir, so even a same-name `vault.db` can't collide
  with the live file.
- **D4 — Structured result, not exceptions, for the expected cases.** The four
  known "bad backup" outcomes are *answers* the user asked for ("no, and here's
  why"), so they return `ok=False` with a friendly reason, not a stack trace.
  This keeps the UI a simple ok/not-ok render.
- **D5 — Post-login placement, mirroring Export.** The affordance is a "Verify
  backup…" button in the Settings dialog next to "Export backup…"
  (`settings.py:189`), emitting a `verify_backup_requested` signal the shell
  handles. (Verify only needs the `.fbk` + its backup password, so it does not
  require the live vault unlocked; a pre-login placement is possible but is left
  out of scope to keep the surface minimal — see Out of scope.)
- **D6 — No new plaintext-at-rest.** The temp `vault.db` is the backup's own
  ciphertext (backup-keyed, HMAC on); `in_memory_temp=True` keeps scratch in
  RAM; cleanup runs on all paths. Verify introduces no new decrypted-to-disk
  surface beyond what restore already does (security-model T4/T5).

## At a glance — invariants

_INV-1..6 are FIBR-0033's own local numbering (distinct from
`docs/security-model.md`'s global list)._

| INV | Statement |
|-----|-----------|
| **INV-1** | Verify never mutates the live vault: `vault_path` and its sidecar are byte-identical (content + mtime) after `verify_backup`, on **every** outcome (valid, wrong password, corrupt, unexpected error). |
| **INV-2** | A valid `.fbk` + correct backup password → `VerifyResult(ok=True)` with `schema_version` equal to the backup's and `table_counts` equal to the backup's actual per-table counts. |
| **INV-3** | A wrong backup password → `VerifyResult(ok=False, reason=…)` (from the caught `DatabaseError`), no exception escapes, live vault untouched. |
| **INV-4** | Each FIBR-0014 tamper class (missing/renamed/extra entry, oversized entry, traversal, bad `format_version`/`sqlcipher_compat`, KDF below floor, `schema_version` > supported) → `VerifyResult(ok=False)` with a reason naming the class — reusing the existing guards, not a second implementation. |
| **INV-5** | Verify leaves **no** temp files behind (temp dir removed on success and on every failure path) and writes no decrypted plaintext to disk (only backup-keyed ciphertext + the `0o600` params temp, both inside the removed temp dir). |
| **INV-6** | `restore_backup`'s externally-observable behaviour is unchanged by the D1 extraction — the FIBR-0014 restore test suite passes untouched. |

## Deliverables

1. **`src/finbreak/services/backup.py`** — extract `_open_backup_vault` (shared
   with `restore_backup`, D1); add `verify_backup(src, backup_password) ->
   VerifyResult`; a frozen `VerifyResult` dataclass (in `models.py` or
   `backup.py`).
2. **`src/finbreak/ui/backup_verify.py`** — a small `BackupVerifyDialog(QDialog)`
   mirroring `BackupExportDialog`: a `.fbk` file picker + backup-password field
   → `verify_requested` signal; a result area that renders ok ("Backup is
   readable — schema v10, N transactions, …") or the failure reason.
3. **`src/finbreak/ui/settings.py`** — a "Verify backup…" button
   (`objectName="settings_verify_backup"`) next to Export, emitting
   `verify_backup_requested`.
4. **`src/finbreak/ui/main_window.py`** — wire `verify_backup_requested` → open
   the verify dialog → run `BackupService.verify_backup` under a wait cursor →
   show the `VerifyResult` in the dialog.
5. **`docs/security-model.md`** — a one-line note on **T5** (crafted `.fbk`
   parsed pre-login) that a backup can now be *verified* read-only through the
   same guards; no new threat surface (verify parses a `.fbk` exactly as restore
   already does). No new §5 INV (verify's guarantees are FIBR-0033-spec-local).
6. **`tests/features/backup/`** — extend with verify legs reusing
   `_export_from_seed` + `_rebuild_fbk`: valid → ok + counts (INV-2); wrong
   password → ok=False (INV-3); each tamper class → ok=False naming it (INV-4);
   live-vault-untouched before/after (INV-1); temp-dir-empty after every path
   (INV-5); a restore-still-works regression (INV-6).
7. **Roadmap** flip FIBR-0033 ✅; CHANGELOG `[Unreleased]/Added`.

## Out of scope

- Pre-login (locked) placement of Verify — post-login Settings only for now.
- Auto-verifying a backup right after export (a possible later nicety).
- Verifying anything about the *live* vault (this is about a `.fbk` file).
- Repairing a bad backup — verify only reports.

## Exit criteria

- `./scripts/ci-local.sh` green (ruff, format, bandit, pip-audit, gitleaks,
  pytest, mypy).
- INV-1..6 each have a falsifiable test; INV-1/INV-5 assert the live vault and
  the temp dir explicitly (content/mtime; dir emptiness).
- The FIBR-0014 restore suite passes unmodified (INV-6).

## Verified API basis

- `services/backup.py`: `restore_backup(src, backup_password, new_master_password,
  *, on_key=None) -> None` L126 (open sequence L156-181); `_read_fbk` L249;
  `_guard_manifest` L214; `_read_capped` L290 (caps `MAX_MANIFEST_BYTES` 64 KiB
  / `MAX_BACKUP_DB_BYTES` 512 MiB L52-53); `_install` L229; entry-name constants
  L60-62; `MANIFEST_FORMAT_VERSION` L46, `SQLCIPHER_COMPAT` 4.
- `vault.py`: `Vault.open(key, *, in_memory_temp=False, cipher_compat=None)`
  L143 (page-1 guard read `SELECT count(*) FROM sqlite_master` L164 → raises
  `sqlcipher3 DatabaseError` on wrong key/tamper); `_connect` L195.
- `migrations.py`: `LATEST_SCHEMA_VERSION = 10` L24; `run_migrations(conn)` L54
  (raises `SchemaVersionError` when the vault is newer, L57-61).
- `crypto.py`: `derive_key(password, salt, params)` L41; `load_and_validate_params`
  L94 (raises `KdfPolicyError`).
- `ui/settings.py`: `SettingsDialog` L46; `export_backup_requested` signal L50;
  "Export backup…" button L189 (`objectName="settings_export_backup"`).
- `ui/backup_export.py`: `BackupExportDialog(QDialog)` L28 (the dialog template).
- `ui/main_window.py`: `_open_backup_export` L833 / `_on_backup_export_requested`
  L843 (the export wiring to mirror); restore wiring `_on_restore_requested` L948.
- Tests: `tests/features/backup/test_backup.py` — `_export_from_seed(tmp_path)`
  L324, `_snapshot_tables(conn)` L306, `_rebuild_fbk(...)` L421,
  `test_INV2_restore_reproduces_every_table` L344; consts `_BACKUP_PW` L41.
- `docs/security-model.md`: T5 (crafted `.fbk` pre-login) L85; §5 tops at INV-10.

## Cold-eyes loop log

_(to be appended by the /cold-eyes runs)_
