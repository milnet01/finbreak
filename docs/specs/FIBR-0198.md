# FIBR-0198 — Reveal the masked account number, with an auto re-mask

**Status:** spec draft (2026-07-30).
**Kind:** feature.
**Source:** ROADMAP FIBR-0198 (in-session-2026-07-30). Split out of FIBR-0113 on
2026-07-30 (§13).
**Blocked by:** FIBR-0113 — this item reveals the account-number **column** and
the account-number **form field** that item creates, and re-uses its
`_mask_account_number` helper, its `_refresh()` fill and its
`_apply_forget_gating()` helper.
**Pairs with:** FIBR-0032 (§3 decision 3 — `_REVEAL_SECONDS` is seeded from that
item's `clipboard_clear_seconds` **default**, and does not track the setting), FIBR-0128 (§4.4 step 6 — its
D3 Forget-button gating, which the reveal must re-apply), FIBR-0193 (stores the value being revealed).

**Layman:** The Accounts tab gets a "Show account numbers" tick-box, so you can
read a full account number when you need to pay someone. It hides itself again
after half a minute, so a number can't be left sitting on screen.

## 1. Goal

The Accounts tab gains one control — a "Show account numbers" checkbox — that
switches the account number from its masked form to the raw stored value on
**both** surfaces that display it: the table cell and the edit form's field. The
reveal is session-scoped, is written to no store, and lapses back to masked after
`_REVEAL_SECONDS` without the user doing anything.

## 2. Problem

FIBR-0113 ships the Accounts table with the account number **always** masked:
`ui/accounts.py::_mask_account_number` renders `"•••• 7890"` in the cell, and the
form's `self._account_number` field is constructed in
`QLineEdit.EchoMode.Password` and never leaves it. That is the right default —
FIBR-0113 §3 decision 2 is defending against shoulder-surfing and screenshots —
but it is not a usable end state.

A user who needs the full number to pay someone has nowhere to get it. The value
is in the vault, encrypted at rest, and finbreak is the only thing that can
decrypt it — so "read it from the file instead" is not an escape hatch, it is a
dead end. FIBR-0113 §8 records "mask with no reveal" as a **rejected**
alternative for exactly this reason; this item is what makes that rejection true
rather than aspirational.

The last 4 characters the mask does show are enough to *disambiguate* two
accounts at different banks, which is what FIBR-0113 §2 consequence 2 asked for.
They are not enough to *use* the number.

## 3. Scope decisions

Decisions 2 and 3 are FIBR-0113's pre-split decisions 4 and 5, settled with the
user on 2026-07-28 and travelled here with the reveal on 2026-07-30. Decision 1
is the framing they sat under, restated here so this document reads standalone:

1. **The reveal is an explicit toggle over a default-masked value** — the
   masking itself, and the shoulder-surfing-and-screenshots reasoning behind it,
   are **FIBR-0113 §3 decision 2's**, which this spec cites rather than
   restates. What is decided here is only that the way back to the raw value is
   a control the user operates deliberately.
2. **Reveal is session-scoped**, never persisted — a reveal that survives a
   restart is a reveal the user did not ask for that day.
3. **A reveal auto-hides after `_REVEAL_SECONDS` (30).** Masking defends
   against someone seeing the screen; a reveal left on indefinitely gives that
   back the moment the user walks away. 30 seconds is taken from
   `services/auth.py::DEFAULT_CLIPBOARD_CLEAR_SECONDS` — the app's other
   "sensitive thing visible for a bounded time" control, recorded in
   `docs/security-model.md` T13 — so the number is inherited rather than
   invented. It is seeded from that **default** and deliberately does not track
   the setting: `clipboard_clear_seconds` is user-settable
   (`ALLOWED_CLIPBOARD_CLEAR_SECONDS = (10, 30, 60, 0)`), so a user who changes
   it has two windows that no longer match, and that is accepted. A fixed
   constant, not a setting: see §9.

## 4. Design

### 4.1 The toggle

A `QCheckBox` — `self._reveal`, labelled "Show account numbers" — sits on its own
row between the table and the form. It is constructed unchecked and its state is
never written to `QSettings`, to the vault settings, or anywhere else.

Reveal cannot outlive a lock without any extra code:
`main_window.py::MainWindow._lock()` locks the vault and then calls
`_clear_live()`, which drops every tab reference and `deleteLater()`s the
workspace (FIBR-0051 INV-4 step 3), and `_build_workspace()` constructs a fresh
`AccountsWidget` on the next unlock — so the checkbox returns to unchecked. This
is a property to *test*, not to implement.

### 4.2 Reveal governs two surfaces

**FIBR-0113 §4.1 owns the masking itself** — the two surfaces, why each uses the
mechanism it does, and the rule that `_mask_account_number` must never reach the
form. None of that is restated here. What this item adds is that both surfaces
now have an *unmasked* state, and that an implementation which reveals one and
not the other satisfies §3 decision 1 nowhere. INV-3 and INV-5 lock one surface
each; §8 records why neither alone would do.

- **The cell.** FIBR-0113's `_refresh()` writes
  `_mask_account_number(account.account_number)` unconditionally; this item makes
  that write conditional — the raw `account.account_number` while the box is
  checked, the masked form while it is not (INV-5, amending FIBR-0113 INV-20).
  The fill reads **`self._reveal.isChecked()`**, not the `bool`
  `_on_reveal_toggled` receives: `_refresh()` also runs from Add, Update, Delete,
  Forget and `MainWindow._refresh_tab`, where there is no toggle event and so no
  parameter to read. INV-5's leg drives one of those paths for exactly this
  reason. This is safe inside the handler too: `QCheckBox` updates its check
  state *before* emitting `toggled`, so `isChecked()` read from step 4's
  `_refresh()` is already the new value.
- **The field.** Its echo mode moves from `Password` to `Normal` and back
  (INV-3), following the idiom already in this codebase:
  `ui/export_dialog.py::ExportDialog._on_show_toggled` is a `QCheckBox.toggled`
  handler taking a `bool` and setting `Normal` / `Password` on its line edits.
  This item copies that shape rather than inventing one — the divergent label
  ("Show account numbers" against that dialog's "Show password") is deliberate,
  since the two controls reveal different things on different screens. It stays fully editable in both states, exactly like any password box
  (user, 2026-07-28), and its `text()` is the raw stored value throughout —
  which is FIBR-0113's rule, unchanged by the reveal.

The field is constructed `Password` by FIBR-0113, and **`_on_reveal_toggled` is
the only thing that changes its echo mode** (§4.4 step 2). It is deliberately
*not* set from `_on_selection_changed`, which §4.4 step 5 suppresses across the
reveal re-selection and which therefore does not run on the one transition INV-3
governs.

### 4.3 The auto-hide timer

Checking the box starts a single-shot timer that re-masks on expiry:

```python
_REVEAL_SECONDS = 30

self._reveal_timer = QTimer(self)          # parented — dies with the widget
self._reveal_timer.setSingleShot(True)
self._reveal_timer.setInterval(_REVEAL_SECONDS * 1000)
self._reveal_timer.timeout.connect(self._on_reveal_timeout)
```

`ui/_clipboard.py::ClipboardAutoClear` is the precedent for *the shape of the
guard only* — a parented single-shot `QTimer` whose `timeout` is wired to a
named slot. Two deliberate differences. Its delay is settings-driven, so it
stops on every use and restarts only when the configured delay is non-zero
(`0` means "Never"), where `_REVEAL_SECONDS` is a constant set once via
`setInterval`. And its docstring says its `timeout` is wired **directly** to
`clear_if_ours`, "so the guard has exactly one implementation" — *directness* is
the property it claims, and it is exactly the one this design departs from:
`timeout` here goes to `_on_reveal_timeout`, which only unchecks the box, so the
re-mask travels the same path as a manual uncheck and the re-masking itself
still has one implementation.

The interval is restarted, not extended, on each fresh reveal, and a `_refresh()`
reached any other way does not touch the timer (INV-2).

The timer is a **convenience bound on exposure, not a security boundary** — a
revealed number is on screen for up to `_REVEAL_SECONDS` regardless, and anything
that can read the process's memory can read the value whether it is masked or
not.

**`_refresh()` gains a `VaultLockedError` guard around its service reads**, and
the timer is the whole reason it needs one — §6 owns the mechanism that makes a
timeout reachable after the vault has closed. `_refresh()` swallows the error
silently, as the four vault-reading `_on_*` handlers in this widget already do.

The guard wraps **the whole pre-fill read block, not a named subset of it**:
`account_ids_with_pdf_password()`, `ReconciliationService.account_statuses()`,
`read_minor_unit_exponent(self._vault.connection)`,
`TransactionService(self._vault).base_currency()` and `list_accounts()` — every
read up to but excluding the `fill_guard` fill. The reason to draw it around all five
rather than around today's first read is **reorder-robustness**:
`Vault.connection` is *where* `VaultLockedError` is raised and all five reach
it, so whichever runs first is the one that raises. A guard scoped to the three
service calls happens to work today only because one of them is currently
first; it would silently stop covering the hazard the day someone moves the
money-display reads up.

It returns before `self._rows`, `self._with_pw` or the table are touched,
leaving the previous render intact. A guard around the whole body would instead
let a lock land mid-fill and leave those three half-updated while §4.4's steps
5–6 run against them.

### 4.4 `_on_reveal_toggled`

`self._reveal.toggled` connects to `_on_reveal_toggled`, the one handler that
owns the transition. Its signature is **`@Slot(bool)`** rather than this
module's `@Slot()` convention, because steps 2 and 3 read the emitted `bool` to
tell which direction the toggle went. (Not for safety: `@Slot()` on a
zero-argument handler connects to `toggled` and runs fine under the pinned
PySide6 — the argument is simply dropped. Verified by probe on 6.11.1.)

Toggling repopulates the table, which under FIBR-0113's clear-then-fill drops the
current selection (its §3 decision 6), so this handler restores it — and the
order is partly forced: step 1 must precede step 4, because the selected id has
to be captured before `_refresh()` replaces `self._rows`, and steps 4 → 5 → 6
must run in that sequence. Steps 2 and 3 are order-free relative to the rest.

1. capture the currently selected account id (via
   `_table_state.selected_index` into `self._rows`), or `None`;
2. set `self._account_number.setEchoMode(...)` — `Normal` if the box just went
   on, `Password` if it went off (INV-3);
3. start the single-shot timer if it went on, stop it if it went off (INV-2);
4. call `_refresh()`, which rebuilds `self._rows` and the table, leaving no
   selection (FIBR-0113 INV-22);
5. if an id was captured, re-select it via `_select_account` **with
   `self._table.blockSignals(True)` around the call**, then unblock. Blocking
   suppresses `itemSelectionChanged`, so `_on_selection_changed` does not fire
   and cannot overwrite half-typed form input from storage (INV-4). The unblock
   goes in a `finally`: a raise between the two would otherwise leave the table
   permanently deaf to selection;
6. call `_apply_forget_gating()` — the one thing step 5 suppressed that still has
   to happen — FIBR-0128 D3's rule that Forget is enabled only for a selected
   account with a saved statement password. It is FIBR-0113's gating-only helper
   (its §3 decision 7), so that rule keeps one implementation rather than
   gaining a third.

`_select_account` is promoted to a production caller here: FIBR-0113 ships it
resolving an account id to a position in `self._rows` before calling
`select_by_index` (its INV-15), used only by tests until step 5 exists.

## 5. Invariants

- **INV-1** — A freshly constructed `AccountsWidget` has reveal off, the toggle
  state is written to no persistent store, and a lock cycle returns it to off.
  *Test:* `tests/features/accounts/test_accounts.py` — a `qtbot` test that (a)
  asserts the checkbox is unchecked on construction; (b) enumerates the window INI's
  keys before and after toggling — through a `QSettings` over
  `paths.window_settings_path()` with `sync()` called first, **not** by parsing
  the file's bytes — and asserts no key outside **`columns/accounts_table`**
  changed. Both halves matter: reading keys rather than bytes is what lets the
  leg *exclude* the one key `remember_columns` may legitimately write, and going
  through `QSettings` is what defeats a persisting implementation whose
  `setValue` is still buffered and has not reached the file during the test; and (c) drives `MainWindow._lock()` then a re-unlock and asserts the
  rebuilt tab's checkbox is unchecked. Leg (c) needs a `MainWindow`, not a bare
  widget — build one, `_enter_unlocked()`, toggle reveal on, `_lock()`,
  re-`unlock()` the service, `_enter_unlocked()` again. It is the only leg in
  this spec that does so (§7), and what it uniquely catches is `_clear_live` ever
  being changed to *reuse* the Accounts tab rather than rebuild it — which legs
  (a) and (b) cannot see.
  *Breaks when:* the toggle is persisted next to the column state in the window
  INI, so a machine left revealed stays revealed across a lock. Excluding the
  whole `columns/` **group** rather than the single `columns/accounts_table`
  **key** would let a flag persisted as `columns/accounts_reveal` pass the leg
  meant to fail on it. **Not covered:** persistence to the *vault* settings store
  (where `clipboard_clear_seconds` lives) — this leg observes the window INI
  only; §11 records that gap.

- **INV-2** — A reveal re-masks on its own after `_REVEAL_SECONDS`; switching it
  off by hand cancels the pending timer so it cannot fire across a later reveal;
  and a `_refresh()` from any other cause leaves the pending interval untouched
  rather than extending it.
  *Test:* `tests/features/accounts/test_accounts.py` — a `qtbot` test driving the
  widget's timer directly rather than waiting `_REVEAL_SECONDS` in real time,
  asserting each clause against a named observable:
  (a) **the re-mask actually happens** — with reveal on, emit
  `self._reveal_timer.timeout` (or call `_on_reveal_timeout`) and assert the
  checkbox is back to unchecked, the form field's `echoMode()` is `Password`
  and the Account-number cell is masked again; (b) on a **freshly constructed
  widget, before any reveal**, `interval()` is `_REVEAL_SECONDS * 1000` and
  `isSingleShot()` is `True` — the read moment is part of the assertion, because
  §4.3 requires `setInterval` in `__init__`, and the precedent's alternative
  shape (`start(seconds * 1000)` per use, as `ClipboardAutoClear` does) leaves
  `interval()` at `0` until the first reveal; (c) after a manual
  uncheck, `isActive()` is `False`; (d) across an unrelated `_refresh()` (an Add),
  `remainingTime()` is **not greater** than it was before **and** is strictly
  below `_REVEAL_SECONDS * 1000` — with a deliberate `qtbot.wait(...)` after the
  reveal and before the first read. The pair is what discriminates: the wait
  makes the second clause fail against an implementation that restarts the
  timer, while the first clause stays non-strict, because a correct
  implementation can return the same integer for two reads a sub-millisecond
  apart and a strict comparison would go red against it.
  *Breaks when:* `timeout` is never connected, or `_on_reveal_timeout` does
  nothing — the reveal simply never lapses, which is the whole of §1, and legs
  (b)–(d) all pass against it because they observe the timer's *configuration*
  rather than its effect. That is why leg (a) leads. Or the timer is left
  repeating, or is not stopped on a manual uncheck — a stale timer then re-masks
  a reveal the user started seconds ago and the box appears to toggle itself. Or
  `_refresh()` restarts it, so editing accounts holds a reveal open
  indefinitely; only leg (d) can see that.

- **INV-3** — The edit form's account-number field is masked whenever reveal is
  off: its echo mode is `Password` with the toggle off and `Normal` with it on,
  and it stays editable in both states.
  *Test:* `tests/features/accounts/test_accounts.py` — select an account with a
  stored number; assert `echoMode()` is `Password` and that **`displayText()`**
  is masked while `text()` still returns the raw value (a `Password` field's
  `text()` is never masked — asserting on it would fail and invite weakening the
  leg); toggle reveal, assert `Normal` and that `displayText()` now equals
  `text()`; type in both states and assert the typed value reaches the service.
  *Breaks when:* only the table cell is revealed and the form field is left in
  `Password` — the user ticks the box, the cell shows the number, and the field
  they are about to copy from still shows dots. The mirror failure (the field
  revealed and the cell not) is INV-5's.

- **INV-4** — Toggling reveal preserves the user's in-progress edit: the selected
  account stays selected, the four form inputs keep whatever the user had typed,
  and the Forget button's enabled state is unchanged.
  *Test:* `tests/features/accounts/test_accounts.py` — select an account, type
  new text into the name and note fields without saving, toggle reveal on and
  off, and assert the selection, all four inputs and the Forget button are as
  they were. **Then repeat the off-transition unattended**: reveal again, type
  again, and this time emit `self._reveal_timer.timeout` instead of unticking.
  Both paths reach this handler, and the timeout one is the case that matters —
  §8 rejects extending the timer on activity, so the spec has *decided* that a
  re-mask can land mid-edit, and this leg is what stops that decision costing
  the user their typing.
  *Breaks when:* `_on_reveal_toggled` re-selects without blocking the table's
  signals (§4.4 step 5), so `_on_selection_changed` fires and repopulates every
  field from storage — silently discarding what the user was typing. The reveal
  toggle is the one control a user is likely to reach for *mid-edit*, which is
  what makes this reachable rather than theoretical.

- **INV-5** — The Account-number **column** renders the raw stored value while
  reveal is on and returns to `_mask_account_number(account.account_number)` when
  it goes off, for every row.
  *Test:* `tests/features/accounts/test_accounts.py` — two accounts, one with a
  10-character number and one with none; with reveal off assert the column's
  `item.text()` is `"•••• 7890"` and `""` (FIBR-0113 INV-20's state); toggle
  reveal on and assert the first is now the full raw value and the second still
  `""`; **then, still revealed, drive a non-toggle `_refresh()` — an Add — and
  assert the column is still raw**; then toggle off and assert it is masked
  again. The Add leg is the only one that exercises §4.2's
  `self._reveal.isChecked()` requirement: a fill that read the toggle's `bool`
  parameter instead would re-mask on every Add, Update, Delete and tab
  activation while the box stayed ticked.
  *Breaks when:* the fill keeps calling `_mask_account_number` unconditionally,
  so the checkbox changes nothing on the surface the user is looking at — the
  whole of §1. INV-3 does not catch it: the form field would still switch echo
  mode, so a user with a row selected would see the number and conclude the
  feature works.

## 6. Failure modes

- **The auto-hide timer fires around a lock.** The timer is parented to the
  widget, so the widget's destruction takes it down — but `_clear_live` uses
  `deleteLater()`, a *deferred* delete, and `_lock()` locks the vault **before**
  it. So an already-queued timeout can still reach `_on_reveal_timeout` →
  `_on_reveal_toggled` → `_refresh()`, which reads the now-locked vault. §4.3's
  guard is the narrow reason `_refresh()` needs one. §4.1's "reveal cannot
  outlive a lock without any extra code" is about the *checkbox state*, which is
  still true — the guard is about the timeout that can land between the vault
  locking and the widget being deleted.
- **The timeout fires inside a modal confirm.** `_on_delete` and
  `_on_forget_password` block on `QMessageBox.question`, which runs a **nested
  event loop** — so a queued re-mask is delivered *inside* the handler, between
  the user seeing the dialog and answering it. `_on_reveal_toggled` then runs a
  full `_refresh()`, replacing `self._rows` and clearing the selection
  (FIBR-0113 INV-22) under a handler that is mid-flight. The requirement this
  imposes: **both confirm-opening handlers — `_on_delete` and
  `_on_forget_password`, the only two that call `QMessageBox.question` — resolve
  their target account before opening the confirm**, and act on that captured
  object afterwards rather than re-resolving `selected_index`. Today's code already does this (it reads
  `currentItem()` first), so the risk is a rewrite quietly reordering it — and
  the reorder is only reachable because this item adds a timer. No leg covers
  it; §11 records the gap.
- **A `_refresh()` while revealed** — triggered by construction, Add, Update, Delete,
  Forget, the reveal toggle itself, or `MainWindow._refresh_tab` on every activation of
  the Accounts tab — re-renders unmasked and does **not** restart the timer, so
  neither editing accounts nor switching tabs can extend the exposure window
  (INV-2).
- **A copied account number outlives the reveal.** The timer bounds what is on
  *screen*; it does not reach the clipboard. Ctrl+C from the revealed field uses
  `QLineEdit`'s built-in copy, which never touches `ClipboardAutoClear` — so the
  value sits on the system clipboard until something else overwrites it, long
  after the field has re-masked. This is the one exposure path the auto-hide
  does not bound, it is a **deliberate gap rather than an oversight** (the user
  copies the number in order to paste it into a payment, and clearing it
  mid-payment would defeat the reason §2 gives for the feature existing), and
  §12 requires T13 to say so rather than letting the row imply otherwise.
- **Sorting the Account-number column across a toggle.** That column sorts by
  whatever is displayed — the last 4 while masked, the full value while revealed
  — so toggling reveal can reorder a live sort on it. Accepted and noted rather
  than fixed; sorting a masked column can only ever expose what the mask exposes.
- **A revealed number widens the column.** Masked display is bounded at 9
  characters; a revealed one is not, and the column is user-resizable and
  remembered (FIBR-0113 INV-17), so this is the user's to size.
- **Reveal on, then the tab is left.** `MainWindow._refresh_tab` re-renders on
  the next activation, still revealed, with the timer still running from when the
  box was ticked — so the exposure window is measured from the reveal, not from
  the last look at the tab. That is the intent of decision 3, recorded here so it
  reads as chosen rather than overlooked.

## 7. Tests

Every leg lands in `tests/features/accounts/test_accounts.py`, alongside
FIBR-0113's. There is no new test file and no new fixture directory.

The legs are `qtbot` widget tests over a constructed `AccountsWidget`: reveal
default and non-persistence (INV-1), the auto-hide timer (INV-2),
the form field's echo mode (INV-3), the mid-edit preservation guarantee (INV-4),
and the cell's two rendered states (INV-5).

Per `docs/standards/testing.md`, each is run against pre-implementation source
and seen to fail before the implementation is written. `test_accounts.py` exists
and will by then carry FIBR-0113's legs, so this is a real check rather than a
formality: each leg is added, run against the always-masked widget FIBR-0113
ships, and seen to fail before the toggle is written.

`tests/features/accounts/spec.md` gains a `## FIBR-0198 — …` INV block with
`Source:` lines, following the per-item convention **FIBR-0113 §7 owns and
states in full** (five blocks will share that file).

The `qtbot` legs assert behaviour on a constructed widget; they do **not** prove
the tab renders correctly inside `MainWindow`. The one exception is INV-1 leg
(c), which builds a `MainWindow` solely to drive a lock cycle — it still asserts
nothing about rendering.

## 8. Alternatives considered (and rejected)

- **Show account numbers in full, with no mask.** Rejected by the user — see
  FIBR-0113 §3 decision 2, which owns that rejection. This is the alternative the whole item exists to avoid; it is
  restated here because "add a reveal" and "stop masking" look similar from
  outside and are opposite decisions.
- **Reveal the cell only, leaving the form field in `Password`.** Rejected: the
  form is where a user selects and copies the number, so a reveal that does not
  reach it does not do the job §2 describes. INV-3 locks it.
- **Reveal the form field only, leaving the cell masked.** Rejected: the field
  shows one account, and a user comparing two accounts at the same bank is
  looking at the column. INV-5 locks it.
- **Persist the toggle**, so a user who works revealed does not re-tick it each
  session. Rejected — see §3 decision 2.
- **No auto-hide; the user unticks when done.** Rejected — see §3 decision 3. It
  is the walk-away case that decision 3 is defending against, and it is the one
  case the user cannot untick for.
- **A per-row reveal (an eye button in each Account-number cell)** rather than
  one toggle for the column. Rejected: it narrows the exposure to one row, but a
  `QTableWidgetItem` cannot host a button, so each row would need
  `setCellWidget` — which breaks `_table_state`'s sort and fill contract that
  FIBR-0113 INV-18 and INV-22 depend on. Cost out of proportion to the gain.
- **Extending the timer on activity in the tab**, so an in-progress edit is not
  interrupted by a re-mask. Rejected: it makes the exposure window unbounded in
  exactly the session where someone is at the keyboard, and INV-2's third clause
  is written to forbid it. The user can re-tick the box, which restarts the full
  interval.

## 9. Out of scope

- **The table, the mask helper and the account-number form field** — all
  FIBR-0113's, this spec's blocker.
- **The storage of the value being revealed** — FIBR-0193's.
- Making `_REVEAL_SECONDS` user-configurable, as the clipboard auto-clear delay
  already is. **A decision, not a deferral — no follow-up id.** One fixed
  constant is the shortest correct form of what was asked for, and a settings key
  nobody has asked to change is scaffolding. If it is ever wanted it follows the
  existing `clipboard_clear_seconds` pattern exactly.
- A reveal that survives a lock or a restart — §3 decision 2, which owns the
  reasoning; no follow-up id.
- Live language switching for the checkbox label — tracked by FIBR-0017 (P12
  i18n). This is a standing **departure from `coding.md` §5.2**, which asks for
  `changeEvent` / `retranslateUi`: no file under `src/finbreak` has either today,
  so every `tr()` string is bound at construction and the label added here
  inherits that.

## 10. Resource cost

No new external dependency, no new module, no new build target. One `QCheckBox`
and one `QTimer` per `AccountsWidget`, both parented to it.

One cost is **unattended** and so worth naming: every reveal toggle *and* every
auto-hide runs a full `_refresh()`, which is `list_accounts()` +
`account_ids_with_pdf_password()` + `ReconciliationService.account_statuses()` (a
statement-period walk per account) + the money-display reads. That is the same
work a tab activation already does, on a table of one row per account, so it is
accepted rather than optimised into a column-only re-render — but it fires on a
timer rather than on a user action, which is why it is recorded here.

## 11. What checks this

| Rule | What catches a breach |
|------|----------------------|
| INV-1 | `tests/features/accounts/test_accounts.py` |
| INV-2 | `tests/features/accounts/test_accounts.py` |
| INV-3 | `tests/features/accounts/test_accounts.py` |
| INV-4 | `tests/features/accounts/test_accounts.py` |
| INV-5 | `tests/features/accounts/test_accounts.py` |
| §4.3 `_refresh()` swallows `VaultLockedError`, guarding the reads only (a timeout landing across a lock) | **nothing** — no leg drives a timeout into a locked vault; the guard mirrors the existing `_on_*` handlers |
| INV-1, persistence to the **vault settings** store rather than the window INI | **nothing** — INV-1's leg observes the window INI only |
| §4.1 the checkbox sits between the table and the form | **nothing** — layout aesthetics have no automated catcher |
| §6 `_on_delete` and `_on_forget_password` resolve their target account **before** opening their confirm, so a re-mask delivered in the nested event loop cannot invalidate it | **nothing** — no leg drives a timeout into an open `QMessageBox` |
| §4.4 step 5's `blockSignals` unblock is in a `finally` | **nothing** — no leg raises inside the blocked window, and a bare unblock leaves the table permanently deaf to selection after any raise |
| §12 `security-model.md` T13 is amended before this ships, so it does not ship false | **nothing** — no check ties a threat-model row to the code that falsifies it |

Eleven rows, **six** with a bolded `nothing` — this spec's error budget. Four
are real gaps in the build contract: INV-1's leg cannot see the vault settings
store, no leg drives a timeout into a locked vault, none drives one into an open
modal, and none raises inside step 5's blocked window. The other two are a presentation surface and a prose-consistency
claim, neither of which this project has an automated catcher for.

## 12. Cross-doc impact

Every bullet below is a **state claim** about the tree, not an instruction, and
carries its status. **Pending is the default** — each is an edit this item still
owes unless it says otherwise. Written as bare instructions these bullets are
falsified the moment one is carried out, which is how they go stale while the
rest of a spec does not.

- `CHANGELOG.md` — **pending.** One `### Added` entry for FIBR-0198 alone.
- `ROADMAP.md` — **pending.** Only FIBR-0198 flips ✅; FIBR-0113 flips on its own
  work, before this.
- `docs/security-model.md` — **pending, and T13 ships false the day this lands.**
  Its mitigation reads "the statement PDF password **and account numbers** are
  **not** copyable (no new secret crosses into the UI; FIBR-0128 INV-1
  preserved)". The clause is about **copyability**, so the surface that falsifies
  it is the **form field**, not the cell: a `QTableWidgetItem` is not copyable,
  but a `QLineEdit` in `Normal` echo mode is (Ctrl+C works; `Password` suppresses
  it). T13 is amended to say **two** things, and the second is the
  one easy to miss: an account number becomes copyable from the form field
  while reveal is on, **and that copy is not auto-cleared**. T13's existing
  mitigation continues "A copied value is **auto-cleared** after a configurable
  timeout (default 30s)", which is true only of the transactions list —
  `ClipboardAutoClear` is constructed in exactly two places
  (`ui/main_window.py`, `ui/transactions.py`), both for that surface, and a
  Ctrl+C out of a `Normal`-echo `QLineEdit` goes to the system clipboard through
  Qt's built-in copy action without passing through it. So an account number
  copied during a 30-second reveal outlives the reveal indefinitely. Amending
  T13 to name the new copyable surface *without* that clause would leave the row
  reading as though the 30-second auto-clear covers it. The file also gains a
  threat-model entry for the reveal control — spec-format §5.5 requires the boundary and the preserved
  defences be stated, and `security-model.md` today contains no mention of
  masking, shoulder-surfing or screenshots. Its numbered §5 "Security
  invariants" checklist gains no entry: the reveal is a bounded-exposure
  convenience, not a new invariant (§4.3), and adding one would promise a
  guarantee the timer does not give. **FIBR-0193 §12's pointer has already moved** — it now reads "**FIBR-0198
  amends T13** (as of the 2026-07-30 reveal split — FIBR-0113 no longer does)".
  The three specs that mention T13 must not give different reasons for one
  edit.
- `docs/specs/FIBR-0113.md` — **already applied.** Its **INV-20** ("the column
  renders `_mask_account_number(...)` for every row") and the §4.1 bullet
  describing that write both carry `amended by FIBR-0198`, marking the narrowing
  INV-5 performs — an annotation rather than a rewrite or a renumber, per
  spec-format §3.7. Its §8 bullet rejecting a permanent mask, its §9
  reveal-control bullet and its §5 withdrawn-invariant **list** (INV-6 / 11 / 16 / 19 → this
  spec's INV-1 / 2 / 3 / 4) already name this item.
- `tests/features/accounts/spec.md` — **pending.** A `## FIBR-0198` INV block
  (§7), and **one annotation on the FIBR-0113 block**: its INV-20 entry states the
  unconditional-mask contract this item narrows.
- `assets/screenshots/{midnight,ledger}/accounts.png` and
  `scripts/capture_screenshots.py` — **pending.** Both are regenerated, with the
  checkbox **visible and unticked**. Reveal-off is this item's default, so the
  *number* is masked in the capture exactly as FIBR-0113 left it — but §4.1 adds
  a control to the tab, so a capture taken before this item shows a tab that no
  longer exists. The surrounding inventory — which capture sets exist, and why
  `site/` and the AppStream metainfo need none — is FIBR-0113 §12's and is
  unchanged here. `assets/screenshots/site/` carries no `accounts.png` and the
  AppStream metainfo lists no Accounts screenshot; stated so the omission reads
  as checked rather than forgotten.
- `README.md` — **pending.** The reveal control joins FIBR-0113's
  columnar-Accounts bullets; that bullet owns the description of where the
  feature list lives.
- `CLAUDE.md` module map — no change; no new module is added.
- **No build plan.** FIBR-0113 §12 owns the reasoning and the **FIBR-0195**
  pointer; the same conclusion holds here, with one clause worth answering
  rather than eliding. spec-format §2 makes a plan mandatory "once the build
  order matters (a migration, a change that must land in a specific sequence, or
  anything a second person will execute)" — and it is that third clause, not the
  first two, that a spec written for an implementer plausibly trips. It does not
  bite here because the build order is four subsections of one file in the order
  they are written: §4.1 → §4.2 → §4.3 → §4.4.

## 13. Cold-eyes loop log

Fresh log. This document was split out of FIBR-0113 on 2026-07-30 and has not
been reviewed under its own id; FIBR-0113's rows 1–5 (and its `5-split` row)
describe a different, larger document and are **not** inherited. The split
reasoning is recorded in FIBR-0113 §13.

| Loop | Date | Lanes | CRIT | HIGH | MED | LOW | Outcome |
|------|------|-------|------|------|-----|-----|---------|
| 1 | 2026-07-30 | 3, run jointly with FIBR-0113 (0113-contract · this doc · cross-doc/split), all strong depth, no breadth pass (loop 1 of a never-reviewed pair) | 0 | 6 | 14 | 10 | 30 verified, 0 unverified. All fixed. This document's share: **§4.3's `VaultLockedError` guard enumerated three of the five reads** it must wrap, and the two it omitted — `read_minor_unit_exponent(self._vault.connection)` and `base_currency()` — are the ones that *raise*, since `Vault.connection` is where the error comes from; a guard built to the letter left the hazard it exists for uncaught. **INV-2's three legs never fired the timeout**: `isSingleShot` / `isActive` / `remainingTime` all observe the timer's *configuration*, so an implementation that never connects `timeout` passed every leg while the reveal never lapsed — the whole of §1. Leg (a) now emits the timeout and asserts the re-mask, and `interval()` is pinned. §6 gained the **nested-event-loop** path: `QMessageBox.question` in Delete / Forget runs a nested loop, so a queued re-mask lands *inside* the handler and rebuilds `self._rows` under it — the requirement (resolve the target before opening the confirm) was never stated. The `ClipboardAutoClear` docstring was misquoted and mis-attributed (it claims *directness*, which this design deliberately departs from). "The two exposure windows match" held only at the default, `clipboard_clear_seconds` being user-settable. §12's T13 bullet was one edit behind FIBR-0193; §3's intro said two decisions travelled where three follow; §7's "four blocks" is five; `(its INV-3)` had no owning spec (FIBR-0051's). §11 went 9 rows / 4 `nothing`s → 10 / 5. Not converged — loop 2 owed. |
| 2 | 2026-07-30 | 3, joint with FIBR-0113 (same partition, cold) | 0 | 7 | 11 | 13 | 30 verified, 0 unverified. All fixed. **Split by origin: ~17 draft defects, ~9 introduced by loop 1's own fixes** — draft defects fell 30 → 17, so the sweep is keeping up and neither stop-trigger fires. This document's share, and the headline is a claim that was **disproved by running it**: §4.4 justified `@Slot(bool)` by asserting that `@Slot()` on a zero-argument handler "would be a runtime `TypeError` here under PySide6". Probed on the pinned 6.11.1 — `QCheckBox.toggled` connects to a `@Slot()` handler with or without a parameter and runs cleanly, the bool simply dropped. The two lanes disagreed about it, which is what forced the probe. The prescription survives (steps 2–3 read the bool); the false mechanism is gone. **Loop 1's own guard rationale was also false**: it said a guard around three of the five reads "leaves the hazard uncaught", but `_refresh()` is synchronous, so whichever read touches `Vault.connection` first is the one that raises — and today that is one of the three. The instruction (guard all five) is right for **reorder-robustness**, which is now what it says. §6's new modal bullet said "each of the four handlers" where only **two** open a confirm. §4.2's `isChecked()` requirement — added last loop — had no leg and no §11 row, so a fill reading the toggle's parameter instead would re-mask on every Add while the box stayed ticked; INV-5's leg now drives a non-toggle `_refresh()`. INV-1 leg (b) parsed the INI's **bytes**, which a buffered `QSettings.setValue` never reaches during a test — it now reads keys through `QSettings` after `sync()`. INV-2 leg (d)'s wait was placed where it could not secure its own strict comparison, making it flaky against a *correct* implementation. §12 claimed the screenshots need **no change** because reveal is off by default — true of the number, false of the tab, which gains a visible checkbox. §4.2 restated FIBR-0113 §4.1's never-reach-the-form rule near-verbatim **while both documents claimed sole ownership of it**; the restatement is deleted in favour of a pointer. Grew 508 → 523 lines. Not converged — loop 3 owed. |
| 3 | 2026-07-30 | 3, joint with FIBR-0113 (same partition, cold) | 0 | 6 | 6 | 9 | 21 verified, 0 unverified. All fixed. **Origin split: ~11 draft defects, ~7 from loop 2's own fixes.** Draft defects have now fallen 30 → 17 → 11 across the three loops, so the sweep is outrunning the fixes and neither stop-trigger fires. This document's share, and the headline is the most consequential finding of the run: **the reveal's copy path escapes the auto-hide entirely, and §12's T13 amendment as written would have hidden that.** T13's mitigation continues "A copied value is **auto-cleared** after a configurable timeout (default 30s)" — true only of the transactions list. `ClipboardAutoClear` is constructed in exactly two places (verified: `main_window.py`, `transactions.py`), both for that surface, and Ctrl+C out of a `Normal`-echo `QLineEdit` uses Qt's built-in copy without passing through it. So an account number copied during a 30-second reveal outlives the reveal indefinitely, and amending T13 to name the new copyable surface *without* that clause would leave the row reading as though the timeout covered it. §6 gains the exposure as a named, deliberate gap. Second: **the codebase already contains this control and the spec never said so** — `ui/export_dialog.py::ExportDialog._on_show_toggled` is the same `QCheckBox.toggled` → `Normal`/`Password` idiom, so §4.2 now cites it as the precedent (`coding.md` §1.3) and states that the divergent label is deliberate. Third: INV-4 guarantees a reveal preserves an in-progress edit, but its leg drove only the *manual* toggle — while §8 has explicitly decided a re-mask may land mid-edit, so the unattended path is the one that matters; the leg now repeats itself off `_reveal_timer.timeout`. INV-2 leg (b) did not say *when* it reads `interval()`, and the precedent's own shape (`start(seconds * 1000)` per use) leaves it at `0` until the first reveal — the leg would have gone red against working code. §11 gained the missing `finally` row (10 / 5 → **11 / 6**). Three facts were stated in full in both §12s — the screenshot inventory, the README heading, the whole no-build-plan paragraph — and are now pointers, with spec-format §2's third clause ("anything a second person will execute") answered rather than elided. Grew 523 → 560 lines. Not converged — loop 4 owed. |
