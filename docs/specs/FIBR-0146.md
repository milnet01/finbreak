# FIBR-0146 — Statement date-format auto-detect + confirm (import robustness)

> **Status:** ✅ **CLEARED FOR CODE** — `/cold-eyes` ran the full project cap of
> **7 loops × 3 cold lanes = 21 independent reviews** (`finbreak/CLAUDE.md`;
> global CLAUDE.md § 14). The **accuracy lane returned zero findings from loop 5
> on**, and the algorithm/money-math lane confirmed **no remaining silent-wrong-day
> path** from loop 5 on; the money-critical substance (every `strptime` claim, the
> ambiguity guarantee, the empty-format 1900-01-01 trap) is verified airtight.
> Loops 6–7 surfaced only **UI-wiring precision** (Qt signal ownership, widget
> visibility, banner wording) — all fixed in-loop; that class is validated
> definitively by the pytest-qt build. Every verified finding across
> CRITICAL/HIGH/MEDIUM/LOW was fixed in its loop. Ready for TDD. See the Cold-eyes
> loop log. This spec numbers its own **D1–D8**; **D4–D8 now live in the
> companion below**, keeping those numbers (ids are permanent — see the split
> row at the end of the loop log).
> **Phase:** a bug-fix + small robustness feature on the FIBR-0007/0009 import
> path. **Kind:** fix.
> **Source:** roadmap **FIBR-0146** — reported 2026-07-16 by an external Windows
> tester: a PDF statement imported with **every** one of 165 rows in Error
> ("0 new · 0 duplicate · 165 error"), the Description column showing the raw
> Python message `time data '20 …' does not match format …`. Root cause
> (full mechanism + cites in **Context**): the import date box defaults to
> `%Y-%m-%d` (`ui/import_wizard.py:163`) while the tester's bank prints dates
> day-first (the failing values march 20, 21, 22 … 31, then 02, 03 — a DD-first
> statement rolling over a month), so every row's `strptime` fails and the raw
> error text is shown. **User
> decision (2026-07-16):** approach = **"auto-detect + confirm"** — read the
> actual dates, pre-select a plain-English format in a friendly picker (never a
> raw `%`-box for the normal user), show the parsed dates so a wrong guess is
> caught before import (**never a silent wrong-day**), and fall back to the
> picker when genuinely ambiguous.
> **Security:** **none new.** No key handling, no plaintext-to-disk, **no
> network.** Detection is a pure in-memory `strptime` over date strings the
> import path already holds; it reads/writes no file and touches no vault. The
> FIBR-0054 `_network_offenders` source scan stays green (no new `urllib`
> importer).
> **Roadmap:** [ROADMAP.md → FIBR-0146](../../ROADMAP.md).
> **Governed by:** `docs/standards/{coding,naming,testing,commits}.md` (no new
> pip dependency — stdlib `datetime`/`csv` only, so `dependencies.md` is n/a;
> `coding.md § 1.1` no scaffolding; `coding.md § 1.3` reuse-before-rewrite — the
> detector feeds the **unchanged** `ColumnMapping` / `CsvImporter.parse` /
> `preview` pipeline; `coding.md § 5.2` the `tr()` / layout rules), and
> [ADR-0005](../decisions/0005-csv-mapping-profiles.md) (per-bank mapping
> profiles — unchanged; this adds a *guess* for the `date_format` field a
> profile already carries).
> Builds directly on [FIBR-0007](FIBR-0007.md) (the `ColumnMapping` /
> `CsvImporter` / `ImportService.preview` pipeline this seeds), [FIBR-0009](FIBR-0009.md)
> (the PDF-extractor → CSV serialisation, D1: a PDF is imported "as a CSV lifted
> off a page", so a PDF reaches the same date-column read), and
> [FIBR-0057](../journal/FIBR-0057.md) (the preview-step where the banner lands;
> that item shipped with a journal, not a spec).

**Theme:** finbreak stops making a person type programmer date codes. Today, when
a bank's date layout doesn't match the box's default, the whole import fails with
a wall of raw error text (the mechanism is in Context; the tester hit exactly
this). This change reads the actual dates, **guesses** the layout, and offers a
**plain-English picker** ("20/07/2026", "2026-07-20", "20 Jul 2026", …) with the
guess pre-selected and a **live preview** of how the dates read — so the common
case just works, and a wrong guess is visible and fixable *before* import rather
than a mystery afterwards. Nothing about the money math changes; only the date
the importer reads, and only after the user can see it.

**Contents:** At a glance (INV-2, INV-3, INV-6) · Context · Design decisions
(D1–D3) · New symbols (signatures) · Deliverables · Test plan (detector,
importer) · Out of scope · Exit criteria · Dependencies · Verified code basis ·
Cold-eyes loop log.

**Companion:** [`FIBR-0146-wizard-date-step.md`](FIBR-0146-wizard-date-step.md)
— the import wizard's date step (**D4–D8**, **INV-1 / INV-4 / INV-5**, the
wizard symbols and the wizard test plan), split out on 2026-08-14 under
**FIBR-0272**. Same id, so every `FIBR-0146 D<n>` citation still resolves. This
file keeps the detector and importer contract, the shared Context, and every
frozen record (Verified code basis, Cold-eyes loop log) for both halves.

## At a glance — invariants

| INV | Guarantee | Upholds |
|-----|-----------|---------|
| **INV-2** | **Pure, deterministic, clock-free detector.** `detect_date_format(samples)` is a pure function: no I/O, no `date.today()`, no `locale.setlocale`, no randomness. It reads only its `samples` argument and the fixed module constant `KNOWN_DATE_FORMATS`. For a given `LC_TIME` locale the same `samples` always yield the same `DateFormatGuess`, and every ambiguity tie breaks by the **fixed candidate order** (never by dict iteration or input order), so the pre-selection is fully deterministic. (The detector never *calls* `locale.setlocale`; the `%b`/`%B` named-month entries do *read* the ambient `LC_TIME` when matching — a month-name mismatch under a different locale is a parse *failure*, never a wrong month — so cross-locale month-name handling is deferred with the rest of i18n, FIBR-0017.) There is **no** year-plausibility window or any other clock-derived input — the detector counts raw `strptime` successes only (D2). | testing.md (deterministic tests); FIBR-0142 INV-2 (pure detector precedent) |
| **INV-3** | **No raw parser internals reach the UI.** A per-row date-parse failure surfaces a **friendly** `RowError` naming the offending value — `could not read the date "<raw>"` — never the raw `strptime` / `%`-format message. `CsvImporter.parse` gains a dedicated date-parse `try` so the date failure is caught **separately** from the amount/`parse_transaction` failures. The amount **conversion** is worded in `CsvImporter._to_decimal` for the same reason (`could not read the amount "<raw>"` / `the amount cell is empty`, FIBR-0271) — it runs before `parse_transaction`, so that function's friendly wording is unreachable from CSV and `decimal.InvalidOperation`'s raw class-repr was reaching the user. `parse_transaction`'s remaining messages ("exactly one of debit / credit must be populated", etc.) are already human and unchanged. The whole-import **banner** (D7) fires when nothing landed (`new_count == 0 and duplicate_count == 0 and errors`) and points the user at the general remedy — for a mapped source (CSV, generic PDF) "go back and check the column mapping and the Date format"; for one that skips the map step (OFX, recognised SB PDF) the per-row reasons below (FIBR-0253) — turning 165 identical cryptic rows into **one** actionable sentence. | FIBR-0009 friendly-`ValueError` boundary; global CLAUDE.md § 0 (layman-facing) |
| **INV-6** | **No new dependency; format-neutral; existing pipeline untouched.** Detection is stdlib `datetime.strptime` only and feeds the **unchanged** `ColumnMapping` → `CsvImporter.parse` → `ImportService.preview` path — it produces a *guess for the `date_format` field*, changing no parse, dedup, or commit code. It applies to any file that reaches the mapping step: **CSV and PDF** (a PDF is serialised to CSV text and read through the same date column, FIBR-0009 D1). OFX and recognised Standard-Bank PDFs skip the map step (self-describing) and are unaffected apart from D7's banner wording (FIBR-0253). | FIBR-0009 D1; coding.md § 1.1 (shortest correct) |

## Context

The import wizard (`ui/import_wizard.py`) has three format paths (`_select_file`):

1. **OFX** — self-describing; parsed and jumped straight to preview, **skipping
   the map step**. No date-format box is ever shown. **Unaffected.**
2. **PDF** — a **recognised Standard Bank** statement is parsed by its own reader
   and skips the map step (**unaffected**); **any other PDF** is extracted to a
   CSV-text table (FIBR-0009 D1) and shown the map step — **in scope**.
3. **CSV** — an exact header-signature match auto-applies its saved profile and
   jumps to preview; **no match** shows the map step — **in scope**.

So the fix lives entirely at **the map step**, reached by an **unmatched CSV or
a generic (non-SB) PDF** — exactly the tester's case (an unrecognised bank, no
saved profile). At that step today, `date_format` is a free-text `QLineEdit`
pre-filled with `%Y-%m-%d` (`import_wizard.py:163`), and `_mapping_from_form`
reads `self._date_format.text().strip()`. The parse itself is
`datetime.strptime(row[mapping.date_column].strip(), mapping.date_format)`
(`csv_importer.py:74`); a failure becomes `RowError(row_number, str(exc))`
(`:88-89`), whose `str(exc)` is the raw `time data '…' does not match format '…'`.

The pipeline downstream of the mapping is **correct and stays untouched** — this
change only produces a better `date_format` guess and a friendlier failure.

## Design decisions

- **D1 — A pure detector module, mirroring `csv_importer`.** New
  `src/finbreak/importers/date_detect.py` — **no Qt, no vault**, unit-testable on
  plain strings the way `CsvImporter` is. It exports the ordered constant
  `KNOWN_DATE_FORMATS: list[tuple[str, str]]` (each `(example, fmt)` — the
  language-neutral example rendering and its `strptime` pattern) and the pure
  `detect_date_format(samples: Sequence[str]) -> DateFormatGuess` (a `Sequence`,
  not a one-shot iterable — D8/New symbols). The example
  strings render one **fixed unambiguous reference date** — day **20**, month
  **07**, year **2026** — so day-first ("20/07/2026") and month-first
  ("07/20/2026") are visually distinct in the picker.

- **D2 — The candidate list + the ambiguity + year-width rules.**
  `KNOWN_DATE_FORMATS` (ordered — ISO first, then day-first, then month-first,
  then named-month, then the 2-digit-year slash variants; the order is the
  ambiguity tiebreak, INV-2):

  | fmt | example (ref 2026-07-20) |
  |-----|--------------------------|
  | `%Y-%m-%d` | 2026-07-20 |
  | `%Y/%m/%d` | 2026/07/20 |
  | `%d/%m/%Y` | 20/07/2026 |
  | `%d-%m-%Y` | 20-07-2026 |
  | `%d.%m.%Y` | 20.07.2026 |
  | `%m/%d/%Y` | 07/20/2026 |
  | `%m-%d-%Y` | 07-20-2026 |
  | `%m.%d.%Y` | 07.20.2026 |
  | `%d %b %Y` | 20 Jul 2026 |
  | `%d %B %Y` | 20 July 2026 |
  | `%d-%b-%Y` | 20-Jul-2026 |
  | `%b %d, %Y` | Jul 20, 2026 |
  | `%B %d, %Y` | July 20, 2026 |
  | `%d/%m/%y` | 20/07/26 |
  | `%m/%d/%y` | 07/20/26 |

  **Detection:** strip each sample, drop blanks. For each candidate, count the
  non-blank samples it parses, where a sample **parses** iff `strptime` succeeds
  (no year window, no clock — INV-2). **2-digit and 4-digit year layouts are
  separated by `strptime` itself, not by any guard:** `%Y` matches **exactly four
  digits** and `%y` **exactly two**, and a leftover digit fails with "unconverted
  data remains" — so `strptime("20-07-26", "%d-%m-%Y")` **raises** (the 2-digit
  "26" is not four digits) and `strptime("20/07/2026", "%d/%m/%y")` **raises**
  ("unconverted data remains: 26"). A `%y`/`%Y` pattern therefore never claims a
  field of the wrong width, so including both the 4-digit and 2-digit variants
  adds no spurious cross-width match (both were confirmed against CPython 3.13,
  the 3.12+ target's stable `_strptime`). `DateFormatGuess.fmt` is the candidate
  with the **highest** parse-count, ties broken by the fixed list order above;
  `fmt` is `None` when **no** candidate parses **any** sample.
  `DateFormatGuess.ambiguous` is `True` when **two or more** candidates share the
  maximum parse-count (> 0). A **uniform-format** column (the in-scope case) ties
  on one of exactly two axes. **Numeric day-first vs month-first**, which
  survives only when **every** sampled day-number is ≤ 12 (a day-number > 12
  makes the month-first `%m` fail those rows → a lower count → no tie; `%m`
  matches only 1–12, confirmed). **Or `%b` vs `%B` in May**, where the
  abbreviated and full English month names are the same three letters, so
  `%d %b %Y` and `%d %B %Y` both parse every row of a May-only statement — a tie
  at any day-number, verified: `detect_date_format(["20 May 2026", "21 May 2026",
  "22 May 2026"])` → `ambiguous=True` while the same call over `"20 Jun 2026"` or
  `"20 January 2026"` → `ambiguous=False`. **That second tie is harmless to the
  parse and wrong for the nudge**: both candidates render May identically, so
  either guess reads the column correctly, but D6's "the day and month might be
  the other way around" describes an axis this tie is not on. Tracked as
  FIBR-0264; a monthly statement is normal input, so it is not a corner case.
  A **mixed-format** column (out of scope) could tie on further axes (e.g. 2- vs
  4-digit width), where the D6 wording is approximate — an accepted limitation
  for an out-of-scope input. Detection is best-effort *pre-selection* only; the picker + previews
  are always shown (INV-1), so a wrong guess is never acted on silently.

- **D3 — Friendly per-row date error (`csv_importer`).** Split the date parse out
  of the shared per-row `try`: bind `raw = row[mapping.date_column].strip()` (the
  stripped cell) and parse it in its own `try`, and on `ValueError` append a
  friendly `RowError` then `continue`: `f'could not read the date "{raw}"'` for a
  non-empty cell, or the clearer `"the date cell is empty"` when `raw == ""` (a
  blank date cell — common in PDF-serialised tables — which under a normal format
  raises `strptime` just like any other unparseable value; the empty-format case
  can't reach here, D4). Either message keeps the failure visible and drops the raw
  `%`-format. The **amount** parse + `parse_transaction` stay in their existing
  `try`, and **the amount branch is worded here too** — `CsvImporter._to_decimal`
  raises `could not read the amount "<raw>"`, or `the amount cell is empty`, for
  both amount styles (FIBR-0271). It has to: the cell becomes a `Decimal`
  **before** `parse_transaction`, so `parse_transaction`'s friendly "amount is not
  a valid number" is unreachable from CSV (it short-circuits on an already-built
  `Decimal`) and the raw `InvalidOperation` text — the literal
  `[<class 'decimal.ConversionSyntax'>]` — reached the preview's Status column
  instead. `parse_transaction`'s remaining messages ("occurred_on must be a valid
  ISO-8601 date", the debit/credit pair rules) are already human and are **not**
  changed; `parse_transaction`'s own date
  re-validation cannot leak raw `%`-format text either, since the `occurred_on`
  passed to it is the freshly-built `strptime(...).date().isoformat()` string, so a
  date *failure* there can't occur). No change to `RowError`'s shape (still
  `row_number` + `reason`), so OFX and the shared `ImportService` are untouched
  (INV-6). The ragged-row guard above is unchanged.

**D4–D8** — the picker, the auto-detect wiring, the live preview, the
whole-import banner and the sampling bound — are in
[`FIBR-0146-wizard-date-step.md`](FIBR-0146-wizard-date-step.md).

## New symbols (signatures)

```python
# importers/date_detect.py  (pure — no Qt, no vault)
# (example rendering of ref date 2026-07-20, strptime pattern) — ORDERED (D2);
# the order is the ambiguity tiebreak (INV-2).
KNOWN_DATE_FORMATS: list[tuple[str, str]] = [ ("2026-07-20", "%Y-%m-%d"), ... ]

@dataclass(frozen=True)
class DateFormatGuess:
    fmt: str | None      # best-guess strptime pattern, or None if nothing parsed any sample
    ambiguous: bool      # >=2 formats tie for the max parse-count (> 0)

def detect_date_format(samples: Sequence[str]) -> DateFormatGuess:
    """Pure best-effort guess of the strptime format for a column of date strings.
    `samples` is a Sequence (NOT a one-shot Iterable): the scan is format-outer —
    each candidate re-scans all samples (D8) — so a generator would exhaust after
    the first format and corrupt the count. A sample counts for a format iff
    strptime succeeds (no year window, no clock). fmt = the highest parse-count
    format; ties break by KNOWN_DATE_FORMATS order (INV-2); None if nothing parses
    any sample. Deterministic."""

# importers/csv_importer.py
@staticmethod
def _to_decimal(raw: str) -> Decimal: ...        # the INV-3 boundary on the amount cell — friendly ValueError, never InvalidOperation's class-repr (D3)
```

`ui/import_wizard.py`'s new privates are in
[`FIBR-0146-wizard-date-step.md`](FIBR-0146-wizard-date-step.md).

## Deliverables

1. **`importers/date_detect.py`** — `KNOWN_DATE_FORMATS`, `DateFormatGuess`,
   `detect_date_format` (D1/D2) — no year constants (INV-2: no year window).
2. **`importers/csv_importer.py`** — the split date-parse `try` + friendly
   `RowError` (D3). No signature/shape change.
3. **`ui/import_wizard.py`** and 4. **`services/import_.py`** — see
   [`FIBR-0146-wizard-date-step.md`](FIBR-0146-wizard-date-step.md).
5. **Tests** — `tests/features/import_date_detect/` (spec.md + the suites below).
6. **Docs** — ROADMAP FIBR-0146 flip on close; CHANGELOG `Fixed` entry; journal.
   `README.md` import bullet unchanged (PDF/CSV import already listed).

## Test plan

**Pure detector (`detect_date_format`) — no Qt, plain strings (the heart):**
- Day-first numeric with a **disambiguating** day > 12 (`["20/07/2026",
  "21/07/2026", "31/07/2026", "02/08/2026"]`, the tester's shape) → `fmt =
  "%d/%m/%Y"`, `ambiguous = False` (month-first fails "20"/"21"/"31").
- ISO (`"2026-07-20"`) → `%Y-%m-%d`; slashes-ISO → `%Y/%m/%d`.
- Month-first US with a disambiguating **second** field > 12 (`"07/20/2026"`) →
  `%m/%d/%Y`, not ambiguous.
- **Dotted, both directions:** `"20.07.2026"` → `%d.%m.%Y`; `"07.20.2026"` →
  `%m.%d.%Y`; a dotted all-days-≤-12 column (`["05.06.2026", "07.08.2026"]`) →
  `ambiguous = True` (the `%m.%d.%Y` counterpart exists, so the tie — and the
  nudge — fire, matching the slash/dash behaviour; no silent day-first).
- **2-digit-year ambiguity:** an all-days-≤-12 2-digit column (`["05/06/26",
  "07/08/26"]`) → `ambiguous = True` (`%d/%m/%y` vs `%m/%d/%y` tie), so the `%y`
  variants get the same nudge as their 4-digit counterparts.
- **Ambiguous** all-days-≤-12 (`["05/06/2026", "07/08/2026"]`) → `ambiguous =
  True`; `fmt` is the **fixed-order** winner (`%d/%m/%Y`, ahead of `%m/%d/%Y`),
  deterministic across runs and input order.
- Named-month: `"20 Jul 2026"` → `%d %b %Y`; `"20 July 2026"` → `%d %B %Y`;
  `"Jul 20, 2026"` → `%b %d, %Y`; `"20-Jul-2026"` → `%d-%b-%Y`.
- **2-digit vs 4-digit year cleanly separated (no guard):** `["20/07/26"]` → `fmt
  = "%d/%m/%y"`, `ambiguous = False` — the 4-digit `%d/%m/%Y` **rejects** the
  2-digit year (`%Y` needs four digits), and conversely a 4-digit `["20/07/2026"]`
  is rejected by `%d/%m/%y` ("unconverted data remains"), so the two never
  cross-claim. (A dashed/dotted 2-digit string like `"20-07-26"` matches **no**
  candidate → `fmt is None`, since the known 2-digit formats are slash-only,
  out-of-scope note below.)
- **No match:** `["not a date", "banana"]` → `fmt is None`, `ambiguous = False`.
- **Empty / blank:** `[]` and `["", "  "]` → `fmt is None` (no samples).
- **Mixed / one junk row:** a clean day-first column with **one** garbage cell →
  the correct format still wins on parse-count (guards against a single bad row
  vetoing the guess); `fmt` correct.
- **Determinism:** the same `samples` yield an identical `DateFormatGuess` across
  calls and regardless of sample order (INV-2).
- **Clock-free (INV-2 falsifier):** a far-past and a far-future day-first column
  (`["20/07/1998", "21/07/1998"]` and `["20/07/2099", "21/07/2099"]`) each yield
  `fmt = "%d/%m/%Y"` — a hidden `date.today()`/year-window dependency would flip a
  decades-out year and fail this, so the "no clock, no year window" claim is
  falsifiable, not just asserted. (The sibling "no `locale.setlocale`, no
  randomness" clauses of INV-2 are code-review-verified — the detector calls
  neither — rather than test-gated.)

**Importer (`CsvImporter.parse`) — friendly date error (D3/INV-3):**
- A row whose date doesn't match `mapping.date_format` yields
  `RowError(reason == 'could not read the date "<raw>"')` — the reason contains the
  raw value and **not** the raw `strptime` internals: assert absence of
  `"does not match format"` and of a format token (`"%Y"`, `"%d"`, …), **not** a
  bare `"%"` (a junk cell like `"50%"` embeds a literal `%` in `<raw>` without any
  parser leak). A blank date cell (`raw == ""`) yields the `"the date cell is
  empty"` message (D3).
- An **amount** failure asserts the message a person can act on, and asserting
  merely that a reason is non-empty is what let `[<class
  'decimal.ConversionSyntax'>]` ship (FIBR-0271): a non-numeric cell yields
  `could not read the amount "<raw>"` under **both** amount styles, a blank one
  `the amount cell is empty`, and no reason contains `decimal`, `class ` or
  `Syntax`. Both debit+credit populated keeps its existing human message — the
  split didn't regress
  amount-error text.
- A valid row still parses (no behaviour change for the happy path).
**Wizard (`ImportWizardWidget`, `qtbot`)** — in
[`FIBR-0146-wizard-date-step.md`](FIBR-0146-wizard-date-step.md).

## Out of scope (v1 — YAGNI)

- **English month names only** — `%b`/`%B` parse English (`strptime` is
  locale-sensitive and the detector deliberately does **not** mutate locale,
  INV-2). Localised month names are deferred to FIBR-0017 (i18n).
- **No per-row mixed formats** — one `date_format` per import (a statement's date
  column is uniform); a file mixing layouts row-to-row is not auto-handled.
- **No auto-resolution of a genuinely ambiguous column** (all day-numbers ≤ 12) —
  by design the user confirms via the live preview (INV-1); finbreak must never
  silently pick day-first vs month-first when the data can't tell them apart.
- **No time-of-day / datetime parsing** — dates only (the ledger stores a date).
- **No detection of the amount / debit-credit columns** — this fix is date-format
  only; column-role guessing is a separate future idea.
- **2-digit-year layouts are slash-only in the known list** (`%d/%m/%y`,
  `%m/%d/%y`) — a dash- or dot-separated 2-digit year (`20-07-26`, `20.07.26`) is
  reachable only via "Custom…", not auto-detected. The two slash variants cover
  the common case; the long tail is the Custom escape hatch's job (INV-4). A
  2-digit year uses Python's fixed 1969–2068 century pivot (`…/69` → 1969, `…/68`
  → 2068) — deterministic and clock-free (INV-2 holds), and shown in the live
  preview (INV-1) so a surprising century is caught before import.

## Exit criteria

- `detect_date_format` pure-unit suite green (every Test-plan detector case,
  including the 2-digit/4-digit separation and the ambiguity tiebreak).
- `CsvImporter.parse` friendly-date-error test green; amount-error text
  unregressed.
- `ImportWizardWidget` `qtbot` suite green (auto-detect seed, live preview,
  Custom round-trip, matched-profile-wins, banner, PDF path).
- Full gate green (`./scripts/ci-local.sh`: ruff/format/bandit/pip-audit/gitleaks/
  mypy 0 + pytest), no new `urllib` offender.
- `/audit` 0 actionable + `/indie-review` cold lanes folded.
- ROADMAP FIBR-0146 → ✅; CHANGELOG `Fixed` entry; journal
  `docs/journal/FIBR-0146.md`.

## Dependencies

FIBR-0007 (`ColumnMapping` / `CsvImporter` / `ImportService.preview` — the
unchanged pipeline this seeds), FIBR-0009 (the PDF→CSV-text extractor, so a PDF
reaches the same date-column read, D1/INV-6), FIBR-0057 (the preview step the
banner lands on). No new pip dependency (stdlib `datetime`/`csv` only).

## Verified code basis (2026-07-16, pre-cold-eyes)

- `src/finbreak/ui/import_wizard.py:163` — `self._date_format = QLineEdit("%Y-%m-%d")`
  (the box replaced by the picker, D4).
- `:143-194` `_build_map_step` (the form the picker + live-preview label + banner
  slot into); `:172` the **Date column** combo `_column_combos["date"]` (the
  `_on_date_column_changed` connect, D5).
- `:647-671` `_mapping_from_form` (`:651` reads `self._date_format.text().strip()`
  today → `_selected_date_format()`, D4).
- `:635` `_on_map_next` → `:673-681` `_run_preview`, whose `except (ValueError,
  FinbreakError)` (`:678`) shows the message on the map step — the seam that
  surfaces `_validate_mapping`'s "choose a date format" (D4 empty-format reject).
- `:598-613` `_apply_profile_to_combos` (`:612` `self._date_format.setText(
  mapping.date_format)` → the picker-select/Custom-fill, D4/INV-4).
- `:279-328` `_select_file` (`:327-328` the CSV **no-match** → `_goto_step(
  _STEP_MAP)`, the D5 trigger point (a)).
- `:509-552` `_continue_after_decrypt` (`:552` the no-match → `_goto_step(
  _STEP_MAP)` that actually navigates — the D5 trigger point (b)) + `:577-588`
  `_apply_pdf_table` / `:590-596` `_on_pdf_table_changed` (which *return* the match
  result and re-fill the combos; they don't navigate — but `_on_pdf_table_changed`'s
  no-match path now also triggers auto-detect, D5).
- `:694-713` `_apply_preview_counts` (`:700` `_summary_label`; the D7 banner home).
- `:731-767` `_fill_preview_table` (the preview-step Date column that shows parsed
  dates — the full "confirm", INV-1).
- `src/finbreak/importers/csv_importer.py:72-90` — the per-row `try`; `:74`
  `datetime.strptime(row[mapping.date_column].strip(), mapping.date_format)`;
  `:88-89` `except (ValueError, InvalidOperation) as exc: errors.append(RowError(
  row_number, str(exc)))` (split for the friendly date error, D3).
- `src/finbreak/importers/base.py:18-25` — `RowError(row_number, reason)`
  (shape unchanged, D3/INV-6).
- `src/finbreak/services/import_.py:59-71` — `ImportPreview(new_count,
  duplicate_count, errors, …)` (the counts the banner reads, D7); `:165-173`
  `preview` (the unchanged parse entry, INV-6); `:340-372` `_validate_mapping`
  (gains the non-empty `date_format.strip()` check → `"choose a date format"`, D4).
- `src/finbreak/models.py:116-133` — `ColumnMapping(… date_format …)` (the field
  the detector guesses; unchanged).
- `src/finbreak/importers/pdf_importer.py:115-123` `table_to_text` /
  `:577-588` (wizard) `_apply_pdf_table` — the PDF→CSV-text serialisation that
  makes `self._text` the detector's input for a PDF (INV-6/D1).

## Cold-eyes loop log

Three cold lanes per loop (A: accuracy / doc-vs-code / cross-ref · B: date-parsing
correctness / algorithm / money-math / edge-cases / perf · C: dedup / completeness
/ testability / structure / token-efficiency), each briefed cold — no prior-loop
findings shared. A run-scoped **shared read-cache** (base brief + cited-code
regions + verified `strptime` facts) was materialised once and reused across
loops, so sibling lanes didn't re-read the source each pass. Every verified finding
across CRITICAL/HIGH/MEDIUM/LOW was fixed in its loop; INFO surfaced only.

| Loop | C | H | M | L | INFO | Headline fixes |
|------|---|---|---|---|------|----------------|
| 1 | 2 | 1 | 3 | 8 | 3 | **Removed the plausible-year guard** — its rationale rested on a false claim (`strptime("20-07-26","%d-%m-%Y")` *raises*; `%Y` is exactly 4 digits, so strptime itself separates 2/4-digit years); fixed the impossible dashed-2-digit fixture; corrected the FIBR-0057 link (journal, not spec); pinned the `fmt=None` default + ambiguity-flag lifetime + empty-samples text. |
| 2 | 0 | 2 | 4 | 6 | 3 | Two stale "year-constant"/"plausible-year" vestiges contradicting INV-2; `detect_date_format(Iterable)` → `Sequence` (a generator exhausts after the first format) + pinned format-outer iteration; PDF-table-switch re-detect; far-future clock-free test. |
| 3 | 0 | 1 | 4 | 6 | 3 | D1's leftover `Iterable` contradiction; **added dotted `%m.%d.%Y`** (a US-dotted statement was silently read day-first with no ambiguity flag); resolved D5 preview-refresh ownership; matched-PDF-table stale-preview; partial-parse + empty-Custom + `%y`-pivot; closed the INV-3 `parse_transaction` question. |
| 4 | 0 | 0 | 3 | 3 | 2 | **`strptime("","")` → 1900-01-01 phantom-date trap** — empty format now rejected at `_validate_mapping`; corrected the wrong "every row errors" claim; split D6's no-samples vs parse-fail messages; exact counts (15/750). |
| 5 | 0 | 0 | 1 | 4 | 5 | D6 "parses all three" was unsatisfiable with <3 samples → "all shown"; pinned the "Date format" row label; "the date cell is empty" message; hardened the INV-3 assertion (format-token, not bare `%`); short-column + 2-digit-ambiguity + refresh-once tests. |
| 6 | 0 | 0 | 2 | 6 | 3 | `_date_samples` now returns **stripped** values (preview can't falsely reject a padded cell the importer accepts); dropped the unfalsifiable perf SLO for the self-proving constant bound; broadened the banner; documented the on-entry date-column dependency. |
| 7 | 0 | 0 | 3 | 3 | 2 | **Polish/wiring convergence** — banner names the general column-mapping remedy (an amount mis-map also zeroes the import); single-owner signal wiring (no double refresh); `_apply_profile_to_combos` reveals the Custom field; qualified INV-2 determinism for a fixed `LC_TIME`; D5 fire-points listed. |
| 8 | — | — | — | — | — | **`review-contract`, 2 cold lanes, post-FIBR-0253 amendment** (severity columns retired upstream; this loop's tally is Q1 ×2 · Q2 ×1 · Q4 ×1 — 4 verified, 0 dismissed, all fixed). Both lanes independently found the amendment's **central claim false**: D7/INV-3 said only the CSV path has a map step, but a **generic (non-SB) PDF** is extracted to a CSV-text table and mapped — this spec's own bug report — so keying the remedy on CSV-ness denied it to the very user D7 was written for. Refixed in code (`_csv_source` → `_has_mapping_step`, also set past the SB reader) with a red-proven test leg on `non_sb.pdf`. Also: D8 specified a bare `csv.DictReader` the tree replaced with the guarded `read_rows` (an unguarded `csv.Error` escapes the Qt slot and kills the app); D2's "the only tie is day-first vs month-first, and only when every day-number ≤ 12" is false for **May**, where `%b` and `%B` are the same three letters — verified by running the detector, day-numbers 20–22, `ambiguous=True` (nudge-wording consequence filed as FIBR-0264); and the Test plan had no clause for the source-dependent banner text, so the CSV-only build passed every listed test. |
| 9 | — | — | — | — | — | **`review-contract` loop 2, 1 cold lane, rebuilt packet** (tally Q2 ×1 · Q3 ×1 · Q4 ×1 — 3 verified, 0 dismissed, all fixed). **Every finding was collateral from loop 8's own fixes, and none was a fresh draft defect** — the lane confirmed both banner strings verbatim against `_banner_text()` and the flag's set/clear sites, then found what the repair had left behind. The Test-plan clause loop 8 added tested CSV vs recognised-SB only: both legs pass under a CSV-only flag, so the plan could not falsify D7's own central claim and the generic-PDF regression would have shipped green (the shipped test already had that leg; the plan did not). New symbols listed neither `_has_mapping_step` nor `_banner_text()`, and nothing stated the flag's value before the first pick — an implementer defaulting it `True` reinstates exactly the advice FIBR-0253 removed. And Out of scope + INV-6 both still said OFX and recognised-SB PDFs were *unaffected*, which D7 had just stopped being true. |
| 10 | — | — | — | — | — | **`review-contract` loop 3, 1 cold lane** (tally Q1 ×1 · Q3 ×1 · Q4 ×1 — 3 verified, 0 dismissed, all fixed). **The FIBR-0253 amendment came back clean**: the lane checked both banner sentences verbatim against `_banner_text()` and the flag's clear/set sites in `_select_file` and `_continue_after_decrypt`, and raised nothing — the convergence this run owed on the thing it was called for. The three findings were elsewhere. One was loop-8's own leftover: D5 still named the `csv.DictReader` that loop had just removed from D8, so the two halves of the document prescribed different readers and the D5 half is the one that kills the app. **The loop also refuted a sentence loop 8 added without running it** — "on a raise the map step shows the friendly message" holds only on the CSV pick; `_continue_after_decrypt` and `_on_date_column_changed` call `_autodetect_date_format` unguarded, so the `ValueError` escapes the slot (filed **FIBR-0268**, and D8 now states the real coverage). Third: D4 specified the Custom field's reveal but never its hide, though the blocker suppresses the slot in both directions and the code hides it. **Stopped here at the skill's default cap of 3**, with the amendment converged and the tail filed. |
| 11 | — | — | — | — | — | **`review-contract`, 1 cold lane, post-FIBR-0268 amendment** (tally Q1 ×2 · Q2 ×2 — 4 verified, 0 dismissed, all fixed; 4 lane open questions, all resolved clean). Run after the FIBR-0268 fix landed and D8 was amended to state the two catchers. **Two findings were that amendment's own collateral**: D5 still enumerated *three* fire points calling the pair directly, where the tree has four and every one goes through the new `_refresh_date_ui`; and New symbols never listed that method, so an implementer building from the signatures block wires the five slots unguarded and reinstates the crash D8's prose describes. **Two were pre-existing and older than this run**: `_has_mapping_step`'s comment said it is set wherever the map step is *really reached*, which `_ask_mapping` reaches without setting it — resolved by verifying the batch never reaches the banner at all (`_on_map_next` hands to `_answer_batch_mapping` and returns before `_run_preview`), so the flag is right and the rule was over-stated; and D6's ambiguity nudge was "prepended **iff** `_date_ambiguous`", where both preview fallbacks return before the prepend. The lane confirmed D2's 15-row table, both D7 banner sentences, D4's empty-format reject and D8's two-catcher claim verbatim against the tree. |
| 12 | — | — | — | — | — | **`review-contract` loop 2, 1 cold lane** (tally Q1 ×3 · Q2 ×2 — 5 verified, 0 dismissed, all fixed; 2 open questions, both resolved). **Two were loop 11's own collateral**: the `_has_mapping_step` gloss loop 11 rewrote said "routes that reach the map step", where `_select_file` sets the flag BEFORE the profile test so a MATCHED CSV (which never shows the map step) carries it too — D7 had it right as the CSV fall-through and the gloss did not; and the D5(d) bullet loop 11 added never said what carries between batch files. **Three were fresh regions neither earlier loop reached.** D8 claimed detection cost is "constant in the row count … a self-proving bound, no separate perf test needed", but D8's own mandated `read_rows` is `list(csv.DictReader(...))` — the read is linear and re-runs on every keystroke in the Custom field, so refresh cost is O(rows) (code half filed as FIBR-0269). D4's empty-format reject was stated only via `_run_preview`, which the batch never reaches — `BatchImportService._scan_csv` calls the same validator itself and fails that one file, so the trap is closed twice and the doc described one. D5(c)'s "overriding any prior manual format pick" contradicted D5's "only when `guess.fmt` is non-`None`". **4a step 3 caught one of this loop's own fixes before it landed**: the new D5(d) sentence asserted nothing carries between batch files; running the refuting case showed `_ask_mapping` clears the Custom field but not the combo, so a previous file's selection stands when the next detects nothing. Reworded to say so. Out of scope, filed: the mapped-source banner says "Go back" and the wizard has no back control (FIBR-0270). |
| 13 | — | — | — | — | — | **`review-contract` loop 3, 1 cold lane — run cap reached (3), tail empty** (tally Q1 ×2 — 2 verified, 0 dismissed, both fixed; 3 open questions, all resolved clean). **Neither finding was collateral; both were regions no earlier loop had read.** D3/INV-3 said the amount branch's messages were already human and unchanged — false: `CsvImporter` builds the amount with `Decimal(cell)` BEFORE `parse_transaction`, so a non-numeric cell surfaced `InvalidOperation`'s own text, the literal `[<class 'decimal.ConversionSyntax'>]`, in the preview's Status column, and `parse_transaction`'s friendly wording is unreachable from CSV. **Reproduced, then fixed in code rather than documented** (`CsvImporter._to_decimal`, FIBR-0271) — INV-3's own headline was false one branch over, and the test that should have caught it asserted only that the reason was non-empty. Second: four passages said the picker "stays at the ISO default" on a `None` guess; nothing resets `self._date_format` per pick, so a second file that detects nothing keeps the previous file's format (visible on the map step before import, so INV-1 holds). Open questions resolved clean: D6's dual `currentIndexChanged`/`textChanged` connect is real (`:303-304`); `_ask_mapping`'s unguarded `read_header` is unreachable, because `_scan_csv` reads the header first inside SCAN's net. **Cap note:** three loops, 11 verified findings, and each loop reached fresh regions rather than re-finding old ones — at 700+ lines against siblings of ~400-650 that is the size signal, not a document that keeps regressing. Filed as FIBR-0272 rather than looping further. |
| 13-split | 2026-08-14 | **none — no reviewer dispatched** | — | — | — | — | Not a review loop. The response to the loop-13 cap, taken instead of dispatching a fourth pass on the document as it stood (ROADMAP **FIBR-0272**, and `spec-format.md` §5.4, whose remedy for a spec that outruns the review's cap is to split along §3.6's by-concern seams rather than keep looping). The wizard half — **D4–D8**, **INV-1 / INV-4 / INV-5**, the `ui/import_wizard.py` symbols and the wizard test plan — moved to **[`FIBR-0146-wizard-date-step.md`](FIBR-0146-wizard-date-step.md)**, a companion under the **same id**, so the ~66 existing `FIBR-0146 D<n>` citations in code, tests and the ROADMAP resolve unchanged and none was touched. **Every moved line is verbatim**: no invariant re-cut, no decision reworded, no id renumbered — which is why the companion's decisions start at D4 and its invariants skip INV-2/3/6. A script asserted all 27 section boundaries before writing and then re-checked that no substantive line of the 746 was absent from both halves. 746 → 422 + 413 lines, both inside the ~400–650 range of sibling specs. **The bullet's candidate seam was the detector half; measurement inverted it** — extracting the detector (~140 lines) would have left this file near 600 and barely moved the gate cost, while the wizard half is both the larger (~300 lines) and, per FIBR-0272, the one that keeps drifting against the code. The shared **Context**, the detector and importer contract, and every frozen record stay here: one home, one copy. Rows 1–13 above are frozen records of reviews of the combined document and are left as written. |

**Convergence note:** the money-critical core (strptime correctness, the ambiguity /
no-silent-wrong-day guarantee, the 1900 trap) converged by loop 4; lane A returned
zero findings loops 5–7 and lane B confirmed no remaining wrong-day path loops 5–7.
Loops 6–7 surfaced only UI-wiring precision, each fixed in-loop. The run hit the
project cap of 7; the user elected to proceed to TDD rather than spend further loops
on the wiring class (which pytest-qt validates against real widgets). Not a forced
convergence — the substantive class was exhausted; only the implementation-wiring
tail remained, and that is better nailed down in code.
