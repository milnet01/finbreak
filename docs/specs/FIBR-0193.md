# FIBR-0193 — Store an optional account number and note on each account

**Status:** spec draft (2026-07-28).
**Kind:** feature.
**Source:** ROADMAP FIBR-0193, split out of FIBR-0113 on 2026-07-28 executing
`/cold-eyes` loop 5's recommendation (`docs/reviews/FIBR-0113-cold-eyes-loop5.md`).
**Blocker for:** FIBR-0113 (the Accounts table renders and edits these two
columns, so it cannot start until they exist).
**Pairs with:** FIBR-0086 (§9 — its account-number *storage* half lands here;
its detection + matching half does not), FIBR-0005 (§12 — this widens the
`Account` shape and the repository signatures that spec pins).

**Layman:** Each account gets two new optional boxes in the encrypted vault — a
reference account number and a free-text note. This item only builds the
storage; the screen that shows and edits them is FIBR-0113.

## 1. Goal

`models.Account` carries an optional `account_number` and `note`, stored in two
new nullable `accounts` columns behind schema migration v13, and readable and
writable through `AccountRepository` and `AccountService` on both the add and
the update path. A blank field is stored as SQL `NULL`, not as an empty string.
No new user-visible behaviour arrives: nothing displays or edits either field
until FIBR-0113 ships. One existing surface does change, invisibly —
`ui/accounts.py::AccountsWidget._on_update` reads the selected account's current
values and passes them straight back, so an Update that edits only the name or
type leaves both stored fields intact (§3 decision 5, INV-7).

## 2. Problem

`models.py::Account` carries `id`, `name`, `type`, `created_at` and nothing
else. Two consequences — the first raised while dogfooding v0.1.0 (FIBR-0086,
`Source: user-request-2026-07-11`), the second by user request on 2026-07-12
(FIBR-0113):

1. **There is nowhere to put an account number.** A user with two "Cheque"
   accounts at different banks has no way to tell them apart, and FIBR-0086's
   import auto-detect has no column to match a statement's account number
   against.
2. **There is nowhere to put a note.** Same cause — the row has no free-text
   field of any kind.

The gap is purely at the storage layer: `AccountRepository.list_all()` and
`.get()` select four columns, `.add()` writes three (`name`, `type`,
`created_at`) and `.update()` two, and
`AccountService.add_account` / `.update_account` pass exactly those through.

## 3. Scope decisions

Settled with the user on 2026-07-28 (1), or by the author from §2 (2–4):

1. **Both fields live in the encrypted vault, not the plaintext window INI**
   (user). An account number is financial reference data. The vault is
   SQLCipher, so both columns are encrypted at rest by the master key with no
   redundant second layer — the same posture `accounts.statement_pdf_password`
   already takes (FIBR-0009 D5).
2. **`AccountService.update_account`'s two new parameters are REQUIRED, not
   defaulted** (author). `AccountRepository.update` issues an unconditional
   `UPDATE … SET`, so a defaulted `None` would make every existing
   three-argument call silently erase a stored account number and note. There
   are six such call sites today, listed in §4.2. Required parameters turn each
   into a loud failure instead of silent data loss in a money app — the same
   loud-refusal posture the rest of the codebase takes. §11 records that this is
   only *partly* mechanically caught.
3. **`add_account`'s two parameters are optional keyword arguments** (author).
   Its callers create an account that genuinely has neither field, and `add`
   issues an `INSERT`, where a default is harmless — an omitted column is
   written `NULL`, which is the value the caller meant. The asymmetry with
   decision 2 is deliberate and is the whole point: the danger is the
   unconditional overwrite, not the insert.
4. **Neither field is validated for format, and neither participates in the
   duplicate-name check** (author). An account number is a reference the user
   types, not a matching key — FIBR-0086 owns normalisation and matching, and
   normalising here would store a value the user did not type while that item
   is still unbuilt. **One carve-out, and it is not optional:** §4.2's
   `_normalise_optional` strips *outer* whitespace and collapses an empty result
   to `NULL`, which INV-5 requires and FIBR-0113 INV-12 makes user-visible. That
   is a blank-versus-`NULL` decision, not format validation — but an implementer
   reading this decision alone would build no strip at all and trip INV-5.
5. **`ui/accounts.py::AccountsWidget._on_update` is updated *in this item*, to
   read the selected account's current `account_number` / `note` and pass them
   back unchanged** (author). It is the only production caller of
   `update_account`, and decision 2 makes the two parameters required — so
   leaving it three-argument raises `TypeError` on every "Update selected"
   click (its `except (ValueError, FinbreakError)` does not catch that) and
   fails the existing FIBR-0005 INV-7f `qtbot` leg. Passing `None, None`
   instead is the exact unconditional-overwrite erasure decision 2 exists to
   prevent — harmless *today* only because no surface can set either field yet,
   which is precisely the kind of "safe for now" landmine this spec refuses.
   Read-and-pass-back is the shortest change that is correct at every point in
   the sequence.

   **The read goes through two new item-data roles, not a new service getter.**
   `AccountsWidget` today stashes each account's fields on its
   `QListWidgetItem` under `_ACCOUNT_ID_ROLE`, `_ACCOUNT_NAME_ROLE`,
   `_ACCOUNT_TYPE_ROLE` and `_ACCOUNT_HAS_PW_ROLE`, all written in `_refresh`
   and each read back by at least one handler — `_on_update` reads the id role,
   `_on_selection_changed` reads name, type **and the has-password flag** (it
   gates the Forget button), `_on_delete` and `_on_forget_password` read the name
   and the id, and `_select_account` reads the id over every item. Not an
   exhaustive map of the reads — the point is only that every role in the set
   has a reader. This item adds
   `_ACCOUNT_NUMBER_ROLE` and `_ACCOUNT_NOTE_ROLE` to that set — two module
   constants, two `item.setData(...)` calls in `_refresh`, two
   `item.data(...)` reads in `_on_update`. Naming the mechanism matters
   because the alternatives are not equivalent: `AccountService` exposes **no
   by-id getter** (its surface is `list_accounts`, `add_account`,
   `update_account`, `delete_account`, `get_pdf_password`, `set_pdf_password`,
   `account_ids_with_pdf_password`), so read-and-pass-back would otherwise
   force the implementer to invent either a new public `get_account(id)` or a
   `list_accounts()` re-query and linear scan on every Update click. The roles
   route extends a pattern this handler already uses (it reads the id role from
   the same item) and adds no public API that FIBR-0113 would then have to
   remove.

   This is the **one** UI change in scope here; the Accounts tab itself remains
   FIBR-0113's (§9), which replaces this passthrough — and these two roles —
   with real form fields. INV-7 locks it.

## 4. Design

### 4.1 Schema — migration v13

`migrations.py::LATEST_SCHEMA_VERSION` becomes 13 and `_MIGRATIONS` gains a
`13:` entry. Two existing steps are *pure* nullable `ADD COLUMN`s with no
backfill — `_migrate_to_v5` (which is also the precedent for adding a nullable
`TEXT` column to `accounts` specifically) and `_migrate_to_v11`. The closest
**shape** match is `_migrate_to_v7`, the only step issuing two `ADD COLUMN`s
inside one `owned_transaction`, though it additionally creates a table.
(`_migrate_to_v6` is *not* a precedent here: it backfills.) The step carries the
same explanatory docstring every existing step does (all eleven have one):

```python
def _migrate_to_v13(conn: dbapi2.Connection) -> None:
    """v12->v13: add the nullable ``accounts.account_number`` and
    ``accounts.note`` columns for FIBR-0193. ..."""
    # Elided above: the explicit-BEGIN / isolation_level="" rationale, copied from
    # the _migrate_to_v11 / _v12 docstrings. owned_transaction's precondition
    # holds here — nothing executes on conn before the block.
    with owned_transaction(conn):
        conn.execute("ALTER TABLE accounts ADD COLUMN account_number TEXT")
        conn.execute("ALTER TABLE accounts ADD COLUMN note TEXT")
        conn.execute("UPDATE schema_version SET version = 13")
```

Both columns are nullable with no default, so SQLite backfills existing rows
with `NULL` in place — no table rebuild, no FK churn. No backfill is possible
or wanted: the data has never been collected. Idempotency is version-gating, as
for every other step: `run_migrations` calls this only for a vault at exactly
v12, so the bare `ALTER`s never replay.

`vault.py::SCHEMA_VERSION` is a **different constant** and stays at 1. It is
the baseline version `create()` stamps on a brand-new vault, which
`run_migrations` then walks up to `LATEST_SCHEMA_VERSION`. Bumping it skips
every step at or below the value it is bumped to — which §11 shows is red in
every case, not only when it is bumped all the way to latest.

Neither column is length-capped, deliberately. `TEXT` is unbounded, and the
reason is not row count — a single pasted 10 MB note would be unbounded however
few accounts exist. It is that nothing downstream parses, indexes, matches or
aggregates either field: both are inert reference strings. The cost of a
pathological value is therefore a wide table cell FIBR-0113's user can resize,
not a slow query or a broken import. This is a deliberate departure from
spec-format §4 ("no unbounded growth ships without a named cap"), recorded here
rather than left implicit: the growth is bounded by what a user types into one
field on one row per account.

### 4.2 Model, repository and service

`models.py::Account` gains two optional fields **appended after `created_at`**,
and gains the field-order docstring its sibling `Transaction` already carries
for exactly this reason (`Transaction`'s says "the ``list_all`` SELECT names all
eight columns in this order so ``Transaction(*row)`` stays aligned"):

```python
@dataclass
class Account:
    """One row of the ``accounts`` table. ``account_number`` / ``note``
    (appended after ``created_at`` at v13, FIBR-0193) are optional reference
    fields; both read paths name all six columns in this order so
    ``Account(*row)`` stays aligned."""

    id: int
    name: str
    type: str
    created_at: str
    account_number: str | None = None
    note: str | None = None
```

**The two defaults are required, and they cost something.** They are needed
because `Account` is constructed positionally with four arguments outside the
repository — `tests/features/pdf_export/test_export_dialog.py` builds **two**
such literals at module scope (`_A` and `_B`, e.g.
`Account(1, "Current", "current", "2026-01-01T00:00:00Z")`) — and defaultless
fields would break every such literal. The cost is that they
also absorb a mistake: the sibling `Transaction` dataclass has **no** defaults,
so a SELECT that was never widened raises `TypeError` on arity there, loudly.
(`StatementPeriod` is the project's direct precedent for the *other* choice —
its `closing_balance_minor: int | None = None` was appended last at v11 for
exactly this reason, accepting the same silent-arity cost.)
Here it would construct silently and yield `None` for both fields. INV-3 is
written to fail on that state rather than rely on an arity error that no longer
happens.

`repositories/accounts.py::AccountRepository` builds `Account(*row)`
positionally in both `list_all()` and `get()`, so both SELECTs grow the two
columns **in dataclass field order** — `id, name, type, created_at,
account_number, note`. `list_all()`'s `ORDER BY name COLLATE NOCASE, id` is
unchanged. `statement_pdf_password` stays out of every listing SELECT, as
today: it is reached only through the dedicated `get_pdf_password` /
`set_pdf_password` / `ids_with_pdf_password` accessors (FIBR-0128 INV-1).

`AccountRepository`'s own writers grow the columns, both with the parameters
**required** — `update` is the layer that issues the unconditional
`UPDATE … SET`, so a default there would defeat §3 decision 2 one layer below
the service, and `add` matches for symmetry rather than because an `INSERT`
default would be dangerous:

```python
def add(self, name: str, type: str,
        account_number: str | None, note: str | None) -> int: ...

def update(self, account_id: int, name: str, type: str,
           account_number: str | None, note: str | None) -> None: ...
```

`add`'s two new parameters are also required, and its blast radius is one call:
`AccountService.add_account` is its **only** caller anywhere in `src`, `tests` or
`scripts`, so widening it breaks nothing else. The longer accounting below
belongs to `update_account` (six call sites) and to the one test that calls
`AccountRepository.update` directly — `add` was checked, not skipped.

Both statements grow the columns, not just the `UPDATE`:

- `update`'s `SET` clause becomes `UPDATE accounts SET name = ?, type = ?,
  account_number = ?, note = ? WHERE id = ?`. Accepting the two parameters
  without adding them to the `SET` clause is the failure INV-6 exists to catch
  (§5).
- `add`'s column list becomes `INSERT INTO accounts(name, type, created_at,
  account_number, note)`. This half is easy to skip precisely because skipping
  it is *quiet*: an `INSERT` that omits both columns writes `NULL` to each,
  which is indistinguishable from an account that genuinely has neither — so
  INV-5's blank-`add_account` leg passes either way. INV-3 is the leg that
  fails on it, which is why INV-3's round-trip is specified to write through
  `add_account` with **both fields populated** rather than through any
  convenient path (§5).

`services/accounts.py::AccountService` grows the fields on both write paths,
asymmetrically per §3 decisions 2 and 3:

```python
def add_account(self, name: str, type: str, *,
                account_number: str | None = None,
                note: str | None = None) -> Account: ...

def update_account(self, account_id: int, name: str, type: str, *,
                   account_number: str | None,
                   note: str | None) -> None: ...
```

The **six** existing three-argument `update_account` call sites, all of which
must be updated (measured 2026-07-28 with
`grep -rn 'update_account(' src tests scripts`):

| Call site | Caught by |
|---|---|
| `ui/accounts.py::AccountsWidget._on_update` | mypy (the one annotated caller), plus the FIBR-0005 INV-7f `qtbot` Update round-trip in `tests/features/accounts/test_accounts.py`. **Changed here, not deferred** — §3 decision 5, locked by INV-7 |
| `tests/features/accounts/test_accounts.py` — four calls | the test run |
| `scripts/seed_demo_vault.py` | **nothing** — see §11 |

Plus one **direct repository** call — `AccountRepository.update` in
`tests/features/accounts/test_accounts.py`'s missing-id no-op test — which the
repository signature change above also breaks, and which is easy to miss
because it does not go through the service.

`_validate` keeps its signature and its single `-> str` return. Optional-field
normalisation is a **separate** module-level helper in the same module, called
by `add_account` **and** `update_account` **inside the service**, so INV-5 holds
for every service caller rather than only for a future UI:

```python
def _normalise_optional(value: str | None) -> str | None:
    """Strip, and collapse an empty result to None — so a blank form field is
    stored as SQL NULL rather than "" (INV-5)."""
    return (value or "").strip() or None
```

It is **idempotent**, which §3 decision 5 depends on: that passthrough re-sends
an already-stored value through `update_account` on every Update click, so the
value is re-stripped each time. `f(f(x)) == f(x)` here, so repeated Updates
cannot walk a value away character by character.

## 5. Invariants

**No security invariant appears here, deliberately.** spec-format §5.5 asks that
a trust-boundary defence be stated as an invariant — but this item crosses no
boundary: the two columns are new *contents* of the already-encrypted vault, not
a new asset class, a new egress or a new UI surface. The defences that cover them
(`security-model.md` A1, A4 and INV-9) are pre-existing and are named rather than
assumed in §12. INV-4 is the closest thing to a security invariant here, and it
is a *containment* check — it keeps `statement_pdf_password` out of the listing
SELECTs these columns are being added to. If a later change gives either field an
egress, that change owns the new invariant.

- **INV-1** — A vault at v12 upgrades to v13 in place, gaining exactly
  `accounts.account_number` and `accounts.note`, **both nullable**, with every
  pre-existing account row still present and its other columns unchanged.
  *Test:* `tests/features/accounts/test_migration_v13.py` — **leg 2** for the
  in-place upgrade clause and **leg 1** for the nullability clause. The split is
  not presentational: §7 leg 2 is the *only* observable for the upgrade clause,
  because leg 1 covers the fresh first-run path instead. Nullability needs a
  **wider reader than the precedent's** — `_cols()` in `test_migration_v11.py`
  returns column *names* only — and §7 leg 1 owns that mechanism. Leg 2
  additionally asserts the seeded `transactions` rows survive and `PRAGMA
  foreign_key_check` returns empty — the FK half of this claim, which a check on
  `accounts` alone cannot see.
  *Breaks when:* the step rebuilds `accounts` (CREATE / COPY / DROP / RENAME)
  instead of issuing two `ADD COLUMN`s, dropping rows or breaking the
  `transactions.account_id` and `statement_periods` FK targets. The nullability
  half breaks on `ADD COLUMN … NOT NULL DEFAULT ''`, which SQLite **accepts** —
  so every existing row silently gains `""` where the design says `NULL`, and
  INV-5's blank-is-`NULL` contract is defeated at the schema level with no
  error anywhere. (Bare `NOT NULL` with no default is rejected by SQLite and
  fails loudly on its own; it is the `DEFAULT ''` variant this flag catches.)

- **INV-2** — `LATEST_SCHEMA_VERSION == 13` and `13 in _MIGRATIONS` — the
  constant and the dispatch table agree.
  *Test:* `.venv/bin/python -c "import sys; sys.path.insert(0,'src'); from
  finbreak.migrations import LATEST_SCHEMA_VERSION, _MIGRATIONS;
  print(LATEST_SCHEMA_VERSION, 13 in _MIGRATIONS)"` → `13 True`.
  (Run 2026-07-28 against pre-implementation source: `12 False` — red, as it
  must be before the work.) Also leg 3 of `test_migration_v13.py`.
  *Breaks when:* the constant is bumped without registering the step —
  `run_migrations`'s `for target in range(current + 1, LATEST + 1)` then
  indexes `_MIGRATIONS[13]` and raises `KeyError` on **every** vault open and
  every vault create, so the app cannot start at all. The reverse — a step
  registered without bumping the constant — would let the columns never appear,
  but it is **not** silent: `tests/features/reconciliation/test_no_schema_change.py::test_INV8_no_unregistered_next_migration`
  asserts `LATEST_SCHEMA_VERSION + 1 not in _MIGRATIONS`, which fails loudly in
  exactly that state (and leg 3 catches it too).

- **INV-3** — `Account`'s field order and the SELECT column order in both
  `AccountRepository.list_all()` and `.get()` agree, so positional
  `Account(*row)` construction assigns each column to the field of that name.
  *Test:* `tests/features/accounts/test_accounts.py` — write through
  **`add_account` with both fields populated** (non-empty, and distinguishable
  from each other and from `name` / `type` / `created_at`), then assert the
  account reads back with `account_number` and `note` equal to what was
  written — by field name, on **both** read paths. The populated write path is
  load-bearing twice over: a blank write cannot tell a mis-ordered SELECT from a
  correct one, and it cannot tell a widened `INSERT` from one that silently
  omits both columns (§4.2).
  *Breaks when:* a column is appended to one SELECT but not the other, or
  inserted before `created_at` in one of them — e.g. `SELECT id, name, type,
  account_number, created_at, note`, which lands `created_at`'s timestamp in
  the `account_number` field and raises nothing. **The dataclass defaults make
  the un-widened case silent too:** a SELECT left at four columns would, without
  defaults, raise `TypeError` on arity — with them it constructs happily and
  yields `None` for both fields, so a read path nobody widened looks exactly
  like an account that has no number. That is the specific state this invariant
  must fail on, and it is why the leg asserts round-tripped *values* rather than
  merely that construction succeeds.

- **INV-4** — Within `repositories/accounts.py`, `statement_pdf_password` is
  named only inside the three dedicated accessors — no listing query in that
  file selects it (re-asserts FIBR-0128 INV-1 at source level, where it has no
  test today). Scoped to that file deliberately: the test walks one module, so
  claiming "no listing query anywhere" would assert more than it exercises.
  *Test:* `tests/features/accounts/test_accounts.py`, walking
  `repositories/accounts.py` with `ast.parse` and asserting the column name
  appears only inside the `get_pdf_password` / `set_pdf_password` /
  `ids_with_pdf_password` function bodies — an AST walk, because a substring
  scan cannot attribute a hit to its enclosing function. The test locates the
  module through the imported symbol
  (`Path(finbreak.repositories.accounts.__file__)`), not a path relative to the
  test file, so it keeps working if either tree moves.
  *Breaks when:* someone widens the `list_all` SELECT to carry the saved-password
  flag directly instead of calling `ids_with_pdf_password()`, putting every
  stored statement password into every account listing.

INV-5 and INV-6 divide by **write path**: INV-5 owns `add_account`, INV-6 owns
`update_account`. INV-5 *states* that `_normalise_optional` applies on both
paths, but it does not test the update half — INV-6's second leg does, by
passing `"   "` to `update_account` and asserting `NULL`. That is deliberate,
and it is the whole reason INV-6 has two legs rather than one: leg 1 (new values
for both fields) fails when the repository drops the columns from its `SET`
clause, leg 2 fails when the service skips normalisation, so the pair localises
the fault to a layer without needing a source-level check to attribute it.

- **INV-5** — A blank account number or note passed through
  `AccountService.add_account` is stored as SQL `NULL`, not as an empty string;
  and `_normalise_optional` is applied on **both** service write paths, so
  `update_account` converts identically. Scoped to service callers deliberately:
  normalisation lives in `services/accounts.py`, so a caller going straight to
  `AccountRepository` bypasses it (§6).
  *Test:* `tests/features/accounts/test_accounts.py` — assert
  `account_number IS NULL` and `note IS NULL` after `add_account` with both
  fields blank, covering `""` and a whitespace-only `"   "` (the strip half and
  the collapse half are separate bugs). The *update* path's normalisation is
  **not** re-tested here; INV-6's second leg is its catcher.
  *Breaks when:* the service passes the raw string to the repository without
  `_normalise_optional`, so `""` is stored and any later `IS NOT NULL` check
  counts a blank field as filled. Normalising in `add_account` only — the
  asymmetric case — is caught by INV-6's second leg, not by this one.

- **INV-6** — `AccountService.update_account` **persists** both new fields:
  editing a stored account number writes the new value, and clearing a filled
  field to blank writes SQL `NULL` back over it.
  *Test:* `tests/features/accounts/test_accounts.py` — add an account with both
  fields set; `update_account` with **new** values for both and assert both
  changed, re-read through `get()`; then `update_account` passing `"   "` for
  **both** fields and assert both are now `NULL`. As in INV-3, every value
  written here must be **distinguishable from the other field and from `name` /
  `type`**: leg 1 is the only guard on `AccountRepository.update`'s `SET` clause
  and bind tuple, and a transposed pair — `SET … account_number = ?, note = ?`
  bound `(name, type, note, account_number, id)` — passes against
  indistinguishable values while silently filing every note as an account number
  on every Update.
  *Breaks when:* `AccountRepository.update` accepts the two new parameters but
  never adds them to its `SET` clause (leg 1), or `update_account` skips
  `_normalise_optional` (leg 2). INV-3 does not catch either — it covers the
  read paths, and a round-trip through `add` alone stays green. INV-5 does not:
  its only leg is on `add_account`. In that state an edited account number is
  silently dropped and a
  cleared field silently keeps its old value. §3 decision 2 defends against
  *callers omitting* the arguments; this defends against the *repository
  ignoring* them.

- **INV-7** — `ui/accounts.py::AccountsWidget._on_update` passes the selected
  account's existing `account_number` and `note` through unchanged, so an
  Update that edits only the name or type leaves both stored fields intact
  (§3 decision 5).
  *Test:* `tests/features/accounts/test_accounts.py` — a `qtbot` leg: seed an
  account with both fields set to **mutually distinguishable** values (same
  reason as INV-3 and INV-6 — a passthrough that swaps the two reads back green
  otherwise), select it in the widget, change the name, press Update, and assert
  the reloaded account still carries both original values **and** the new name.
  *Breaks when:* `_on_update` passes `None, None` to satisfy the new required
  parameters. Every other invariant here stays green — the repository writes
  exactly what it was given, and no other test drives that handler with a
  populated account — while the unconditional `UPDATE … SET` erases both fields
  on any edit. That is precisely the silent data loss §3 decision 2 exists to
  prevent, arriving through the one caller the decision cannot protect by making
  the parameters required. It is unreachable *today* (nothing can set either
  field yet) and reachable the moment FIBR-0113 ships, which is why it is
  locked now rather than left to that item.

## 6. Failure modes

- **The schema bump breaks unrelated tests — 14 files.** The version is pinned
  three distinct ways, and a grep for one misses the others. Measured
  2026-07-28 with
  `grep -rlE 'LATEST_SCHEMA_VERSION|SELECT version FROM schema_version|manifest\["schema_version"\]' tests/ --include='*.py' | sort -u | wc -l`
  → `14`. The three shapes, each counted by its own command:
  (a) the constant pinned to the literal — `grep -rlE 'LATEST_SCHEMA_VERSION *== *12' tests/ --include='*.py' | wc -l`
  → **10**. Three *further* files reference `LATEST_SCHEMA_VERSION` without
  pinning it to `12` and need **no** shape-(a) edit: `accounts/test_accounts.py`
  and `backup/test_backup.py` use it symbolically (`+ 1`, or
  `== LATEST_SCHEMA_VERSION`), and `vault/test_vault.py` names it in a comment
  only. Those three are why the union grep returns 13 for this shape and the
  pin count is 10 — the distinction matters, because an implementer using this
  as a checklist would otherwise hunt `== 12` in three files that do not contain
  it.
  (b) a `SELECT version FROM schema_version` read compared to `12` — 12 files.
  (c) a backup **manifest dict** key, `manifest["schema_version"] == 12` — the
  only *pin* in `tests/features/backup/test_backup.py` (that file also
  references the constant symbolically, per (a)). The per-shape counts overlap,
  so they do not sum to 14; the union is the stable figure. Expected churn rather
  than breakage — the guards keep their meaning — and exactly the cost FIBR-0144
  exists to remove.
- **Six test functions encode the version in their *names*** and must be
  renamed, not just re-numbered. Measured 2026-07-28 with
  `grep -rnoE 'def test_[A-Za-z0-9_]*(v1[0-9]|_1[0-9])[A-Za-z0-9_]*' tests/ --include='*.py'`,
  then read — because the grep alone cannot tell a name that pins the *latest*
  version (churns) from one that names a *step* (does not). That grep returns
  **20** hits: 6 renamed here, 2 keeping their names (below), 5 already-stale
  `_is_10` names (next bullet), and **7 unrelated** — `release_integrity`'s
  `…_inv13` (a security-model INV id), five `import_date_detect` names (dates),
  and `windows_build::test_sqlcipher3_imports_and_engine_is_4_12_0` (an engine
  version). The 7 are named so this accounting is exhaustive rather than
  apparently so.
  Five name the latest version: `spending_alerts::test_INV14_latest_schema_version_is_12`
  and `::test_INV14_fresh_vault_is_v12_with_alert_dismissals` (the second of
  which a constant-only grep never finds),
  `db_performance::test_INV1_latest_schema_version_is_12`,
  `forecast::test_INV9_latest_schema_version_is_12`, and
  `reconciliation::test_INV8_schema_version_at_current_latest_v12`.
  The sixth is `spending_alerts::test_INV14_v11_vault_upgrades_to_v12_cleanly`,
  and it reads like a step name but is not: its body chains `build_v9_vault` up
  to v11 (`_migrate_to_v10` then `_migrate_to_v11`), then calls
  **`run_migrations`** — which walks to *latest*, not to v12 — and asserts
  `== 12`. At v13 that assertion churns to 13 and the name's stated destination
  is wrong, so it is renamed with the rest (e.g.
  `..._v11_vault_upgrades_to_latest_cleanly`).
  Two names the grep returns keep their **names** — but they are **not** exempt
  from the assertion sweep, and the reason is narrower than it looks:
  `db_performance::test_INV1_v9_upgrades_to_v10_adding_indexes` and
  `forecast::test_INV9_v10_upgrades_to_v11_adding_closing_balance_column` both
  call `run_migrations` and both assert `== 12`, exactly like the sixth name
  above — so both are already in shape (b)'s 12 files and both churn to 13.
  What makes them different is only that each name states the **effect its step
  adds** (indexes; the `closing_balance_minor` column), and each leg still
  asserts that effect (`_index_names(conn) == _EXPECTED_INDEXES`;
  `"closing_balance_minor" in _cols(...)`). A name that states an effect stays
  true as latest advances; a name that states a *destination* does not. Do not
  read "left alone" as "needs no edit" — their version assertions churn with
  everything else.
- **The `v12`-as-latest *comment* sweep is a different, smaller file set than
  the 14 — five files, and two of them pin nothing.** No assertion-shape grep
  finds a comment, so the 14-file blast radius above does **not** bound it.
  Measured 2026-07-28 with `grep -rln 'v12' tests/ --include='*.py'` → **5**:
  `forecast/test_migration_v11.py`, `reconciliation/test_no_schema_change.py`
  and `spending_alerts/test_migration_v12.py` are also in the 14;
  `spending_alerts/test_alert_dismissals.py` and
  `spending_alerts/test_alert_service.py` are **not** — neither names
  `LATEST_SCHEMA_VERSION`, reads `schema_version`, or touches the backup
  manifest, so every count in the bullet above excludes them. Both nonetheless
  carry comments that go wrong at v13: `# migrates straight to latest (v12)` and
  `svc.first_run(...)  # v12`, plus "a real (tmp_path) v12 vault" in each module
  docstring. Their *other* v12 mentions are historical (`alert_dismissals` really
  was added at v12) and correctly stay — which is why this is a read-and-judge
  sweep, not a replace-all.
- **Five *further* names are already stale and stay that way** — `categorisation`,
  `import_`, `pdf_import`, `recurring` and `transfers` each carry a
  `test_..._latest_schema_version_is_10` whose body already asserts `== 12`.
  Recorded so the rename sweep is not mistaken for a full reconciliation: they
  are pre-existing drift, out of scope here, and the standing evidence for why
  FIBR-0144 exists.
- **`reconciliation`'s *unregistered-next* guard stays green; its sibling and
  its prose do not.** `test_no_schema_change.py` holds two tests, and they
  behave differently: `test_INV8_no_unregistered_next_migration` asserts
  `LATEST_SCHEMA_VERSION + 1 not in _MIGRATIONS` — symbolic, so it needs no edit
  at v13 — while `test_INV8_schema_version_at_current_latest_v12` both churns its
  `== 12` assertion and is in the rename set above. The file's `spec.md` INV-8
  row says the guard pins "that no *unregistered next* version (**v13**)
  exists", which names the version this item registers, and the module docstring
  names **both** v12 ("It now asserts the current latest (v12)") and the same
  stale v13; all three are prose fixes (§12). Only the two **docstring** strings
  fall under the comment sweep — that sweep greps `tests/**/*.py`, which cannot
  reach `reconciliation/spec.md`; the `spec.md` row is listed separately in §12.
  FIBR-0172's INV-14a is the precedent: it advanced the same guard's
  documented next-version from v12 to v13.
- **A vault newer than the app.** Unchanged behaviour: `run_migrations` already
  refuses a vault whose version exceeds `LATEST_SCHEMA_VERSION` with an
  upgrade-finbreak message.
- **Migration interrupted mid-step.** `owned_transaction` makes the two
  `ALTER`s and the version stamp one atomic unit, so a crash leaves a
  re-openable v12 with **neither** column added — SQLite DDL is transactional,
  so the rollback is genuine rather than best-effort.
- **A backup restored from an older vault.** `services/backup.py` compares
  against `LATEST_SCHEMA_VERSION` and migrates on open, so a v12 backup
  restores and upgrades; a backup taken at v13 refuses to restore into an older
  build, as it already does.
- **A pathologically long note or account number.** §4.1 imposes no cap and owns
  the reasoning; the cost is layout in FIBR-0113's table, not correctness.
- **A caller reaching `AccountRepository` directly.** `_normalise_optional` lives
  in the service, so a direct repository write can store `""`. The hazard is
  **structural, not currently realised**: the only direct writer *of these
  columns* today is `test_accounts.py`'s missing-id no-op
  (`repo.update(999_999, …)`), which
  matches no row and therefore stores nothing. INV-5 is scoped to service
  callers for exactly this reason rather than claiming more than it exercises.

## 7. Tests

New file `tests/features/accounts/test_migration_v13.py`, mirroring
`tests/features/forecast/test_migration_v11.py` — the nearest precedent, same
nullable-`ADD COLUMN` shape. That file holds **12** test functions, of which
**five** concern the migration (the other seven cover the
`StatementPeriodRepository`); this file takes all five migration legs, not
three:

1. a fresh first-run vault is v13 and has both columns (precedent:
   `test_INV2a_first_run_vault_carries_the_column`), **and both are nullable** —
   this leg reads whole `PRAGMA table_info(accounts)` rows and asserts each new
   column's `notnull` field is `0`. The precedent's `_cols()` helper returns
   column names only, so it must be widened or a second reader added; copying it
   verbatim leaves INV-1's nullability clause with no observable;
2. a vault at exactly v12 upgrades cleanly with its rows intact. This leg is the
   **only** observable for INV-1's central clause — leg 1 covers the *fresh*
   first-run path, not the upgrade path — so it must assert all four parts of
   that clause, not just the FK half:
   (i) `SELECT version FROM schema_version` is **13**;
   (ii) both columns are present and each one's `notnull` is `0`, read the same
   way leg 1 reads it;
   (iii) the pre-existing accounts row survives with its `name`, `type`,
   `created_at` and `statement_pdf_password` **unchanged** — "gaining exactly"
   two columns means the others are untouched, and nothing else asserts that.
   **This clause is vacuous unless the leg seeds those values first:** the only
   `accounts` row in a `build_v9_vault` chain is the one `_migrate_to_v2`
   inserts, and that `INSERT` names just `name, type, created_at` — so
   `statement_pdf_password` is `NULL` before the step *and* after it, and the
   assertion compares `NULL` to `NULL`. Before running the step this leg
   therefore stamps a non-`NULL` `statement_pdf_password` and renames the row
   away from `DEFAULT_ACCOUNT_NAME`, so every value it asserts is distinguishable
   from what a table rebuild would leave behind. Same non-vacuousness care as
   (iv)'s `foreign_key_check`;
   (iv) the seeded `transactions` rows survive and `PRAGMA foreign_key_check`
   returns empty, which is the half of INV-1 a check on `accounts` alone cannot
   see. The v11 precedent asserts (i) and (ii); (iii) and (iv) are this leg's
   own.
   **Both named FK targets must actually have a row, or the check is vacuous.**
   `build_v9_vault` seeds only what the caller passes as `rows`, and it seeds
   them as `transactions` — **seven of its ten existing call sites pass `[]`**
   (measured 2026-07-28 with `grep -rn 'build_v9_vault(' tests/ --include='*.py'`,
   excluding the definition), so an implementer copying the idempotency
   precedent gets a `foreign_key_check` over a database carrying no
   `transactions` and no `statement_periods` rows — one that cannot fail. (The
   database is not otherwise empty: `build_v1_vault` seeds `settings`,
   `_migrate_to_v2` seeds the Default account and `_migrate_to_v3` the category
   tree. It is empty of exactly the FK-bearing rows INV-1 names.) This leg
   therefore passes a **non-empty** `rows` list *and* inserts one
   `statement_periods` row before the step, since nothing in the
   `build_v9_vault` → `build_v1_vault` chain ever writes that table and INV-1
   names it explicitly. `test_INV9_v10_upgrades_to_v11_adding_closing_balance_column`
   shows the exact INSERT.
   For the starting vault, the closest precedent is **not** the v11 file (which
   runs a single intervening step, `_migrate_to_v10`) but
   `spending_alerts/test_migration_v12.py::test_INV14_v11_vault_upgrades_to_v12_cleanly`,
   which already chains two (`_migrate_to_v10`, `_migrate_to_v11`) off
   `conftest.build_v9_vault`. Reaching v12 needs three — add `_migrate_to_v12`
   to that chain. Neither precedent hand-writes DDL, and this leg must not
   either;
3. `LATEST_SCHEMA_VERSION == 13` **and** `13 in _MIGRATIONS`. The membership
   half is not in the v11 file, which asserts the constant only — take it from
   `tests/features/spending_alerts/test_migration_v12.py`, which does assert
   `12 in _MIGRATIONS`;
4. **atomicity** — starting from the same v12 vault leg 2 builds, wedge the
   *second* `ALTER` with
   `conftest.raising_conn(conn, "ADD COLUMN note", "injected ALTER failure")`
   (an unambiguous trigger: no other statement in the step contains that
   substring) and assert the vault is still a re-openable **v12 with neither
   `account_number` nor `note` present**. The precedent
   (`test_INV9_migration_is_atomic`) asserts both the version *and* the column's
   absence; asserting the version alone would pass against a step that added the
   first column and left it there. §6 asserts this rollback as fact, so it needs
   a leg;
5. **idempotency at latest** — `run_migrations` on an already-v13 vault is a
   no-op (precedent: `test_INV9_migration_is_idempotent_at_latest`). §4.1
   asserts this as fact, so it needs a leg.

Locks INV-1, INV-2.

Extensions to `tests/features/accounts/test_accounts.py` for INV-3 to INV-7:

- a round-trip through `add_account` with **both fields populated**, read back on
  both read paths (INV-3);
- **one** AST scan, over `repositories/accounts.py`, for INV-4. There is no
  second AST scan — INV-5's both-paths clause is falsified behaviourally by
  INV-6's leg 2, not at source level (§5);
- a blank-to-`NULL` leg on `add_account`, covering `""` and `"   "` (INV-5);
- **two** legs for INV-6 — new values for both fields, then `"   "` for both.
  Writing only the first loses the update path's normalisation check *and* the
  fault localisation §5 argues for. Both legs, and INV-7's, carry INV-3's
  **distinguishable-values** requirement — without it a transposed
  `SET`/bind pair in `AccountRepository.update` is green everywhere;
- **one `qtbot` leg** for INV-7's `_on_update` passthrough — the only
  widget-level test this item carries, and it exists because §3 decision 5 puts
  one UI change in scope;
- `test_INV8_account_cycle_logs_no_secret` is **already** one of the
  `update_account` call sites §4.2 widens, so while it is open it gains sentinel
  values for both new fields and asserts each is absent from `caplog`. Today it
  sweeps only for the master password and the derived key hex, so a leaked
  account number or note passes it — and §12 leans on `security-model.md` INV-9's
  wording to claim the new columns are already covered. Two extra arguments and
  two asserts, in a test this item edits regardless, give that claim an
  observable instead of leaving it as wording.

**The two new item-data roles narrow an existing security sweep, and it must be
widened with them.** `test_INV1_widget_never_renders_or_reads_the_secret`
(FIBR-0128 INV-1) sweeps a **literal** role list — `AccessibleTextRole`,
`UserRole`, `UserRole + 1`, `+ 2`, `+ 3` — looking for the stored statement
password. `_ACCOUNT_NUMBER_ROLE` / `_ACCOUNT_NOTE_ROLE` land at `UserRole + 4`
and `+ 5`, outside that list, so the guard keeps passing while covering strictly
less than it did. Extend the list in the same change that adds the constants.
Nothing fails if this is missed, which is exactly why it is written down.

**The test-side naming convention matters as much as the `spec.md` one.**
`test_accounts.py` already holds a `test_INV1_`…`test_INV8_` set for FIBR-0005
*and* a second `test_INV1_` / `test_INV2_` / `test_INV3_` / `test_INV4_` /
`test_INV5_` set for
FIBR-0128, separated by a `# FIBR-0128 — …` banner comment and a
`(FIBR-0128 INV-N)` docstring line. This item's INV-3 to INV-7 land in the same
module, so they take the same treatment plus a distinct name suffix. A duplicate
function name in one pytest module **silently shadows** the earlier definition —
the test disappears with no error, which is the one failure mode this file's
existing convention is built to avoid.

`tests/features/accounts/spec.md` already carries two `INV-N` blocks that each
restart at INV-1 — FIBR-0005's under a plain `## Invariants` heading and
FIBR-0128's under `## FIBR-0128 — …`, disambiguated by per-invariant
`Source: FIBR-0128 INV-N` lines. This spec's invariants follow that same
existing convention: a third block under a `## FIBR-0193 — …` heading with
`Source:` lines, rather than inventing a third id style for one file.
FIBR-0113 adds a fourth block the same way; the two must not collide, which the
per-block headings and `Source:` lines already prevent. Note that those headings
are **unnumbered**, so `testing.md` §2.3's citation form (`# INV-3 from
spec.md § 2.1`) resolves here by heading *name*, not section number.

Per `docs/standards/testing.md`, every one of these is run against
pre-implementation source and seen to fail before the implementation is
written. INV-2's clause has already been run red (`12 False`) and is recorded
in §5; the rest are red trivially until `test_migration_v13.py` exists, so the
meaningful red evidence is per-leg once the file is created.

**Two of the items above are exceptions to that, per `testing.md` §1's exception
list — recorded here rather than left to read as unrun evidence.** INV-4's AST
scan is green on pre-implementation source *by construction*:
`repositories/accounts.py` already confines `statement_pdf_password` to
`get_pdf_password` / `set_pdf_password` / `ids_with_pdf_password`, which is
exactly why §5 describes INV-4 as re-asserting FIBR-0128 INV-1 "where it has no
test today". Widening the FIBR-0128 role sweep is the same shape — it protects a
property that already holds, which is why §7 says nothing fails if it is missed.
Both are **standing guards on already-satisfied behaviour**, not red-first
regression tests; a red run for either would mean the codebase was already
broken.

## 8. Alternatives considered (and rejected)

- **Defaulting `update_account`'s new parameters to `None`.** Rejected — see
  §3 decision 2; it turns the six existing call sites into silent data loss.
- **A sentinel default (`_UNSET`) that means "leave this column alone", or a
  read-modify-write inside `update_account`.** Either would keep all six
  existing call sites compiling untouched, and would close §11's bolded
  `nothing` for `scripts/seed_demo_vault.py` — which is a real advantage, so the
  option deserves stating rather than passing over. Rejected because it moves
  the decision from the caller to the callee: a caller that *means* to clear a
  field and one that simply forgot to mention it become indistinguishable at the
  signature, which is exactly the ambiguity decision 2 removes. It also adds a
  third state (`None` vs `_UNSET` vs a value) to a method whose whole contract is
  "this is the account's new content". The loud failure at six call sites is a
  one-time cost paid at test time; the sentinel's ambiguity is permanent. §3
  decision 5 handles the one caller that genuinely wants passthrough,
  explicitly, at that call site.
  **The objection is to hiding the choice in the callee, not to read-modify-write
  as such** — decision 5 adopts a read-modify-write, one layer up, and inherits
  the same non-atomicity (the values are read at `_refresh` time and written at
  Update time, with no transaction spanning the pair). That is accepted here for
  the reason it is not accepted in the service: at the call site it is *visible*,
  it is confined to one handler that FIBR-0113 then deletes, and a concurrent
  writer to the same vault is outside this app's single-process model anyway.
- **Defaulting `AccountRepository.update`'s parameters too.** Rejected: it
  defeats decision 2 one layer below the service, where the unconditional
  `UPDATE … SET` actually lives.
- **Normalising and storing account numbers stripped of spaces and dashes.**
  Rejected: FIBR-0086 owns normalisation *for matching*, and normalising at
  storage time would show the user a value they did not type while that item is
  still unbuilt. Store verbatim apart from the **outer**-whitespace strip
  `_normalise_optional` applies (§4.2) — what is rejected here is normalising
  *internal* spaces and dashes; normalise those at match time.
- **Putting normalisation in the repository rather than the service.**
  Rejected: the repository is the persistence seam and the service is where
  every other validation rule already lives (`_validate`). Splitting them would
  put two validation homes in one feature.
- **A `CHECK` constraint or length cap on either column.** Rejected — see §4.1;
  nothing downstream parses either field, so a cap buys nothing and a migration
  that adds one cannot be applied in place.
- **Carrying the Accounts-table UI here too.** Rejected: that fold-in is what
  took FIBR-0113 to the **985** lines its own §13 records, and five
  non-converging review loops. FIBR-0113
  owns it and depends on this.

## 9. Out of scope

- **The Accounts tab UI** — the 5-column table, the masked account-number cell
  and form field, the reveal toggle and its auto-hide timer. Tracked by
  **FIBR-0113**, which this item unblocks.
- **Detecting** an account number on an imported statement and matching it to an
  account — tracked by FIBR-0086. That bullet currently also claims the
  *storage* half ("Store an account number on each account — a new column in the
  ENCRYPTED vault … schema migration, currently v7 -> v8"), which this spec
  delivers instead and at v13, not v8. FIBR-0086's bullet **must be amended** to
  say so (§12); otherwise a later implementer re-adds an existing column at a
  stale version.
- Per-account currency, which would add a third new column — tracked by
  FIBR-0087. (FIBR-0087's body carries an equivalently stale migration note —
  its wording is `Schema migration (currently v7 -> v8).`, not FIBR-0086's
  phrasing — and it is left alone here because this spec does not deliver any
  part of FIBR-0087.)
- Removing the per-bump `LATEST_SCHEMA_VERSION` test churn described in §6 —
  tracked by FIBR-0144. The five already-stale `_is_10` names are part of that
  item's evidence, not of this one's work.
- Finishing FIBR-0084 — unrelated to storage, and blocked on FIBR-0192.
  FIBR-0084 stays 📋 when this ships **and** when FIBR-0113 ships.

## 10. Resource cost

Two nullable `TEXT` columns on a table that holds one row per account. No cap is
imposed; **§4.1 owns that argument** and is not restated here. No new external
dependency, no new module, no new build target.

One knock-on: `AccountService._validate` calls `list_all()` on every add and
update, which now pulls two extra `TEXT` columns per account. Measured against a
real vault this is one row per account — `scripts/seed_demo_vault.py` builds
**three** (the seeded default, renamed, plus Savings and Credit card) — so the
added read is two short strings per row on a query that already runs. Noted
rather than optimised; no timing figure is quoted because none was measured, and
an invented one would be worse than none.

## 11. What checks this

| Rule | What catches a breach |
|------|----------------------|
| INV-1 | `tests/features/accounts/test_migration_v13.py` legs 1–2 |
| INV-2 | `tests/features/accounts/test_migration_v13.py` leg 3, plus the §5 command |
| INV-3 | `tests/features/accounts/test_accounts.py` |
| INV-4 | `tests/features/accounts/test_accounts.py` AST scan |
| INV-5 | `tests/features/accounts/test_accounts.py` — a behavioural leg on `add_account` only; its both-paths clause is falsified by INV-6 leg 2 |
| INV-6 | `tests/features/accounts/test_accounts.py` — two legs (new values; blanks), which together attribute the fault to the repository or the service |
| INV-7 | `tests/features/accounts/test_accounts.py` `qtbot` leg |
| §6 the migration is atomic (a wedged second `ALTER` rolls back) | `tests/features/accounts/test_migration_v13.py` leg 4 |
| §4.1 the migration is idempotent at latest | `tests/features/accounts/test_migration_v13.py` leg 5 |
| §4.1 `vault.py::SCHEMA_VERSION` is not bumped — neither to `LATEST_SCHEMA_VERSION` nor to any intermediate value | `tests/features/accounts/test_migration_v13.py` leg 1, which is red in **either** case. Stamped at 13, `run_migrations` iterates an empty range and the fresh vault never gains the columns. Stamped at any value ≥ 2, `_migrate_to_v2` is skipped, so `accounts` is never created and the first `ALTER TABLE accounts` to run raises — `Vault.create()` closes the connection and re-raises, so first-run fails outright rather than silently |
| §3 decision 2, the **five** non-seeder `update_account` call sites, plus the one direct `AccountRepository.update` caller | the **test run** for the four test callers (they raise `TypeError`) and for the direct `AccountRepository.update` caller; `ui/accounts.py::_on_update` is not pytest-collected as a *caller* but is driven by the FIBR-0005 INV-7f `qtbot` leg, and mypy flags it besides — it is the one annotated caller, since `[tool.mypy]` sets no `check_untyped_defs` and the four test callers are unannotated, so their bodies are not checked. **Five, not six:** §4.2's six `update_account` sites include `scripts/seed_demo_vault.py`, which the next row owns; the direct repository call is a *sixth item* here but not a sixth `update_account` call site |
| §3 decision 2, the `scripts/seed_demo_vault.py` call site | **nothing** — `scripts/` is outside pytest collection *and* outside mypy's configured `files`, so it fails only when the demo seeder is run |
| §4.1 neither column is length-capped | **nothing** — a deliberate departure from spec-format §4; no query, index or import reads either field, so there is nothing to degrade |
| §6 the `v12`-as-latest **comment** sweep (a different, smaller file set than the 14 — see §6) | **nothing** — no assertion-shape grep finds a comment, and the gate does not read prose |
| §7 the FIBR-0128 role sweep is widened to `UserRole + 4` / `+ 5` | **nothing** — the literal role list keeps passing while covering strictly less than it did; no assertion-shape grep finds a *narrowed* sweep |

Fifteen rows, **four** with a bolded `nothing` — this spec's error budget. All
four are real rather than presentational: the seeder sits outside both gates
(which is why §4.2 lists it explicitly rather than leaving it to a grep), the
missing length cap is an accepted departure, comment drift has no mechanical
catcher anywhere in this project, and the FIBR-0128 role sweep is a guard that
narrows *silently* — widening it is the one requirement in §7 whose omission
turns nothing red.

The two `SCHEMA_VERSION` mis-bumps share one row because leg 1 is red for both,
so neither is a `nothing`: a full bump empties `run_migrations`' range, and any
intermediate bump skips `_migrate_to_v2`, leaving no `accounts` table for the
step's `ALTER` to widen.

## 12. Cross-doc impact

Documents first, then the two **code** sites that are listed here rather than
left to a grep because neither is reachable from any gate this spec runs
(spec-format §3.13 scopes this section to documents; these two are called out
deliberately, not filed here by mistake).

- `CHANGELOG.md` — one `### Added` entry for FIBR-0193 alone.
- `ROADMAP.md` — **only FIBR-0193 flips ✅.** FIBR-0113 stays 🚧 (it depends on
  this and ships after); FIBR-0084 stays 📋 until FIBR-0192 ships. **FIBR-0086's
  bullet is amended** to drop its account-number storage half and its stale
  "v7 -> v8" migration note, keeping detection + matching (§9).
- `src/finbreak/ui/accounts.py` — two new item-data role constants
  (`_ACCOUNT_NUMBER_ROLE`, `_ACCOUNT_NOTE_ROLE`) written in `_refresh` and read
  in `_on_update`, which gains the two new arguments and passes them back
  unchanged (§3 decision 5, INV-7). The **only** UI change in this item, and the
  only production caller of `update_account`.
- `scripts/seed_demo_vault.py` — its one `update_account` call gains the two new
  arguments, **passing `None` for both**. This is the one place an explicit
  `None` is correct rather than the erasure §3 decision 2 guards against: the
  call renames the freshly-created Default account moments after
  `first_run`, so there is no stored value to erase. Outside both gates (§11),
  so it is listed here rather than left to be discovered.
- `tests/features/accounts/spec.md` — a third `## FIBR-0193` INV block (§7).
- `docs/specs/FIBR-0113.md` — **one edit needed, and it also pins this spec's
  invariant numbering.** The edit: its §4.2 says the **four** `_ACCOUNT_*_ROLE`
  constants "are the complete set" and are deleted, and its §11 carries a
  matching row. §3 decision 5 here makes them **six**, and names FIBR-0113 as
  the deleter — so both sites go four → six, annotated `amended by FIBR-0193`.
  Left alone, FIBR-0113's implementer deletes four of six roles, leaving two
  writing account data into a table that no longer reads them, and leaves the
  FIBR-0128 sweep narrowed. Its *Withdrawn invariants* block redirects its
  own INV-1→FIBR-0193 INV-1, INV-2→INV-2, INV-3→INV-3, INV-4→INV-4 and
  INV-10→**FIBR-0193 INV-5**. It also pins this spec three further ways: its
  §4.3 cites "FIBR-0193 §4.2", its **§6** failure-mode bullet on a
  pathologically long note cites §4.1, and its **§4.2** cites **FIBR-0193
  INV-4** (in the same role-deletion bullet the edit above touches; its §4.1 is
  *Masking* and cites nothing from here).
  Most constraining of all, its **INV-12 names `_normalise_optional` itself** —
  "FIBR-0193's `_normalise_optional` strips, so `\" 1234 \"` comes back as
  `\"1234\"`" — so §4.2's *private helper name and its strip semantics* are
  pinned from another spec, not just this one's section numbers. Together these
  are why INV-1..INV-5 may never be renumbered (spec-format §3.7), why §4.1 and
  §4.2 may not be re-lettered, and why `_normalise_optional` may not be renamed
  or made to strip differently without editing FIBR-0113 in the same change.
  INV-6 and INV-7 are new to this spec and are not cited from there.
- The 14 test files that pin the schema version — **§6 owns the shape list, the
  rename set and the comment sweep**; it is not restated here.
- The feature `spec.md` files that pin `LATEST_SCHEMA_VERSION == 12` —
  `db_performance`, `categorisation`, `recurring`, `reconciliation`,
  `transfers`. `reconciliation`'s row carries **two** stale figures rather than
  one: its INV-8 pins `== 12` *and* names the unregistered next version as
  `v13`, which becomes `v14` here. Prose fixes only; those tests stay green
  (§6).
- `docs/specs/FIBR-0172.md` — **no edit needed.** Its **INV-14** row ("Migration
  v12 creates `alert_dismissals`; `LATEST_SCHEMA_VERSION == 12`; a v11 vault
  upgrades cleanly; drift-guards updated") and its **INV-14a** row (the
  `reconciliation` guard "reworked … so the whole suite is green after the v12
  bump") both record what *that* item shipped; neither is a live pin on current
  source. That is the same reading its `### D5 — Dismissal persistence (schema
  v12)` and `### D6 — Migration v12` headings get, and the same treatment
  FIBR-0005's "Data model (v2)" DDL block gets below — editing any of them would
  falsify a correct historical record. Note also what INV-14a's own wording is
  loose about: the assertion it describes is **symbolic**
  (`LATEST_SCHEMA_VERSION + 1 not in _MIGRATIONS`), so no *code* line in it
  advances on a version bump — what FIBR-0172 advanced, and what this item
  advances again, is that guard's **documented** next-version in prose, which is
  exactly the precedent §6 cites it for. §6 owns both halves: the `== 12` pin
  rename in `reconciliation/test_no_schema_change.py` (one of the 14) and the
  three prose sites.
- `docs/specs/FIBR-0128.md` — its **INV-1** row enumerates the swept roles ("the
  concrete stashed roles (id / name / type / has-pw) and `AccessibleTextRole`")
  and its **Deliverable 3** records "`UserRole`, `+1`, `+2` are already taken by
  id/name/type; `+3` is free". §7 widens that sweep to `UserRole + 4` / `+ 5`,
  so both sites under-enumerate the moment this ships. Both are **annotated**
  `amended by FIBR-0193` rather than rewritten, per spec-format §3.7 — the same
  treatment FIBR-0005 and FIBR-0013 get below.
- `docs/specs/FIBR-0005.md` — states `Account(id, name, type, created_at)` and
  the `.add` / `.update` signatures as INV-1's contract; both are widened here.
  Its INV-1 is **annotated** `INV-1 amended by FIBR-0193` rather than rewritten
  or renumbered, per spec-format §3.7. Two sites state the field list and both
  are annotated: the INV-1 bullet and the Deliverables entry for `models.py`.
  Its "At a glance" row says only "reads back field-for-field" and needs no
  change; its "Data model (v2)" DDL block is a historical v1→v2 snapshot and is
  deliberately left alone.
- `docs/specs/FIBR-0013.md` — pins `Account`'s field list **twice**: in its
  `## Verified code basis` section and in the body's `- **Accounts.**` bullet
  ("`Account` carries `id`, `name`, `type`, `created_at`"). Both go stale after
  §4.2; both are annotated `amended by FIBR-0193`.
- `CLAUDE.md` module map — no change; no new module is added (the migration
  lands in the existing `migrations.py`, the helper in the existing
  `services/accounts.py`).
- `docs/security-model.md` — **no change, and the existing defences that cover
  the new columns are named rather than assumed** (spec-format §5.5). The two
  columns are new *contents of the vault*, not a new boundary: A1's asset row
  already scopes the vault's contents, INV-9's "no transaction contents,
  passwords, keys, or decrypted data in logs" already covers a decrypted account
  number in *wording* — neither `AccountRepository` nor `AccountService` logs
  field values, `log.info("account updated")` carries no payload, and §7 gives
  that wording an **observable** by having the already-edited
  `test_INV8_account_cycle_logs_no_secret` sweep `caplog` for both new field
  values, which it does not do today. A4 is the dedicated row
  for the *other* optional sensitive account column,
  `statement_pdf_password`, whose posture §3 decision 1 matches. No new egress
  and no new UI surface arrives here. **FIBR-0113 amends T13**, because T13's
  mitigation currently asserts that "the statement PDF password and account
  numbers are **not** copyable", and FIBR-0113 puts the account number into an
  editable form field the user can select and copy. The trigger is
  *copyability*, not display — FIBR-0113's own §12 makes the same point ("the
  clause is about **copyability**, so the surface that falsifies it is the form
  field, not the cell"), and the two specs must not give different reasons for
  one edit.
- **No build plan.** spec-format §2 makes a plan mandatory "once the build order
  matters (a migration, …)", **and this spec has a migration, so the rule
  bites**. `docs/plans/` does not exist anywhere in this project and none of its
  49 specs has a plan, so this item is not an outlier — but prior non-compliance
  is not a waiver, and the gap is now tracked by **FIBR-0195** (adopt
  `docs/plans/` or record the departure once, project-wide, instead of in every
  spec). The build order that would have lived here is §4.1 → §4.2 → the
  `ui/accounts.py` passthrough (§3 decision 5), with the §6 schema-churn sweep
  landing alongside §4.1.

## 13. Cold-eyes loop log

| Loop | Date | Lanes | CRIT | HIGH | MED | LOW | Outcome |
|------|------|-------|------|------|-----|-----|---------|
| 1 | 2026-07-28 | 3 (schema/migration · model-repo-service · cross-doc) + a deterministic pre-pass; strong depth only, no breadth pass (loop 1 of a never-reviewed doc) | 1 | 3 | 13 | 12 | 29 verified, 0 unverified. All fixed. The critical was corroborated independently by two lanes: making `update_account`'s new parameters required breaks `ui/accounts.py::_on_update`, the only production caller, and the draft attributed it to a catcher that ships **later** ("rewritten by FIBR-0113 anyway") — so the app would raise `TypeError` on every Update between the two items, and the obvious patch (`None, None`) is exactly the erasure decision 2 exists to prevent. Added §3 decision 5 (read-and-pass-back) and INV-7 to lock it. INV-6's stated division of labour with INV-5 was false — INV-5's own update leg would have caught the state INV-6 claimed was invisible to it — so the two were re-split: INV-5 owns `""`→`NULL`, INV-6 owns whether a written value lands. §6's "a sixth name stays accurate and must not be renamed" was wrong: that test calls `run_migrations` (which walks to *latest*), so its `== 12` assertion churns to 13 — six names to rename, not five, and two genuine step-names identified as the ones to leave. Shape (a)'s count was a mention-count not a pin-count (13 vs **10**). INV-2's "the reverse is silent" was falsified by a test the same document cites two bullets later. The dataclass defaults turned out to be load-bearing (four-argument `Account(...)` literals in `test_export_dialog.py`) and to silently absorb an un-widened SELECT — now stated, with INV-3 rewritten to fail on it. §10 cited §4.1 while giving the rationale §4.1 explicitly rejects. Two leaked tool-call closing tags were found at EOF and stripped from this file **and** from `FIBR-0113.md` (an authoring artifact; the tag text is deliberately not reproduced here, so no spec carries a literal close-tag). Surfaced not fixed: the project-wide `docs/plans/` gap, filed as FIBR-0195. |
| 2 | 2026-07-28 | 3 (same partition), all at strong depth, **no breadth pass** — the skill forbids a cheap lane certifying a doc that produced a verified CRITICAL the loop before | 0 | 2 | 8 | 9 | 20 verified, 0 unverified. All fixed. Loop 1's critical did not recur, which is the evidence its fix held — but the fix itself was underspecified: §3 decision 5 mandated a read-and-pass-back with **no stated way to do the read**, since `AccountService` exposes no by-id getter and `_refresh` stashes only id/name/type/has-password on the item. Two lanes independently found §11's "six non-seeder `update_account` call sites", which contradicts §4.2 on both count and membership (five, plus a direct repository caller that is not an `update_account` site). INV-5's AST leg was dropped outright: INV-6's *two* legs already localise the fault between repository and service, so the source-level check added nothing and the §5 preamble's claim that the blank-update case was "stated once" was false. §4.2 specified the widened `UPDATE` but never the widened `INSERT` — the quiet half, since an omitted column writes the same `NULL` a genuinely-empty field does; INV-3 now pins its write path to `add_account` with both fields populated. The v12 **comment** sweep was scoped to the wrong set entirely: it is 5 files, not the 14 assertion-churn files, and two of them pin nothing and were invisible to every count in §6. §7 leg 2 claimed a `foreign_key_check` covering `statement_periods`, but nothing in the `build_v9_vault` chain ever writes that table, so that half was unexercised. §11's `SCHEMA_VERSION` row named a catcher that only catches a maximal bump. §1 said the handler "stops compiling" — Python arity-checks at call time. §12's T13 rationale said *display* where the threat row and FIBR-0113 both say *copyability*. **Renamed** `FIBR-0193-account-storage-fields.md` → `FIBR-0193.md`: `naming.md` mandates bare-ID and 48 of 49 siblings comply, so this spec was the sole departure; the standards conflict behind it is filed as FIBR-0196. Not converged — 2 HIGH and 8 MEDIUM are substantive, so loop 3 is owed. |
| 3 | 2026-07-28 | 3 (same partition), all at strong depth, **no breadth pass** — loop 2 produced 2 verified HIGH, so the rule still bound | 0 | 2 | 8 | 11 | 21 verified, 0 unverified. All fixed. **The dominant signal this loop: 4 of the 8 MEDIUMs and 1 HIGH were defects loop 2's own fixes introduced.** §7 still promised "two AST scans" including the INV-5 scan loop 2 deleted; §11's rewritten `SCHEMA_VERSION` row named a catcher *and* conceded an uncaught surface in one cell, the exact shape spec-format §5.8 forbids (split; the error budget was understated at three and is four); §1 grew a third copy of mechanism owned by §3 and §11; and §12's "its resource-cost section cites §4.1" was false — the citation is in FIBR-0113's §6, and it entered this document because a loop-2 lane reported it and **it was written in without being verified**. That is the process failure to carry forward, not a wording slip. Independently: §6 claimed two tests were "genuinely step names" that "assert that step's own destination" — both call `run_migrations` and assert `== 12`, i.e. the exact criterion the same bullet used four lines earlier to disqualify a third test, so the document reached opposite conclusions from identical code. INV-1's central upgrade-path clause had no stated observable (leg 1 covers only first-run; leg 2 asserted neither version, nor column presence, nor that other columns survive). `build_v9_vault` has ten call sites, not three. And the two new item-data roles silently narrow FIBR-0128's `test_INV1_widget_never_renders_or_reads_the_secret`, which sweeps a **literal** `UserRole`..`+3` list — a security guard that keeps passing while covering less; §7 now requires the list be widened in the same change. Not converged — loop 4 is owed. |
| 4 | 2026-07-30 | 3 (same partition), all at strong depth, **no breadth pass** — loop 3 produced 2 verified HIGH, so the rule still bound | 0 | 4 | 9 | 9 | 24 verified (23 fixed, 1 deferred; 1 open question dismissed after checking). **The build contract did not move again — every HIGH this loop was cross-doc bookkeeping, and §12 has now been wrong in four consecutive loops.** §11's `SCHEMA_VERSION` row, *split* in loop 3 to satisfy spec-format §5.8, asserted a `nothing` that is false in both halves: `_migrate_to_v2` is what creates `accounts`, so any partial bump leaves the step's `ALTER` with no table, and `Vault.create()` closes and re-raises — leg 1 is red for a partial bump exactly as it is for a full one. Its consolation ("fails loudly elsewhere — a fresh vault missing `transactions`") was also false: `create()` builds `transactions` itself. The two rows are back to one, and the error budget's *composition* changed while its count did not. §12 claimed FIBR-0113 needed **no edit** while this spec's decision 5 makes its `_ACCOUNT_*_ROLE` deletion set six rather than four — in FIBR-0113's §4.2 *and* its §11 row, both now amended; §12 omitted `FIBR-0128.md` entirely, whose INV-1 enumerates the swept roles and whose Deliverable 3 records "`+3` is free", both now annotated; and §11 had no row for the role-sweep widening §7 itself says nothing catches. §12's instruction to advance FIBR-0172's INV-14a was the inverse error — that row *records what FIBR-0172 shipped*, so editing it would falsify a correct historical record, the same treatment §12 already gives that spec's D5/D6 headings. **Walking the standing pair list found a defect no lane reported:** FIBR-0172's **INV-14** is the same class as INV-14a, so the bullet was recast for both. INV-6 leg 1 and INV-7 lacked INV-3's *distinguishable-values* clause, leaving a transposed `SET`/bind pair in `AccountRepository.update` green everywhere while filing every note as an account number. §7's blanket "every one of these is run red first" was false for INV-4's AST scan, which `repositories/accounts.py` already satisfies. §3 decision 4 ("neither field is validated") contradicted §4.2's mandatory strip, reconciled only in §8 four sections later. §12 leaned on `security-model.md` INV-9's *wording* to cover the new columns with no observable behind it; §7 now has the already-edited `test_INV8_account_cycle_logs_no_secret` sweep `caplog` for both values. **The post-fix sweep earned its keep twice:** it caught a contradiction this loop's own §12 fix introduced against §6 (over whether anything in INV-14a's guard "advances"), and it caught `979` going stale in the run-state file because this loop's collateral edit grew FIBR-0113 to 981. Deferred: `pdf_import` / `import_` `spec.md` pin `LATEST_SCHEMA_VERSION == 5`, a pre-existing staleness outside this item. Not converged — 4 HIGH is substantive, so loop 5 is owed. |
