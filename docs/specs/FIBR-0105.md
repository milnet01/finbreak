# FIBR-0105 — User-configurable amount display (negative sign + colour)

> **Status:** 📝 DRAFT — pending `/cold-eyes` (project `--max-loops 7`), then TDD.
> **Phase:** near-term user request (dogfooding v0.1.4). The Home **Amount**
> column renders negatives in accounting **parentheses** (`(ZAR25,000.00)`) — the
> QLocale default — with no colour. The user asked why some values are bracketed
> and requested a **Settings choice** over the style. **Both styles are offered on
> purpose:** brackets stay available so an accountant using the app keeps the
> notation they're familiar with, while minus is the friendlier default for a
> non-accountant. Ships in **v0.1.5** with the inline update notes.
> **Security:** LOW — display-only, no network, no new stored secret (two short
> opaque strings in the already-encrypted vault `settings` table).
> **On upgrade:** an existing vault has neither key set, so it inherits the new
> default (**minus + colour ON**) on its next post-unlock render — a deliberate,
> reconfigurable display change, not a migration (INV-1: stored data is untouched).

## Verified current state (2026-07-11)

- `src/finbreak/ui/home.py:245` — `_format_amount(display: Decimal, symbol: str)
  -> str` today returns `QLocale().toCurrencyString(float(display), symbol,
  decimals)` (the accounting-parentheses negative on en_US/en_ZA). This feature
  adds the third `negative_style` arg (default `"minus"`).
- `src/finbreak/ui/home.py:160-162` — the Amount cell is set with a **fresh**
  `QTableWidgetItem(_format_amount(...))` on every `refresh()`, so a re-render can
  never carry a stale foreground (INV-4).
- `HomeView.__init__(transactions, categorization, prefs=None, parent=None)`
  (`home.py:58-66`) — `prefs` (FIBR-0083) already sits before `parent`; the **7**
  direct-construction call sites (1 prod `main_window.py:466` + 6 tests) pass at
  most 3 positional args (ending at `prefs`), **none passes `parent` positionally**,
  so inserting `amount_prefs` before `parent` with a default is non-breaking.
- **QLocale negative-currency form is locale-dependent** (verified): `QLocale.c()
  .toCurrencyString(-25000,"ZAR")` → `-25000.00ZAR` (minus, symbol suffix);
  `en_US`/`en_ZA` → `(ZAR25,000.00)` (parentheses). This is why "brackets" is
  built by the formatter, not delegated to QLocale (INV-3 / D2).

## At a glance — invariants

| INV | Guarantee | Upholds |
|-----|-----------|---------|
| INV-1 | **Display-only; stored data is never mutated.** Sign/colour are applied at the display layer; `transactions.amount_minor` keeps its stored value. Test: render `HomeView`, then re-read the row through `TransactionService` and assert the stored amount is unchanged. | ADR-0003 (data integrity) |
| INV-2 | **Two independent prefs, persisted in the vault.** `amount_negative_style` (`"minus"` / `"brackets"`) and `amount_colour` (`"true"` / `"false"`) are `SettingsRepository` keys in the vault `settings` table (mirroring `auto_lock_minutes` / the FIBR-0083 datetime keys). **No schema migration.** Read on unlock; display is post-unlock only. Default (absent) = **`"minus"` + colour ON**. Test: round-trip via `AuthService.set_amount_prefs` → `AuthService.amount_prefs`. | FIBR-0055 / FIBR-0083 pattern; ADR-0003 |
| INV-3 | **Negative-sign style is an explicit, locale-independent display choice; positives are unchanged.** Both styles format the **magnitude** via `QLocale().toCurrencyString(float(abs(display)), symbol, decimals)` (so grouping / decimals / symbol placement stay locale-correct), then apply the sign notation **explicitly**: `"minus"` → a leading `"-"` on a negative (`-ZAR25,000.00`); `"brackets"` → the magnitude wrapped in parentheses on a negative (`(ZAR25,000.00)`). Negativity is `display < 0`, so `Decimal("0.00")` **and** `Decimal("-0.00")` (neither is `< 0`) render bare, as does any positive. **Brackets are built by the formatter, NOT delegated to QLocale's negative pattern** — that pattern is a *minus sign* under the C locale and many others (verified: `QLocale.c().toCurrencyString(-25000,"ZAR")` → `-25000.00ZAR`), so delegating would make "brackets" a no-op on those locales and fail hermetically in CI. Test (hermetic — holds under any locale incl. the CI `QLocale.c()`): `_format_amount(Decimal("-25000"), "ZAR", "brackets")` contains `(` and `)` and no leading `-`; `…, "minus")` starts `-` with no `(`/`)`; `_format_amount(Decimal("69"), …)` and `_format_amount(Decimal("0.00"), …)` have neither, under both styles. | user request |
| INV-4 | **Colour marks direction when on; nothing when off.** With colour ON → a **negative** (`display < 0`) amount cell is red (`_NEGATIVE_TEXT`), a **positive** (`display > 0`) cell green (`_POSITIVE_TEXT`), an amount of **zero** left at the default text colour; with colour OFF → no per-cell foreground is set (every amount cell is the default colour). Colour applies to the **Amount cell only**, never other columns. Because `refresh()` builds a **fresh** `QTableWidgetItem` per render (`home.py:161`), toggling colour ON→OFF via `set_amount_prefs` + re-render leaves no stale colour behind — a transition test pins this, not just a static OFF state. Test: render `HomeView` with colour ON — a negative row's Amount item `foreground().color()` equals `_NEGATIVE_TEXT`, a positive row's equals `_POSITIVE_TEXT` (unwrap the `QBrush` with `.color()` — assert against the `QColor`, not the brush); then push colour OFF via `set_amount_prefs` and assert the Amount item carries no foreground override (`item.data(Qt.ItemDataRole.ForegroundRole) is None`). | user request |
| INV-5 | **Fail-safe on a bad/unknown stored pref, each key independently.** An `amount_negative_style` not in `{"minus","brackets"}` resolves to `"minus"`; an `amount_colour` not in `{"true","false"}` resolves to `True` (ON) — the defaults, never a crash. Test: a bogus `amount_negative_style` yields `"minus"` **and** a bogus `amount_colour` yields `True` (each key asserted independently); neither raises. | robustness |
| INV-6 | **(i18n / geometry.)** Every new fixed-word label / combo item / checkbox uses `self.tr("literal")` (never `self.tr(variable)`); currency samples are data shown verbatim. The controls have **no fixed-geometry calls** — covered by the FIBR-0051 `test_INV10_no_fixed_geometry_in_new_ui` glob over `ui/*.py`. (Only the no-fixed-geometry half is a runnable check; the `tr()`-wrapping half is a `coding.md § 5.2` review item, not a runnable test — mirrors FIBR-0083 INV-7.) | `coding.md § 5.2`; FIBR-0051 INV-10 |
| INV-7 | **First-run collects the choices and persists them only when the vault is created** (mirrors FIBR-0083 INV-8 / base-currency timing): the wizard pre-fills the two controls with the defaults; a successful vault creation writes them via `AuthService.set_amount_prefs`; a cancelled first-run persists nothing. Test: a first-run happy path asserts the prefs are stored; a cancel asserts `set_amount_prefs` was **never called** (spy). | FIBR-0004 INV-5 |

## Design decisions

- **D1 — Mirror the FIBR-0083 display-prefs shape exactly.** A frozen
  `AmountPrefs` dataclass + `AuthService.amount_prefs()` / `set_amount_prefs()` over
  `SettingsRepository`, the shell reads it once post-unlock and passes it as a value
  to `HomeView`, and re-pushes on Settings Save. This is the established pattern
  (FIBR-0083 D3/D7) — reuse, not re-invent. *(No shared "DisplayPrefs" umbrella
  refactor: `AmountPrefs` and `DateTimePrefs` are separate frozen dataclasses,
  separate keys, separate controls — the Rule-of-Three isn't met by a second prefs
  group.)*
- **D2 — `_format_amount` gains a `negative_style` arg; the sign notation is built
  explicitly; colour lives at the call site.** The sign shaping is a pure string
  transform (`_format_amount(display, symbol, negative_style: str = "minus") ->
  str`) — the `"minus"` **default** keeps the existing 2-arg test caller
  (`tests/features/app_shell/test_app_shell.py:509`) valid, so the gate stays green;
  `HomeView.refresh` passes the pref explicitly. **Both** styles format the
  **magnitude** the same way — `QLocale().toCurrencyString(
  float(abs(display)), symbol, decimals)`, so grouping / decimal separator / symbol
  placement stay locale-correct (and `decimals` is still derived from
  `display.as_tuple().exponent` as today). The formatter then adds the sign notation
  **itself** for a negative (`display < 0`): `"minus"` → `"-" + magnitude`;
  `"brackets"` → `"(" + magnitude + ")"`. A non-negative amount is the bare
  magnitude under both. **We do NOT delegate brackets to QLocale's negative-currency
  pattern** (as the old `toCurrencyString(float(display), …)` did) — that pattern is
  parentheses only on *some* locales (en_US/en_ZA) and a minus sign on the C locale
  and others, so delegating would make "brackets" non-deterministic and a no-op for
  the accountants it exists to serve (INV-3). Colour is **not** in `_format_amount`
  (a string has no colour) — `HomeView.refresh` sets the Amount `QTableWidgetItem`'s
  foreground from `amount_prefs.colour` + `display < 0` / `> 0`.
- **D3 — Colour constants readable on the app's dark theme (and acceptable on
  light).** `_NEGATIVE_TEXT = QColor(224, 108, 117)` (a soft red) and
  `_POSITIVE_TEXT = QColor(152, 195, 121)` (a soft green) — module constants in
  `home.py`, mid-tone so they read on both palettes. *(Palette-adaptive theming is
  FIBR-0014; these fixed tones are the pragmatic choice for a dark-default app,
  ADR-0002.)*
- **D4 — Settings + first-run controls follow the existing idioms.** A
  **combo** for the negative style (`settings_amount_negative`, two items:
  *Minus (-)* → `"minus"`, *Brackets ( )* → `"brackets"`; `userData` = the stored
  token, preselected via `select_combo_data`) — the label uses the **ASCII hyphen-
  minus `-` (U+002D)**, the same glyph the formatter emits (`"-" + magnitude`), so
  what the combo advertises matches what the cell shows — and a **checkbox** for
  colour
  (`settings_amount_colour`), matching the auto-lock combo + update checkbox already
  in the dialog. Persisted **inside `_on_save`** under the same `VaultLockedError`
  guard as auto-lock / the datetime write. First-run writes them at the post-create
  `_on_derived` site (D6-style, FIBR-0083). Radio buttons are **not** used (the
  dialog has no radio idiom; a 2-item combo is consistent).

## Deliverables

1. **`src/finbreak/services/auth.py`** — `AmountPrefs` (frozen dataclass:
   `negative_style: str`, `colour: bool`); `AuthService.amount_prefs() ->
   AmountPrefs` (reads `amount_negative_style` default `"minus"`, `amount_colour`
   default `True`, with the INV-5 fail-safe) and `set_amount_prefs(prefs)` (writes
   both keys — `colour` as `"true"`/`"false"`). Mirrors `datetime_prefs` /
   `set_datetime_prefs`.
2. **`src/finbreak/ui/home.py`** — `_format_amount(display, symbol,
   negative_style="minus")` (D2 sign logic; the default keeps the existing 2-arg
   test caller valid); the `_NEGATIVE_TEXT` / `_POSITIVE_TEXT` `QColor` constants;
   `HomeView` gains an `amount_prefs: AmountPrefs` constructor param **after `prefs`,
   before `parent`** (defaults to `AmountPrefs("minus", True)` when omitted, so the
   existing **7** direct-construction call sites — 1 production `main_window.py:466`
   + 6 tests — stay valid; verified none passes `parent` positionally, so inserting
   before it is safe — same low-churn default as FIBR-0083's `prefs`), consumed by
   `refresh()` (sign + per-cell colour), plus a `set_amount_prefs(prefs)` method that
   stores + re-renders.
3. **`src/finbreak/ui/settings.py`** — the negative-style combo + colour checkbox
   (D4), preselected from `service.amount_prefs()`, persisted in `_on_save` under the
   existing `VaultLockedError` guard.
4. **`src/finbreak/ui/first_run.py`** — the same two controls, pre-filled with the
   defaults, persisted at `_on_derived` after `complete_first_run` (INV-7).
5. **`src/finbreak/ui/main_window.py`** — read `self._amount_prefs =
   self._service.amount_prefs()` once post-unlock in `_build_workspace` (beside the
   FIBR-0083 `self._prefs` read), pass it to `HomeView`, and on Settings Save re-read
   + push via `HomeView.set_amount_prefs` (beside the existing datetime push).
   `StatementsWidget` is **not** touched (no amount column).
6. **Tests** — `tests/features/app_shell/` (the `_format_amount` sign legs + a
   `HomeView` colour render leg — INV-3/4, and the INV-1 no-mutation leg);
   `tests/features/settings/` (the prefs round-trip INV-2/5 + the Settings combo +
   checkbox qtbot legs); `tests/features/first_run/` (INV-7 happy-path persist +
   cancel spy, via the synchronous `DeriveWorker` stand-in); the geometry glob
   (INV-6).
7. **Docs** — a CHANGELOG **Added** entry in the **`[0.1.5]`** block. That block is
   **unreleased** despite carrying a date: this project dates a version at
   version-bump time (there is **no `v0.1.5` git tag / GitHub release yet**), and it
   already holds the inline-update-notes entry this feature ships alongside. Append
   here, not `[0.1.6]`.

## Data model (vault `settings` keys; no schema change)

| key | value | default (absent) |
|-----|-------|------------------|
| `amount_negative_style` | `"minus"` or `"brackets"` | `"minus"` |
| `amount_colour` | `"true"` or `"false"` | `"true"` (ON) |

## Out of scope

- **Per-account / per-currency amount rules** — a single base currency today
  (FIBR-0087 multi-currency later).
- **Palette-adaptive colour** (re-tinting red/green to the OS light/dark theme) —
  FIBR-0014; fixed mid-tones here (D3).
- **Colouring other columns / a running-balance column** — Amount cell only.
- **Number-locale formatting** (grouping/decimal separators) — already via QLocale
  (FIBR-0017 owns any change).

## Exit criteria

- `./scripts/ci-local.sh` green (ruff/format/bandit/pip-audit/gitleaks + mypy 0 +
  `pytest -m "not perf"`).
- Home Amount shows the chosen sign style and, when colour is on, money-out red /
  money-in green; changing either pref in Settings updates the open Home tab live;
  the stored amounts are untouched.

## Dependencies

- FIBR-0083 (the display-prefs pattern this mirrors; `HomeView` already carries a
  `prefs` value + a live-push on Save) — **shipped**.
- No new runtime dependency (QLocale / QColor are Qt-core/-gui, already used).
