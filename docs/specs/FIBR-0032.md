# FIBR-0032 — Clipboard copy + auto-clear for sensitive values

- **Roadmap:** FIBR-0032 (security-account-recovery).
- **Kind:** security.
- **Depends on:** FIBR-0014 (Settings screen — shipped), FIBR-0012
  (transactions view — shipped), FIBR-0151 (the optional-collaborator
  `x or Default()` injection pattern on `TransactionsView` — shipped).

**Contents:** [Context](#context) · [The mechanism](#the-mechanism) ·
[Design decisions](#design-decisions) · [Invariants](#at-a-glance--invariants)
· [Deliverables](#deliverables) · [Out of scope](#out-of-scope) ·
[Exit criteria](#exit-criteria) · [Verified API basis](#verified-api-basis) ·
[Cold-eyes loop log](#cold-eyes-loop-log).

## Context

Users want to lift a transaction's amount or description out of finbreak — to
paste into a calculator, a search box, or a note. Today there is **no copy
affordance anywhere** in the app (grep of `src/` for `QClipboard` /
`clipboard()` returns zero app-wired hits — FIBR-0032 introduces the first).
The catch: the system clipboard is a **shared, plaintext, cross-application
surface**. Anything copied there lingers until overwritten — readable by any
other running app, and often captured by clipboard-history managers. For a
finance app that is a small but real exposure the moment a copy affordance
exists.

So this feature is deliberately **two halves that ship together**: (1) add the
copy affordance the user asked for — "Copy amount" / "Copy description" on the
transactions list — and (2) make it **self-cleaning**: the copied value is
wiped from the clipboard after a short, configurable timeout, **but only if the
clipboard still holds the value we put there** (never clobber something the user
copied in the meantime).

**Scope resolved by the user (2026-07-18):** the only copyable values are a
transaction's **amount** and **description**. The stored statement PDF password
stays **non-copyable** (no regression of FIBR-0128 INV-1 — that secret never
crosses into the UI), and account numbers are **not** copyable in this pass.
Adding a "copy the PDF password" affordance would be a new secret exposure and
is explicitly excluded.

## The mechanism

Two pieces: a tiny reusable clipboard helper, and the transactions-list wiring
that calls it.

**1. `ClipboardAutoClear` (`src/finbreak/ui/_clipboard.py`) — a `QObject`.**

```
ClipboardAutoClear(clipboard: QClipboard, *, seconds_provider: Callable[[], int], parent=None)
    .copy(text: str) -> None       # set clipboard + arm the auto-clear timer
    .clear_if_ours() -> None       # clear the clipboard IFF it still holds our value
```

- `copy(text)`:
  1. `self._clipboard.setText(text)` — writes to the **default `Clipboard`
     mode** (`QClipboard.Clipboard`; the X11 middle-click *Selection* is not
     touched — D6).
  2. `seconds = self._seconds_provider()` — read **live** at copy time (so a
     mid-session Settings change takes effect on the next copy).
  3. If `seconds > 0`: record `self._pending = text` and arm the timer —
     `self._timer.stop(); self._timer.start(seconds * 1000)` on a single owned
     `QTimer(self)` with `setSingleShot(True)`. If `seconds == 0` ("Never"):
     `self._timer.stop()` and set `self._pending = None` — copy still works, it
     is just never auto-cleared.
  A second `copy()` before the first fires simply replaces `_pending` and
  restarts the one timer, so the **newest** copied value always governs the
  full timeout and a stale pending clear can never fire against a newer value.
- `clear_if_ours()` — **the guard, in one place.** `if self._pending is not
  None and self._clipboard.text() == self._pending: self._clipboard.clear()`,
  then `self._pending = None`. Reading the current clipboard text and comparing
  to the exact string we set is what guarantees we only wipe *our* value — if
  the user copied anything else since (even from another app), the strings
  differ and we leave it alone.
- The single-shot timer's `timeout` signal is connected **directly** to
  `clear_if_ours` (no separate intermediate slot). So the guard has exactly one
  implementation, it is exercised by the auto-clear path (and its tests, INV-3),
  and the deferred lifecycle-clear follow-up (Out of scope) reuses the **same**
  method rather than duplicating the comparison. `clear_if_ours()` is therefore
  **not** dead code this pass — the timeout is its caller.

**2. Transactions-list wiring (`src/finbreak/ui/transactions.py`).**

The transactions table is a `QTableWidget` that already has a custom context
menu (`_show_context_menu`, `transactions.py:345`, currently one action "Set
category…" whose `triggered` connects to the **named slot** `self._on_set_category`,
`transactions.py:350`). Extend that menu, for a selected row, with **"Copy
amount"** and **"Copy description"** above a separator, then the existing "Set
category…"; each new action's `triggered` connects to a **named slot** —
`self._on_copy_amount` / `self._on_copy_description` — mirroring
`_on_set_category`, so a test invokes the slot **directly** (the project's
context-menu test pattern: `_show_context_menu` calls the **blocking**
`menu.exec()`, so tests drive the named slot, not the live menu):

- Row → data: the menu already early-returns unless `self._selected_txn()` is
  non-`None`, which guarantees **exactly one selected row** (`selected_index`
  returns `None` for a non-single selection). Both copy slots reuse that guard
  (each early-returns when `_selected_txn()` is `None`).
- **`_on_copy_amount`** copies the **already-rendered Amount cell text** of the
  selected *visible* row: `row = next(iter({i.row() for i in
  self._table.selectedItems()}))` (the single selected visible row — the same
  reduction `selected_index` does internally; two sites, so a shared
  `_table_state` helper is deferred until a third per Rule-of-Three), then `item
  = self._table.item(row, _COL_AMOUNT)` and `text = item.text() if item is not
  None else ""`. The `None`-guard is required, not cosmetic: `QTableWidget.item()`
  is typed `QTableWidgetItem | None`, so an unguarded `.item(...).text()` fails
  the gate's **mypy** step (the selected row is always populated, so the guard
  never fires at runtime — but the contract must be mypy-clean). This is the
  exact string the column already shows (built
  once at fill time via `_format_amount`), so it is WYSIWYG by construction
  **and issues no vault read** — critically, it does *not* call `base_currency()`.
  See D1 for why that matters (auto-lock safety).
- **`_on_copy_description`** captures `txn = self._selected_txn()` (the shared
  no-selection guard — mirroring `_on_set_category`, `transactions.py:353-356`),
  then copies `txn.description` (identical to the Description cell, built as
  `QTableWidgetItem(transaction.description)`, `transactions.py:278`) — also no
  vault read, and mypy-clean via that guard (`_selected_txn()` is `Transaction |
  None`, so the un-narrowed `self._selected_txn().description` would not be).
- Both slots route through the injected `ClipboardAutoClear.copy(...)`.

**3. The timeout setting (`clipboard_clear_seconds`).** Mirrors the existing
auto-lock-minutes pattern exactly (an `AuthService` typed getter/setter with a
default constant; the key is **defaulted in code**, not seeded in a migration —
`migrations.py` issues no `INSERT INTO settings`):

- `AuthService.clipboard_clear_seconds() -> int` — reads the
  `clipboard_clear_seconds` settings key; falls back to
  `DEFAULT_CLIPBOARD_CLEAR_SECONDS = 30` on an absent / non-int / out-of-set
  value. `ALLOWED_CLIPBOARD_CLEAR_SECONDS = (10, 30, 60, 0)` (0 = "Never").
- `AuthService.set_clipboard_clear_seconds(seconds: int) -> None` — **raises
  `ValueError`** for a value outside `ALLOWED_CLIPBOARD_CLEAR_SECONDS` (before
  any write), then `SettingsRepository(...).set("clipboard_clear_seconds",
  str(seconds))` — exactly as `set_auto_lock_minutes` raises `ValueError`
  (`auth.py:326-329`), minus that setter's `_arm_timer()` call (there is no live
  clipboard timer to re-arm; the timeout is read fresh per copy — D3).
- `SettingsDialog`: a `QComboBox` (`objectName="settings_clipboard_clear"`)
  whose items come from a **value-keyed dict of literal `tr()` labels** —
  `{10: self.tr("10 seconds"), 30: self.tr("30 seconds"), 60: self.tr("1 minute"),
  0: self.tr("Never")}`, each a `self.tr("<literal>")` and **never**
  `self.tr(variable)` over a loop (which `lupdate` cannot extract) — mirroring
  the auto-lock combo's literal-label dict (`settings.py:73-80`). Preselected on
  open via `select_combo_data(self._combo, service.clipboard_clear_seconds())`,
  persisted in `_on_save` via `self._service.set_clipboard_clear_seconds(self.
  _combo.currentData())` (inside the existing `try/except VaultLockedError`).

**4. Injection.** The view's `__init__` gains an optional `clipboard:
ClipboardAutoClear | None = None` that **defaults to a self-built fallback**
when `None` — exactly mirroring FIBR-0151's `transfers=None` parameter
(`transactions.py:74`; `self._transfers = transfers or
TransferDetectionService(...)`, `:85`). The fallback builds a
`ClipboardAutoClear` over `QGuiApplication.clipboard()` with a constant provider
returning `DEFAULT_CLIPBOARD_CLEAR_SECONDS` imported from
`finbreak.services.auth` (its single home — no second copy lives in `_clipboard.py`),
enough for standalone / test construction.

For the **production** path the shell injects a service-wired instance, so the
timeout is read from the *single* settings source (`AuthService`) rather than a
second reader: `main_window` builds `ClipboardAutoClear(QGuiApplication.
clipboard(), seconds_provider=self._service.clipboard_clear_seconds)` and passes
it as a new `clipboard=` argument at the existing `TransactionsView(...)`
construction (`main_window.py:608`, currently four positional args). **Note —
this shell injection is *new*:** FIBR-0151 added the `transfers` parameter but
left it on its internal fallback and did **not** inject at `main_window.py:608`.
So only the `clipboard=None` fallback mirrors FIBR-0151; the shell-side
`clipboard=` injection is the first of its kind at that call site.

## Design decisions

- **D1 — "Copy amount" copies the already-rendered Amount cell text, with no
  vault read.** Copy reads the selected row's `_COL_AMOUNT`
  `QTableWidgetItem.text()` — the exact string the column already rendered
  (currency symbol, locale grouping, and the user's negative style, `-$1,234.56`
  or `($1,234.56)`), produced once by `_format_amount` at fill time. Rationale:
  (a) it is WYSIWYG by construction — the literal displayed cell, the
  least-surprising "copy this cell" behaviour; (b) it reuses the *rendered
  output* rather than re-deriving, so there is no second money-format path and,
  crucially, **no `base_currency()` call** — the copy handler touches the vault
  not at all. That last point is a **correctness guard, not tidiness:** the
  context menu runs inside a `menu.exec()` **nested event loop** in which the
  idle **auto-lock** `QTimer` can fire (`main_window` wires `service.on_auto_lock
  = self._lock`, `main_window.py:254`). Re-deriving the amount via
  `base_currency()` — a `Vault.connection` read — would raise an **uncaught
  `VaultLockedError`** if the vault locked between opening the menu and clicking,
  exactly the failure every other vault-touching slot in `transactions.py`
  guards with `except VaultLockedError: return`. Reading the cell text sidesteps
  it entirely (INV-8). (The same auto-lock path *also* `deleteLater()`s the
  workspace — `_lock` → `_clear_live()`, `main_window.py:1371` — but that cannot
  corrupt a copy: a triggered menu action runs **atomically** within one event
  dispatch and does **no** vault read, so `_lock`'s deferred delete cannot
  interleave with a running handler; and because the menu is a `QMenu(self)`
  child of the view — which `workspace.addTab(self._transactions_tab, …)` parents
  under the workspace `_clear_live` deletes (`main_window.py:634`) — that teardown
  destroys the menu and ends its `exec()` loop *before* a copy action could fire
  at all — so the handler is never reached
  against a torn-down table, and no `RuntimeError`-liveness guard is needed.)
  **Considered and rejected:** re-deriving via
  `_format_amount(display, base_currency(), negative_style)` — identical output
  but adds a vault read + a needed guard for no benefit; and copying a plain
  machine value (`1234.56`) — forks the number format and picks a paste target
  we can't know (a trivial follow-up if requested).
- **D2 — Clear only if the clipboard still holds our value.** `clear_if_ours()`
  (the method bound to the timer's `timeout`) compares the live clipboard text
  against the exact string we set and clears only on an exact match. (An empty
  copied string is not special-cased: `copy("")` sets and arms as usual, and the
  guard matches `""` and clears it on timeout.) This is the roadmap's explicit requirement ("don't
  wipe something the user copied since"). Unavoidable edge: if the user
  independently copies a *byte-identical* string elsewhere, we cannot tell it
  apart from ours and will clear it on timeout — an accepted, negligible corner
  (identical content is indistinguishable, and the loss is only a re-copy).
- **D3 — Timeout read live, per copy.** `seconds_provider()` is called inside
  `copy()`, not cached at construction, so changing the setting in Settings
  applies to the very next copy without rewiring. Cheap (one settings `SELECT`).
- **D4 — 0 = "Never" means copy-without-auto-clear, not copy-disabled.** A user
  who sets "Never" still gets the copy actions; the value simply persists like
  any normal clipboard copy. The feature never *removes* the ability to copy.
- **D5 — The setting mirrors auto-lock's shape.** Same shape (a `QComboBox` of
  allowed values with an `AuthService` typed getter/setter and a code default),
  same `settings_*` objectName convention, same `_on_save`/`VaultLockedError`
  persistence path, no migration change (the key is defaulted in code, exactly
  as `auto_lock_minutes` and the amount prefs are). One deliberate **difference**
  from auto-lock: auto-lock lists "Never" **last** so a `select_combo_data` miss
  safe-fails to index 0 (the most-aggressive lock); here the getter normalises
  an absent / invalid value to `DEFAULT_CLIPBOARD_CLEAR_SECONDS = 30` *before*
  `select_combo_data` runs, so the combo order is presentation-only and carries
  no safe-fail meaning.
- **D6 — Only the default `Clipboard` mode.** We `setText`/`clear` the standard
  clipboard (Ctrl-C/Ctrl-V). We deliberately do **not** write or clear the X11
  *Selection* (primary/middle-click) buffer: we never put the value there, so
  there is nothing of ours to clear, and touching a user's primary selection
  would be surprising cross-platform behaviour.
- **D7 — The helper is a testable seam.** `ClipboardAutoClear` takes its
  `QClipboard` and `seconds_provider` by injection, so tests drive it with a
  real `QGuiApplication.clipboard()` (qtbot) or a fake and assert the guard
  branches deterministically (interval + active-state after `copy`, and the
  clear / no-clear behaviour by invoking the timeout slot) — no 30-second waits,
  no global-clipboard pollution in the UI tests (which inject a recording fake).

## At a glance — invariants

_These are FIBR-0032's own local numbering, enforced by
`tests/features/clipboard/`._

| INV | Statement |
|-----|-----------|
| **INV-1** | **Copy affordances exist.** For a selected transaction row, the context menu offers **"Copy amount"** and **"Copy description"** (and still "Set category…"). With no row selected, the menu early-returns (unchanged). |
| **INV-2** | **What gets copied.** "Copy amount" places on the clipboard the exact text of the selected row's rendered Amount cell (`item(row, _COL_AMOUNT).text()` — the WYSIWYG string the column already shows: symbol + grouping + the configured negative style); "Copy description" places the in-memory `Transaction.description` (identical to the Description cell text). Neither handler issues a vault read; nothing else is transformed. |
| **INV-3** | **Auto-clear, guarded.** After a copy with a positive timeout, once the timeout elapses the clipboard is cleared **iff** it still holds the exact value we set. If the clipboard content changed after the copy (the user copied something else), the timeout leaves it untouched. |
| **INV-4** | **Configurable + "Never".** The timeout is read live from `AuthService.clipboard_clear_seconds()` (default 30; allowed `(10, 30, 60, 0)`). With the setting at `0` ("Never"), a copy still writes the value and **no** clear timer is armed (the value persists). |
| **INV-5** | **Settings control.** `SettingsDialog` exposes the timeout as a `QComboBox` (`objectName "settings_clipboard_clear"`), preselected to the stored value on open and persisted through `AuthService.set_clipboard_clear_seconds` on Save; the setter **raises `ValueError`** on a value outside the allowed set (before any write), and the getter falls back to the default on an absent / non-int / out-of-set stored value. |
| **INV-6** | **No new secret exposure.** No "Copy password" / "Copy account number" affordance is added anywhere. The stored statement PDF password remains non-copyable and never crosses into the UI (FIBR-0128 INV-1 preserved); the transactions menu exposes only amount + description. |
| **INV-7** | **Clipboard mode + i18n + no network.** Copy and clear target `QClipboard`'s default `Clipboard` mode only (the X11 Selection buffer is never written or cleared). All user-facing strings — the "Copy amount" / "Copy description" menu items, the four combo item labels ("10 seconds" / "30 seconds" / "1 minute" / "Never"), and the settings field label — go through `tr()`; this is a **convention (ruff / reviewer-enforced), not a unit-test assertion** (asserting a *rendered* label can't prove `tr()` was called). The new modules import nothing network-related (the vault-suite static scan covers them). |
| **INV-8** | **Copy is lock-safe.** Neither copy handler performs a `Vault.connection` read (amount = rendered cell text, description = in-memory `Transaction.description`), so triggering either **after the vault has locked** (e.g. the idle auto-lock fired while the context menu's `exec()` loop was open) copies the value **without raising `VaultLockedError`** — matching the `except VaultLockedError: return` discipline every other vault-touching slot in `transactions.py` follows. (Defense-in-depth: per D1 the *live* menu is torn down before a post-lock click can land — this leg asserts the slot is safe even if it somehow did.) |

## Deliverables

1. **`src/finbreak/ui/_clipboard.py`** — new `ClipboardAutoClear(QObject)`:
   inject `QClipboard` + `seconds_provider`; `copy(text)` sets text + arms a
   single-shot `QTimer(self)` for `seconds*1000` (no timer when seconds == 0); the
   timer's `timeout` connects **directly** to `clear_if_ours()`, which clears
   **iff** the clipboard still equals the copied value (no separate slot — see
   [The mechanism](#the-mechanism)). It defines **no** module-level default —
   `DEFAULT_CLIPBOARD_CLEAR_SECONDS` has a single home in `auth.py`
   (Deliverable 2), imported where needed (as `settings.py` imports
   `ALLOWED_AUTO_LOCK_MINUTES` from `auth`, never redefining it).
2. **`src/finbreak/services/auth.py`** — `ALLOWED_CLIPBOARD_CLEAR_SECONDS =
   (10, 30, 60, 0)`, `DEFAULT_CLIPBOARD_CLEAR_SECONDS = 30` — the **single home**
   for both constants (imported elsewhere, never redefined);
   `clipboard_clear_seconds() -> int` (default on absent/invalid) and
   `set_clipboard_clear_seconds(seconds: int) -> None` (validates ∈ allowed) —
   mirroring `auto_lock_minutes` / `set_auto_lock_minutes`.
3. **`src/finbreak/ui/transactions.py`** — `__init__` gains `clipboard:
   ClipboardAutoClear | None = None` (fallback built over
   `QGuiApplication.clipboard()` + a constant provider when `None`);
   `_show_context_menu` adds "Copy amount" + "Copy description" (above a
   separator, before "Set category…") — both labels via `tr()` — each action's
   `triggered` wired to a **named slot** (`_on_copy_amount` /
   `_on_copy_description`, mirroring `_on_set_category`). `_on_copy_amount` reads
   the selected visible row's `_COL_AMOUNT` cell `.text()` (no vault read);
   `_on_copy_description` captures `txn = self._selected_txn()` then copies
   `txn.description`; both early-return on no selection and call
   `self._clipboard.copy(...)`.
4. **`src/finbreak/ui/settings.py`** — a `QComboBox`
   (`objectName="settings_clipboard_clear"`) for the timeout, its items added
   from a value-keyed dict of **literal `self.tr("…")` labels** (not `tr()` over
   a loop variable — `lupdate` can't extract that; mirror `settings.py:73-80`)
   plus a `tr()` field label, preselected via `select_combo_data`, persisted in
   `_on_save` via `set_clipboard_clear_seconds`.
5. **`src/finbreak/ui/main_window.py`** — construct one `ClipboardAutoClear`
   (over `QGuiApplication.clipboard()` + `self._service.clipboard_clear_seconds`)
   and add it as a new `clipboard=` argument to the existing `TransactionsView(...)`
   construction at `main_window.py:608` (a **new** explicit injection — FIBR-0151
   did not inject `transfers` there; it left the param on its internal fallback).
6. **`docs/security-model.md`** — a new **T13** threat row ("A copied sensitive
   value lingers on the shared clipboard"), its mitigation written in the doc's
   residual-honesty convention: copy is user-initiated and limited to non-secret
   amount/description (the PDF password and account numbers are **not**
   copyable); the value is auto-cleared after a configurable timeout **iff** it
   still holds our value; **three named residuals** — a clipboard-history manager
   that snapshots on copy is outside auto-clear's reach; on a mid-timeout app
   exit or vault lock the pending timer dies unfired, so the value can outlive
   its timeout (auto-clear is best-effort, not guaranteed on process exit —
   lifecycle-clear is deferred, see Out of scope); and `0` ("Never") is an
   accepted user choice that forgoes auto-clear (the parallel to T3's honestly-
   stated "Never"). No new §5 INV (the guarantees are FIBR-0032-spec-local,
   mirroring FIBR-0033). Also **bump the doc's Status line** (currently "amended
   through FIBR-0095") to name FIBR-0032, and — because adding T13 is a material
   edit to a contract doc — **run `security-model.md` through `/cold-eyes`** for
   that edit (the doc's own "re-run on each material edit" instruction + CLAUDE.md
   §14).
7. **`tests/features/clipboard/`** — new `spec.md` + `test_clipboard.py`
   (reproduce-first): the headline RED is a UI test that right-clicks a
   transaction row, triggers "Copy amount", and asserts the injected fake
   clipboard received the formatted amount (fails today — the actions don't
   exist). Plus INV-1..8 legs (see Exit criteria), including a **lock-safe** leg
   (INV-8): trigger copy-amount / copy-description against a locked vault and
   assert no `VaultLockedError` escapes and the value is still copied.
8. **Roadmap** flip FIBR-0032 ✅; CHANGELOG `[Unreleased]/Added`.

## Out of scope

- **Lifecycle-clear (on lock and on app-exit)** — wiping a still-pending
  clipboard value the instant the vault locks (manual / idle, via
  `main_window._lock`, `main_window.py:551`) **or the app exits** (the same
  lifecycle seam that wipes the key on exit — security-model T3) is a natural
  strengthening and cheap: both just call the existing
  `ClipboardAutoClear.clear_if_ours()`. Left out **this pass** to hold the
  resolved scope (copy + timed auto-clear). **Accepted residual:** on a
  mid-timeout lock *or quit*, the copied value can outlive its intended timeout
  on platforms where the clipboard survives the app (Windows / macOS /
  Wayland-with-manager); with the 30 s default far below the 10 min auto-lock
  default this window is small, and since `clear_if_ours()` already exists (it
  is the timeout guard, not new code) the follow-up is a one-line wire-up per
  event. Tracked as a roadmap follow-up.
- Copying account numbers or the statement PDF password (a deliberate
  non-goal — FIBR-0128 INV-1, INV-6 above).
- Copying arbitrary cells / whole rows, or a "Copy as CSV" export.
- Clearing or writing the X11 *Selection* (primary/middle-click) buffer (D6).
- A clipboard-history-manager opt-out (`x-kde-passwordManagerHint` MIME and the
  like) — a possible later hardening, not needed for the resolved scope.

## Exit criteria

- `./scripts/ci-local.sh` green (ruff, format, bandit, pip-audit, gitleaks,
  pytest, mypy).
- INV-1..8 each have a falsifiable test (one documented exception: INV-7's
  clipboard-mode leg is falsifiable only on a real display — CI skips it, see
  that leg):
  - INV-1/INV-2/INV-6 — a qtbot `TransactionsView` test: with a row selected,
    calling `view._on_copy_amount()` / `view._on_copy_description()` pushes the
    expected string into an injected fake clipboard (the direct-slot pattern
    `_on_set_category` uses — `menu.exec()` blocks, so the test doesn't drive the
    live menu); and the menu's **non-separator** action texts — captured by
    monkeypatching `QMenu.exec` to a non-blocking stub that records `[a.text()
    for a in menu.actions() if not a.isSeparator()]` (the menu adds a separator
    whose `.text()` is `""`, so an unfiltered `actions()` list would carry a
    phantom fourth entry) — are exactly "Copy amount", "Copy description", "Set
    category…" with **no** password/account action; and calling either copy slot
    with **nothing
    selected** writes nothing to the clipboard and does not raise (the slots'
    `_selected_txn()` early-return).
  - INV-8 — a qtbot `TransactionsView` test: with the row populated then the
    vault locked, calling `view._on_copy_amount()` / `view._on_copy_description()`
    raises no `VaultLockedError` and still copies the rendered / in-memory value.
    Guard against a vacuous pass with a **positive control**: assert a genuine
    vault read (e.g. `service.base_currency()`) *does* raise `VaultLockedError`
    in the same locked-fixture state, so "copy did not raise" proves lock-safety
    rather than an un-locked fixture.
  - INV-3/INV-4 — `ClipboardAutoClear` unit tests: after `copy` the timer is
    active with `interval == seconds*1000`; invoking the guard directly (i.e.
    `clear_if_ours()`) clears an unchanged clipboard and **does not** clear a
    changed one; `seconds == 0` arms no timer and leaves the value in place.
    Plus a **real-elapse** leg that proves the wiring, not just the guard: arm a
    1-second copy and `qtbot.waitSignal(helper._timer.timeout)` (or `waitUntil`)
    to assert the clipboard is actually cleared when the timer fires — a
    mis-wired `timeout → clear_if_ours` connection would pass every other leg yet
    never auto-clear. Plus a **live-per-copy** leg (INV-4 / D3): with the
    `seconds_provider` returning 10, `copy` and assert `interval == 10_000`; flip
    the provider to return 30, `copy` again, assert the new `interval == 30_000`
    — proving the provider is re-read each copy, not cached at construction (a
    cache-at-construction regression passes every other leg yet ignores a
    Settings change until restart).
  - INV-7 — the `tr()` coverage is a **convention** (ruff / reviewer-enforced),
    not a unit assertion (asserting a rendered label can't prove `tr()` was
    called); the **Clipboard-mode-only** clause is a `ClipboardAutoClear` unit
    leg — pre-seed the X11 *Selection* buffer with a sentinel
    (`setText(sentinel, QClipboard.Selection)`), run `copy` + `clear_if_ours`,
    then assert the sentinel **survived** (proving we never wrote or cleared
    Selection), guarded by `if not clipboard.supportsSelection(): pytest.skip(…)`
    — the headless-offscreen CI platform (`QT_QPA_PLATFORM=offscreen`,
    `tests/conftest.py:17`) silently drops Selection writes, which would make a
    bare *unchanged*-assert pass vacuously, so it must be an explicit skip, not a
    false green (consequence: under CI's offscreen platform this clipboard-mode
    leg **always skips** — it is falsifiable only on a real-display run; the
    **no-network** static-scan and the `tr()` convention carry INV-7 in the
    gate); the **no-network** clause is covered by the existing vault-suite
    network static-scan over the new modules. So the clipboard-mode guarantee
    does not rest on a skipped test alone: a **CI-gated source backstop** (like
    the no-network scan) asserts `_clipboard.py` never references
    `QClipboard.Selection` / the Selection mode — a regression that wrote to
    Selection fails the gate even when the runtime leg skipped.
  - INV-5 — a qtbot `SettingsDialog` test: the combo preselects the stored
    value and Save calls `set_clipboard_clear_seconds` with the chosen data.
    Plus `AuthService` unit legs for the clauses the combo can't reach:
    `set_clipboard_clear_seconds(99)` raises `ValueError` and writes nothing,
    and `clipboard_clear_seconds()` returns the default (30) for an absent /
    non-int / out-of-set stored value.
- No global-clipboard pollution: UI tests inject a fake; helper tests that use
  the real `QGuiApplication.clipboard()` restore/clear it.

## Verified API basis

- `ui/transactions.py`: `_build_table()` (QTableWidget) L145; context-menu
  wiring `setContextMenuPolicy` + `customContextMenuRequested.connect(
  self._show_context_menu)` L170-171; `_show_context_menu(self, pos) ` L345
  (`menu.exec(self._table.viewport().mapToGlobal(pos))`); the existing action's
  named-slot wiring `action.triggered.connect(self._on_set_category)` L350 (the
  pattern the copy slots mirror); `_selected_txn()` L401
  (`self._rows[index][0]` via `selected_index`); `self._rows` tuple shape
  `(Transaction, Decimal, str, str)` L93-95; amount render call
  `_format_amount(display, symbol, self._amount_prefs.negative_style)` L268
  (`symbol = self._transactions.base_currency()` L253); column constants
  `_COL_AMOUNT = 1` L57 / `_COL_DESCRIPTION = 2` L58; cells set `setItem(row,
  _COL_AMOUNT, amount_item)` L276 and `QTableWidgetItem(transaction.description)`
  L278 — so the rendered Amount cell `.text()` and the Description cell text are
  exactly what Copy reads (no vault read); the `self._rows` declaration is L95
  (L93 is `self._master`); row tag `tag_row(..., row, row)` L284;
  `self._amount_prefs` L91; the optional-collaborator pattern the `clipboard=None`
  fallback mirrors — `transfers: TransferDetectionService | None = None` L74 →
  `self._transfers = transfers or TransferDetectionService(transactions.vault)`
  L85 (an internal fallback, **not** a shell injection).
- `ui/_table_state.py`: `selected_index(table)` L87 returns the selected row's
  parallel-list **tag** (single-selection only; the tag survives a re-sort); the
  single selected *visible* row is `{i.row() for i in table.selectedItems()}`
  (L91 — the set `selected_index` itself reads), which Copy-amount uses to index
  the rendered cell; `tag_row` L79; `SortableItem` L42.
- `ui/_amount.py`: `_format_amount(display: Decimal, symbol: str,
  negative_style: str = "minus") -> str` L24 (QLocale currency string + explicit
  sign; symbol + grouping included).
- `ui/settings.py`: `SettingsDialog(QDialog)` L46; `saved = Signal()` L47;
  auto-lock combo pattern `self._combo.setObjectName("settings_auto_lock")` +
  `select_combo_data(self._combo, service.auto_lock_minutes())` L81-89;
  `_on_save` persistence + `try/except VaultLockedError` L238-259; Ok routes to
  `_on_save` (not `accept`) L208-211.
- `services/auth.py`: `auto_lock_minutes() -> int` L304 / `set_auto_lock_minutes`
  L322 (**raises `ValueError`** on an out-of-set value, L326-329, before any
  write; the clipboard setter omits its `_arm_timer()` call, L333);
  `DEFAULT_AUTO_LOCK_MINUTES = 10` L55, `ALLOWED_AUTO_LOCK_MINUTES` L63 (the
  pattern to mirror); "defaulted in code … no schema change" note L70-71.
- `repositories/settings.py`: `get(key) -> str | None` L20, `set(key, value)`
  L27 (self-committing upsert); table `key TEXT PRIMARY KEY, value TEXT NOT NULL`.
- `migrations.py`: no `INSERT INTO settings` — setting keys are defaulted in
  code, not seeded (confirms D5).
- `ui/main_window.py`: `TransactionsView(...)` construction site L608 — four
  positional args (`TransactionService`, `CategorizationService`, `self._prefs`,
  `self._amount_prefs`), **no `transfers=` kwarg** — the call the new `clipboard=`
  injection is added to; central lock funnel `_lock(self)` L551 (manual action +
  `service.on_auto_lock = self._lock` L254) — the lifecycle-clear hook noted in
  Out of scope; `QTimer.singleShot(0, self, ...)` idiom L270 / L1380 (`QTimer`
  imported L31).
- `ui/unlock.py`: recurring-timer idiom `self._countdown = QTimer(self)` L56.
- Clipboard: **no existing app-wired `QClipboard` / `clipboard()` use in
  `src/`** — FIBR-0032 adds the first. `QGuiApplication.clipboard() ->
  QClipboard`, `QClipboard.setText(str)` / `.text()` / `.clear()` default to
  `QClipboard.Clipboard` mode (Qt 6 / PySide6). Not project code, so no cite.
- `docs/security-model.md`: threat table T1..T12 (§3, L81-92); §5 tops at INV-10
  — FIBR-0032 adds T13 and no new §5 INV.

## Cold-eyes loop log

Run 2026-07-19, project cap `--max-loops 7`, 2 cold lanes/loop (accuracy/reuse
+ security/consistency). **Cap reached — loop 7 done, consulting user.** Loop 7
still surfaced two substantive MEDIUMs (both now fixed: a two-home constant, and
a menu-assertion that miscounted the separator), so the loop has **not** provably
converged at the project cap `--max-loops 7`. No HIGH since loop 3; the tail is
polish + regressions introduced by earlier loops' own fixes.

| Loop | C | H | M | L | Substantive fixes |
|------|---|---|---|---|-------------------|
| 1 | 0 | 2 | 3 | 6 | The "mirrors FIBR-0151 `transfers=` injection" precedent was **false** — FIBR-0151 satisfies its `transfers` param via the *internal* `x or Default()` fallback, not shell injection (corrected: `clipboard=None` fallback mirrors FIBR-0151; the shell `clipboard=` at `main_window.py:608` is a *new* injection). `clear_if_ours()` reframed as the single guard the timeout uses (not dead code / not duplicated). T13 mitigation must name residuals honestly + bump the security-model Status line + re-cold-eyes that doc. Setter raises `ValueError`; combo item labels + menu labels via `tr()` (marked convention, not asserted). |
| 2 | 0 | 1 | 2 | 4 | **HIGH:** "Copy amount" as specced called `base_currency()` — a **vault read** — inside the context menu's `exec()` nested loop where the idle auto-lock timer can fire → uncaught `VaultLockedError`. Fixed: copy the **already-rendered Amount cell text** (`item(row, _COL_AMOUNT).text()`, no vault read); added **INV-8** (copy is lock-safe). T13 gains a **third** residual (app-exit / lock-mid-timer). Combo labels must be **literal `tr()` per value** (a value-keyed dict), never `tr(variable)` (the `lupdate` extraction trap, `settings.py:73-84`). D5 "mirrors exactly"→"mirrors shape" (+ the "Never"-ordering safe-fail difference). Dismissed one unverified LOW (a `main_window.py:607` citation — the construction opens at L608, confirmed by `read_region`). |
| 3 | 0 | 1 | 2 | 2 | **HIGH:** Deliverable 1 still named a private `_on_timeout` slot that The-mechanism deleted (the `timeout` connects **directly** to `clear_if_ours`, no separate slot) — a stale-draft contradiction that would have an implementer build a slot the API contract + Exit criteria say doesn't exist (fixed D1; + D2 "timeout slot"→`clear_if_ours` guard). **MEDIUM×2:** Exit criteria over-promised "INV-1..8 each falsifiable" — INV-7's Clipboard-mode-only + no-network clauses had **no** leg (added a Selection-buffer-unchanged unit leg + cited the vault-suite network static-scan); and no leg proved the `timeout → clear_if_ours` *connection* fires on real elapse — a mis-wire would pass every listed leg yet never auto-clear (added a `qtbot.waitSignal` real-elapse leg). Resolved both lanes' open question: the auto-lock-during-`menu.exec()` path *also* `deleteLater()`s the workspace (`_clear_live`, `main_window.py:1371`), but it is **harmless** — `QMenu(self)` is a child of the view, so ancestor teardown ends `menu.exec()` *before* any copy action fires (no `RuntimeError` guard needed; a guard would be unreachable). Tightened auth.py cites (`_arm_timer` L332→L333; ValueError L326-328→L326-329). |
| 4 | 0 | 1 | 1 | 4 | **HIGH:** the copy actions were routed inline (`self._clipboard.copy(...)`) but never given **named slots**, yet every Exit-criteria leg "calls the copy-amount/description handlers" — and the project's context-menu test pattern invokes the *named* slot directly (`_on_set_category`, `transactions.py:350`) because `menu.exec()` blocks. Named the slots `_on_copy_amount` / `_on_copy_description` across §mechanism + Deliverable 3, and rewrote the INV-1/2/6/8 legs to invoke them directly (+ a `QMenu.exec` monkeypatch to assert the menu's action set carries no password/account entry). **MEDIUM:** INV-5's setter-`ValueError` + getter-fallback clauses had no leg (the combo only offers valid data) — added `AuthService` unit legs. **LOW:** citation precision (`auth.py:326`→`326-329`; `_clear_live` `deleteLater()` L1356→**L1371**; auto-lock dict `settings.py:73-84`→`73-80`) + the visible-row shown as a bare `set` → `next(iter(...))` scalar. Folded an INFO: the load-bearing auto-lock-safety reason is handler **atomicity** + no-vault-read (a deferred delete can't interleave a running handler), with menu-teardown as the secondary guard. |
| 5 | 0 | 0 | 1 | 5 | **MEDIUM:** the loop-4 INV-7 Selection-buffer leg was **vacuous under CI** — the suite runs `QT_QPA_PLATFORM=offscreen` (`conftest.py:17`), where `text(QClipboard.Selection)` is always `""`, so an "unchanged"-assert can never fail; reworked to a **sentinel-survival** check + `supportsSelection()` skip-guard. **LOW×5:** the copy-amount contract used an unguarded `self._table.item(...).text()` — `item()` is `QTableWidgetItem | None`, so it would fail the gate's **mypy** step (added the `None`-guard + rationale); verified D1's menu-teardown parenting assumption against code and cited it (`workspace.addTab(self._transactions_tab, …)`, `main_window.py:634`); named the empty-string copy edge (not special-cased); flagged the visible-row reduction as a deliberate two-site duplication (Rule-of-Three, not yet extracted); added an INV-8 "defense-in-depth vs D1" clarifier. No HIGH for a second loop — severity is polish-tier. |
| 6 | 0 | 0 | 1 | 4 | **MEDIUM:** INV-4's "timeout read **live per copy**" (D3) had no falsifiable leg — a provider cached at construction would pass every leg yet ignore a Settings change until restart; added a two-copy provider-flip leg (10→30, assert the second `interval == 30_000`). **LOW×4:** gave the INV-8 leg a **positive control** (assert a real vault read *does* raise in the same locked fixture, so "copy didn't raise" isn't vacuous); documented that the INV-7 Selection leg **always skips under offscreen CI** and softened the "each falsifiable" umbrella; retargeted two citations (`auth.py` code-default note L69-71→**L67-69**; auto-lock dict Deliverable-4 `settings.py:73-84`→**73-80**, matching §mechanism). **INFO folded:** added a no-selection leg (both copy slots early-return, write nothing). Third loop with no HIGH; the tail is testability-rigor polish. |
| 7 | 0 | 0 | 2 | 4 | **MEDIUM×2:** (a) `DEFAULT_CLIPBOARD_CLEAR_SECONDS = 30` was defined in **two** modules (`_clipboard.py` + `auth.py`) — two sources of truth, contradicting the "mirror auto-lock's single home" claim; gave it one home in `auth.py` (the view fallback imports it, as `settings.py` imports `ALLOWED_AUTO_LOCK_MINUTES`). (b) The loop-4 menu-contents leg asserted "exactly three action texts", but `QMenu.actions()` includes the **separator** (text `""`) → filter `if not a.isSeparator()`. **LOW×4:** `_on_copy_description` used `self._selected_txn().description` (mypy-unclean, `Transaction \| None`) — rephrased to the guarded `txn = _selected_txn(); …txn.description`, symmetric with the amount slot; added a **CI-gated source backstop** for INV-7's Selection guarantee (its unit leg skips on offscreen CI, so a static check asserts `_clipboard.py` never references `QClipboard.Selection`); fixed a loop-6 citation **regression** (`auth.py` code-default note L67-69→**L70-71** — the loop-6 retarget had moved it off the phrase) and tightened `self._rows` L93-95→**L95**. Fourth loop with no HIGH — findings are polish + two fixable regressions from earlier loops. |
| — | | | | | _Cap `--max-loops 7` reached without provable convergence — paused to consult the user._ |
