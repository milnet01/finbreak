# FIBR-0171 — Cash-flow forecast

**Status:** ✅ **SHIPPED** (code) — TDD `tests/features/forecast/`; tag
`FIBR-0171-complete`; ROADMAP ✅. Reviewed under `review-contract`; § 8 carries
every loop, and the last run (FIBR-0324) ended at its cap rather than
converging. § D1 carries the FIBR-0179 cash-only-anchor amendment.
**Kind:** feature
**Section:** features-accessibility
**Depends on:** FIBR-0142 (recurring-money detection — the confirmed set this
projects, and the `_add_cadence_n` occurrence stepper it reuses), FIBR-0007 /
FIBR-0052 (statement-period records — where the anchor balance is persisted),
FIBR-0008 (importer `ParseResult` contract).

---

## 1. Summary

Turn finbreak from backward-looking ("what did I spend?") into forward-looking
("can I afford this?"). A new **Forecast** tab projects the account balance
forward from **confirmed** recurring income and expenses (FIBR-0142), drawing a
projected-balance line to a chosen horizon.

The forecast's **starting number is a real, current balance** — not a guess and
not a stale figure. Every bank statement prints a closing balance; the importers
already parse it (the Standard Bank importer computes it for its completeness
checksum, then discards it). This feature **persists** that closing balance
(D3), then **brings it current to today** by adding the actual transactions
finbreak has already imported since the statement's date (D1). The forecast
anchors to the sum of those per-account current balances, then projects the
confirmed recurring flows forward.

When no statement has recorded a balance yet (e.g. a CSV-only account), the
forecast falls back to an honest **net-flow** framing: a line from zero showing
the projected *change* ("+R2,400 net by month-end"), never a fabricated balance.

Correctness-critical: finbreak handles people's money, so the forecast must never
show a confidently-wrong number. Every rand shown is either a real balance
brought current from persisted data (with its provenance) or an explicit
projected *change*.

---

## 2. Design decisions

**D1 — Anchor = persisted statement balance brought current with real
transactions.** A statement's closing balance is the only real absolute balance
finbreak can know (summing all imported transactions gives net *change* since the
first import, not a true balance). So for each account we take its **latest**
statement that recorded a closing balance (INV-6), then **add the account's
actual transactions dated in the half-open window `(period_end, today]`** — i.e.
strictly after the statement's `period_end` (rows on `period_end` are already in
the closing balance) and no later than today (future-dated rows are excluded from
the *current* balance) — the running-balance roll-forward every bank does. The
vault-wide anchor is the sum of these per-account current balances. Because each
account is independently rolled to *today*, the summed anchor is coherently "as
of today" (no mixed-date figure), and any income/expense already imported since
the last statement is included, not omitted. The projection window is the
**disjoint** `(today, horizon]` (D6), so no flow is counted in both the anchor
and the forecast. This assumes the post-statement imports are gap-free; a gap is
the reconciliation concern tracked separately (FIBR-0177), and is the best any
local app can do.

> **Amended by FIBR-0179 (post-ship fix).** "For each account" above is narrowed
> to **cash accounts only** (`current`, `savings`) — the same gate FIBR-0177 D1
> applies, for the same reason: a debt product prints its closing balance in the
> *owed* convention (positive = debt), the opposite sign to `amount_minor`, so
> the roll-forward moved it the wrong way and an owed figure was summed into a
> cash total. Debt / investment / other accounts now contribute nothing to the
> anchor and are listed as excluded by D10's provenance line. Contract:
> `tests/features/forecast/spec.md` INV-14.

**D2 — Net-flow fallback when no balance is known.** If no *cash* account
(`current` / `savings`, per D1's FIBR-0179 narrowing) has a recorded closing
balance, the forecast runs in `NET_FLOW` mode: the projected
line starts at 0 and is framed as a projected *net change*, not a balance. Mode
is a vault-wide switch (INV-2). In **both** modes, an account that does not
contribute to the anchor — no recorded balance, or not a cash account — is named
as excluded in the provenance line, with its own reason (D10, as amended by
FIBR-0204 and FIBR-0179). In `ANCHORED` mode the headline also states the account
coverage, so a partial total is never mistaken for a whole-vault balance.

**D3 — Persist the closing balance: schema v11.** The migration chain is
currently at **v10** (`LATEST_SCHEMA_VERSION = 10`; `_migrate_to_v10` already
exists — the hot-path indexes). This feature adds **`_migrate_to_v11`**:
`statement_periods` gains a nullable `closing_balance_minor INTEGER` column,
inside an `owned_transaction` block that ends `UPDATE schema_version SET version
= 11`. `LATEST_SCHEMA_VERSION` bumps **10 → 11**, and `_MIGRATIONS` gains the
`11: _migrate_to_v11` entry. The balance's effective date is the row's existing
`period_end` (a closing balance is as-of the statement end date) — no separate
date column. **No backfill** is possible: historical statements' balances were
discarded at import and are gone; only statements imported *after* this ships (or
re-imported) populate the column. Old rows stay `NULL` and contribute nothing to
the anchor. Idempotency comes from **version-gating** — `run_migrations` only
calls `_migrate_to_v11` for a vault at exactly v10 (`range(current+1,
LATEST+1)`), so the bare `ALTER TABLE ADD COLUMN` runs once and is never replayed
(it is *not* replay-safe on its own). **The version bump is a repo-wide test
sweep, not a two-file edit.** Every assertion that pins the *fully-migrated /
first-run* schema version to `10` bumps to `11` — grep `tests/features` for
`== 10` in a migrate-to-latest / first-run context and for `LATEST_SCHEMA_VERSION
== 10`. The grep is **authoritative**; treat the following list as illustrative, not
exhaustive — it spans ~11 files (db_performance, ofx_import, import_, pdf_import,
categorisation, recurring, transfers, categories, vault, accounts, and the backup
first-run manifest `schema_version == 10`), plus the version stated in the
affected feature `spec.md` prose. **Leave untouched:**
intermediate-version assertions in the per-feature migration atomicity/rollback
tests (`== 2 … == 9`), and the `LATEST_SCHEMA_VERSION + 1` self-tracking sites
(e.g. `accounts` too-new-vault-rejection) which track the constant automatically.
Where an assertion means "the latest version", prefer asserting against
`LATEST_SCHEMA_VERSION` (the codebase's own self-tracking idiom) over a fresh
hard-coded `11`. Note `accounts/test_accounts.py` has **both** kinds — three
hard-coded `== 10` first-run/migrate assertions that DO bump, alongside its
`LATEST_SCHEMA_VERSION + 1` rejection test that does not.

**D4 — Thread the balance through the whole importer→persist pipeline.**
`ParseResult` gains `closing_balance_minor: int | None = None` (its **last**
field; the default `None` keeps every existing 4-arg positional construction and
every existing importer/test unchanged — INV-8). But `commit_import` consumes an
`ImportPreview`, not a `ParseResult`, so the balance must be carried across the
preview boundary too:
- `StandardBankImporter.parse` sets `closing_balance_minor = _minor(closing,
  exponent)` when a closing prints, else `None` (Family A / Savings prints none).
- `OfxImporter._parse_statement` sets it from the OFX ledger balance via
  `getattr(statement, "balance", None)` (ofxparse leaves `balance` **unset**, not
  `None`, when no `<LEDGERBAL>` is present — a bare attribute access raises
  `AttributeError`, and `_parse_statement` runs outside the wizard's boundary
  `try/except`), converting a present `Decimal` to minor with
  `services.transactions.to_minor_storable(value, exponent)`. **Not the bare
  `to_minor`**: this is the one amount in an OFX file that bypasses
  `parse_transaction`, so it inherits none of its money contract, and an
  unstorable `<BALAMT>` must be rejected as a `ValueError` rather than escaping
  as an `OverflowError` from a path with no boundary handler (FIBR-0223).
- CSV and manual entry leave it `None` (no balance column in v1).
- `ImportPreview` gains `closing_balance_minor: int | None`, threaded through
  `_preview_from_result` / `_build_preview` (read off the `ParseResult`) and
  preserved by `retarget` (a re-target must not drop it).
- `ImportService.commit_import` passes `preview.closing_balance_minor` to
  `StatementPeriodRepository.add(...)`.
- **Span-reuse path** (an identical `(account, period_start, period_end)`
  re-import returns an existing id): call a new
  `StatementPeriodRepository.update_closing_balance(period_id, balance_minor)`
  whenever the incoming balance is non-`None` — the incoming-`None` test is the
  caller's, since the method takes an `int`. **The fill-only guard against the
  STORED value lives inside the method, not in the caller**: it writes only when
  the stored value is currently `NULL` (filling a gap left by a prior CSV-only
  import), so the caller never inspects what is there. A `WHERE
  closing_balance_minor IS NULL` form would skip silently, where reading first
  lets a disagreement be *detected*. A non-`NULL` stored balance is **never**
  overwritten — for one span the closing balance is fixed, so an incoming
  *different* non-`NULL` value signals a parse/wrong-file error; keep the stored
  value and **log a warning naming the span (the period id) ONLY**. Never the
  balances: they are decrypted vault content, `security-model.md` INV-9 forbids
  logging that, and with no handler configured the record reaches
  `logging.lastResort` — stderr or the desktop journal, outside the encryption
  boundary (INV-12).

**D5 — Project the confirmed recurring set only.** The projection consumes
`RecurringService.confirmed(today)` — the user-vetted arrangements. Suggested
(un-decided) and dismissed items never appear. The recurring engine is
vault-wide, so the forecast is vault-wide.

**D6 — Pure projection core, integer-minor only, reusing the recurring stepper.**
The service prepares each confirmed item into a Decimal-free input and calls a
clock-free pure function:
```
project_forecast(
    anchor_minor: int | None,          # None => NET_FLOW mode
    items: list[ForecastInput],        # signed integer-minor amounts, prepared
    today: date,
    horizon: date,                     # inclusive end
    anchor_sources: list[AnchorSource] | None = None,  # service-built; carried onto the result (normalised to [])
) -> Forecast
```
`ForecastInput` carries `amount_minor: int` (**signed**: `+` for `IN`, `-` for
`OUT`), `anchor: date` — the item's `last_seen`, its **last observed**
occurrence — `cadence: Cadence`, `merchant: str`, `direction: Direction`. The
first due date is derived by the roll-forward below, so it is not carried.
There is **no `exponent` and no `Decimal` in the core** — the service
does the exact minor conversion once (D7) and passes in the `anchor_sources` it
assembled (the core has no account context of its own, so it just carries them
onto the returned `Forecast` — this is the pure-vs-service seam §7 tests split
on). For each item the core generates future occurrences by **reusing
`finbreak.services.recurring._add_cadence_n`** (the same calendar-aware stepper
FIBR-0142 uses — month/year day-clamped, Jan 31 → Feb 28/29; weekly/fortnightly
fixed-day): every date is the **n-th step from `anchor`**,
`_add_cadence_n(anchor, cadence, n)`, rolling `n` forward while the date is
`<= today`, then emitting each occurrence while `<= horizon`. **Never chained,
and never anchored on `next_expected`** — INV-4 owns the reason. So every
projected
event falls strictly after today, in the **disjoint** `(today, horizon]` window
(the anchor already owns `(period_end, today]`, D1) — no double-count. This also
guarantees the Forecast tab and Recurring tab never disagree about an item's
dates (rule 3, reuse). Each occurrence is a signed event.

The per-item events are then **merged across all items and stable-sorted by
`(on, merchant, direction)`** (a stable sort over that key, fed by the
deterministic `confirmed()` order, so two occurrences that tie on all three keep
a stable, reproducible position) before the running balance is accumulated:
`start_minor = anchor_minor or 0`; walking the sorted events, each
`event.running_after_minor` is the running total after applying it; `end_minor =
start_minor + Σ event.amount_minor` (order-independent, INV-1). The step-line
`points` is `(today, start_minor)`, then one `(event.on, event.running_after_minor)`
per event, then a terminal `(horizon, end_minor)` — so a zero-event forecast
still has two points and draws a flat line to the horizon (INV-13). Two benign
degenerate cases: an event landing exactly on `horizon` yields a point equal to
the terminal vertex (a duplicate `(horizon, end_minor)` — harmless, the line has
a zero-length final segment); and `horizon == today` (the "End of this month"
preset on the last day of a month) yields an empty `(today, horizon]` window and
two coincident points `(today, start_minor)` — an honest zero-width forecast, not
an error. Termination is guaranteed: `_add_cadence_n` is strictly increasing
in `n`, so each step advances the date by ≥ 7 days.

**D7 — Amount → minor conversion is exact, and lives in the service.**
`RecurringItem.amount` is `to_display_decimal(median_low_minor, exponent)` =
`Decimal(minor).scaleb(-exponent)`; converting it back with the same exponent
(`to_minor(amount, exponent)`, its exact inverse) recovers the exact minor
integer (INV-7). `ForecastService` does this once per confirmed item (applying
the direction sign) and passes integer minor amounts into the core, so the core
is Decimal-free and money-safe. The `Account` model carries **no**
per-account currency field; the vault has a single base-currency exponent (the
`minor_unit_exponent` setting), so summing `closing_balance_minor` across
accounts can never mix exponents (INV-13). Multi-currency is out of scope (D12).

**D8 — Horizon: presets in the UI, an explicit date in the core.** The tab offers
**End of this month** (default), **30 days**, **60 days**, **90 days**. The UI
computes the concrete horizon date from the preset and `today` (`today` ≤
horizon always); the pure core takes the date. End date is inclusive (INV-11).

**D9 — Surface: a new Forecast tab**, added to the workspace immediately after
Recurring (`main_window._build_workspace`). `ForecastWidget(service: AuthService)`
mirrors `RecurringWidget` — it takes the `AuthService` and builds
`ForecastService(service.vault)` internally. It contains:
- a **headline figure** — `ANCHORED`: "Projected balance <coverage>: ~R X by
  <horizon>", where `<coverage>` names the account scope when not all **cash**
  accounts contributed (e.g. "(Cheque only)") — a non-cash account is D10's
  exclusion line, never a headline suffix; `NET_FLOW`: "Projected net change: +R X by
  <horizon>". The double-empty case (NET_FLOW *and* zero confirmed items) reads
  "Projected net change: R0 by <horizon>" (start 0, end 0);
- an **anchor-provenance** line (D10);
- a **horizon picker** (`QComboBox`);
- a themed projected-balance **line chart** — a new
  `build_forecast_chart(points, theme, exponent) -> QChart` returning a `QChart`
  (matching every existing `ui/charts.py` builder, which return `QChart`; the
  widget wraps it in a `QChartView`). It
  draws a `QLineSeries`, which the existing `_themed_chart` helper does **not**
  accept (its union is `QPieSeries | QBarSeries`), so it uses its own small
  themed-line path — it is **not** modelled on `build_trend_chart` (a bar chart);
- an **upcoming-events table** (date · merchant · signed amount · running balance
  after), reusing the `RecurringWidget` table idiom.
Empty states: zero confirmed recurring → `points` is just `[(today,
start_minor), (horizon, start_minor)]`, so the chart draws a flat line from today
to the horizon, and the table shows "No confirmed recurring items yet — confirm
some on the Recurring tab"; the headline still renders per the rules above.
(While editing `_build_workspace`, correct its stale "seven-tab" docstring — it
already adds eight tabs; Forecast makes nine.)

**D10 — Anchor provenance is explicit and honest.** The provenance line states
that the starting figure is brought current to today and names each contributing
account with its source statement date and how many transactions were rolled in,
e.g. "Starting balance R8,600 as of today — from Cheque's statement of 2026-06-30
+ 4 later transactions, Savings' statement of 2026-07-15 + 1 later transaction".
The "+ N later transaction(s)" clause is correctly pluralised and **omitted
entirely when N is 0** (a freshly-imported statement adds no "+ 0"). A negative
brought-current balance (overdrawn account) is legitimate and renders as a
negative figure. **Two** exclusion clauses, with distinct reasons, because a
credit card that HAS an imported balance is excluded for a different reason than
an account that has none: an account with no recorded balance is named as
excluded from the starting figure, and a non-cash account (debt / investment /
other, per D1's FIBR-0179 narrowing) is named as excluded *because it is not
spendable cash* — never as "no recorded balance yet".
In `NET_FLOW` mode the line instead explains there is no spendable-cash balance
yet and the chart shows projected change only.

> **Amended by FIBR-0204 (2026-08-03).** As drafted, D10 built the exclusion
> reasons only in the `ANCHORED` branch, so the `NET_FLOW` sentence stood alone.
> But `NET_FLOW` is exactly the mode a debt-only vault lands in — `_anchor`
> contributes no cash source — so a credit-card holder with an imported
> statement was told "no known balance yet" and their account was never named,
> contradicting FIBR-0179's INV-14. The exclusion clauses are now appended in
> **both** modes, and the `NET_FLOW` wording says *spendable-cash* balance, which
> is true of an empty vault and of a debt-only one alike.

**D11 — Themed + translatable.** Chart colours come from `ChartTheme`; all
user-facing strings go through `tr()` / the established translate context, like
`build_trend_chart` and `RecurringWidget`.

**D12 — Explicitly out of scope for v1** (roadmap follow-ups, logged not dropped):
a user-typed manual balance override for balance-less accounts; per-account
(rather than vault-wide) forecasts; multi-currency accounts; scenario/what-if
one-off inputs; a CSV balance-column mapping.

---

## 3. New / changed models

In `models.py`:
- `StatementPeriod` **gains** `closing_balance_minor: int | None` as its **last**
  field (the three repo SELECT column lists append it to keep `StatementPeriod(*row)`
  aligned — the codebase's field-order convention).
- `ForecastMode(StrEnum)`: `ANCHORED`, `NET_FLOW`.
- `ForecastEvent` (`@dataclass`, field order pinned): `on: date`, `merchant: str`,
  `direction: Direction`, `amount_minor: int` (**signed**; its sign is
  authoritative for INV-1 sums — `direction` is carried only for display/colour
  and must agree), `running_after_minor: int`.
- `ForecastPoint` (`@dataclass`): `on: date`, `balance_minor: int` — the step-line
  vertices (the start point plus one after each event).
- `Forecast` (`@dataclass`): `mode: ForecastMode`, `start_minor: int`,
  `end_minor: int`, `horizon: date`, `points: list[ForecastPoint]`,
  `events: list[ForecastEvent]`, `anchor_sources: list[AnchorSource]`.
- `AnchorSource` (`@dataclass`): `account_id: int`, `account_name: str`,
  `statement_balance_minor: int`, `as_of: date` (the source statement's
  `period_end`), `since_txn_count: int`, `current_balance_minor: int` — one per
  contributing account (empty in `NET_FLOW` mode).

`ForecastInput` (D6) lives in `services/forecast.py` (a service-internal prep
type, not persisted). `StatementRow` (the Statements-tab view model) is
**unchanged** — showing the balance on the Statements tab is out of scope.

---

## 4. Deliverables

1. `migrations.py` — `_migrate_to_v11` (additive `ALTER TABLE ADD COLUMN` in an
   `owned_transaction`, version-gated); `LATEST_SCHEMA_VERSION` 10 → 11;
   `_MIGRATIONS[11]`.
2. `models.py` — `StatementPeriod.closing_balance_minor`; the forecast models (§3).
3. `importers/base.py` — `ParseResult.closing_balance_minor: int | None = None`.
4. `importers/standard_bank.py` — set the field from the captured `closing`.
5. `importers/ofx_importer.py` — `getattr(statement, "balance", None)` → minor.
6. `repositories/statement_periods.py` — `add(...)` gains the balance param; the
   three SELECTs (`list_for_account`, `list_all`, `get`) append the column; new
   `update_closing_balance(period_id, balance_minor)` (commit-free, fill-only per
   D4/INV-12); new `latest_closing_balances() -> list[tuple[int, int, str]]`
   (`account_id, closing_balance_minor, period_end`) = per account, the row with
   the greatest `period_end` (tie: greatest `id`) whose `closing_balance_minor`
   is non-`NULL`.
7. `repositories/transactions.py` — `sum_after(account_id, after_date, up_to) ->
   tuple[int, int]` returning `(COALESCE(SUM(amount_minor), 0), COUNT(*))` where
   `account_id = ?` AND `occurred_on > after_date` AND `occurred_on <= up_to`
   (the half-open `(period_end, today]` window, D1). The `COALESCE` makes the
   zero-later-transactions case (a fresh statement) return `(0, 0)`, not `NULL`;
   the count feeds `AnchorSource.since_txn_count` + the provenance line.
8. `services/import_.py` — `ImportPreview.closing_balance_minor`; thread it
   through `_preview_from_result` / `_build_preview` / `retarget`; `commit_import`
   passes it to `add(...)`; span-reuse calls `update_closing_balance` (fill-only).
9. `services/forecast.py` — `ForecastInput`; the pure `project_forecast` (D6);
   `ForecastService` (composes `latest_closing_balances` + `sum_after` per account
   into the current-balance anchor and `AnchorSource`s, reads
   `RecurringService.confirmed` + the vault exponent, prepares `ForecastInput`s,
   returns a `Forecast`; anchor is `None` when no cash account has a recorded balance).
10. `ui/charts.py` — `build_forecast_chart(points, theme, exponent) -> QChart`
    (themed line series; its own path, not via `_themed_chart`). `points` already
    carries the terminal `(horizon, end_minor)` vertex (D6), so the builder needs
    no separate horizon argument to reach the right edge. The `exponent` is the
    vault's `minor_unit_exponent`, which `ForecastWidget` reads — `Forecast`
    itself carries none — and each vertex is plotted as
    `to_display_decimal(balance_minor, exponent)`, i.e. **major units**, so the
    y-axis matches the D9 headline and events table. Plotting `balance_minor`
    raw puts the axis 100x above them.
11. `ui/forecast.py` — `ForecastWidget(service: AuthService)` (headline +
    provenance + horizon picker + chart + events table; `refresh()`; empty states).
12. `ui/icons/forecast.svg` — the tab icon (theme-retinted like the others).
13. `main_window.py` — construct + `addTab` after Recurring; `refresh()` on tab
    activation like the siblings; fix the stale "seven-tab" docstring.
14. Bundling parity — confirm the existing `ui/icons/*.svg` glob carries the new
    icon into the frozen bundle (it does for the sibling icons; add an explicit
    entry/guard only if it does not).
15. `tests/features/forecast/spec.md` + tests (§7); the repo-wide schema-version
    drift-guard sweep (D3) — bump every fully-migrated / first-run `== 10`
    assertion (and the affected `spec.md` prose) to `11`, preferring
    `LATEST_SCHEMA_VERSION` where the assertion means "latest"; leave the
    intermediate-version and `+ 1` self-tracking assertions alone.
16. ROADMAP flip ✅ + CHANGELOG `Added` entry + the D12 follow-up bullets logged.

---

## 5. Invariants (test targets)

- **INV-1** — `end_minor == start_minor + Σ event.amount_minor` (signed), exact
  integer, both modes.
- **INV-2** — `mode == NET_FLOW` ⟺ `anchor_minor is None` ⟺ no **cash**
  (`current` / `savings`) account has a recorded closing balance — the D1
  narrowing, so a debt-only vault runs `NET_FLOW`; in `NET_FLOW`
  `start_minor == 0` and `anchor_sources`
  is empty. Mode is a single vault-wide switch.
- **INV-3** — only `confirmed` recurring items are projected; a suggested or
  dismissed item never produces an event.
- **INV-4** — every projected event date is in the half-open `(today, horizon]`.
  Occurrences are the **n-th step from the item's last observed occurrence**
  (`_add_cadence_n(last_seen, cadence, n)`), rolled forward while `<= today` so
  the first emitted date is strictly `> today`.
  **Not chained, and not anchored on `next_expected`.** Chaining re-feeds a
  clamped date back in, and `next_expected` is *itself* `_add_cadence(last_seen,
  …)` — one clamped step — so either form degrades a month-end item for the rest
  of the projection: a Jan-31 debit order becomes Feb 28, then Mar 28, Apr 28,
  May 28, where the bank debits the 31st, 30th, 31st. Testable via a monthly
  item anchored on Jan 31 projecting Feb 28 → Mar 31 → Apr 30.
  *(Amended 2026-08-31, FIBR-0318. As first written this INV prescribed the
  chained form, contradicting the implementation's own docstring. D6 stated the
  chained form too and was corrected in the same gate, along with § 7's test
  row. Gate run 2026-09-02 under FIBR-0324.)*
- **INV-5** — an `IN` item raises the running balance, an `OUT` item lowers it;
  each `ForecastEvent.amount_minor` sign matches its `direction`.
- **INV-6** — a **contributing account is a cash account** (`current` /
  `savings`, per D1's FIBR-0179 narrowing) that has at least one
  `statement_periods` row with a non-`NULL` `closing_balance_minor`; its balance
  is the latest of **those** rows (`max period_end`, tie `max id`). **Filter
  first, then pick** — a later CSV-only (`NULL`) span does not hide an earlier
  balance-bearing one, which matters because D3 leaves old rows `NULL` and makes
  mixed histories the normal case. An account with no such row contributes
  nothing. This is §5's definition of *contributing*, which INV-13 uses.
- **INV-7** — a persisted closing balance round-trips exactly (Decimal ↔ minor);
  a statement with no closing (Savings) stores `NULL` and imports fine; the
  service's amount→minor prep recovers the exact `median_low` minor value.
- **INV-8** — existing importers/tests unaffected: `ParseResult`'s new field
  defaults `None`; the CSV/manual path stores `NULL`; a pre-v11 `StatementPeriod`
  round-trips with `closing_balance_minor is None`.
- **INV-9** — the `_migrate_to_v11` **step** turns a v10 vault into a v11 one:
  the column exists, old rows are `NULL`, and after that step the version reads
  11. `run_migrations` does not stop there — it continues to
  `LATEST_SCHEMA_VERSION`, which has moved on since this shipped, so assert
  against that constant rather than a literal. The step is version-gated (never
  replayed).
- **INV-10** — the Forecast tab is present after Recurring and renders without
  crashing when there are zero confirmed recurring items and/or zero statements.
- **INV-11** — horizon presets compute the correct dates (end-of-month; `today +
  N days`) with `today <= horizon`; the projection window is `(today, horizon]`;
  the pure core is clock-free.
- **INV-12** — span-reuse balance write is **fill-only**: it writes iff the stored
  value is `NULL` and the incoming is non-`None`; a stored non-`NULL` balance is
  never overwritten, even by a different incoming value.
- **INV-13** — the anchor is the sum over contributing accounts of
  (`statement_balance_minor` + `sum_after(account_id, period_end, today)[0]`),
  i.e. the statement balance brought current with transactions in `(period_end,
  today]`; because `Account` has no per-account currency and the vault has one
  `minor_unit_exponent`, this cross-account sum never mixes exponents. The
  step-line is consistent — `len(points) == 2 + len(events)`, `points[0] ==
  (today, start_minor)`, `points[-1] == (horizon, end_minor)`, point dates
  non-decreasing, and each interior `point.balance_minor` equals
  `event.running_after_minor` for the event that produced it.

---

## 6. Known limitations (documented, honest)

- Bringing the balance current assumes the imports **since** the last statement
  are gap-free. A missed import between the statement and today would make the
  starting figure off by the gap — this is the account-reconciliation concern,
  tracked separately (FIBR-0177), and is the best a local, import-driven app can
  do. The provenance line's "+ N later transactions" makes the roll-forward
  visible so a surprising figure is traceable.
- Accounts with **no** statement carrying a balance (CSV-only, older pre-v11
  imports) contribute nothing to the anchor until a balance-bearing statement is
  imported; they are named as excluded. A later balance-less import does not
  displace an earlier balance (INV-6).
- The forecast projects only *recurring* items; one-off future costs a user knows
  about are not modelled (a possible what-if follow-up, D12).
- The anchor owns dates through today and the projection owns dates strictly
  after today (D1/D6), so a recurring item *expected today but not yet imported*
  is neither in the anchor nor projected until its next cadence — a ≤1-cadence
  edge, accepted for a clean disjoint boundary rather than risking a double-count
  at today.

---

## 7. Test plan (`tests/features/forecast/`)

- **Pure projector** (`test_forecast.py`): anchored `end == start + Σ` (INV-1);
  net-flow start 0 + framing (INV-2); confirmed-only (INV-3); roll-forward past a
  stale anchor + `_add_cadence_n` stepping, a **monthly** item anchored on Jan 31
  asserting the full sequence Feb 28 → Mar 31 → Apr 30 — stopping at Feb 28
  passes under the chained form too — + horizon boundary (INV-4); IN/OUT signs +
  running balance (INV-5); step-line consistency `len(points)==2+len(events)`
  (the terminal `(horizon, end_minor)` vertex) and each interior
  `point.balance_minor` matches the post-event total (INV-13); end-of-month +
  N-day horizon math incl. the degenerate `horizon == today` case (INV-11). No
  `Decimal` in any core call.
- **Migration** (`test_migration_v11.py` or folded in): v10 → v11 additive,
  version-gated, old rows NULL, version == 11 (INV-9); the repo-wide drift-guard
  sweep (D3) leaves the suite green.
- **Importer capture**: Standard Bank sets `closing_balance_minor` when a closing
  prints and `None` for a closing-less Savings statement (INV-7); `ParseResult`
  default is `None` (INV-8); OFX sets it from a ledger balance and does **not**
  crash on a balance-less statement (the `getattr` guard).
- **Preview threading**: `ImportPreview` carries the balance through
  `_build_preview` and survives `retarget`; `commit_import` persists it; a
  span-reuse over a NULL fills it, over a non-NULL leaves it (INV-12).
- **Repository**: `latest_closing_balances` picks the greatest-`period_end`
  non-NULL row per account, ignores NULL rows, ties on id (INV-6); `sum_after`
  sums only strictly-later rows for the account (INV-13); the three SELECTs stay
  `StatementPeriod(*row)`-aligned.
- **Service integration**: `ForecastService` on a seeded vault (accounts +
  statements-with-balances + post-statement transactions + confirmed recurring)
  returns the expected **brought-current** anchored `Forecast` and correct
  `AnchorSource`s (INV-13); a balance-less vault returns `NET_FLOW` (INV-2/INV-6).
- **Qt tab** (`test_forecast_tab.py`, `qtbot`): the tab exists after Recurring and
  renders; empty state (no recurring / no statements) does not crash (INV-10); the
  headline + provenance text reflect the mode and account coverage.

---

## 8. Cold-eyes loop log

- **Loop 1** — 2 cold lanes (doc-vs-code accuracy; consistency/architecture).
  CRITICAL 2 · HIGH 3 · MEDIUM 5 · LOW 5 · INFO 4 (all verified against source).
  Fixed: migration was off-by-one (chain already at v10 → this is **v11**;
  `_migrate_to_v10`/drift-guards corrected, F1/F2); `commit_import` takes an
  `ImportPreview` not a `ParseResult` → thread the balance across the preview
  boundary (F3); span-reuse needs an `update_closing_balance` repo method, made
  fill-only with a disagreement guard (F4/M1/INV-12); OFX `statement.balance`
  raises `AttributeError` when absent + runs outside the boundary catch → `getattr`
  + named converter (F5/L3); chart builders return `QChart` not `QChartView` and
  `_themed_chart` rejects a line series → own themed-line path (F6); **mixed-date
  stale anchor** re-architected to bring each balance current to today with real
  post-statement transactions (H1 → D1/INV-13, adds `sum_after`); month-cadence
  stepping undefined → **reuse `_add_cadence`** (H2/M3/INV-4); Decimal leaked into
  the "integer-only" core → conversion moved to the service, core is Decimal-free
  (M2/D7); pinned the double-empty headline (L1), single-currency precondition
  (L2/INV-13), version-gated idempotency (L4/INV-9), points↔events consistency
  (L5/INV-13); noted the signed-amount/direction authority (I1) and the
  `AuthService`-in widget idiom + stale "seven-tab" docstring (INFO).
- **Loop 2** — 2 cold lanes. HIGH 2 · MEDIUM 4 · LOW 3 · INFO 3 (all verified).
  Fixed: the drift-guard sweep was **grossly under-counted** (`== 10` appears in
  18 test files; ~11 hold fully-migrated/first-run schema-version assertions that
  bump — the spec had named two files, and wrongly exempted `accounts`) →
  D3/Deliverable-15/§7
  now specify a repo-wide sweep preferring `LATEST_SCHEMA_VERSION` (HIGH); the
  shared-`today`-endpoint **double-count** + unbounded `sum_after` → anchor window
  is now half-open `(period_end, today]` and projection strictly `(today,
  horizon]`, disjoint (H1 → D1/D6/INV-4/INV-11/INV-13, `sum_after` gains an
  `up_to` bound); `AnchorSource.since_txn_count` had no source → `sum_after`
  returns `(COALESCE sum, count)` (M1/L2); the pure core couldn't fill
  `anchor_sources` → they're passed into `project_forecast` and carried onto the
  result (M2); the chart couldn't reach the horizon / a 1-point line can't draw →
  `points` gains a terminal `(horizon, end_minor)` vertex, `len == 2 +
  len(events)` (M3/INV-13); global event ordering was undefined → merge-sort by
  `(on, merchant, direction)` before accumulating running totals (M4/D6);
  span-reuse disagreement now **logs a warning** instead of silent discard (L3);
  single-currency is now grounded in `Account` having no currency field (I3/D7);
  pluralised/zero-case provenance + negative-balance note (I1/I2).
- **Loop 3** — 2 cold lanes. HIGH 1 · LOW 3 · INFO/polish 4 (all verified;
  lane A found the spec "highly accurate against source", one mis-citation).
  Fixed: §7 still said `len(points)==1+len(events)` (stale — INV-13/D6 say
  `2+len(events)` after the Loop-2 terminal-vertex fix) → aligned (HIGH,
  mechanical); D4 mis-cited `_minor` as the `.scaleb` form (it uses `value *
  10**exponent`) → corrected; `anchor_sources: list = ()` default would fail the
  mypy gate → `| None = None` normalised to `[]`; "deterministic total order" →
  "stable sort over `(on, merchant, direction)`"; noted the benign on-`horizon`
  duplicate vertex and the `horizon == today` zero-width case; corrected the
  Loop-2 log's site count and framed the D3 grep as authoritative (list
  illustrative).
- **Loop 4** — 1 thorough cold convergence lane (accuracy + consistency +
  testability). **CONVERGED — polish only.** Zero CRITICAL/HIGH/MEDIUM/LOW; every
  code citation re-verified accurate against source (schema v10 / `_add_cadence` /
  `_minor` multiplication form / `to_display_decimal` round-trip / `ImportPreview`
  shape / `_themed_chart` line rejection / `Direction` StrEnum tie-safety); the
  boundary story (`(period_end, today]` anchor vs `(today, horizon]` projection),
  the `2 + len(events)` point count (all four sites), and the mypy-clean signatures
  all confirmed consistent. Polish observations (INV-13 density, D10 provenance
  has no dedicated INV — covered by the Qt test) judged non-actionable. Spec
  signed off for TDD.

- **Loop 5** — 3 cold lanes, genre pinned `spec`, cap 3 (finbreak override).
  Q1 3 · Q2 2 · Q3 0 · Q4 1 — six verified, six fixed, none dismissed. First
  loop of a new run, armed by FIBR-0318's INV-4 amendment (FIBR-0324). **All
  three lanes independently found the same four**, the highest agreement this
  document's log records. The worst: D6 still prescribed the **chained** form
  the amendment forbids — `reusing _add_cadence`, `start at next_expected` —
  so the two normative sections gave an implementer two different stepping
  algorithms, and the one D6 gave is the ratchet defect FIBR-0318 fixed in the
  code. D6's `ForecastInput` field list still named `next_expected`, where the
  shipped dataclass carries `anchor`. The **amendment note itself** claimed D6
  "argues for anchored stepping" — it did the opposite, which is how the
  contradiction survived the edit that created it. And § 7's INV-4 test row
  still asserted only Jan 31 → Feb 28, **which the chained form also passes**,
  so the invariant's own test could not falsify the defect it was amended to
  catch. Two lanes separately found INV-2, D2 and deliverable 9 never narrowed
  after D1's FIBR-0179 cash-only change, so a credit-card-only vault reads as
  `ANCHORED`; confirmed against `forecast.py`'s `CASH_TYPES`. One lane found
  the Depends-on line naming the wrong stepper. **Collateral, out of scope,
  corrected in place:** `tests/features/forecast/spec.md` carried the same
  three defects — a stale stepper name, INV-2 unqualified against its own
  INV-14, and an INV-4 row contradicting INV-4a two lines below it. **A packet
  defect, not the document's:** my "Verified source facts" said `ForecastInput`
  has six fields and listed five; all three lanes flagged it and no finding
  rested on it.

- **Loop 6** — 3 cold lanes, same brief, packet rebuilt from disk. Q1 1 · Q2 4
  · Q3 2 · Q4 0 — seven verified, seven fixed, none dismissed. **Loop 5's fixes
  held**: all three lanes independently confirmed D6, INV-4 and § 7 now agree
  with each other and with `_occurrences`, which is what a cold re-read is for.
  **Not one finding was loop 5's collateral** — every one was pre-existing, and
  all seven trace to the same root: **FIBR-0179's cash-only narrowing was
  applied to D1 and never carried to the passages that depend on it.** D2
  confined the exclusion clause to `ANCHORED`, contradicting D10's FIBR-0204
  amendment, so a debt-only vault would name no excluded account at all. D10
  carried one exclusion clause where the two reasons differ — a credit card
  that HAS a balance is excluded for a different reason than an account with
  none, and the test contract's INV-14 forbids the wrong label by name. INV-6
  is § 5's only definition of *contributing* and omitted the cash gate, which
  INV-13 then inherits. D9's coverage denominator was left undefined. Two lanes
  found the D10 defect; the others came from one lane each. Separately, D4's
  OFX bullet named the bare `to_minor`, where the code calls `to_minor_storable`
  so an unstorable `<BALAMT>` is a `ValueError` rather than an `OverflowError`
  escaping a path with no boundary handler; and D4 put the fill-only guard on
  the caller where the repository method owns it. One Q1: the spec gave
  `build_forecast_chart` two arguments in both places and the function takes
  three — a `TypeError` for anyone building from it, and no instruction to
  source the exponent.

- **Loop 7** — 3 cold lanes, same brief, packet rebuilt from disk. Q1 1 · Q2 3
  · Q3 1 · Q4 0 — five verified, five fixed, none dismissed. **Cap reached
  (3, the finbreak override); the run files its tail and exits.** **A VIOLENT
  cap: three of the five landed on text loop 6 wrote**, so the run was
  repairing itself and nothing suggests a fourth loop would stop. Loop 6's
  INV-6 rewrite inverted the filter order — it read *the latest row, if
  non-`NULL`* where `latest_closing_balances` filters non-`NULL` INSIDE the
  partition and then picks the latest, so a later CSV-only span would have
  hidden an earlier balance and dropped a single-account vault to `NET_FLOW`.
  Two lanes found it. Loop 6's D4 rewrite left *"whenever the incoming balance
  is non-`None`"* one sentence from *"the caller passes unconditionally"*, and
  reinforced a pre-existing false claim with a rationale: the method logs the
  period id **only**, deliberately, because a closing balance is decrypted vault
  content that `security-model.md` INV-9 forbids logging and no handler is
  configured, so the record reaches stderr. Two lanes found that too, and the
  same wrong sentence was in `update_closing_balance`'s own docstring —
  corrected there, since a docstring contradicting its body is worse than
  silence. Two pre-existing: D9 named the chart's `exponent` and never said what
  to do with it, where the shipped comment records the axis having read 100x the
  headline; and INV-9 conflated the v11 STEP with `run_migrations`, which
  continues to `LATEST_SCHEMA_VERSION` — 13 today, so a test written from § 5
  is red. **Routing at the cap:** the document goes to implementation rather
  than a fourth cold read. Its gate is discharged.
