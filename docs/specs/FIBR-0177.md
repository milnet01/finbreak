# FIBR-0177 — Account-level balance reconciliation

**Status:** ✅ **SHIPPED** (code) — TDD `tests/features/reconciliation/`; tag `FIBR-0177-complete`; ROADMAP ✅. (Spec `/cold-eyes` CONVERGED loop 3, 2026-07-24.)
**Kind:** feature.
**Roadmap:** FIBR-0177 (features-accessibility).
**Depends on:** FIBR-0171 (persisted `statement_periods.closing_balance_minor`,
schema v11) · FIBR-0050 (Standard Bank parser, import-time reconciliation) ·
FIBR-0007 (canonical signed `amount_minor`).

---

## 1. Summary

Give every **cash account** an ongoing, at-a-glance health check: *do the
imported transactions add up to the balance the bank actually stated?* Today the
only reconciliation is a **silent hard gate inside the Standard Bank PDF parser**
(`_verify_checksum` raises `ValueError` and aborts the whole import) — there is no
persistent, visible, per-account "✓ / off by R-X" indicator, and it only runs at
Standard-Bank import time.

This feature generalises that check into a **bank-agnostic, persisted-data**
reconciliation: for each account, walk its statement **closing balances** (now
persisted by FIBR-0171) in date order, and confirm the transactions between each
consecutive pair bridge one closing balance to the next. A gap (a missed import,
a missing period) shows as a discrepancy. The result surfaces as a per-account
marker on the **Accounts** tab, mirroring the existing "🔑 statement password
saved" suffix.

**No new stored balance is invented.** The check is built entirely from data the
app already holds: persisted closing balances + the signed `amount_minor` sums the
transactions repository already computes. **No schema change.**

### The reconciliation identity

For two consecutive balance-bearing statements of one account — closing `C_prev`
as-of date `P_prev`, and closing `C_curr` as-of `P_curr` (`P_prev ≤ P_curr`) — the
transactions dated in the **half-open window `(P_prev, P_curr]`** must bridge them:

```
C_curr  ==  C_prev  +  Σ amount_minor  for occurred_on ∈ (P_prev, P_curr]
```

The sum is the `Σ amount_minor` component of
`TransactionRepository.sum_after(account_id, P_prev, P_curr)` (the half-open
primitive FIBR-0171 introduced — it returns `(Σ, count)`, so the service takes
`[0]`). The window is half-open because a transaction dated **on** `P_prev` is
already inside `C_prev`, and one dated **on** `P_curr` is inside `C_curr` (INV-2).
An account **reconciles** iff *every* consecutive pair satisfies this identity
**exactly** (integer minor units, no tolerance). This is a **signed** equality —
deliberately **stricter** than the SB parser's *magnitude* completeness gate
(`abs(reconciled) != abs(closing_m)`, a truncation heuristic): a signed check also
catches an asset account that dips into **overdraft** (a legitimately negative
closing), which a magnitude comparison would miss.

---

## 2. Design decisions

### D1 — v1 scope: **current + savings accounts only**

The persisted `closing_balance_minor` is stored **as the statement printed it**
(`standard_bank._capture_closing` → `_signed_balance`, the trailing-`-` sign only).
For the different account types that printed convention is **not** uniform:

- **Asset cash accounts** (`current`, `savings`): the printed closing is a
  canonical balance (positive = money you have), the **same convention** as the
  canonical `amount_minor` (debit −, credit +; FIBR-0007). So the identity above
  (`C_prev + Σ`) holds directly.
- **Debt products** (`credit_card`, `home_loan`, `personal_loan`): the printed
  closing is the **amount owed** (positive = debt) — the *opposite* convention.
  This is precisely why `standard_bank._verify_checksum` reconciles Family C
  (credit card) with `opening − total`, not `+`. Applying the asset identity here
  would misreconcile by `2·Σ`.
- **Investment** (`investment`): the balance moves with the **market**, not only
  with transactions, so `C_prev + Σ transactions` legitimately ≠ `C_curr`. Not a
  reconciliation target.
- **`other`**: a grab-bag whose sign/semantics are unknown; excluded to avoid
  false "off" warnings.

v1 therefore reconciles **only `current` and `savings`** — the cash accounts where
transactions fully and canonically determine the balance. Every other type gets a
quiet `NOT_SUPPORTED` status (no marker), and its reconciliation is deferred to v2
(see §6). Rationale: in a money app a **false** "off by R-X" is worse than a
missing check; v1 ships zero false alarms for the common case. (INV-5.)

### D2 — Cross-statement telescoping, exact, over persisted closings

Reconciliation is between **consecutive persisted closing balances**, not against a
per-statement opening balance (no opening balance is persisted anywhere — only the
transient SB parser local, discarded). This is the account-level generalisation of
the SB check: statement N's "opening" **is** statement N−1's "closing". The ROADMAP
bullet frames the check as "opening balance + running sum vs the latest statement
balance"; consecutive-closing telescoping is that same idea realised over the data
the app actually persists (closings), with each statement's opening supplied by its
predecessor's closing. Comparison is **exact** on integer minor units — no epsilon
anywhere in the codebase, and money is never silently rounded (FIBR-0007). (INV-1,
INV-6.)

### D3 — The statement chain

Per account, the chain is the statement periods whose `closing_balance_minor IS NOT
NULL`, ordered by **`(period_end, id)` ascending** (the balance *date*, tie-broken
by insert order — the same *columns* `latest_closing_balances` orders on, but
**ascending** here vs its `period_end DESC, id DESC`).
Statements without a closing balance (CSV / manual imports) are **not** chain
nodes, but their transactions still count inside a bridge window (D5). A new
repository method `closing_balances_for_account(account_id)` returns
`[(period_end, closing_balance_minor), …]` — a sibling of the existing
`latest_closing_balances`. (INV-3.) Date ordering and the bridge window rely on the
codebase-wide invariant that `period_end` / `occurred_on` are stored as ISO
`YYYY-MM-DD` strings, so lexicographic `ORDER BY` / `>`/`<=` comparison equals
chronological order (the same property `sum_after` and the forecast anchor already
depend on).

### D4 — Status taxonomy

`ReconciliationStatus` (StrEnum):

- **`RECONCILED`** — asset account, ≥2 balance-bearing statements, *every*
  consecutive pair satisfies the identity exactly.
- **`OFF`** — asset account, ≥2 balance-bearing statements, ≥1 pair fails.
- **`NOT_ENOUGH_DATA`** — asset account with **fewer than two** balance-bearing
  statements (nothing to bridge). Never an error. (INV-4.)
- **`NOT_SUPPORTED`** — account type ∉ {`current`, `savings`} (D1). No chain built.

### D5 — Rollup fields (money-safe, integer minor units)

The pure core returns an `AccountReconciliation`:

- `discrepancy_minor: int` — signed **net** `Σ (actual − expected)` over all pairs
  (0 when reconciled), where for each pair **`expected = C_prev + bridge_sum`** (what
  the closing *should* be) and **`actual = C_curr`** (the stated closing), so
  `diff = C_curr − (C_prev + bridge_sum)`. A positive discrepancy means the stated
  balance runs *ahead* of what the imported transactions explain.
- `off_pair_count: int` — number of pairs whose `diff = (actual − expected) ≠ 0`.
- `checked_pair_count: int` — number of consecutive pairs examined, `max(0, nodes −
  1)` (so a 0- or 1-node account is `0`, never a nonsensical `−1`).

For a status with no pairs to examine, all three fields are **`0`**:
`NOT_ENOUGH_DATA` (< 2 nodes) → `(0, 0, 0)`; `NOT_SUPPORTED` (service-assigned,
D8) → `(0, 0, 0)`.

**Honesty rule:** status is `RECONCILED` iff *every* per-pair diff is exactly 0 —
**not** iff the net is 0. Two offsetting gaps (net 0, `off_pair_count == 2`) are
`OFF`, never falsely reconciled. (INV-7, INV-12.)

A bridge window that **misses** an entirely un-imported intervening period yields a
non-zero diff → `OFF` — this *is* the "catch import gaps" guarantee (INV-10). A
bridge legitimately sums transactions imported by **any** source (CSV between two
SB/OFX statements is counted) → reconciliation validates across mixed import
sources (INV-11).

### D6 — Surface: Accounts-tab per-account marker

The result surfaces exactly where the analogous per-account flag already lives:
`ui/accounts.py::_refresh` appends a translated suffix to each account row (today it
appends `·  🔑 statement password saved` from an id-set). `AccountsWidget.__init__`
(which today builds `self._accounts = AccountService(service.vault)`) gains a sibling
`self._reconciliation = ReconciliationService(service.vault)` — there is **no**
`self._auth` on the widget; the vault comes from the `service: AuthService`
constructor arg. `_refresh` then reads `statuses =
self._reconciliation.account_statuses()` **once** and, per account row, looks up
`statuses[account.id]` (the dict has an entry for **every** account — D8/§3 — so a
direct index is safe) and appends:

- `RECONCILED` → `·  ✓ balances reconcile`
- `OFF`, `off_pair_count == 1` → `·  ⚠ off by {money}`
- `OFF`, `off_pair_count > 1` → `·  ⚠ {n} periods don't reconcile`
- `NOT_ENOUGH_DATA` / `NOT_SUPPORTED` → **no marker** (quiet, like an account with
  no saved password).

`{money}` is the **magnitude** `|discrepancy_minor|` rendered through the existing
display path, exactly as the sibling widgets do it (`ui/forecast.py`,
`ui/recurring.py`, `ui/home.py`): `_format_amount(display, symbol)` from
`ui/_amount.py` takes the currency symbol as an **argument** (it does *not* own it),
so the call is `_format_amount(to_display_decimal(abs(discrepancy_minor),
read_minor_unit_exponent(service.vault.connection)),
TransactionService(service.vault).base_currency())`. The spec's literal "R" examples
are illustrative, not hard-coded — the symbol comes from `base_currency()`, the
exponent from the vault **connection** (not the `AccountService`/vault field).
`AccountsWidget.__init__` therefore also captures `service.vault` (it already holds
`service` transitively via `AccountService(service.vault)`; store the vault or the
`AuthService` so `_refresh` can reach `.connection` and build a `TransactionService`).
This read follows the existing `_refresh` contract —
it runs only when the widget is shown on an **unlocked** vault (the same
precondition the current `account_ids_with_pdf_password()` read relies on), so it
adds no new lock-state handling. A precise **per-statement** "which period is off"
column on the Statements tab is a deliberate v2 follow-up (§6), not v1 — v1 is the
at-a-glance account health tick the roadmap's layman line promises.

### D7 — No schema change

Everything reuses FIBR-0171's v11 `closing_balance_minor` column and the existing
`sum_after` primitive. **`LATEST_SCHEMA_VERSION` stays 11**; no `_migrate_to_v12`;
the schema-version drift-guard assertions across the suite (many feature tests
assert `== 11`; `backup.py` gates on `== LATEST_SCHEMA_VERSION`) are **untouched**
(INV-8). This is a read-only analytics feature over already-persisted data.

### D8 — Layering: pure core + thin service (mirrors FIBR-0171)

- **Pure** `reconcile_account(account_id, closings, bridge_sums) ->
  AccountReconciliation` — clock-free, I/O-free, **no `Decimal`**, signed integer
  minor units only. `closings` is the ordered `[(period_end, closing_minor)]`;
  `bridge_sums[i]` is `Σ amount_minor` over the window between node `i` and `i+1`
  (so `len(bridge_sums) == max(0, len(closings) − 1)`). The math consumes only each
  tuple's `closing_minor`; the `period_end` is carried for symmetry with
  `closing_balances_for_account`'s return shape (and test readability), not read by
  the core. The pure core has **no account-type argument**, so it emits only three of
  the four statuses —
  `RECONCILED` / `OFF` / `NOT_ENOUGH_DATA`; it never emits `NOT_SUPPORTED`. Testable
  in isolation exactly like `project_forecast`.
- **`ReconciliationService(vault)`** composes it: lists accounts; for a type ∉
  {current, savings} it assigns `NOT_SUPPORTED` directly (**without** calling the
  core, D1); otherwise it fetches the chain (`closing_balances_for_account`) and the
  bridges (`sum_after(...)[0]` per gap) and calls the pure core. It returns an entry
  for **every** account (see `account_statuses()` below). Vault-scoped, mirrors
  `ForecastService` / `RecurringService`.

`account_statuses() -> dict[int, AccountReconciliation]` — the service's public
read: **exactly one entry per account** in the vault, keyed by `account.id` (a
*total* map, not the present-only id-set that `account_ids_with_pdf_password`
returns). The Accounts UI indexes it directly (D6). An account with **zero**
statements is a valid asset-type account with `< 2` nodes → `NOT_ENOUGH_DATA`,
`(0, 0, 0)`; an empty vault yields `{}`.

### D9 — Out-of-scope finding logged, not fixed (stay-in-lane, §11)

Investigating D1 surfaced a **pre-existing latent bug in FIBR-0171's forecast
anchor**: `ForecastService._anchor` sums `latest_closing_balances()` across **all**
account types with `current = balance_minor + roll_minor` (a plain `+`). For a debt
account the persisted closing is in **owed** convention (D1), so `+ roll` moves the
anchor the wrong direction — and it also folds an "owed" figure into a vault-wide
"cash" total as if it were an asset. This is reachable (a credit-card account with a
balance-bearing statement contributes to the anchor). It is **out of FIBR-0177's
lane** — logged as **FIBR-0179** (a planned `fix`; verify-first when picked up),
**not** touched here.

---

## 3. New / changed models

```python
# models.py
class ReconciliationStatus(StrEnum):
    RECONCILED = "reconciled"
    OFF = "off"
    NOT_ENOUGH_DATA = "not_enough_data"
    NOT_SUPPORTED = "not_supported"

@dataclass
class AccountReconciliation:
    account_id: int
    status: ReconciliationStatus
    discrepancy_minor: int      # signed net Σ(actual − expected); 0 when reconciled
    off_pair_count: int         # pairs with a non-zero diff
    checked_pair_count: int     # consecutive pairs examined = max(0, nodes − 1)
```

Field order pinned (the repo convention: dataclass field order == SELECT column
order where persisted; this one is computed, never stored). For any status with no
pairs — `NOT_ENOUGH_DATA`, `NOT_SUPPORTED` — the three int fields are all `0`
(never `−1`). The pure core emits only `RECONCILED` / `OFF` / `NOT_ENOUGH_DATA`;
`NOT_SUPPORTED` is assigned by the service (D8).

---

## 4. Deliverables

1. **`models.py`** — `ReconciliationStatus` + `AccountReconciliation` (above).
2. **`repositories/statement_periods.py`** — `closing_balances_for_account(
   account_id) -> list[tuple[str, int]]`: `(period_end, closing_balance_minor)` for
   the account WHERE `closing_balance_minor IS NOT NULL`, `ORDER BY period_end, id`.
   Literal column list (bandit B608), sibling of `latest_closing_balances`.
3. **`services/reconciliation.py`** — pure `reconcile_account(...)` + the
   `_RECONCILABLE_TYPES = {AccountType.CURRENT, AccountType.SAVINGS}` gate +
   `ReconciliationService(vault)` whose `account_statuses() -> dict[int,
   AccountReconciliation]` returns **one entry per account** (D8).
4. **`ui/accounts.py`** — `AccountsWidget.__init__` builds `self._reconciliation =
   ReconciliationService(service.vault)` (sibling of the existing `self._accounts =
   AccountService(service.vault)`); `_refresh` reads `account_statuses()` once and
   appends the per-row suffix (D6); a small helper renders the "off by" magnitude
   via `to_display_decimal` + the `ui/_amount.py` formatter.
5. **`tests/features/reconciliation/`** — `spec.md` + pure-core tests + a repo test
   (`closing_balances_for_account`) + service tests (in-memory vault) + the
   migration-guard test (`test_no_schema_change.py`, INV-8) + a UI marker test.
6. **ROADMAP + CHANGELOG** (at `/close-phase`) — flip FIBR-0177 ✅; add a CHANGELOG
   `Added` entry. (House-format deliverable; the close-phase workflow performs it.)

---

## 5. Invariants (test targets)

| ID | Invariant | Test surface |
|----|-----------|--------------|
| INV-1 | An account `RECONCILED` iff **every** consecutive balance-bearing pair satisfies `C_prev + bridge == C_curr` exactly. | pure + service |
| INV-2 | The bridge is the **half-open** `(P_prev, P_curr]` window (`sum_after`): a txn on `P_prev` is excluded, one on `P_curr` included. | service |
| INV-3 | Only `closing_balance_minor IS NOT NULL` statements are chain nodes; ordered `(period_end, id)` asc. | repo + service |
| INV-4 | Asset account with < 2 balance-bearing statements → `NOT_ENOUGH_DATA`; never raises. | pure + service |
| INV-5 | Account type ∉ {current, savings} → `NOT_SUPPORTED`; no chain built, no false "off". | service |
| INV-6 | Money-safe: pure core uses **integer minor units only**, no `Decimal`, exact comparison (no epsilon). | pure |
| INV-7 | `discrepancy_minor == Σ(actual − expected)`; `off_pair_count ==` count of non-zero per-pair diffs; `checked_pair_count == max(0, nodes − 1)` (never `−1`); all three are `0` for `NOT_ENOUGH_DATA` / `NOT_SUPPORTED`. | pure + service |
| INV-8 | **No schema change** — `LATEST_SCHEMA_VERSION == 11`; no `_migrate_to_v12`; drift-guard assertions unchanged. | migration guard |
| INV-9 | Accounts-tab marker: `✓` for RECONCILED, `⚠ off by {money}` for OFF (1 gap) / `⚠ {n} periods don't reconcile` (>1), **nothing** for NOT_ENOUGH_DATA / NOT_SUPPORTED. `account_statuses()` has an entry for every account. | UI |
| INV-10 | A missing (un-imported) intervening period → a non-zero bridge diff → `OFF` (catch-gaps guarantee). | service |
| INV-11 | Transactions imported by **any** source (CSV between two SB/OFX statements) count inside the bridge. | service |
| INV-12 | Two offsetting gaps (net 0, `off_pair_count == 2`) → `OFF`, never falsely `RECONCILED`. | pure |

---

## 6. Known limitations (documented, honest)

- **Debt & investment accounts not reconciled in v1** (D1). Credit-card / home-loan
  / personal-loan need the owed-convention identity (`C_prev − bridge`); investment
  needs market-aware handling. Deferred to a v2 roadmap item.
- **The earliest closing is not independently verified.** Telescoping has no opening
  anchor (D2), so the first chain node's closing — and any transaction dated
  on-or-before that node's `period_end` — falls outside every bridge and is
  unchecked. A wrong first closing surfaces only once a *later* statement disagrees.
  Acceptable: v1 catches gaps *between* statements, which is where missed imports
  hide.
- **Per-account rollup, not per-statement location.** v1 says *whether* an account
  reconciles and by how much; it does not yet point at *which* statement/period is
  off. A per-statement Statements-tab column (which pairs naturally with FIBR-0038
  coverage/gap detection) is the v2 follow-up.
- **CSV-only accounts** (no closing balance ever persisted) → `NOT_ENOUGH_DATA`.
  FIBR-0178(a)/(d) (a manual balance override, a CSV balance-column mapping) would
  let them contribute; out of scope here.
- **Standard Bank *savings* statements print no closing-balance line** — the SB
  Savings layout carries no closing marker, so `_capture_closing` finds none and
  returns `None` (it returns `None` on a *missing marker*, not unconditionally for a
  family). An account fed only by SB savings PDFs therefore persists no closing and
  lands `NOT_ENOUGH_DATA`, never `RECONCILED`. This does **not** affect the primary
  `current` target: SB *current* statements — also parsed by Family A — **do** print
  a closing and reconcile normally. `savings` stays a valid v1 target (D1) because
  OFX and other sources *do* populate a savings closing; only the SB-savings PDF path
  does not. (So no test should expect an SB-savings-*only* account to reconcile.)
- **Overlapping coverage is fine.** The chain is by balance *date* (`period_end`),
  not coverage span; two statements whose spans overlap still bridge correctly as
  long as transactions aren't double-imported — which the **import pipeline** already
  guarantees (statement span-uniqueness + cross-source dedup at import time), an
  import-side property, **not** a FIBR-0177 invariant (this spec's INV-6 is the
  integer-minor / no-epsilon rule). Two statements with the **same** `period_end` but
  different
  closings reconcile only if those closings are equal (an empty `(P, P]` bridge) —
  which correctly flags a genuine same-date balance disagreement as `OFF`.

---

## 7. Test plan (`tests/features/reconciliation/`)

**Pure core** (`test_reconcile.py`, no DB, no Qt):
- INV-1: three ascending closings bridged exactly by their sums → `RECONCILED`.
- INV-4/INV-7: 0 and 1 closings → `NOT_ENOUGH_DATA` with `checked_pair_count == 0`
  (never `−1`) and `(0, 0, 0)`; empty never raises.
- INV-6: all-integer, exact — a 1-minor-unit gap → `OFF` (no epsilon swallow).
- INV-7: `discrepancy_minor` / `off_pair_count` / `checked_pair_count` values on a
  mixed reconciled/off chain (`checked_pair_count == nodes − 1`).
- INV-12: `[+100 gap, −100 gap]` → net 0 but `off_pair_count == 2` → `OFF`.

**Repository** (`test_closing_balances_for_account.py`, in-memory vault):
- INV-3: `closing_balances_for_account` returns only `closing_balance_minor IS NOT
  NULL` rows, ordered `(period_end, id)` ascending; a NULL-closing period is
  excluded; two rows sharing a `period_end` are ordered by `id`. (The load-bearing
  ordering falsifier, mirroring FIBR-0171's `latest_closing_balances` repo test.)

**Service** (`test_reconciliation_service.py`, in-memory SQLCipher vault):
- INV-1/INV-2: a `current` account with two statements + bridging transactions
  (incl. boundary rows on `P_prev` and `P_curr`) → `RECONCILED`; a boundary-row
  mis-placement falsifier.
- INV-5: a `credit_card` (and a `home_loan`) account → `NOT_SUPPORTED`, regardless
  of its data.
- INV-3: a statement with `NULL` closing (CSV import) is skipped as a node but its
  txns still count in the surrounding bridge.
- INV-10 / INV-7 (service): drop an intervening period's transactions → `OFF` with
  the exact `discrepancy_minor` (signed, `C_curr − (C_prev + bridge)`),
  `off_pair_count`, and `checked_pair_count` asserted end-to-end through the service
  (the service-side rollup falsifier INV-7's "pure + service" surface names).
- INV-11: CSV transactions between two balance-bearing statements are summed in.
- INV-4: a `savings` account with a single balance-bearing statement →
  `NOT_ENOUGH_DATA`; an account with **zero** statements → `NOT_ENOUGH_DATA`,
  `(0, 0, 0)`.
- INV-9 (contract): `account_statuses()` has an entry for **every** account in the
  vault (a total map), including `NOT_SUPPORTED` / `NOT_ENOUGH_DATA` ones.

**Migration guard** (`test_no_schema_change.py`):
- INV-8: `LATEST_SCHEMA_VERSION == 11`; `12 not in _MIGRATIONS`.

**UI** (`test_reconciliation_marker.py`, `qtbot`):
- INV-9: each status renders the right suffix (or none) — a `RECONCILED` account
  shows `✓`, a 1-gap `OFF` shows `⚠ off by …`, a >1-gap `OFF` shows `⚠ {n} periods
  …`, and `NOT_ENOUGH_DATA` / `NOT_SUPPORTED` rows show no reconciliation suffix.

Reproduce-first: the pure-core INV-1/INV-12 and the service INV-10 gap test are
written **RED** first (feature absent) and drive the implementation.

---

## 8. Cold-eyes loop log

_(to be appended per loop by `/cold-eyes --max-loops 7`)_
- **Loop 1 (2026-07-24) — 2 cold lanes (correctness + structure)** — CRITICAL 0 · HIGH 1 · MEDIUM 3 · LOW 4 · INFO 4. All verified + fixed. HIGH: D6/D4 cited a non-existent `self._auth.vault` on AccountsWidget — corrected to build `self._reconciliation = ReconciliationService(service.vault)` in __init__ (mirrors the existing `self._accounts`). MEDIUM: (a) `account_statuses()` key set was ambiguous — pinned to a TOTAL map, one entry per account; (b) INV-3 named a repo test surface with no repo test — added `test_closing_balances_for_account.py`; (c) `checked_pair_count = nodes−1` unclamped → −1 for 0-node accounts — clamped to `max(0, nodes−1)`, and pinned `(0,0,0)` for NOT_ENOUGH_DATA/NOT_SUPPORTED. LOW: signed-exact vs the SB *magnitude* gate reworded (don't reach for abs()); `sum_after` returns `(Σ,count)` → take `[0]`; marker money via `to_display_decimal`+_amount helper (currency not hard-coded "R"); pure core emits only 3 of 4 statuses (service assigns NOT_SUPPORTED). Confirmed correct (no defect): the reconciliation math + half-open window, the D1 asset/debt sign argument, and the D9 out-of-lane forecast-anchor bug (real). Also dropped an over-specified VaultLockedError-silent clause to match the sibling `_refresh` contract; added SB-savings caveat (Family A prints no closing). Not converged — HIGH+MEDIUM were substantive; re-running cold.
- **Loop 2 (2026-07-24) — 2 cold lanes (correctness + structure)** — CRITICAL 0 · HIGH 0 · MEDIUM 3 · LOW 3 · INFO 4. Correctness lane: all high-risk claims re-verified correct (math, half-open window vs sum_after SQL, D1 sign argument, D9 anchor bug, INV-1..12) — only a LOW (pin discrepancy sign) + INFO. Structure lane: coverage complete, testable — 3 MEDIUM: (a) §6 mis-cited THIS spec's INV-6 (integer/no-epsilon) for the span/cross-source dedup guarantee — reworded to the import-pipeline property, not a FIBR-0177 invariant; (b) D9 claimed the forecast-anchor bug was already ROADMAP-logged but it wasn't — FILED it as FIBR-0179 (planned fix) and cited the real id; (c) D6 money-render undercited the real call — pinned _format_amount(display, symbol) with symbol from TransactionService(vault).base_currency() and exponent from vault.connection. LOW: SB-savings caveat over-generalised (_capture_closing returns None on a MISSING marker, not for all of Family A — SB *current* still reconciles); pinned discrepancy sign (expected=C_prev+bridge, actual=C_curr). Added completeness notes: earliest closing unverifiable (no opening anchor); ISO-lexicographic date assumption. Not converged — MEDIUMs were mechanical/accuracy; re-running cold.
- **Loop 3 (2026-07-24) — CONVERGED (2 cold lanes)** — CRITICAL 0 · HIGH 0 · MEDIUM 0 · LOW 2 · INFO 2 — polish convergence. Correctness lane: no substantive findings — all high-risk claims re-verified correct (math, half-open window vs sum_after SQL, D1 sign, D9→FIBR-0179, INV-1..12, edge cases) + the D6 money-render citations confirmed to match real signatures. Structure lane: no CRITICAL/HIGH/MEDIUM — every cross-reference verified accurate (FIBR-0179 exists + matches D9; D6 call byte-accurate vs ui/forecast.py; ordering/account_statuses contract pinned; full deliverable↔INV↔test mapping). 2 LOW fixed: added the migration-guard test to Deliverable 5; strengthened the service INV-10 test to assert the INV-7 rollup fields (discrepancy_minor/off_pair_count/checked_pair_count) end-to-end. 1 INFO noted (pure core ignores the carried period_end — documented). CLEARED FOR CODE.
