# FIBR-0083 — User-configurable timezone + date/time display format

> **Status:** ✍️ **DRAFT** — not yet `/cold-eyes`-converged (global rule § 14
> gates code on convergence).
> **Phase:** near-term user request. The Statements **Imported** column showed a
> raw UTC ISO timestamp (`2026-07-11T06:49:15.506928+00:00`) — surfaced
> dogfooding **2026-07-11**. Built as the feature release the user self-updates to
> (validating FIBR-0054 end-to-end), before FIBR-0054 is closed.
> **Kind:** feature.
> **Source:** user-request-2026-07-11 — the user asked to set their **timezone**
> and **date/time format**, in **both** the Settings page and the **first-run
> wizard**.
> **Security: LOW.** Display-only. No new stored financial data, no network, no
> vault-schema change. The three prefs are a timezone id + two format tokens.
> **Roadmap:** [ROADMAP.md → FIBR-0083](../../ROADMAP.md). **Subsumes FIBR-0048**
> (user-configurable date-display format) — delivered here.
> **Governed by:** `docs/standards/{coding,naming,testing}.md` (`coding.md § 5.2`
> `tr()` / i18n for the new controls; `naming.md § 4` for the new module
> `datetime_format.py`); and
> [ADR-0003](../decisions/0003-sqlcipher-local-only-storage.md) (the vault whose
> `settings` table holds the prefs; this feature adds no network).
> **Builds on:** [FIBR-0004](FIBR-0004.md) (the first-run wizard + the vault
> `settings` table), [FIBR-0055](FIBR-0055.md) (the Settings dialog + the
> `SettingsRepository` + the `auto_lock_minutes` vault-settings pattern this
> mirrors),
> [FIBR-0052](FIBR-0052.md) (the Statements **Imported**/**Period** columns),
> [FIBR-0051](FIBR-0051.md) (the Home transaction-date column), and **FIBR-0047**
> (a ROADMAP-only shipped item, `ROADMAP.md:795` — it fixed the date **input**
> pickers to `yyyy/MM/dd`; this reuses those Qt format tokens for **display**).

**Contents:** At a glance (invariants) · Context · Design decisions (D1–D7) ·
Invariants (INV-1..8) · Deliverables · Data model (vault settings; no schema
change) · Out of scope · Exit criteria · Dependencies · Performance note ·
Verified API basis · Cold-eyes loop log.

**Theme:** every date and timestamp finbreak shows renders in the user's chosen
**date format**, **time format**, and (for real timestamps) **timezone** —
auto-detected from the system by default, overridable in the first-run wizard and
in Settings. It is **display-only**: stored data stays UTC/ISO, so the change is
reversible and cannot corrupt anything.

**The core distinction (D2):** finbreak stores two kinds of "when":

- **Calendar dates** — `transactions.occurred_on`, `statement_periods` start/end
  (e.g. `2025-06-19`, date-only). These are **labels the bank already assigned in
  its own context**; a timezone does **not** apply. They are **reformatted only**,
  **never** timezone-converted (converting a date-only value risks shifting it to
  the wrong day and breaking the match to the printed statement).
- **Timestamps** — `statement_periods.imported_at` (stored as `datetime.now(UTC)
  .isoformat()`, a real instant; surfaced on the Statements tab as
  `StatementRow.imported_at`). These are **converted from UTC to the user's
  timezone** and then formatted (date + time). "When did I import this" is
  genuinely local-time-meaningful.

## At a glance — invariants

| INV | Guarantee | Upholds |
|-----|-----------|---------|
| INV-1 | **Display-only; stored data is never mutated.** Formatting happens at the display layer; `occurred_on`, the `statement_periods` rows, and `imported_at` keep their stored UTC/ISO values. Test: drive a `StatementsWidget`/`HomeView` render (which formats), then re-read the row through its repository and assert the stored string is byte-for-byte unchanged. | ADR-0003 (data integrity) |
| INV-2 | **Calendar dates are reformatted, NEVER timezone-converted.** `format_date(iso, date_pref)` takes **no** timezone parameter and returns the same calendar day in the chosen format. Test: `2025-06-19` under any `timezone` pref keeps the same day (only the format string changes); the function signature has no tz argument. | statement fidelity (D2) |
| INV-3 | **Timestamps are converted UTC → the user's timezone, then formatted.** `format_timestamp(iso_utc, tz_pref, date_pref, time_pref)` shifts the instant into the resolved zone before formatting. Test: `2026-07-11T06:49:15.506928+00:00` in `Africa/Johannesburg` (UTC+2) with `yyyy/MM/dd`+`HH:mm` → `2026/07/11 08:49`. | user request |
| INV-4 | **Defaults are auto-detected from the system; zero-config works.** An absent pref (or the explicit sentinel `"system"`) resolves **at display time** to the current system timezone (`QTimeZone.systemTimeZoneId()`) and the system-locale format (`QLocale.system()`), so a fresh vault shows sensible values with no choice made — and **follows** the OS if the user later changes it. An **explicit** selection is pinned. | user request ("auto-detect, allow override") |
| INV-5 | **The three prefs persist in the vault, hold no sensitive data.** `timezone`, `date_format`, `time_format` are `SettingsRepository` keys in the vault `settings` table (mirroring `auto_lock_minutes`), each a short opaque string (a zone id or a format token / `"system"`). **No schema migration** — the `settings` table is v1 key-value. Read on unlock; display is post-unlock only. Test: round-trip each via `AuthService.set_datetime_prefs` → `AuthService.datetime_prefs`. | FIBR-0055 (auto-lock pattern); ADR-0003 |
| INV-6 | **Fail-safe on a bad/unknown stored pref.** An unrecognised timezone id (`QTimeZone(id).isValid()` is `False`) or a stored format value not in our preset set resolves to the system default rather than crashing or showing a garbled value; an unparseable stored date/timestamp string renders as its raw stored text (never an exception to the UI). Test: feed a bogus zone id and a bogus token and assert a sane fallback string, no raise. | robustness |
| INV-7 | **(i18n / geometry.) The new controls are translatable and layout-managed.** Every new fixed-word label/combo-item uses `self.tr("literal")` (never `self.tr(variable)`; timezone ids and format samples are data, shown verbatim); the controls use layout managers, no fixed geometry — covered by the FIBR-0051 `test_INV10_no_fixed_geometry_in_new_ui` glob (`tests/features/app_shell/test_app_shell.py:487`, globs all `ui/*.py`) and the `coding.md § 5.2` review item. | `coding.md § 5.2`; FIBR-0051 INV-10 |
| INV-8 | **First-run collects the choices and persists them only when the vault is created.** The wizard pre-fills the three controls with detected defaults; on a **successful** vault creation it writes the selections via `AuthService.set_datetime_prefs`. A cancelled/failed first-run creates no vault and persists nothing (mirrors base-currency handling, FIBR-0004 INV-5). Test: a first-run happy path asserts the prefs are stored (via `datetime_prefs`); a cancel asserts `set_datetime_prefs` was **never called** (a spy — no vault exists to inspect). | FIBR-0004 INV-5 |

## Context

**Verified current state (2026-07-11):**

- `statements.py::StatementsWidget.refresh` (`ui/statements.py:105`) sets the
  **Imported** cell to `statement.imported_at` **verbatim** (a raw UTC ISO string)
  and the **Period** cell (`:100`) to `f"{statement.period_start} –
  {statement.period_end}"` (two raw ISO dates). `StatementsWidget.__init__` takes
  `service: AuthService` (`:50`) and builds its `StatementService`/`AccountService`
  from `service.vault` — it retains **those derived services** (not the
  `AuthService` itself) for data; this feature only adds a formatting input.
- `home.py` (`ui/home.py:146`) sets the **Date** cell to `transaction.occurred_on`
  **verbatim** (a raw ISO date). `HomeView.refresh()` takes no arguments.
- Stored values are all UTC/ISO: **repositories** write timestamps via
  `datetime.now(UTC).isoformat()` (`repositories/statement_periods.py:47`;
  `repositories/transactions.py:35` stamps `created_at`, which this feature never
  displays), while the **importers** write the date
  strings via `date().isoformat()` (`importers/csv_importer.py:78`,
  `ofx_importer.py:107`) and the repositories store them verbatim.
- Persistence pattern: `SettingsRepository(connection).get/set(key)` (vault
  `settings` table, v1); `AuthService.auto_lock_minutes()` /
  `set_auto_lock_minutes()` read/write via it (`services/auth.py:218,236`).
  `base_currency` is set at vault creation through `complete_first_run(raw,
  params, base_currency)` (`auth.py:127`).
- The first-run wizard (`ui/first_run.py`) collects password + `base_currency`
  (a `QComboBox`, `:51`) and, on submit, derives the key on a `DeriveWorker`, then
  `complete_first_run` — the vault is **open before the wizard returns** (`:139`).
  The Settings dialog (`ui/settings.py`, FIBR-0055) holds `service` and an
  auto-lock combo persisted **inside** its own `_on_save` (`:111`,
  `service.set_auto_lock_minutes(...)`), a read-only currency label, and the
  FIBR-0054 update checkbox.
- **Qt API confirmed** (PySide6 6.11, headless probe): `QTimeZone
  .systemTimeZoneId()` → `Africa/Johannesburg`; `QTimeZone.availableTimeZoneIds()`
  → 643 ids (machine-specific); `QTimeZone(id).isValid()`; `QDateTime.fromString
  (iso, Qt.DateFormat.ISODateWithMs)` parses the stored timestamp **with or
  without** fractional seconds (`isoformat()` omits microseconds when they are
  zero — both parse `valid=True`); `.toTimeZone(QTimeZone(b"…"))` converts;
  `.date().toString(token)` / `.time().toString(token)` formats; `QDate.fromString
  (iso, Qt.DateFormat.ISODate).toString(token)` reformats a date; `QLocale.system()
  .toString(qdate, QLocale.FormatType.ShortFormat)` → `7/11/26` and `.toString
  (qtime, QLocale.FormatType.ShortFormat)` → `2:30 PM` (the `"system"` resolutions).

## Design decisions

- **D1 — Qt's date engine (`QDateTime`/`QDate`/`QTimeZone`/`QLocale`), not Python
  stdlib.** The format tokens (`yyyy/MM/dd`) are the **same** ones the FIBR-0047
  date **pickers** already use (`ui/import_wizard.py:234`, `ui/manual_entry.py:50`),
  so input and display stay consistent; and the calls are testable headless (no
  event loop). *Rejected:* `zoneinfo` + `strftime` (a second, `%Y`-style token
  vocabulary to map and keep in sync); a user-typed custom format string
  (error-prone, poor UX — curated presets instead, D5).

- **D2 — Dates reformatted only; timestamps timezone-converted.** The semantic
  split above. `format_date` has **no** timezone parameter by design.

- **D3 — Prefs live in the vault via `SettingsRepository`, not the plaintext
  `window.ini`.** Display happens **only post-unlock** (the Statements/Home tabs
  are vault-dependent), so the prefs never need pre-unlock access; keeping them in
  the vault mirrors `auto_lock_minutes` and `base_currency`. No schema migration
  (key-value `settings` table). **Written dialog-side** — exactly like auto-lock:
  `SettingsDialog._on_save` calls `service.set_datetime_prefs(...)` (the dialog
  already holds `service`), and the first-run wizard calls it after
  `complete_first_run` (D6). *(This is deliberately the auto-lock pattern, NOT the
  FIBR-0054 update-flag pattern where the shell persists a network-free checkbox.)*

- **D4 — A `"system"` sentinel means "resolve dynamically at display time."** The
  stored pref is either the sentinel `"system"` (→ resolved at each render:
  timezone via `systemTimeZoneId()`, date/time via `QLocale.system()`, so it
  **follows** the OS) or a pinned specific value (a zone id / a Qt format token).
  Absent ⇒ treated as `"system"`. This is exactly "auto-detect by default,
  override to pin" (the user's ask). The first-run/Settings combos show **System
  default (&lt;detected&gt;)** as the first, pre-selected item, which stores
  `"system"`.

- **D5 — Curated preset lists (wide variety), plus a prepended "System default".**
  `DATE_PRESETS` / `TIME_PRESETS` (Deliverable 1) hold **only concrete `(token,
  sample_label)` rows**; the UI **prepends** a dynamic *System default
  (&lt;detected&gt;)* item that maps to the stored `"system"` sentinel (its label
  is dynamic, so it is not a static table row). Dates: System · `2026-07-11`
  (`yyyy-MM-dd`) · `2026/07/11` (`yyyy/MM/dd`) · `11/07/2026` (`dd/MM/yyyy`) ·
  `07/11/2026` (`MM/dd/yyyy`) · `11 Jul 2026` (`dd MMM yyyy`) · `11 July 2026`
  (`dd MMMM yyyy`) · `Jul 11, 2026` (`MMM dd, yyyy`). Times: System · `14:30`
  (`HH:mm`, 24h) · `14:30:00` (`HH:mm:ss`) · `2:30 PM` (`h:mm AP`, 12h).
  **Timestamp composition (the `"system"`-aware rule):** `format_timestamp` first
  resolves the **timezone** — `tz_pref=="system"` → the system zone; else
  `QTimeZone(tz_pref.encode())` (the stored `str` id is encoded to the
  `QByteArray` the constructor takes; `.isValid()` `False` → system, INV-6) —
  parses the UTC instant (`QDateTime.fromString(iso, ISODateWithMs)`) and
  `.toTimeZone(...)`s it into that zone; then resolves the two format halves
  **independently** and joins with a single space. Each half is **three-way**
  (this is what upholds INV-6): `pref=="system"` →
  `QLocale.system().toString(dt.date()|dt.time(), ShortFormat)`; `pref` is a
  **known preset token** (a member of `DATE_PRESETS`/`TIME_PRESETS`) →
  `dt.date().toString(token)` / `dt.time().toString(token)`; **any other value** (a
  stored token no longer in the preset set) → **system default** — never passed
  raw to `toString`, which would render unknown format chars as literal garble. So
  a `"system"` or unknown half is never concatenated as a raw token — each half is
  formatted, then joined. `format_date` parses via `QDate.fromString(iso,
  ISODate)` and applies the same **three-way** date-part rule (no time, no tz).

- **D6 — First-run persists after the vault exists.** Settings can only be written
  once the vault is created, so the wizard writes the three prefs in the
  `DeriveWorker`-success path (right after `complete_first_run` returns with the
  vault open, `auth.py:127-148`), via `service.set_datetime_prefs`.
  `base_currency` stays on its existing `complete_first_run` path — untouched.

- **D7 — One data-flow: the shell resolves prefs once and passes them as a
  value.** The shell (`MainWindow`) resolves `DateTimePrefs` once via
  `self._service.datetime_prefs()` and passes that **plain value** (not the
  `AuthService`) to the display widgets. `HomeView` and `StatementsWidget` each
  gain a `prefs: DateTimePrefs` constructor parameter **positioned before
  `parent`** — `HomeView(transactions, categorization, prefs, parent=None)` and
  `StatementsWidget(service, prefs, parent=None)` — with their positional call
  sites in `main_window.py` updated in lockstep. The param is consumed by their
  existing `refresh()` render loops, **and** each gains a `set_datetime_prefs(prefs)`
  method that
  stores the new value and re-renders. On **Settings Save**, the shell updates its
  held prefs and calls `set_datetime_prefs` on the open Home/Statements tabs, so a
  change takes effect immediately without a relaunch. The widgets keep their
  existing **data-service objects** (`StatementsWidget`'s
  `StatementService`/`AccountService`, built from `service.vault`; `HomeView`'s
  `TransactionService`/`CategorizationService`) for data — this feature only adds
  the formatting *input*, computed once by the shell and passed as a value. (INV-1
  forbids formatting *mutating* data, not the widgets reading it.)

## Invariants

INV-1..INV-8 are the "At a glance" rows above; each names its test.

## Deliverables

1. **`src/finbreak/datetime_format.py`** *(new, pure, Qt-core only — no Qt Widgets
   import, so unit-testable without `qtbot`)* — the formatter:
   `format_date(iso: str, date_pref: str) -> str`;
   `format_timestamp(iso_utc: str, tz_pref: str, date_pref: str, time_pref: str)
   -> str` (the D5 independent-halves composition); the preset tables
   `DATE_PRESETS: list[tuple[str, str]]` and `TIME_PRESETS: list[tuple[str, str]]`
   (each `(stored_token, sample_label)`, concrete tokens only — the UI prepends the
   System item); `system_timezone_id() -> str` (decodes the `QByteArray` id via
   `.data().decode()`); and the `"system"`-sentinel
   resolution + INV-6 fallbacks — invalid zone (`QTimeZone(id.encode()).isValid()`
   `False`) / **unknown token** (not a member of `DATE_PRESETS`/`TIME_PRESETS`) →
   system default; unparseable input string → raw text.
2. **`src/finbreak/services/auth.py`** — `DateTimePrefs` (frozen dataclass:
   `timezone: str`, `date_format: str`, `time_format: str`, each a stored string);
   `AuthService.datetime_prefs() -> DateTimePrefs` (reads the three
   `SettingsRepository` keys, each defaulting to `"system"` when absent) and
   `AuthService.set_datetime_prefs(prefs: DateTimePrefs)` (writes the three keys).
   Mirrors `AuthService.auto_lock_minutes` / `set_auto_lock_minutes`.
3. **`src/finbreak/ui/settings.py`** — three controls added to `SettingsDialog`: a
   **timezone** combo (`settings_timezone`; first item *System default
   (&lt;detected&gt;)* → `"system"`, then the sorted `availableTimeZoneIds()`;
   editable + completer for
   search), a **date-format** combo (`settings_date_format`), a **time-format**
   combo (`settings_time_format`). **Combo convention:** each item is
   `addItem(sample_label, stored_token)` — the timezone combo's first item is
   `addItem("System default (<detected>)", "system")`, then one item per sorted zone id
   (`QByteArray` ids decoded to `str`); the combos are `setEditable(True)` + a
   completer with `setInsertPolicy(NoInsert)` (type-to-search only — free text
   cannot create an item). Pre-selected from `service.datetime_prefs()` via the
   existing `select_combo_data(combo, value)` helper (`ui/_widgets.py`, as the
   auto-lock combo does — reuse, not re-roll); on Save the persisted value is
   `currentData()` (the stored
   token / `"system"`), written **inside `_on_save`** via
   `service.set_datetime_prefs(...)` (dialog-side, like auto-lock, D3).
4. **`src/finbreak/ui/first_run.py`** — the **same** three controls, pre-filled
   with detected defaults (the `"system"` items selected); on a successful vault
   creation, persisted via `service.set_datetime_prefs` (D6).
5. **`src/finbreak/ui/statements.py`** — **Period** → `format_date` each endpoint;
   **Imported** → `format_timestamp`, using the widget's held `DateTimePrefs`.
6. **`src/finbreak/ui/home.py`** — **Date** → `format_date`, using the held prefs.
7. **`src/finbreak/ui/main_window.py`** — hold the resolved prefs in `self._prefs`
   (`self._service.datetime_prefs()`, resolved at workspace build and re-resolved
   on Settings Save); pass `self._prefs` to the `HomeView`/`StatementsWidget`
   constructors. `SettingsDialog`/`FirstRunDialog` are **not** passed the value —
   they read the current prefs and write the new ones through their own held
   `service` (`datetime_prefs()`/`set_datetime_prefs()`, exactly like auto-lock).
   On Settings Save, re-resolve `self._prefs` and push it to the open
   Home/Statements tabs via their `set_datetime_prefs` (D7). (Mirrors the FIBR-0055
   auto-lock wiring, extended with the live-refresh push.)
8. **Tests** — `tests/features/datetime_format/{spec.md,test_datetime_format.py}`
   (offscreen). The **token / explicit-zone** legs are hermetic (INV-2/3/6: date
   reformat with no tz-shift; UTC→+2 timestamp with a fixed zone id + tokens;
   bad-zone/bad-token → system fallback). The **`"system"` legs are made
   deterministic** by pinning `LC_ALL` + `TZ` (or injecting a fixed `QLocale` /
   zone id) — mirroring the existing `test_INV10_format_amount_localised` C-locale
   pin (`test_app_shell.py:499`) — because `QLocale.system()` /
   `systemTimeZoneId()` read the runner's env and would otherwise flake in CI
   (INV-4). Plus the prefs round-trip (INV-5); the display-only leg (INV-1 — drive
   a widget render, assert the repository row unchanged); and `qtbot` legs for the
   Settings + first-run controls (defaults detected, selection persisted, cancel
   persists nothing — INV-8) and the geometry glob (INV-7).
9. **Docs** — at close: mark **FIBR-0048 → ✅** and annotate its ROADMAP bullet as
   subsumed by FIBR-0083; note the `yyyy/MM/dd` token reuse in the
   `datetime_format.py` module docstring (FIBR-0047 has no spec file to annotate);
   update the "user-configurable format in Settings (FIBR-0014)" TODO comments in
   `ui/import_wizard.py` / `ui/manual_entry.py` to point here (this spec + FIBR-0048
   now own that, not FIBR-0014); CHANGELOG entry.

## Data model (vault `settings` keys; no schema change)

| key | value | default (absent) |
|-----|-------|------------------|
| `timezone` | `"system"` or an IANA id (e.g. `"Africa/Johannesburg"`) | `"system"` |
| `date_format` | `"system"` or a Qt date token (the D5 preset set, e.g. `"yyyy/MM/dd"`) | `"system"` |
| `time_format` | `"system"` / `"HH:mm"` / `"HH:mm:ss"` / `"h:mm AP"` | `"system"` |

## Out of scope

- **Per-transaction time-of-day** — transactions store a date only (`occurred_on`),
  no time; nothing to zone-convert there.
- **Multi-currency / per-account currency** — [FIBR-0087].
- **Amount/number locale formatting** — [FIBR-0017] (currency already via QLocale).
- **Custom user-typed format strings** — curated presets only (D5).
- **Changing the date-*input* pickers** — FIBR-0047 fixed those to `yyyy/MM/dd`;
  input parsing is untouched (this is display-only).
- **Correcting a timezone-bearing OFX `<DTPOSTED>` at import** — [FIBR-0042], an
  import-time calendar-date concern, not display.

## Exit criteria

- `./scripts/ci-local.sh` green (ruff/format/bandit/pip-audit/gitleaks + mypy +
  `pytest -m "not perf"`), mypy 0.
- The Imported column shows the timestamp in the user's zone + format; Home dates
  and Statement periods show the chosen date format; changing a pref in Settings
  re-renders the open tabs and persists across relaunch (and first-run persists
  its selection).
- `/cold-eyes` converged; `/audit` + `/indie-review` clean (or folded) at close.

## Dependencies

- **FIBR-0004** (✅) — first-run wizard, vault `settings` table.
- **FIBR-0055** (✅) — the Settings dialog + `SettingsRepository` + the
  `auto_lock_minutes` vault-settings pattern this mirrors.
- **FIBR-0052 / FIBR-0051** (✅) — the Statements + Home display sites.
- **FIBR-0047** (✅, ROADMAP-only) — the Qt date-token vocabulary reused for display.

## Performance note

`format_date` / `format_timestamp` are called **per cell** inside the
`refresh()` render loops (`ui/home.py`, `ui/statements.py`), each constructing a
`QDateTime`/`QTimeZone`. This is **deliberately un-cached**: the tables are
user-scrolled and hold tens–hundreds of rows, so the per-row cost is negligible
(no hot loop, no large N). No perf invariant is pinned; if a future dataset makes
this measurable, a resolved-pref cache is the obvious lever.

## Verified API basis

Verified against source + a live PySide6 6.11 probe at draft time (global rule § 13):

- Display sites: `ui/statements.py:100,105` (Period f-string with en-dash +
  `imported_at` verbatim), `ui/home.py:146` (`occurred_on` verbatim);
  `StatementsWidget.__init__(service, parent)` (`:50`), `HomeView.refresh()`
  (no args).
- Persistence: `repositories/settings.py:20,27` (`SettingsRepository.get/set`);
  `services/auth.py:218,236` (`auto_lock_minutes` / `set_auto_lock_minutes`, the
  mirror); `auth.py:95,121,127` (`validate_first_run` / `first_run` /
  `complete_first_run(raw, params, base_currency)`); `ui/first_run.py:51,139`
  (currency `QComboBox` + `DeriveWorker` → `complete_first_run`, vault open before
  return); `ui/settings.py:111` (auto-lock persisted dialog-side in `_on_save`).
- Stored formats: timestamps via `datetime.now(UTC).isoformat()`
  (`statement_periods.py:47`, `transactions.py:35`); dates via `date().isoformat()`
  in importers (`csv_importer.py:78`, `ofx_importer.py:107`).
- Qt: `QTimeZone.systemTimeZoneId()`, `.availableTimeZoneIds()`,
  `QTimeZone(id).isValid()`; `QDateTime.fromString(iso,
  Qt.DateFormat.ISODateWithMs)` (parses with/without fractional seconds) +
  `.toTimeZone(...)` + `.date()/.time().toString(token)`; `QDate.fromString(iso,
  Qt.DateFormat.ISODate).toString(token)`; `QLocale.system().toString(qdate |
  qtime, QLocale.FormatType.ShortFormat)`. Sample verified:
  `2026-07-11T06:49:15.506928+00:00` → JHB (UTC+2) → `2026/07/11 08:49`.

## Cold-eyes loop log

_(global rule § 14 — loop until a cold pass returns zero verified actionable
findings; loop 2+ run cold, no fix-briefing.)_

Three cold lanes per loop: **implementability/testability · accuracy vs source ·
cross-doc/consistency.** Per-loop verified severity tally:

| Loop | CRIT | HIGH | MED | LOW | INFO |
|------|------|------|-----|-----|------|
| 1 | 0 | 4 | 5 | 9 | 1 |
| 2 | 0 | 1 | 2 | 7 | 1 |
| 3 | 0 | 1 | 5 | 6 | 1 |

**Loop 1** — HIGH: the D5-vs-D7 data-flow conflict (`datetime_prefs()` is an
`AuthService` method, but D7 said the widgets "hold no `AuthService`", while
`StatementsWidget` in fact takes `service`) → one flow pinned (shell resolves once,
passes the value; widgets keep `service` for data); the D7 widget wiring had no
signatures → `prefs` constructor param + `set_datetime_prefs` setter + live push
on Save; the `"system"` sentinel was unspecified for the **time** half and the
timestamp compose → the independent-halves rule (each half token-or-`QLocale`,
then joined); the `[FIBR-0047](FIBR-0047.md)` link was dead (ROADMAP-only, no spec
file) → cited bare + Deliverable 9 re-targeted. MED: persist-owner conflict (shell
vs dialog) → dialog-side like auto-lock; preset-table `"system"` membership → tables
hold concrete tokens, UI prepends System; INV-8 cancel observation → spy that
`set_datetime_prefs` is never called; `statements.imported_at` → the real table
`statement_periods.imported_at`; the ROADMAP-vs-spec version note → spec no longer
pins a version. LOW: INV-1 reframed to drive a widget render; the isoformat/date
attribution (repository vs importer); `QLocale.FormatType.ShortFormat` fully
qualified; the microsecond sample made canonical; a perf note added; `timezone`
term normalised; the ADR-0003 link added; a Contents TOC added.

**Loop 2** (cold; the loop-1 HIGHs did not resurface) — HIGH: Deliverable 7 still
listed the two *dialogs* as receiving the prefs value, conflicting with D3/D4's
"dialogs self-read/write via their held `service`" → dropped; only Home/Statements
get the value, the shell holds `self._prefs`. MED: the combo persist path was
unspecified (`currentData` vs `currentText`, free-typed text) → pinned
`addItem(label, token)` + `NoInsert` + persist `currentData()`. LOW: reworded the
"widgets keep `service`" claim to name the real data-service objects
(`StatementsWidget` drops `service` after construction; `HomeView` never takes
one); labelled `transactions.py:35` as `created_at` (not displayed); noted the
`QByteArray`→`str` decode; enumerated `date_format` via the D5 set; folded the
timezone-resolution step into the D5 compose; `format_date` parses via
`QDate.fromString`; the Contents TOC gained the Performance-note entry.

**Loop 3** (cold; the loop-1/2 HIGHs did not resurface — the data-flow is settled)
— HIGH: the D5 compose was **two-way** (system vs token) but INV-6 requires a
**third** case → each half made three-way (`"system"` / known-preset-token /
else→system), so a stale/unknown token can never reach `toString` as garble. MED:
the `str`/`bytes` `QTimeZone` boundary (stored id is `str`, constructor takes
`QByteArray`) → pinned `QTimeZone(tz_pref.encode())`, else an explicit zone choice
would silently never apply; the new `prefs` constructor param had no pinned
**position** → `HomeView(transactions, categorization, prefs, parent=None)` /
`StatementsWidget(service, prefs, parent=None)`; the `"system"` test legs were
**not hermetic** (read the runner's locale/zone) → pin `LC_ALL`/`TZ` like the
existing `test_INV10_format_amount_localised`; the Context "keeps `service`"
wording contradicted the code → reworded to the derived services; `SettingsRepository`
was mis-attributed to FIBR-0004 → moved to FIBR-0055 (which creates it). LOW:
reuse the existing `select_combo_data` helper for the pre-select; standardised the
`System default (<detected>)` placeholder; noted this supersedes the FIBR-0014
format-TODO comments in `import_wizard`/`manual_entry`.
