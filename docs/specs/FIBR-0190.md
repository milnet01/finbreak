# FIBR-0190 — Family E: the Standard Bank "Payments / Deposits" current-account layout

**Status:** ✅ **SHIPPED (2026-08-04)** — built by TDD, red first, commit
`acbd701`; 8 synthetic fixtures + 46 legs in
`tests/features/standard_bank_pdf/`; ROADMAP ✅. Spec `/cold-eyes` CONVERGED
after 4 loops (2 cold lanes each); §13 is the authoritative loop ledger.
Every mutation §5 names as a breaker was applied to the shipped code and each is
caught by at least one leg.
**Kind:** fix.
**Covers:** **FIBR-0190** only. One spec per roadmap item (`spec-format.md` §2).
**Extends:** [`FIBR-0050`](FIBR-0050.md) — the Standard Bank reader contract.
This spec adds a fifth layout family to that design and does not restate it.
**Invariant numbering:** this spec's invariants are **`INV-1`..`INV-11`**, bare,
as `spec-format.md` §7 requires for `spec_query` to parse them.
`tests/features/standard_bank_pdf/spec.md` already uses bare `INV-1`..`INV-14`
for **FIBR-0050's** invariants, so the two collide *in that file* — resolved the
way this project already resolves it everywhere else: the test contract
qualifies at the citation site (`FIBR-0190 INV-3`), exactly as
`tests/features/auto_update/spec.md` writes `FIBR-0131 INV-1` and
`tests/features/accounts/spec.md` writes `Source: FIBR-0005 INV-1`. The spec is
not renamed; the citation is qualified.
**Depends on:** FIBR-0050 (the reader), FIBR-0009 (`_normalise_to_plaintext`,
the page/row caps), FIBR-0171 (`closing_minor`, the forecast anchor),
FIBR-0216 (`_draft`'s per-row degradation).
**Source:** ROADMAP FIBR-0190, `user-request-2026-07-28`.

**Layman:** A real Standard Bank current-account statement imports **nothing**
today — not one of its 182 transactions. The reader already handles four page
layouts, and the bank prints this one in a fifth: the money-out column is headed
**Payments** instead of **Debits**, and money-in **Deposits** instead of
**Credits**. Because no reader recognises those words, the app falls back to its
generic table reader, which cannot tell the columns apart on a page with no
ruled lines — so it offers zero rows. This adds the missing reader.

**Contents:** Goal · Problem (**§2.2 is the measured evidence**) · Scope
decisions (D1–D11) · Design (**§4.3 the row grammar, §4.5 the completeness gate
and the `closing_minor` wiring**) · Invariants (INV-1..INV-11) · Failure modes ·
Tests · Alternatives rejected · Out of scope · Resource cost · What checks this ·
Cross-doc impact · Cold-eyes loop log.

---

## 1. Goal

A Standard Bank current-account statement printed in the
`Date Description Payments Deposits Balance` layout imports every one of its
transaction rows, with money-out negative in budget view, under the same
all-or-nothing integrity contract FIBR-0050 gives every other family.

Non-goal: the generic PDF fallback is **not** improved. It cannot split these
columns (§2.1) and this spec does not try to make it — the dedicated reader is
the fix.

---

## 2. Problem

### 2.1 Both import paths fail, so the user sees nothing

`detect_standard_bank` returns `None` for this layout, so the wizard falls
through to the generic FIBR-0009 PDF path, which finds the table but cannot
column-split its rows — the page carries no ruling lines for `pdfplumber` to
split on, so each row lands whole in cell 0. The result the ROADMAP bullet
records is `drafts=0, errors=183`.

The legal marker **is** present, so this is unambiguously a Standard Bank
statement; every family signature simply misses:

| Family | Signature it wants | Why it misses |
|---|---|---|
| A | `debits` + `credits` + `date` + `balance` | the word "credits" appears nowhere in the document |
| B | `posting` + `effective` + `debit` + `credit` + `balance` | none of them print |
| C | `date` + `description` + `amount` (+ card marker) | no "amount" column |
| D | `withdrawals` + `deposits` + `balance` | "deposits" prints, "withdrawals" does not |

So the money-out column is headed **Payments** and money-in **Deposits** — a
fifth transactional layout. "Payments" as a money-**out** heading inverts the
ordinary reading of the word and is the single most confusable thing in this
design; §4.3 and the implementation both carry a comment saying so.

### 2.2 What the reference statement actually contains — measured

Verified 2026-08-03 against the real 12-page, 3-month SBSA current-account
statement named in the ROADMAP bullet
(`/mnt/Emulators/storage_backup_2026-05-08/…`, **local-only — real payees and
balances; never committed, and nothing derived from it enters
`tests/fixtures/` un-anonymised**). Every figure below is an output of a probe
script run against that file, not a reading of it; the scripts print verdicts
and shapes only, never an amount or a payee.

**Document shape**

| Fact | Value |
|---|---|
| Pages | 12 |
| Transaction rows | **182** |
| Money tokens per row | **exactly 2, on every row** (histogram `{2: 182}`) |
| Rows money-out / money-in | 132 / 50 |
| Header, reprinted on every **transaction** page | `Date Description Payments Deposits Balance` — pages **1–11**; the page-12 summary carries none |
| Opening anchor | `STATEMENT OPENING BALANCE <amount>`, page 1 only |
| Closing-balance line | **none, anywhere in the document** |
| `Statement from … to …` period line | **absent** — the summary prints `From:` / `To:` instead |
| Last-page column totals | `Payments -R<amount>` / `Deposits R<amount>`, **each alone on its own line** |

**Row token shapes** — the evidence the grammar in §4.3 rests on, and the part
a token count alone cannot establish (`_MONEY` matches the numeric core whether
or not an `R` or a trailing `-` sits beside it):

| Token | Measured form | Count |
|---|---|---|
| Date | `D[D] Mon YY`, **Title-case** (`28 Feb 26`) | 182 / 182 Title-case, **0** upper-case |
| Balance sign | **leading** minus (`-999.99`) | **0** rows carry a trailing-minus balance; **30** carry a leading-minus one |
| Amount / balance prefix | **bare** — no `R` on either money token | the grammar in §4.3, which admits no `R`, matches **182 / 182** |
| `R` elsewhere on a row | inside the **description** only | 3 rows — see D3 |

**Integrity, run against the proposed grammar**

| Check | Result |
|---|---|
| Rows matched by §4.3's grammar | **182 / 182** |
| Per-row \|delta\| == printed amount, sign included | **182 ok, 0 mismatched** |
| `Payments` total == Σ money-out | **True** |
| `Deposits` total == Σ money-in | **True** |
| Totals fall inside a transaction region | **No** — the summary page's `_table_region` is empty (`0:0`) |
| Rows surviving `_table_region`'s per-page terminator scan | **182 of 182** — no generic terminator (`account summary`, `fee structure`, …) fires mid-page |
| In-region date-leading lines that are **not** transactions | **0** — so `dmy_lead`'s promotion hazard (§4.2) does not fire on this statement |
| Fold group heads / continuation lines folded in | **183** (182 rows + the opening anchor) / **182** |

Four things this reconciles or corrects:

- **The ROADMAP bullet says 183 rows; the truth is 182.** The generic fallback's
  "183 data rows" counted the `STATEMENT OPENING BALANCE` line as a data row.
  182 + 1 = 183. The bullet is annotated rather than rewritten (§12).
- **The balance's minus is leading, which is the opposite of Family A.**
  FIBR-0050 records Family A printing `730.55-`; E prints `-730.55`. This is the
  single most load-bearing measurement in the document, because a grammar built
  on A's convention would reject every overdrawn row and refuse the statement.
  D3 records what the grammar does about the other convention.
- **Dates are Title-case on every row**, so `_MON_RE` — which is Title-case and
  carries no `re.I` — is correct as-is. Recorded because "the header prints in
  caps" makes the opposite assumption look reasonable.
- **`opening + Σ amounts == final balance` is *not* independent evidence** and is
  therefore not listed above. It is entailed by the per-row check: the deltas
  telescope, so it restates the row above rather than corroborating it.

The probe scripts live in this session's scratchpad, not the repo — they read a
file that must never be committed, so they cannot ship as tests. What ships
instead is the anonymised fixture (§4.6), against which the same assertions run
in CI on committed bytes.

---

## 3. Scope decisions

- **D1 — A new Family E, not a Family-A variant.** Detection signature, row
  grammar, opening anchor, period handling and completeness gate all differ
  from A. A variant flag would branch at every one of those five points.
- **D2 — E is tried immediately after D and before B**, giving the full order
  **C → D → E → B → A**. Measured: zero collisions today (§4.1), so the slot is
  not load-bearing for the current corpus; it is defence for a future
  Money-Market statement that prints the word "payments" near its header.
  Before **B** rather than after is arbitrary on specificity grounds — both
  signatures carry five tokens and they share none, so neither can shadow the
  other; E is placed first only so the balance families stay grouped in
  most-recently-added order. INV-2 records that no test discriminates this.
  FIBR-0050 INV-2a fixes the old order string and is updated with it (§12).
- **D3 — The row grammar composes `_MONEY` and admits no `R` prefix and no
  trailing minus.** Two decimals are what stop a reference number inside a
  description being read as the amount. **The mechanism that actually
  protects the description is that `_E_ROW` is `$`-anchored with a lazy
  description, so the *last two* money tokens on the line win** — the
  two-decimal requirement is an additional filter that excludes reference and
  rate tokens, not the primary defence. Stating it the other way round would
  lead an implementer to a `re.search` without the anchor, or to a greedy
  description, either of which ships a wrong amount. Three of the 182 rows carry an
  `R`-prefixed token inside the description, and measurement settles what that
  does and does not argue: the token is `R000395711`, a reference number with no
  decimals, so `_MONEY` cannot match it either way — the two-decimal requirement
  is what protects the description, **not** the `R` exclusion. `R` is excluded
  from the row grammar simply because every measured row token is bare (§2.2),
  and admitting an optional prefix that never occurs buys nothing.
  Trailing-minus is likewise excluded: it does not occur (0 / 182), and were a
  future statement to print one, failing the grammar gives an all-or-nothing
  refusal, which is the safe outcome. Silently accepting it would risk a
  wrong-signed balance.
- **D4 — `_cc_iso` is renamed `_dmy_iso` and shared,** not copied — it already
  converts `DD Mon YY` → ISO. Its signature is unchanged: it takes one
  space-joined `"28 Feb 26"` string and splits it internally, so
  `_parse_family_e` passes `f"{d} {mon} {yy}"` from its three capture groups
  rather than the signature growing three parameters for one caller.
- **D5 — `_looks_like_row`'s widening is opt-in per family,** not global.
  E's rows lead with a date, which no existing family's rows do; widening the
  shared predicate for everyone would let a Family-A continuation line that
  happens to start `12 Jan 25` be promoted to a row and then rejected by A's
  strict grammar — turning a working import into an all-or-nothing refusal.
- **D6 — `_anchor_balance` and `_capture_opening` *are* widened globally**, in
  deliberate contrast to D5, because the risk is not symmetric: they match a
  literal marker string, and adding `statement opening balance` can only change
  behaviour for a statement that prints that phrase, and no A/B/C/D fixture
  does — **measured by extracting each committed fixture's text**, 0 of 13.
  (A repo-wide `workspace_search` would *not* establish this: the fixtures are
  binary PDF blobs whose text streams a text search cannot read, so it would
  return 0 hits whether or not the phrase were present. §7.10 ships the check
  as a test.)
  D5's hazard is a *shape* predicate that fires on incidental text; this is an
  exact phrase.
- **D7 — E has no printed period, so it must NOT join Family A's
  `period is None` refusal.** `parse` raises `_MISPARSE` when `period is None`
  **for Family A only**, because A needs the period for year inference. E carries
  a 2-digit year on every row and needs no inference, and §2.2 measured that E
  prints no `Statement from … to …` line at all — so an implementer who extends
  A's branch to cover E bricks every E statement. `_span` falls back to min/max
  parsed date, which is correct here.
- **D8 — The completeness gate uses the printed `Payments` / `Deposits`
  totals,** which are exact column sums (§2.2, measured). This is the part of
  the design that is *better* than Family A's: the per-row running-balance chain
  catches an interior dropped row (the next row's delta stops matching) but is
  **blind to a truncated tail** — if the final page's rows were dropped, every
  surviving row still reconciles. The totals catch exactly that.
- **D9 — The totals gate is optional-when-absent, and verifies whichever
  totals print — each independently.** One statement has been observed; a
  mandatory gate would refuse a valid statement whose summary is shaped
  differently, so absent totals degrade E to Family-A behaviour (the per-row
  gate alone). But "both or nothing" was the wrong shape: checking one total is
  **strictly stronger** than checking neither, since a lone `Payments` total
  still catches a dropped money-out tail, and skipping it catches nothing at
  all. A statement with no money-in rows plausibly prints no `Deposits` total,
  and that must not disable the half that *is* printed. `closing_minor` is the
  one thing that does require both (D10) — the derived final balance is only
  corroborated when both halves of the sum were checked.
- **D10 — `closing_minor` is supplied only when **both** totals printed and
  verified.** E prints no closing balance, but its final row's running balance
  *is* the closing figure, and the totals gate is what makes it trustworthy.
  `closing_minor` is derived as `opening + Σ amounts`, so a single verified
  total corroborates only one half of that sum; with one or neither total it is
  unverified, and FIBR-0171's forecast anchor gets `None` rather than a number
  nothing checked. §4.5 specifies the wiring.
- **D11 — Eight fixtures** (§4.6): the happy path, the no-totals degradation, a
  totals-mismatch refusal on **each** half, a matching-lone-total case, and an
  opening-marker-removed refusal. A gate with no red fixture is a gate nobody
  has seen fail — and a gate with a red fixture for only one of its two halves
  is half a gate nobody has seen fail.

No change to the generic PDF fallback (§1).

---

## 4. Design

All symbols below are in `src/finbreak/importers/standard_bank.py` unless said
otherwise.

### 4.1 Detection

`Family` gains `E = "E"`. `detect_standard_bank` gains, immediately after the
Family-D test and before the Family-B test (D2):

```python
if _signature_present(
    full_text, "date", "description", "payments", "deposits", "balance"
):
    return Family.E
```

The resulting order is **C → D → E → B → A**.

`_signature_present`'s default `window=3` is used because it is the value the
other **balance-family** signatures (D, B, A) use; Family C is the exception and
passes `window=1`. E's header prints on one line, so `window=1` would also work
and would be marginally tighter — 3 is retained because a header that wraps
under a different page width is the failure this parameter exists for, and no
false positive is reachable at 3 (measured below). Note the tolerance is not
symmetric: `_table_region` bounds the region with a **2**-line window, so a
header wrapping across 3 lines would detect as E and then mis-bound its region.

**Measured — no existing statement is stolen.** Running the current detector
and the proposed E signature over all 13 committed fixtures gives zero rows
where both fire:

```
fixture                      current  E-sig fires?
family_a_current.pdf         A        False
family_b_homeloan.pdf        B        False
family_c_creditcard.pdf      C        False
family_d_moneymarket.pdf     D        False
non_sb.pdf                   None     False
   … 13 fixtures, 0 collisions
```

### 4.2 Region and fold

`_table_region` needs **no change**. Its non-C branch already accepts E's
header: the window carries "balance" and "date", and the guard that the header
line itself carry no money token is what rejects the
`Transaction details Available Balance:R…` summary line printed immediately
above it. Verified against the reference statement — the region starts at the
`STATEMENT OPENING BALANCE` line, which is what §4.4 needs.

**The last page's totals are outside every region** (§2.2: its `_table_region`
is empty, `0:0`, because that page reprints no column header). So they cannot be
folded into the final transaction's description, and no new `_TERMINATORS` entry
is needed.

`_fold` gains a **pass-through keyword only** — it forwards `dmy_lead` to the
predicate and its own logic is unchanged. Both signatures:

```python
def _fold(lines: list[str], *, dmy_lead: bool = False) -> list[tuple[str, list[str]]]:
def _looks_like_row(line: str, *, dmy_lead: bool = False) -> bool:
```

`dmy_lead=True` additionally accepts a line leading with a validated
`D[D] Mon YY` date, or carrying the E opening marker. Every existing call site
keeps today's behaviour by omitting the keyword (D5), so only
`_parse_family_e` passes it.

**E accepts for itself the hazard D5 refuses to impose on A/B/D.** Under
`dmy_lead=True`, an in-region continuation or footer line that happens to lead
with `D[D] Mon YY` is promoted to a row and then rejected by `_E_ROW`, refusing
the whole statement. That is deliberate and is the safe direction: the
alternative is silently folding a real transaction into the previous row's
description. It is bounded by the region (`_table_region` excludes the
letterhead and everything past the terminator), and §2.2 measures both halves of
the safety claim directly: **0** in-region date-leading lines are
non-transactions, and all **182** rows survive the per-page terminator scan.

### 4.3 The row grammar

```
DD Mon YY  <description>  <signed amount>  <balance>
```

Money-out prints a **leading** `-`; money-in prints none. The balance may also
be negative and likewise carries a **leading** minus — the opposite of Family A's
trailing convention, measured in §2.2 and the reason D3 excludes the trailing
form rather than accepting both.

```python
_E_ROW = re.compile(
    rf"^\s*(\d{{1,2}}) ({_MON_RE}) (\d{{2}})\s+(.*?)\s+"
    rf"(-?{_MONEY.pattern})\s+(-?{_MONEY.pattern})\s*$"
)
```

No `re.I`: `_MON_RE` is Title-case and §2.2 measured every one of the 182 rows
as Title-case. Composing `_MONEY` (D3) requires two decimals, so a reference
number or a masked card number inside a description cannot be mistaken for the
amount.

**`parse` must gain an explicit dispatch branch, and this is the one omission
that would silently mis-parse rather than fail.** The family chain in `parse`
ends `else: result = _parse_family_c(region_lines, exponent, fmt)` — a bare
`else`, not `elif family is Family.C`. So an implementer who adds detection, the
grammar and the `closing_minor` wiring but touches nothing else routes **every
Family-E statement into the credit-card parser**, which does not fail cleanly:
`_split_credit_card_line` finds the `28 Feb 26` boundaries and the Family-C
segment grammar matches with the **balance** captured as the amount and its
sign flipped. Wrong money, no exception. The branch goes **before** the terminal
`else`:

```python
elif family is Family.D:
    result = _parse_family_d(region_lines, exponent, fmt)
elif family is Family.E:
    result = _parse_family_e(region_lines, exponent, fmt)
else:
    result = _parse_family_c(region_lines, exponent, fmt)
```

Note E takes **no `period`** argument (D7).

```python
def _parse_family_e(lines: list[str], exponent: int, fmt: Fmt) -> ParseResult:
```

`_parse_family_e` mirrors `_parse_family_d`'s shape: fold (with `dmy_lead=True`),
seed `prev_balance` from the opening anchor, then per row take
`balance = _signed_balance(bal_tok, fmt)` — **not** `_parse_amount`, which
strips the sign and would read all 30 leading-minus balances as positive — and
compute
`delta = balance - prev_balance` and call
`_verify_row(delta, amt_tok, fmt, check_sign=True)`. The draft's signed amount is
`delta`, exactly as A/B/D do it — the printed sign is the cross-check, never the
source (FIBR-0050 INV-7).

The implementation carries a comment on the one genuinely confusable point:
**"Payments" here is the money-OUT column**, despite the word ordinarily
suggesting money in.

### 4.4 The opening anchor

E prints `STATEMENT OPENING BALANCE <amount>` instead of Family A's
`Balance Brought Forward`. A module constant

```python
_E_OPENING = "statement opening balance"
```

is recognised by `_anchor_balance` and by `_capture_opening`, both of which
already scan for `_BROUGHT_FORWARD` and gain the E marker alongside it rather
than a second function (D6 explains why global widening is safe here where D5
forbids it for `_looks_like_row`).

`_capture_opening` must learn the marker because it otherwise **raises** on
every E statement — not because a checksum consumes its value. For E,
`_verify_checksum` returns early (§4.5), so the opening's real consumers are
`_parse_family_e`'s running-balance seed and the totals gate's final-balance
derivation.

**Which error a statement with no opening marker actually raises.** `parse`
runs the family parser *before* `_capture_opening`, so the message the user sees
is `_parse_family_e`'s, not `_capture_opening`'s. The two differ:

| Raiser | Message |
|---|---|
| `_parse_family_e` (`prev_balance is None`) — **the one that fires** | `couldn't find the opening balance on this statement — try your bank's CSV or OFX export` |
| `_capture_opening` — reachable **only** when the statement has zero transaction rows | `couldn't find the opening balance on this statement` |

`_parse_family_e` reuses the existing suffixed wording verbatim; §7.5 asserts
that string on a fixture that has rows.

**The zero-row case takes the other branch.** A quiet-month E with no opening
marker never reaches `_parse_family_e`'s `prev_balance is None` check, because
that check sits inside the per-row loop and there are no rows — so `parse` falls
through to `_capture_opening`, which raises the **un-suffixed** message. Both are
friendly and both refuse the statement, so this is a wording difference rather
than a behaviour gap; it is recorded because §7.5 asserts a string verbatim and
would fail if pointed at a zero-row fixture.

### 4.5 The completeness gate and the `closing_minor` wiring

`_CLOSING_MARKERS` gains `Family.E: ()` — an explicit empty tuple, so
`_capture_closing` returns `None` for E rather than raising `KeyError`, and the
"E prints no closing figure" fact is stated in the table where a reader looks
for it. `_verify_checksum`'s `closing is None` arm returns early for E as it
does for A.

E's real gate is a new module function with **one** call site:

```python
_E_TOTAL = re.compile(
    rf"^\s*(Payments|Deposits)\s+-?R?({_MONEY.pattern})\s*$", re.I
)


def _verify_e_totals(
    full_text: str,
    drafts: list[TransactionDraft],
    opening: Decimal,
    exponent: int,
    fmt: Fmt,
) -> int | None:
    ...
```

- **`fmt` is required, not optional.** `_MONEY` matches both decimal
  conventions (`1,234.56` and `1.234,56`) and `_parse_amount(token, fmt)` is
  what disambiguates them — which is exactly why `_capture_closing` takes `fmt`
  too. Without it a European-format E statement would parse `239.206,04` as the
  wrong `Decimal` and the gate would raise on a valid statement or, worse, pass
  on a wrong one. FIBR-0050 INV-8 requires per-statement detection, and `parse`
  already has `fmt` in scope at the call site.
- It iterates `full_text.splitlines()` and matches each line, exactly as
  `_capture_closing` does. It does **not** `search` the whole blob: the pattern
  is line-anchored, and without `re.MULTILINE` a blob scan would match at most
  the document's first or last line rather than each candidate.
- The `R` prefix is **optional** (`-?R?`). The reference statement prints it
  (§2.2) and the fixture reproduces that form (§4.6), but the row tokens are
  bare, so a summary printing bare totals is plausible and must not silently
  disable the gate.
- The sign is **not** captured. Comparison is on **magnitudes, in minor
  units** — and **both conversions must be written out**, because the two sides
  arrive in different units: `d.amount_minor` is already an `int` in minor
  units, whereas `_parse_amount(tok, fmt)` returns a **major**-unit `Decimal`.
  Comparing them directly is `123456 == Decimal("1234.56")`, which is false for
  every valid statement. So:

  ```python
  abs(sum(m for m in amounts_minor if m < 0)) == to_minor(_parse_amount(payments_tok, fmt), exponent)
  abs(sum(m for m in amounts_minor if m > 0)) == to_minor(_parse_amount(deposits_tok, fmt), exponent)
  ```

  `_verify_checksum` converts the same way, and this is why `exponent` is a
  parameter. Sign is already owned
  by INV-10 and the per-row gate, and a signed comparison would raise on a valid
  statement printing `Payments R<amount>` without the minus.
- **Each label is verified independently** (D9). A label that prints is checked;
  a label that does not is skipped. A mismatch on either raises.
- **A label matching more than once is treated as absent**, not as a value to
  pick. Per-page subtotals under the same label are plausible on a statement
  shape not yet observed, and first-wins or last-wins would silently compare
  against a subtotal. Skipping degrades to Family-A behaviour, which D9 already
  accepts; guessing would fail a valid statement or pass a wrong one.
- **Return value:** `to_minor(opening, exponent) + Σ d.amount_minor` — the final
  running balance in minor units — when **both** labels were present and
  verified (D10); `None` otherwise.

**The mismatch message is its own string**, not a reused one.
`tests/features/standard_bank_pdf/spec.md` requires the per-row and completeness
gates to raise distinguishable messages, and the existing
`_verify_checksum` / `_verify_row` wordings all describe a *running balance*
disagreement — which is factually wrong here, since D8's signature case is a
truncated tail where every surviving running balance reconciles:

> `this statement didn't add up — its printed Payments/Deposits totals don't
> match its transactions; try your bank's CSV or OFX export`

§7.6 asserts that string verbatim.

**Wiring — the exact anchor.** `parse` assigns
`closing_minor = None if closing is None else to_minor(closing, exponent)`
*after* the `_verify_checksum` call, and rebuilds the returned `ParseResult`
from scratch, so a value returned by `_parse_family_e` would be discarded and a
branch placed merely "after `_verify_checksum`" would be **overwritten by that
assignment on the next line**. The E branch therefore goes **immediately after
the existing `closing_minor` assignment**, overriding it:

```python
closing_minor = None if closing is None else to_minor(closing, exponent)
if family is Family.E:
    closing_minor = _verify_e_totals(
        full_text, result.drafts, opening, exponent, fmt
    )
```

For every other family `closing_minor` keeps today's derivation.

**Rows dropped by `_draft`'s per-row degradation do not affect the gate.**
FIBR-0216 lets a row become a `RowError` instead of a draft, and such rows are
absent from `drafts`. The reachable case is a printed `0.00`, which contributes
0 to both Σ and to the running balance, so the totals still reconcile.

**A quiet month** (zero transactions — FIBR-0050 INV-11 carries the same case
for A and D) returns an empty-draft `ParseResult`; Σ = 0 is compared against
whichever totals print, and `closing_minor` is the opening balance in minor
units **when both totals printed**, `None` otherwise. Its **coverage span is
`(None, None)`**, which is a genuine and accepted gap rather than an oversight:
E prints no `Statement from … to …` line, so `period is None`; with no drafts
`_span` falls through to `_statement_date`, whose `_DATE_YMD` and `_DATE_DMY`
patterns both key on a literal `Date <numbers>` that E does not print (it prints
`From:` / `To:` — measured, §2.2). FIBR-0050 INV-11's stated purpose for the
empty-draft path is "so the period is recorded", and E cannot honour that
without parsing the `From:` / `To:` summary lines. That is **out of scope here**
(§9) and recorded as a `nothing` row in §11 rather than silently inherited.

### 4.6 The fixtures

Eight synthetic `reportlab` fixtures in
`tests/features/standard_bank_pdf/fixtures/`, authored the way every existing
one was (invented merchants, invented amounts, the fake account number
`00 000 000 0`, the SB legal marker). `reportlab` stays a probe/authoring tool
and does **not** become a project or test dependency — FIBR-0050 and
`tests/features/pdf_import/spec.md` already fix that rule.

- `family_e_current.pdf` — **3 pages**: two transaction pages, each reprinting
  the column header, the second ending in the `Please verify` terminator; then a
  third summary page that carries **no column header** (so its `_table_region`
  is empty, mirroring the reference statement's page 12 — §2.2) and prints the
  two totals, each alone on its line and **`R`-prefixed** exactly as measured
  (`Payments -R<amount>` / `Deposits R<amount>`). The opening line is on page 1.
  Its rows deliberately include the shapes §2.2 measured: a wrapped description,
  a description containing digits and a masked card number, a description
  containing an `R`-prefixed reference token, a money-in row, and a row that
  takes the running balance **negative with a leading minus**.
- `family_e_no_totals.pdf` — the same statement with the totals removed (D9).
- `family_e_totals_fail.pdf` — **rows untouched and internally consistent**;
  only the *printed total* is altered, as a dropped tail would leave it. Editing
  rows instead would trip the per-row chain first and the leg would pass against
  an implementation with no totals gate at all (§7). It prints **only the
  `Payments` total**, deliberately: that makes one fixture prove both halves of
  INV-6 — that a lone total is still verified (an implementation gating on both
  totals would *skip* it and import cleanly, failing the leg) and that a
  mismatch raises.
- `family_e_deposits_fail.pdf` — the mirror of the above: only a
  (mismatched) `Deposits` total. Without it the `Deposits` half of INV-6 is
  unfalsifiable (§7.6).
- `family_e_one_total.pdf` — only a `Payments` total, and it **matches**. The
  import succeeds and `closing_minor` must be `None` (§7.8a, D10).
- `family_e_amount_corrupt.pdf` — one row's **printed amount** altered so it
  disagrees with its balance delta.
- `family_e_sign_flipped.pdf` — one row's printed amount with the right
  magnitude and the **wrong sign**. With the row above, the only two fixtures
  that falsify INV-4 (§7.11); a `_verify_row` call that was deleted or given
  `check_sign=False` is green without them.
- `family_e_no_opening.pdf` — the opening-marker line removed, with rows
  present so the error is `_parse_family_e`'s suffixed one (§4.4, §7.5).

The running balance in each is recomputed so the per-row gate passes, exactly
as the existing standard_bank fixtures were produced.

---

## 5. Invariants

Every `*Test:*` clause names a leg in §7. Only INV-2's can be run today
(§7.2 — detect-as-today over the 13 committed fixtures needs no new code), as
can §7.10's fixture-text check. The rest describe code
this spec is the contract for, so per `/write-spec`'s drafting rules they carry
no pasted output rather than a manufactured one; each was instead checked by the
mutation-on-paper question, and §7 records the one leg that changed as a result.

- **INV-1** — A document carrying the SB legal marker and a header block with
  `date`+`description`+`payments`+`deposits`+`balance` detects as `Family.E`.
  *Breaks it:* dropping any one of the five tokens from the signature.
  *Test:* §7.1 — `detect_standard_bank` on the **text extracted from**
  `family_e_current.pdf` returns `Family.E`.
- **INV-2** — Adding Family E changes no existing family's detection.
  *Breaks it:* loosening the signature to a subset of the five tokens — e.g.
  dropping `payments`, after which the Family-D fixture's
  `withdrawals`/`deposits`/`balance` header also satisfies E. (Re-ordering E
  against C/D/B/A is *not* a breaker this test can catch, since §4.1 measured
  that E fires on none of the 13 fixtures; D2 is defence, not a tested
  property.) *Test:* the 13 committed fixtures each still detect as the family
  they detect as today (§7.2) → run today against the *proposed* signature,
  13 fixtures, 0 collisions (§4.1).
- **INV-3** — Every **group head** `_fold` returns other than the opening
  anchor (which `_anchor_balance` consumes first) parses under `_E_ROW`;
  continuation lines are folded into the preceding description and never reach
  the grammar. A group head that does not parse raises the all-or-nothing `_MISPARSE` ValueError rather than being skipped.
  *Breaks it:* a `continue` in place of the raise — which is what turns a
  mis-parse into a silent under-import.
  *Test:* §7.9 — a region line the fold accepts but `_E_ROW` rejects, asserting
  the raise; plus §7.4's accept/reject legs for the grammar itself.
- **INV-4** — An E row's stored amount is the running-balance **delta**, and
  the printed amount must agree with it in magnitude *and* sign.
  *Breaks it:* taking the printed amount as the stored value; the row would
  then survive a balance column the parse had mis-read.
  *Test:* §7.11 — a **corrupted-printed-amount** fixture and a
  **sign-flipped-printed-amount** fixture, each asserting `_verify_row`'s
  existing message. §7.3's "amount equals the bracketing balance difference"
  is **not** a test of this invariant: §4.3 stores `delta`, so that assertion is
  true by construction and stays green even with the `_verify_row` call deleted
  or copied from `_parse_family_b` with `check_sign=False`. That would remove
  what FIBR-0050 INV-11 calls the primary correctness gate, and when the totals
  are absent (D9) nothing else would catch a mis-read amount column. D11's own
  principle — a gate with no red fixture is a gate nobody has seen fail —
  applies here as much as to the totals gate.
- **INV-5** — The opening balance comes from `STATEMENT OPENING BALANCE`;
  a statement carrying neither that marker nor `brought forward` raises
  `_parse_family_e`'s suffixed opening-balance error (§4.4). *Breaks it:*
  seeding `prev_balance` from the first row's balance, which would silently
  swallow the first transaction. *Test:* §7.5 — `family_e_no_opening.pdf`
  raises, with the suffixed message asserted verbatim.
- **INV-6** — Each column total that prints is verified **independently**, on
  magnitudes in minor units: |Σ money-out| against `Payments`, |Σ money-in|
  against `Deposits`. A mismatch on either raises, with §4.5's own message.
  *Breaks it:* dropping the final page's rows — the case the per-row chain
  cannot see (D8); or gating on both totals being present, which disables the
  half that printed (D9). *Test:* §7.6, over **both** red fixtures —
  `family_e_totals_fail.pdf` and `family_e_deposits_fail.pdf` — each raising
  with §4.5's message asserted verbatim.
- **INV-7** — When neither total prints, the import still succeeds on the
  per-row gate alone; when exactly one prints, that one is verified and the
  import still succeeds if it matches (D9). *Breaks it:* making the totals
  mandatory, which refuses a valid statement; or gating on both being present,
  which skips the half that printed.
  *Test:* §7.7 (`family_e_no_totals.pdf` imports, drafts non-empty) and §7.8a
  (exactly one total, matching).
- **INV-8** — `closing_minor` is `opening + Σ amount_minor` when **both**
  totals printed and verified, and `None` otherwise (D10). *Breaks it:*
  supplying it unconditionally, which hands FIBR-0171's forecast an anchor
  nothing verified; supplying it when only one total was checked, which
  corroborates only half the sum; or placing the E branch **before** `parse`'s
  existing `closing_minor` assignment, which overwrites it with `None` on the
  next line (§4.5). *Test:* §7.7 (the `None`
  arm), §7.8 (the present arm) and §7.8a — the only leg that discriminates the
  one-total breaker.
- **INV-9** — Families A, B and D fold identically before and after this
  change. *Breaks it:* widening `_looks_like_row` globally instead of behind
  the `dmy_lead` keyword (D5). *Test:* §7.4's `_looks_like_row` opt-in leg is
  the real closure; §7.2 and the existing A/B/D end-to-end legs run unchanged
  around it — see §11 for what this does *not* cover.
- **INV-10** — A row in the `Payments` column stores a **negative**
  `amount_minor`. *Breaks it:* negating `delta` before handing it to `_draft`,
  or storing `_parse_amount(amt_tok, fmt)` — the unsigned magnitude — in place
  of `delta`. (Note the breaker is *not* "reading the Payments column as
  money-in": §4.3 stores the running-balance delta and never consults which
  column a figure came from, so no conforming implementation can make that
  mistake. The reachable failures are sign inversions in the handoff.)
  *Test:* §7.3 — assert a known money-out row's `amount_minor < 0`.
- **INV-11** — Each draft's `occurred_on` is the ISO form of its row's printed
  `D[D] Mon YY` date, with the two-digit year read as `2000 + YY` (`_dmy_iso`).
  *Breaks it:* transposing the components when re-joining `_E_ROW`'s three
  capture groups for `_dmy_iso` — `f"{mon} {d} {yy}"` instead of
  `f"{d} {mon} {yy}"`. Note which transposition is dangerous: month↔day always
  **raises**, because `_dmy_iso` splits and calls `_MON3.get("28")`, which is
  `None`. The **silent** one is day↔year (`f"{yy} {mon} {d}"`), which yields a
  valid ISO date on the wrong day. A wrong day is a wrong
  **monthly total** in a budgeting app, and this is a new date path: no other
  family re-assembles a date from separate groups.
  *Test:* §7.3 — assert every draft's `occurred_on`, not just the amounts.
  `tests/features/standard_bank_pdf/spec.md` already requires this of Family C
  ("dates asserted alongside amounts"); E was the only family exempt.

---

## 6. Failure modes

| Case | Behaviour |
|---|---|
| Header present, no opening marker | `_parse_family_e`'s suffixed opening-balance ValueError (INV-5, §4.4) |
| A folded line that is not a valid E row | `_MISPARSE` ValueError, whole statement refused (INV-3) |
| Row amount disagrees with its balance delta | existing "didn't add up" ValueError (INV-4) |
| **Any** printed total disagrees with its Σ | all-or-nothing ValueError (INV-6, §4.5's own message) |
| Zero transactions (quiet month) | empty-draft `ParseResult`; totals compare against Σ = 0; `closing_minor` is the opening balance when **both** totals printed; **coverage span is `(None, None)`** — an accepted gap, §4.5 and §9 |
| Either total absent, or a label printed twice | that label's half of the gate is skipped; `closing_minor` is `None` (D9, §4.5) |
| A European-format (`1.234,56`) statement | `fmt` is detected per statement and passed to the totals gate (§4.5); no separate code path |
| Balance printed with a **trailing** minus | `_MISPARSE` — deliberate, D3; does not occur in the reference statement (0 / 182) |
| A printed `0.00` row | degrades to a `RowError` for that row only, statement still imports — existing `_draft` behaviour (FIBR-0216), unchanged by this spec |
| Statement exceeds the page / row caps | existing FIBR-0009 caps, unchanged |

---

## 7. Tests

New legs in `tests/features/standard_bank_pdf/test_standard_bank.py`, with the
contract rows added to that directory's `spec.md` under the `INV-*` names.

1. **Detection** — text extracted from `family_e_current.pdf` →
   `Family.E` (INV-1).
2. **No regression** — a parametrised leg over the 13 existing fixtures
   asserting each still detects as today's family (INV-2, INV-9).
3. **End-to-end** — parse `family_e_current.pdf`; assert the row count, that
   each draft's `amount_minor` equals its bracketing balance difference
   (INV-4), **that every draft's `occurred_on` is the ISO form of its printed
   `D[D] Mon YY` date (INV-11)**, that money-out rows are negative (INV-10),
   that the wrapped description is folded whole, and that the leading-minus
   negative-balance row parses.
4. **Grammar unit legs (INV-3's accept/reject set, and INV-9's closure)** —
   a direct `_looks_like_row("12 Jan 25 PAYMENT -10.00 90.00") is False` leg
   with the keyword omitted, proving the widening really is opt-in; then
   `_E_ROW` against
   synthetic strings: a description containing a 2-decimal-looking reference, a
   masked card number, an `R`-prefixed description token, a leading-minus
   balance, a money-in row, and a **rejection** leg for a trailing-minus balance
   (D3, and §6's row for it).
5. **Opening** — `family_e_no_opening.pdf` raises, message asserted verbatim
   (INV-5).
6. **Totals gate red, both halves** — parametrised over two fixtures:
   `family_e_totals_fail.pdf` (a lone, mismatched `Payments` total) and
   `family_e_deposits_fail.pdf` (a lone, mismatched `Deposits` total). Each
   raises, with §4.5's message asserted verbatim. Both are needed: a single
   `Payments`-only red fixture leaves an implementation that verifies
   `Payments` and never checks `Deposits` passing every leg, which would let a
   truncated money-in tail import silently — the exact case D8 added the gate
   for. Together they also discriminate the "gate on both totals present"
   mutation, which would skip a lone total and import cleanly (INV-6, D9).
7. **Totals absent** — `family_e_no_totals.pdf` imports, `closing_minor is
   None` (INV-7, INV-8).
8. **`closing_minor` present** — `family_e_current.pdf` yields
   `opening + Σ amount_minor` in minor units (INV-8).
8a. **`closing_minor` withheld on a lone total** — `family_e_one_total.pdf`
   prints only a `Payments` total that **matches**; the import succeeds (the
   lone total is verified, D9) and `closing_minor is None` (only half the sum
   was corroborated, D10). Without this leg INV-8's "supplying it when only one
   total was checked" breaker is caught by nothing: §7.7 removes both totals and
   §7.8 has both present, so an implementation returning the anchor whenever
   *any* label verified passes both.
9. **Mis-parse raises, never skips** — a synthetic region line the fold accepts
   under `dmy_lead=True` but `_E_ROW` rejects (a date-led line with one money
   token), asserting `_MISPARSE` (INV-3).
11. **Per-row gate red** — `family_e_amount_corrupt.pdf` (a printed amount that
   disagrees with its balance delta) and `family_e_sign_flipped.pdf` (right
   magnitude, wrong printed sign) each raise `_verify_row`'s existing message,
   asserted verbatim. Mirrors FIBR-0050 INV-11a's corrupted-amount blob, and is
   the only thing that falsifies INV-4 (§5).
10. **D6's premise, as a test** — extract each of the 13 existing fixtures' text
   and assert none contains `statement opening balance`. This is what makes
   widening `_anchor_balance` / `_capture_opening` globally safe, and it must be
   a text-extraction leg rather than a repo grep, which cannot read a binary
   PDF's text stream.

**Legs 2 and 10 can be run today** — detect-as-today over the 13 committed
fixtures, and the fixture-text extraction behind D6; §4.1 and D6 record both as
already run. Legs 1, 3–9, 8a and 11 cannot until the code exists. Each was checked
by the mutation-on-paper question — *which rule makes this fixture fail, and is it the
rule under test?* Two needed the check and changed as a result:

- **Leg 6** — a totals-mismatch fixture built by editing *rows* would fail the
  per-row chain first, so the leg would pass against an implementation with no
  totals gate at all. The fixture therefore alters **only the printed totals**
  (§4.6).
- **Leg 9** — was originally folded into leg 3, which asserts only on a
  well-formed statement and so could never exercise the raise-vs-skip branch
  INV-3 names. It is now its own leg with its own fixture text.

---

## 8. Alternatives considered (and rejected)

- **Fix the generic PDF fallback to split columns without ruling lines.**
  Rejected: it would need x-coordinate clustering across the whole page, a far
  larger and riskier change than a 5-column grammar, and it would still be
  guessing where a dedicated reader knows.
- **Family-A variant flag instead of a new family.** Rejected under D1.
- **Global widening of `_looks_like_row`.** Rejected under D5 — it can convert
  a working Family-A import into a refusal. (`_anchor_balance` and
  `_capture_opening` *are* widened globally; D6 says why the two cases differ.)
- **Accepting a trailing-minus balance as well as a leading one.** Rejected
  under D3 — it does not occur, and refusing is safer than guessing a sign.
- **Skip the completeness gate, matching Family A.** Rejected under D8: the
  totals are printed, they are exact, and without them a truncated tail imports
  silently and wrongly. This is a money-critical project (`CLAUDE.md`).
- **Make the totals gate mandatory.** Rejected under D9 — one observed
  statement is not enough evidence to refuse every statement without them.
- **Require *both* totals before verifying either.** Considered and
  **reversed**: this was the design until a review pointed out the argument for
  it was invalid. "Half a gate passes a statement whose deposits were dropped"
  is true, but both-or-nothing passes that case *and* the payments case, so
  verifying whichever total prints is strictly stronger. D9 now does that;
  `closing_minor` is what still requires both (D10).
- **A signed comparison in the totals gate.** Rejected under §4.5 — it raises on
  a valid statement printing `Payments R<amount>` with no minus.
- **Adding `payments` to `_HEADER_TOKENS`.** Rejected as speculative: the
  header is excluded from the region by `_table_region` before `_is_boilerplate`
  is ever consulted (§4.2), and `_HEADER_TOKENS` deliberately excludes
  `description`, so an E header could never match it anyway.

---

## 9. Out of scope

- The generic PDF fallback (§1).
- The other banks blocked on sample statements (FIBR-0074).
- **Parsing E's `From:` / `To:` summary lines into a coverage span.** A
  quiet-month E therefore reports `(None, None)` (§4.5). Deferred deliberately:
  it affects only a statement with zero transactions, the fallback chain is
  FIBR-0050's not this spec's, and inventing a fourth date-line pattern for an
  unobserved case is the kind of speculative code D3 already refuses elsewhere.
  Recorded as a `nothing` row in §11, and worth a ROADMAP bullet if a real
  quiet-month E statement ever appears.
- Re-verifying against the real statement in CI — impossible by construction;
  the verification is local and one-time, and the fixture carries it forward.

---

## 10. Resource cost

Two passes per E statement, with **different bounds**: one regex pass over the
transaction region, linear in `len(region_lines)` and bounded directly by
`_MAX_PDF_ROWS`; and one scan of `full_text.splitlines()` for the totals, linear
in the whole document's line count, which **no constant bounds directly** —
`_MAX_PDF_PAGES` caps pages, not lines per page, and `_MAX_PDF_ROWS` caps only
`region_lines`. In practice it is one anchored regex per line over a document
already read into memory by `pdfplumber`, so the cost is bounded by what the
page cap already admits; it is stated this way rather than attributed to a
constant that does not govern it. Neither pass runs for any other family. No new dependency, no schema change, no migration.

---

## 11. What checks this

| Invariant | What checks it |
|---|---|
| INV-1 | Detection leg on `family_e_current.pdf` (§7.1). |
| INV-2 | Parametrised leg over all 13 existing fixtures (§7.2). |
| INV-3 | §7.9 — the fold-accepts / grammar-rejects leg, plus §7.4's grammar accept/reject legs. |
| INV-4 | §7.11's two red fixtures. §7.3's bracketing-difference assertion is true by construction and checks nothing here (§5). |
| INV-5 | §7.5 — `family_e_no_opening.pdf`, message asserted verbatim. |
| INV-6 | §7.6, parametrised over `family_e_totals_fail.pdf` **and** `family_e_deposits_fail.pdf` — each half has its own red fixture. |
| INV-7 | §7.7 (neither total) and §7.8a (exactly one, matching). |
| INV-8 | §7.7 (`None`), §7.8 (present) and §7.8a — the only leg that discriminates the one-total breaker. |
| INV-9 | §7.2's parametrised leg asserts **detection only** — it never calls `_fold`, so it cannot catch INV-9's named breaker on its own. The existing A/B/D end-to-end legs are what exercise the fold, and they cover it only incidentally: no committed fixture is known to contain a region line leading `D[D] Mon YY`, so a global widening might be caught by **nothing**. A direct unit leg — `_looks_like_row("12 Jan 25 …")` is `False` with the keyword omitted — is the cheap closure, added to §7.4. |
| INV-11 | §7.3 — every draft's `occurred_on` against its printed date. |
| INV-10 | §7.3 — a `Payments`-column row is negative. |
| That the anonymised fixture faithfully reproduces the real layout | **nothing** — the fixture is hand-authored from measured shapes, and no test can compare it against a file that must not be committed. The one-time local re-verification (§9) is the only check, and it is not repeatable in CI. |
| That a future SB statement variant is detected | **nothing** — a sixth layout would fail exactly as this one did. |
| That the `Payments`/`Deposits` totals print on statement periods other than 3 months | **nothing** — one statement observed; D9 degrades safely rather than guessing. |
| D2's detection slot (E before B) | **nothing** — E fires on none of the 13 fixtures, so no test discriminates the order (INV-2 records this). |
| A quiet-month E's coverage span | **nothing** — it is `(None, None)` by construction (§4.5), no fixture exercises it, and §9 defers the fix. |
| The totals gate on a **European**-format statement | **nothing** — `fmt` is threaded through (§4.5) but no EU-format E fixture exists; the EU path is covered for Family A only (`family_a_rcp_euro.pdf`). |
| A summary printing a total label **twice** | **nothing** — §4.5 specifies skipping, but no fixture reproduces a per-page subtotal. |
| Family **C**'s fold after the `_cc_iso` → `_dmy_iso` rename | **nothing beyond the existing C legs** — INV-9 names A/B/D, but C is the only existing family whose *code* this spec edits (the rename's one call site). The existing `family_c_creditcard.pdf` / `family_c_fail.pdf` legs run unchanged and would catch a broken rename. |

Eight `nothing` rows.

---

## 12. Cross-doc impact

- **ROADMAP FIBR-0190** — annotate (not rewrite) with the corrected row count:
  182 transactions, not 183; the 183rd was the opening line. Also record that
  the statement prints **no closing balance** and **no period line**, that the
  balance's minus is **leading** (opposite of Family A), and that the
  `Payments`/`Deposits` totals are exact column sums.
- **`docs/specs/FIBR-0050.md`** — more than a framing change, and the sentence
  that used to sit here ("the invariants are otherwise unchanged") was **false**.
  E is a fifth running-balance family, so every enumeration that says
  "(A, B, D)" or names Savings as the sole closing-less family now excludes the
  family this spec adds:
  - the *At a glance* preamble ("A, B and D are **running-balance** families");
  - **INV-2a**'s order string `C→D→B→A` → `C→D→E→B→A`;
  - **INV-7 / INV-7b** — the balance-family sign and `|delta|` rules, which E
    obeys (`_verify_row(..., check_sign=True)`);
  - **INV-10** — which families print a closing figure; E prints none, so
    Savings is no longer the sole case;
  - **INV-11 / INV-11a** — "the primary correctness gate for the balance
    families (A/B/D)", and the clause saying a statement lacking an independent
    closing "relies on the per-row gate only", which is **false for E** (it has
    the totals gate). INV-11 also promises "one friendly `ValueError`" and a
    quiet-month path that records the period; E adds a fourth distinct message
    (§4.5) and cannot record a period (§4.5, §9).

  Each gets E added or an explicit "E is exempt, see FIBR-0190 §4.5" carve-out.
  This is the largest cross-doc item in this spec and is **not** optional: two
  documents disagreeing about the same module's money gate is exactly what the
  contract exists to prevent.
- **`src/finbreak/importers/standard_bank.py` module docstring** — "Four layout
  families dispatch inside this single module (D1)" gains E. The neighbouring
  "One reader for all six of the user's Standard Bank statement **types**" is
  **left alone**: E is a second *layout* of the Current account, which Family A
  already covers, so the account-type count is unchanged and only the family
  count moves. Further in-module statements go stale and must move with it —
  `_looks_like_row`'s "A transaction/anchor line for Families A/B/D",
  `_anchor_balance`'s and `_capture_opening`'s "brought-forward" wording, and
  `_verify_row`'s "where a sign prints — A/D, not the sign-less B", alongside: `detect_standard_bank`'s docstring ("most-specific-first C->D->B->A
  (D4)"); the module docstring's "the balance families (A/B/D) take the printed
  magnitude…" and its "a per-row ``|delta| == printed`` gate (A/B/D) plus a
  completeness gate against the statement's independently-printed closing
  figure"; `_verify_checksum`'s "skip for Family A (Savings legitimately prints
  none), raise for B/D/C" (E must skip too, §4.5); and `_capture_closing`'s
  "or ``None`` when the family prints none (Savings; INV-10)".
- **`tests/features/standard_bank_pdf/spec.md`** — new rows for INV-1..INV-11,
  each cited **`FIBR-0190 INV-N`** because that file's bare `INV-N` already
  means FIBR-0050's; this is the project's existing convention
  (`FIBR-0131 INV-1` in `tests/features/auto_update/spec.md`). Its
  `detect_standard_bank` line is also updated for the new order, as is its
  opening sentence ("detection, the four family line-grammars, budget-view
  signs").
- **CHANGELOG** — one `Fixed` entry on ship.
- **`_cc_iso` → `_dmy_iso` rename (D4)** — the blast radius is **one call site**
  (`_parse_family_c`) plus one comment mentioning the name, measured
  `workspace_search "_cc_iso"` → 3 hits in source: the definition, the call, and
  the comment at the `_MON_RE` declaration. The test module does **not** import
  it, so no test moves with the rename.

---

## 13. Cold-eyes loop log

| Loop | Date | Lanes | Findings (C/H/M/L) | Outcome |
|---|---|---|---|---|
| 4 | 2026-08-03 | 2 cold (general-purpose, opus) | 0/5/8/12 verified | All fixed. **Converged — stopped here on the collateral signal, not the loop cap** (the project override allows 7). **Zero CRITICAL, and both lanes independently opened with "nothing at this level"** after tracing the grammar, detection slot, `parse` dispatch and `closing_minor` anchor against the module. Almost every finding was damage from loop 3's own fixes: loop 3 added INV-11 and two fixtures without updating the header's `INV-1..INV-10`, §12's test-contract instruction, §4.6's "Four synthetic" opener or D11's count — so following §12 literally would have dropped the **date** invariant from the test contract, and building "four" fixtures would have dropped exactly the two that falsify INV-6's `Deposits` half and INV-8's one-total breaker. Two findings genuinely changed the contract. First, a unit mismatch in the totals gate: Σ is `int` minor units but `_parse_amount` returns a **major**-unit `Decimal`, so the comparison as written was `123456 == Decimal("1234.56")` — false on every valid statement. Both conversions are now written out. Second, and the sharper one: **INV-4's test was tautological.** §4.3 stores `delta`, so "amount equals the bracketing balance difference" is true by construction and stays green with the `_verify_row` call deleted or copied from `_parse_family_b` with `check_sign=False` — i.e. an implementer could satisfy every invariant and every leg with FIBR-0050's *primary correctness gate* removed. §7.11 now carries corrupted-amount and sign-flipped red fixtures, mirroring INV-11a; fixtures are eight. A lane also corrected loop 3's own INV-11 prose: month↔day transposition always **raises** (`_MON3.get("28")` is `None`); the silent transposition is day↔year. Also fixed: `_signed_balance` was never named for the balance group (an implementer reaching for `_parse_amount` would strip the sign on all 30 leading-minus balances); §6's totals row was narrower than INV-6; the in-module staleness list was under-counted and missed the docstrings on the functions this spec actually edits; and four §11 rows still cited the pre-loop-3 leg set. **Why this is convergence and not loop 5:** no verified finding remained that changes what gets built, the two that did were fixed in this pass, and the residue was cross-reference drift the Phase-4b sweep now covers. Draft defects fell 25 → 22 → 26 → 0-critical/mostly-collateral across the four loops; per `/cold-eyes` Phase 5, collateral outnumbering draft defects means sweep harder, not read again. |
| 3 | 2026-08-03 | 2 cold (general-purpose, opus) | 2/5/8/11 verified | All fixed. **Unlike loop 2 this was mostly DRAFT defects, not collateral — and two were structural things two prior cold reads had walked past.** CRITICAL 1: the spec never specified `parse`'s family-dispatch branch at all. The chain ends in a bare `else: result = _parse_family_c(...)`, so an implementer who added detection, the grammar and the `closing_minor` wiring — everything the spec *did* specify — would route every Family-E statement into the credit-card parser, which does not fail cleanly: `_split_credit_card_line` finds the date boundaries and the C grammar matches with the **balance** captured as the amount, sign-flipped. Wrong money, no exception. §4.3 now shows the `elif` in situ and names the terminal-else hazard. CRITICAL 2: nothing anywhere asserted a parsed **date**. `_dmy_iso` is fed from three re-joined capture groups — a transposed f-string ships green with every amount right and every day wrong, which in a budgeting app is a wrong monthly total. The project's own test contract already requires "dates asserted alongside amounts" for Family C; E was the only family exempt. Now INV-11 and an assertion in §7.3. One lane demolished D6's evidence: `workspace_search "statement opening balance"` returning 0 hits proves nothing, because the fixtures are binary PDFs whose text streams a repo grep cannot read. Re-measured by extracting each fixture's text — 0 of 13 — and the check now ships as §7.10 rather than living as a claim. The `Deposits` half of the totals gate had **no red fixture** (loop 2's own doing: making the fail fixture `Payments`-only left an implementation that never checks `Deposits` passing every leg — the exact truncated-money-in-tail case D8 added the gate for), and INV-8's "only one total checked" breaker had no discriminating leg. Both closed, fixtures now six. §12's "the FIBR-0050 invariants are otherwise unchanged" was simply false — E is a fifth running-balance family, so its *At a glance* preamble, INV-2a, INV-7/7b, INV-10 and INV-11/11a all enumerate "(A, B, D)" or name Savings as the sole closing-less family; §12 now lists each. Also fixed: three §2.2 rows the design cited but §2.2 did not contain (re-measured — 182/182 rows survive the per-page terminator scan, 0 in-region date-leading non-transactions, 183 fold heads); D3's stated mechanism was wrong (right-anchoring is what protects the description, not the two-decimal filter); §4.1's `window=3` rationale claimed every signature uses it when Family C uses `window=1`; `_capture_opening` is reachable after all, on a zero-row statement; `_parse_family_e`'s signature was never given; a duplicated §6 row; and INV-3's "every folded line" now says "every group head". |
| 2 | 2026-08-03 | 2 cold (general-purpose, opus) | 1/4/6/11 verified | All fixed. **This loop was majority fix-collateral — roughly half its findings were defects loop 1's own fixes introduced — so the remedy was a harder sweep, not just per-item fixes.** The CRITICAL is loop 1's: `_verify_e_totals` was given a signature with no `fmt`, while `_MONEY` matches both decimal conventions and `_parse_amount` needs `fmt` to disambiguate — so a European-format statement would have mis-parsed its own completeness gate. `fmt` is now threaded from `parse`, where it was already in scope. Both lanes independently found loop 1's `closing_minor` wiring: it said the E branch goes "after the family dispatch and the existing `_verify_checksum` call", which lands *before* `parse`'s own `closing_minor = ...` assignment and is overwritten by `None` on the next line — the exact failure INV-8's breaker names. §4.5 now pins the anchor to that assignment and shows the three lines in situ. Two measured contradictions, both settled by re-probing: the header is reprinted on pages **1–11**, not "every page" (§2.2 said one thing and §4.2's region argument depended on the other), and the three `R`-prefixed description tokens are `R000395711` — a reference number with no decimals — so D3's claim that admitting `R` "would become ambiguous" was wrong; the two-decimal requirement is what protects the description. One lane found a genuinely invalid argument in loop 1's D9: "half a gate passes a statement whose deposits were dropped" is true, but both-or-nothing passes that case *and* the payments case, so verifying whichever total prints is strictly stronger. D9 reversed; `closing_minor` still requires both (D10), and §8 records the reversal rather than hiding it. Also fixed: the totals-mismatch message was unspecified and every existing one describes a *running-balance* disagreement, which is factively wrong for a truncated tail — it now has its own string, asserted verbatim; a duplicate total label is specified as skipped rather than guessed; `_E_TOTAL`'s `R` prefix is optional so a bare-totals summary cannot silently disable the gate; INV-10's breaker was unreachable in the specified design (nothing consults the column) and is restated as the reachable sign inversions; §7's blanket "cannot be run until the code exists" contradicted §5; leg 4 belonged to no invariant; §10 attributed the totals scan to a constant that does not bound it; and the quiet-month case was found to yield a `(None, None)` span, now stated, deferred in §9 and carried as a `nothing` row. |
| 1 | 2026-08-03 | 2 cold (general-purpose, opus) | 0/6/9/10 verified (25 verified, 4 unverified) | All fixed. **Both lanes independently led with the same CRITICAL — that `_E_ROW` accepts only a leading minus while the prose promised `_signed_balance` "handles both ends" — and re-running the measurement refuted it:** the reference statement carries **0** trailing-minus balances and **30** leading-minus ones, so the grammar was right and the *evidence* was missing. Same for the second CRITICAL (`_MON_RE` case-sensitivity): 182/182 rows are Title-case. Both are now measured rows in §2.2, which had recorded document shape but no **row token shapes** — the real defect behind both findings, and the one a token-count histogram cannot catch. Two more lane findings were refuted by measurement: the totals are not foldable into the last description (the summary page's region is empty, `0:0`) and are each alone on their line. Verified and fixed: `closing_minor` was unimplementable as specified (`parse` rebuilds its `ParseResult` and would discard a family-returned value) — §4.5 now names `_verify_e_totals`, its call site and its return; `_E_TOTAL` was specified line-anchored but "scanned over the full document text", which matches nothing without `re.MULTILINE`; the totals comparison left signed-vs-magnitude to the implementer on a money gate; INV-3's named breaker (`continue` in place of the raise) was caught by **no** leg in §7, so an implementer could ship a silent under-import green — now §7.9. Verification also surfaced a defect no lane could see: **E prints no `Statement from … to …` line**, so an implementer extending Family A's `period is None → _MISPARSE` branch to cover E would brick every E statement — now D7. A lane correctly found that this spec's `INV-N` collide with FIBR-0050's inside `tests/features/standard_bank_pdf/spec.md`; the first fix — renaming them `E-INV-*` — was **reverted by 4c's re-lint**, which caught that `spec_query` then parsed **0** invariants (`possible_untabled_invariants: 12`), breaking the machine-readability `spec-format.md` §7 requires. The project already had the answer: qualify at the citation site (`FIBR-0131 INV-1` in `tests/features/auto_update/spec.md`), which §12 now specifies. Recorded because the lane's *finding* was right and its *remedy* was wrong, and only the deterministic re-check separated the two. Also fixed: the ambiguous detection slot ("between the D and A tests" — the real order is C→D→B→A), the unstated `_dmy_iso` signature, the undefined quiet-month case, the un-reasoned global widening of `_anchor_balance`/`_capture_opening` (now D6), the wrong opening-balance error message in §7.5, `_MAX_PDF_ROWS` mis-attributed as the totals scan's bound, a fixture count that disagreed with §7.5, and a "six shapes / seventh" vs "four families / fifth" counting collision. |
