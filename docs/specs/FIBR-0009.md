# FIBR-0009 — P07: PDF statement import (with in-memory decrypt)

> **Status:** ✍️ **DRAFT** — `/cold-eyes` **not yet run**. This is the pre-review
> draft (global rule § 14: every spec runs through `/cold-eyes` to convergence
> **before** implementation). Do **not** start code until the loop log at the
> foot records a zero-finding pass. Next after convergence: TDD-implement (write
> `tests/features/pdf_import/` red → `importers/pdf_importer.py` + the v5
> migration + the `ImportService` PDF seam + the wizard PDF branch + `pyproject`
> `pdfplumber==0.11.10` green → `/close-phase`).
> **Phase:** P07 — PDF statement import. **Kind:** implement.
> **Source:** planned (roadmap FIBR-0009).
> **Security: YES.** This phase is genuinely security-relevant, unlike P05/P06:
> it (a) **decrypts** password-protected PDFs — done **in memory only** (`pikepdf`
> / qpdf), the decrypted bytes never written to disk (INV-2); (b) optionally
> **stores a credential** (the statement password) — opt-in, off by default, held
> **inside** the already-encrypted vault (INV-4/D5); and (c) makes a **schema
> change** (the v4→v5 migration adding `accounts.statement_pdf_password`, INV-8).
> It also adds `pdfplumber`, whose **transitive tree is native-heavy** — `Pillow`
> (compiled), `pypdfium2` (Google's PDFium native binary, shipped in the
> `pypdfium2_raw` companion wheel), and `cryptography` (Rust, via `pdfminer.six`)
> — a **vendored-native-lib** surface (`dependencies.md § 2`) that MUST travel
> into the FIBR-0003 bundle (§ Dependencies; a hard DoD item). No network (INV-11
> re-asserts the no-network posture). Governed additionally by
> `docs/security-model.md` **INV-5a** (input inert — `pdfplumber`/`pikepdf` never
> `eval`/shell; enforced by the P01 `bandit` gate + INV-9's inertness leg),
> **INV-5b** (input bounded — the byte/page caps, D12/INV-9), and **INV-9**
> (clean logs — the password is **never** logged nor placed in an error message,
> INV-11).
> **Roadmap:** [ROADMAP.md → FIBR-0009](../../ROADMAP.md).
> **Governed by:** `docs/standards/{coding,naming,testing,commits,dependencies}.md`
> (`naming.md` governs the new public `PdfImporter` name and the
> `statement_pdf_password` column); [ADR-0004](../decisions/0004-qt-native-pdf-over-weasyprint.md)
> (`pikepdf` is "the encryption/decryption layer regardless of renderer" — this
> phase exercises the **decryption** half); [ADR-0005](../decisions/0005-csv-mapping-profiles.md)
> (a PDF's extracted table needs the same per-bank column mapping a CSV does).
> Builds directly on [FIBR-0007](FIBR-0007.md), reusing its entire CSV
> mapping→parse→preview→dedup→commit pipeline **unchanged** (the canonical reuse
> list is in § Context).

**Theme:** P05 built the CSV importer and the whole write pipeline; P06 added OFX
as a second importer that *skips* mapping (self-describing). PDF is the **third**
importer — and the hard one. A CSV is a tidy grid and an OFX file is a
self-describing standard; **a PDF statement is a printed page.** There is no
universal way to know which text on the page is a transaction. This phase makes
the **tractable** version of the problem: many banks print their transactions as
a real **ruled table**, and `pdfplumber` lifts such a table off the page as rows
of cells. So the design is: **extract the transaction table, serialise it to CSV
text, and feed the existing CSV pipeline verbatim** — a PDF is imported as "a CSV
we happened to lift off a page." The genuinely new surface is small: the
extractor, the in-memory decrypt of locked PDFs, an opt-in remembered password
(one new nullable column, a v5 migration), and a wizard branch. **Free-text
(non-table) statements are explicitly out of scope** (a later phase; § Out of
scope).

**Contents:** At a glance (invariants) · Context · Design decisions (D1–D12) ·
Invariants (INV-1..INV-11) · Deliverables · Data model (v5 migration) · Out of
scope · Exit criteria · Dependencies · Verified API basis · Cold-eyes loop log.

## At a glance — invariants

| INV | Guarantee | Upholds |
|-----|-----------|---------|
| INV-1 | The pure-ish `PdfImporter` extracts transaction tables from a PDF (`pdfplumber` `extract_tables()` over every page) and serialises **one chosen table** to CSV text (`csv.writer`; an empty/`None` cell → `""`). That text feeds the **existing, unchanged** CSV path — `read_header` → `match_profile`/mapping → `CsvImporter.parse` → `preview` → `_dedup` → `commit_import` — so a PDF-imported amount obeys the **identical** money + dedup contract as a CSV one, with **no duplicated parsing** | design.md pluggable-importer interface; FIBR-0007 D5 (`parse_transaction`); ADR-0005; coding.md § 1.3 (reuse) |
| INV-2 | **(Security spine.)** A password-protected PDF is decrypted **in memory only**: `pikepdf.open(BytesIO(raw), password=…)` → `pdf.save(BytesIO())` produces the decrypted bytes as an in-memory `BytesIO`, handed straight to `pdfplumber`. The module **never** writes decrypted (or encrypted) PDF bytes to disk — no `tempfile`, no `Path.write_bytes`, no `NamedTemporaryFile`; the only filesystem read is the user-picked source path | roadmap FIBR-0009 ("decrypted in memory only … never written decrypted to disk"); ADR-0004; security-model INV-5 |
| INV-3 | A **wrong or missing** password re-prompts, never aborts: `pikepdf` raises `pikepdf.PasswordError` on a bad/absent password (verified), which the wizard catches to **re-show** the password dialog; the import is not cancelled and nothing crashes. A stored password (INV-4) that later fails falls through to the same re-prompt | roadmap FIBR-0009 ("a wrong PDF password re-prompts rather than aborting"); coding.md § 2 |
| INV-4 | "**Remember this password**" is **opt-in, off by default**. Unchecked → **nothing is stored** (the default: prompt each time). Checked → the password is stored **inside the vault** (SQLCipher-encrypted at rest, protected by the master password — no redundant second layer, D5) against the account (the v5 `accounts.statement_pdf_password` column) and **auto-tried** on the next PDF import into that account before prompting | roadmap FIBR-0009 ("opt-in remember … stored encrypted in the vault … default: prompt each time, store nothing") |
| INV-5 | A PDF with **no extractable table** surfaces one friendly `ValueError` ("couldn't read a table from this PDF — try your bank's CSV or OFX export") — no crash, no partial import; the wizard shows the message | coding.md § 2 ("no silent data loss"); § Out of scope (free-text deferred) |
| INV-6 | A **multi-table** PDF surfaces its candidate tables (grouped across pages by identical header, D8) for the user to choose **which** is the transactions table (defaulting to the largest by data-row count); no table is silently assumed to be the right one | design.md "import to insight"; coding.md "no silent data loss" |
| INV-7 | Import-wizard PDF round-trip (INV-7a–f, `qtbot`): picking a `.pdf` → **if encrypted**, a password dialog (masked field + a "remember for this account" checkbox); a wrong password re-prompts (INV-3); **if >1 candidate table**, a chooser (INV-6); then the **familiar column-mapping step** (PDF is table-with-columns, so it does **not** skip mapping the way OFX does); then preview → **Import** inserts exactly the new rows and records the period; a second import of the same statement previews as all-duplicate and adds zero (INV-10); a remembered password auto-applies with no prompt | FIBR-0007 INV-10; FIBR-0008 INV-7; coding.md § 5.2 |
| INV-8 | The v4→v5 migration adds the **nullable** `accounts.statement_pdf_password TEXT` column via `ALTER TABLE … ADD COLUMN` (in-place — a nullable add needs no table rebuild, unlike the v2 NOT-NULL+FK case), inside an explicit `BEGIN … COMMIT`/`ROLLBACK` (the runner owns the transaction under `isolation_level=""`); forward-only; `LATEST_SCHEMA_VERSION` 4→5. The `Account` dataclass and its repository `SELECT` are **unchanged** — the new column is **not** selected into the broadly-passed `Account` (credential hygiene: a remembered password never rides along with an account listing / log line, D6) | FIBR-0005 INV-4 (migration runner); FIBR-0007 D-migration pattern; security-model INV-9 |
| INV-9 | PDF is **untrusted input** and is **resource-bounded** (security-model INV-5a/INV-5b, D12): a file larger than `_MAX_IMPORT_BYTES` (16 MiB) is refused with a friendly `ValueError` **before** its bytes are read into memory (the shared cap OFX also uses, D10); extraction is bounded to `_MAX_PDF_PAGES` (500) pages, refused whole-file beyond that; the input is **inert** — `pdfplumber`/`pikepdf` never `eval`/shell/derive a path from content (asserted by INV-9's inertness leg + the `bandit` gate) | security-model INV-5a/INV-5b (T5); coding.md "Error handling" |
| INV-10 | PDF import feeds the **same** `ImportService` write pipeline as CSV/OFX (unchanged): the multiset-delta dedup on `(occurred_on, amount_minor, normalised description)` account-scoped, the atomic `BEGIN … COMMIT` insert of deduped rows + the `statement_periods` record, and span-dedup — so re-importing the same statement adds **zero** transactions and no second period row | roadmap FIBR-0009 ("on the P05 pipeline"); FIBR-0007 INV-5/6/7 |
| INV-11 | The new module + dependency add **no** network import and log **no** secret — in particular the **PDF password** is never logged, never interpolated into an exception message, and not held on the widget beyond the operation that needs it; the `pdfplumber`/`pikepdf`/`pdfminer` loggers are not configured to emit at the app's level | security-model INV-8 (no-network), INV-9 (clean logs) |

## Context

The app is layered — **UI → Services → Repositories → SQLCipher** (design.md) —
and its headline flow is **import → categorise → insight**. FIBR-0007 built the
first importer (CSV) and the entire write path; FIBR-0008 added OFX. This phase
adds the **third** importer. Because a PDF's transaction table is
**columns-that-need-mapping** (like CSV) rather than self-describing (like OFX),
PDF reuses the **CSV** half of the pipeline — and it does so by turning the
extracted table back into CSV text (D1), so the reuse is total.

This phase **reuses, unchanged**, the machinery FIBR-0007 shipped:

- **`CsvImporter.parse(text, mapping, exponent)`** — the pure CSV parser. The PDF
  path serialises its chosen table to CSV text and calls this **verbatim** (D1),
  so a PDF amount obeys the identical money contract as a manually-typed one.
- **`ImportService.preview(text, mapping, account_id)`**,
  **`match_profile(header)`**, **`save_profile(name, header, mapping)`**,
  **`signature_for(header)`** — the CSV mapping-profile machinery. A PDF table's
  header feeds these **unchanged**, so a PDF bank layout is remembered per-bank
  exactly like a CSV one (D9), for free.
- **`ImportService.commit_import(preview, period_start, period_end,
  source_filename)`**, the **multiset-delta dedup** (`_dedup`), and the
  `statement_periods` span-dedup — **not touched**; the PDF path produces the same
  `ImportPreview` and calls the same `commit_import`.
- **`parse_transaction`**, **`TransactionDraft`**, the **`ParseResult` /
  `RowError`** value objects (`importers/base.py`), and
  **`read_minor_unit_exponent(conn)`**.
- **The import wizard** (`ImportWizardWidget`) — the PDF path is a **branch**
  inside `_select_file` (beside the CSV and OFX branches), converging onto the
  existing map/preview steps, not a new screen.
- **`pikepdf`** — already a pinned runtime dependency (ADR-0004, the PDF-export
  encryption layer). This phase exercises its **decryption** half; no new crypto
  dependency for the locked-PDF path.

So the genuinely new code is: the `PdfImporter` extractor, the in-memory decrypt
helper, the v5 migration + two repository accessor methods, the password dialog,
the wizard PDF branch, and one pinned dependency (`pdfplumber`).

## Design decisions baked into this spec

These are the non-obvious calls; `/cold-eyes` gates them before code. **D1–D4,
D7–D11 are engineering calls; D5, D6, D12 carry a security/scope decision**
(remembered-credential storage model, credential hygiene, and the native-bundle
surface).

- **D1 — Extract-then-CSV-adapter (the core reuse decision).** The `PdfImporter`
  is an **extractor**, not a second parser: it lifts a table with `pdfplumber`
  and serialises it to CSV **text**, which the *existing* `CsvImporter` /
  `ImportService.preview` consume unchanged. Rejected alternative: a parallel
  `PdfImporter.parse(rows, mapping, exponent)` mirroring `CsvImporter.parse` — it
  would duplicate the ragged-row guard, the date/amount parse, the `RowError`
  collection, and the span logic. Per coding.md § 1.3 + the Rule of Three, at the
  **second** table-shaped importer the right move is to reuse the CSV parser via a
  thin text adapter, not to fork it. Six-month legibility: "a PDF is imported as a
  CSV lifted off a page" is one sentence a future reader holds in their head.
- **D2 — `pikepdf`/qpdf is the decryptor, not `pdfminer`'s `password=`.**
  Empirically **both** decrypt AES-256/R6 (see § Verified API basis:
  `pdfplumber.open(BytesIO, password=…)` succeeded). We deliberately route
  decryption through **`pikepdf` (qpdf)** anyway: qpdf is the industry-standard
  PDF decryptor and handles the widest range of real-bank encryption variants far
  more robustly than `pdfminer`'s crypto, it is **already** a bundled dependency
  (ADR-0004), and the roadmap names it explicitly. `pdfplumber` then only ever
  sees **already-decrypted** bytes — it is never handed a password. Both paths
  keep bytes in memory (INV-2).
- **D3 — Encryption detected by an open-attempt, not a heuristic.** Detect a
  user-password PDF by `pikepdf.open(BytesIO(raw))` **without** a password: it
  raises `pikepdf.PasswordError` (verified) → prompt. A PDF with **owner-only**
  encryption (no user password) opens without one and extracts normally (no
  prompt). An unencrypted PDF opens and extracts directly (the decrypt round-trip
  is **only** taken when a password is actually required, so a large plaintext PDF
  is not needlessly re-serialised).
- **D4 — In-memory decrypt is a tested invariant, not a comment.** The decrypt
  helper takes `bytes` and returns `bytes` (via `BytesIO`); it imports neither
  `tempfile` nor writes any `Path`. INV-2 is tested two ways: (a) a positive test
  that an encrypted fixture decrypts+extracts purely from bytes, and (b) a guard
  that the module's source contains no `tempfile` / `NamedTemporaryFile` /
  `.write_bytes(` / `open(…, "w")` disk-write token (the negative is otherwise
  unobservable at runtime).
- **D5 — Remembered password: stored plaintext inside the encrypted vault, no
  second layer.** The vault is SQLCipher (AES-256, keyed by the Argon2id-derived
  master key). A value stored in a vault table **is** encrypted at rest by that
  key — so "store it encrypted in the vault" (roadmap) is satisfied by a plain
  column value; adding a second, app-level encryption of the password on top would
  be redundant ceremony (over-engineering, coding.md § 2) with no threat it
  answers (anyone who can read the column has already unlocked the vault, i.e.
  has the master password — the same trust boundary as every transaction). The
  remembered password is therefore protected **exactly as strongly as the
  financial data itself**, and only when the user opts in.
- **D6 — Credential hygiene: the password column is kept off the `Account`
  object.** The v5 column is read/written by **dedicated** repository methods
  (`get_pdf_password(account_id)` / `set_pdf_password(account_id, value|None)`),
  and is **not** added to the `Account` dataclass nor its main `SELECT`. So a
  remembered credential never rides along with the account listings that populate
  combos, tables, or a log line — it is fetched only at the moment a PDF import
  needs it (INV-11). (This also keeps the `Account(*row)` positional-construction
  contract and every account call-site unchanged — INV-8.)
- **D7 — Multi-table chooser lives on the map step, default = largest table.**
  When >1 candidate table is extracted (D8), a chooser (a `QComboBox`, shown only
  for >1, mirroring the OFX statement chooser) sits above the column combos on the
  map step; selecting a table repopulates the mapping combos from **that** table's
  header. The default selection is the table with the most data rows (the
  transactions table, empirically, dwarfs a summary/header table). Because the
  user still maps columns, a wrong table pick is fully recoverable (re-choose /
  the columns won't map). No auto-commit on a guessed table.
- **D8 — Tables grouped across pages by identical header.** `extract_tables()` is
  per-page, but a statement's transaction table routinely continues across pages
  under a **repeated header row**. After extraction, tables whose **header row**
  (row 0, whitespace-folded) is identical are **grouped** into one logical
  candidate table — the first group member keeps its header; subsequent members
  contribute their data rows with any repeated header row dropped. So a 3-page
  statement yields **one** transactions candidate, not three. A page's tables with
  a unique header stay separate candidates.
- **D9 — A PDF layout is remembered per-bank via the existing profile machinery.**
  The serialised table's header row is a `list[str]` — exactly what
  `signature_for` / `match_profile` / `save_profile` key on. So a saved PDF
  mapping profile works identically to a CSV one, keyed by the PDF table's header
  signature; a second import from the same bank auto-applies it (FIBR-0007
  INV-10a, reused). No new profile concept.
- **D10 — Generalise `read_file_bytes` to a format-neutral size-capped read.**
  FIBR-0008's `read_file_bytes` + `_MAX_OFX_BYTES` are OFX-named but format-generic
  (a size-capped byte read). Rename `_MAX_OFX_BYTES` → `_MAX_IMPORT_BYTES` and the
  refusal message to be format-neutral so OFX **and** PDF share one capped read
  (coding.md § 1.3 refactor-to-reuse (b) — existing OFX call-site benefits, its
  behaviour unchanged). The OFX import suite must stay green over the rename.
- **D11 — The password prompt is a small custom `QDialog`.** A masked
  (`QLineEdit.Password`) field + a "Remember this password for <account>"
  `QCheckBox` (unchecked by default) + OK/Cancel. Re-shown on a wrong password
  (INV-3); Cancel abandons the import cleanly. (A bare `QInputDialog.getText`
  cannot carry the remember checkbox, so a purpose-built dialog is warranted.)
- **D12 — `pdfplumber` pinned at 0.11.10; its native transitive surface is a
  bundle obligation.** `pdfplumber==0.11.10` (latest, verified) pulls
  `pdfminer.six` (→ `cryptography` (Rust, native) + `charset-normalizer`),
  `Pillow` (native), and `pypdfium2` (→ the `pypdfium2_raw` PDFium **native
  binary**). This is the heaviest single dependency addition in the project and,
  like FIBR-0008's `lxml`, it is a **vendored-native-lib blind spot**
  (`dependencies.md § 2`) that must travel into the FIBR-0003 self-contained
  bundle. The FIBR-0003 build smoke-test is a **hard DoD item** for this phase,
  and the build script must `--collect-all pypdfium2` / `--collect-all PIL` (their
  native payloads are data/binaries PyInstaller does not follow by `import`
  alone). Pinned per dependencies.md (latest-version policy).

## Invariants (testable)

The table under *At a glance* is the canonical list (INV-1..INV-11). Sub-invariants
for the wizard round-trip:

- **INV-7a** — Picking a `.pdf` routes to the PDF branch (`_looks_like_pdf`: a
  `.pdf` extension, else a bounded 5-byte `%PDF-` magic sniff for a mis-named
  file — never the whole file, so the size cap cannot be bypassed by the sniff,
  mirroring `_looks_like_ofx`).
- **INV-7b** — An **encrypted** PDF with **no** stored password shows the password
  dialog; the correct password proceeds; a **wrong** password re-shows the dialog
  (INV-3) and does not crash or advance.
- **INV-7c** — A **>1-table** PDF shows the table chooser defaulted to the largest
  table; switching tables repopulates the mapping combos (INV-6/D7).
- **INV-7d** — After extraction the PDF path lands on the **map step** (unlike OFX,
  which skips it), and an exact-signature profile match auto-applies + jumps to
  preview (FIBR-0007 INV-10a reused).
- **INV-7e** — **Import** inserts exactly the new rows + records the period; a
  second import of the same statement previews all-duplicate and inserts zero
  (INV-10).
- **INV-7f** — With "remember" checked, the password is stored (INV-4) and a
  subsequent PDF import into that account **auto-applies** it with **no** dialog;
  a stored password that no longer works falls back to the prompt (INV-3).

The security invariants INV-2 (in-memory decrypt), INV-3 (wrong-password
re-prompt), INV-4 (opt-in remembered password), INV-9 (resource bounds +
inertness), and INV-11 (no-secret-logged) each carry a dedicated headless test
(no Qt needed) built on `pikepdf`-encrypted fixtures.

## Deliverables

1. **`src/finbreak/importers/pdf_importer.py`** — `PdfImporter` (naming.md):
   - `extract_tables(pdf_bytes: bytes, password: str | None = None) ->
     list[list[list[str]]]` — decrypt-in-memory if a password is given/needed
     (D2/D4), then `pdfplumber.open(BytesIO)` and collect every page's
     `extract_tables()`; group across pages by identical header (D8); enforce the
     page cap (INV-9). Raises `pikepdf.PasswordError` (re-exported as the module's
     signal) on a bad/absent password (INV-3), a friendly `ValueError` on no
     tables (INV-5) / an over-large page count.
   - `is_encrypted(pdf_bytes: bytes) -> bool` — the D3 open-attempt detector.
   - `table_to_text(table: list[list[str | None]]) -> str` — `csv.writer`
     serialisation, `None`→`""` (D1).
   - No `tempfile` / disk-write import anywhere in the module (INV-2/D4).
2. **`src/finbreak/migrations.py`** — `_migrate_to_v5` (nullable
   `accounts.statement_pdf_password TEXT`, atomic; INV-8); `LATEST_SCHEMA_VERSION`
   4→5; `_MIGRATIONS[5]` wired.
3. **`src/finbreak/repositories/accounts.py`** — `get_pdf_password(account_id) ->
   str | None` and `set_pdf_password(account_id, value: str | None)` (D6); the
   `Account` dataclass + main `SELECT` unchanged (INV-8).
4. **`src/finbreak/services/import_.py`** — a PDF seam mirroring the OFX one:
   reuse the renamed format-neutral capped read (`read_file_bytes` +
   `_MAX_IMPORT_BYTES`, D10); a thin method (or the wizard directly) driving
   `PdfImporter` + the remembered-password accessors. `preview` / `commit_import`
   / `_dedup` **unchanged**.
5. **`src/finbreak/ui/import_wizard.py`** — the PDF branch in `_select_file`
   (`_looks_like_pdf` → `_select_pdf`): detect encryption, auto-try a stored
   password, else the password dialog (D11) with re-prompt (INV-3) + remember
   (INV-4); the table chooser on the map step (D7); converge onto the existing
   map/preview tail. Add `*.pdf` to the file-dialog filter.
6. **`src/finbreak/ui/` password dialog** — the small `QDialog` (D11).
7. **`pyproject.toml`** — `pdfplumber==0.11.10` added to runtime deps (D12); the
   FIBR-0003 build script `--collect-all` for `pypdfium2` + `PIL`.
8. **`tests/features/pdf_import/{spec.md, test_pdf_import.py}`** — INV-1..INV-11,
   including `pikepdf`-encrypted fixtures, the in-memory-decrypt guard (D4), the
   wrong-password re-prompt, the remembered-password round-trip, and the
   multi-table/multi-page grouping. Plus the v5 migration test (atomic add,
   forward-only) beside the existing migration suite, and the schema-version
   ripple (the `== 4`→`== 5` "lands-at-latest" assertions in the vault/accounts
   suites — enumerated at implementation from a grep, as FIBR-0007 did for 3→4).

## Data model (v5 migration)

One additive, in-place migration. `accounts` gains a **nullable** column:

```sql
ALTER TABLE accounts ADD COLUMN statement_pdf_password TEXT;   -- nullable → in-place, no rebuild
```

Wrapped in the runner's explicit `BEGIN … COMMIT`/`ROLLBACK` (INV-8; the
`isolation_level=""` transaction-ownership rule, migrations.py D2). `NULL` =
"no remembered password" (the default for every existing and new account). No
other table changes; `import_profiles` and `statement_periods` (v4) already hold
everything a PDF import needs (a PDF profile is just an `ImportProfile`, D9).

## Out of scope (deferred to named phases)

- **Free-text (non-table) PDF statements.** Statements printed as flowing lines
  with no ruled table are **not** importable in this phase (INV-5 shows a friendly
  "try CSV/OFX" message). A later phase could add per-bank line-pattern (regex)
  rules; explicitly **not** built now (YAGNI; the table path covers the common
  case and reuses everything).
- **Scanned / image-only PDFs (OCR).** No text layer → no extractable table; same
  friendly message. OCR is out of scope for a local, dependency-light app.
- **Automatic table detection tuning** (custom `table_settings` per bank). The MVP
  uses `pdfplumber`'s default line-based detection + the user's table choice (D7);
  per-bank extraction tuning is deferred.
- **PDF *export* password-locking** (the *encryption* half of ADR-0004) — that is
  **FIBR-0013** (P11 locked PDF export), a separate phase. This phase only
  **decrypts** input PDFs.

## Exit criteria (Definition of Done)

1. `./scripts/ci-local.sh` green — ruff + format-check + bandit + pip-audit +
   gitleaks + `pytest -m "not perf"`; **mypy 0**. Any bandit hit **root-caused**,
   not suppressed (global rule § 1).
2. **FIBR-0003 build smoke re-run GREEN** (`./scripts/ci-local.sh --build` /
   `build-smoke.sh`): both the onefile and AppImage print `FINBREAK_SELFTEST_OK`
   in the Python-free clean-room, proving the **new native stacks travel**
   (`Pillow`, `pypdfium2`/PDFium, `cryptography`) — the `--self-test` leg extends
   to import `pdfplumber` + open a tiny in-test PDF (mirroring the ofxparse leg
   added in FIBR-0008). **Hard gate** (D12).
3. Every INV (INV-1..INV-11, INV-7a–f) has a passing test;
   `tests/features/pdf_import/` added.
4. The **CSV and OFX import suites stay green** over the D10 rename and the shared
   pipeline reuse (INV-1/INV-10 reuse-unchanged).
5. Security: INV-2 (decrypted bytes never on disk) and INV-11 (password never
   logged / never in an exception message) each have a passing test; the
   `docs/audit-allowlist.md` is consulted before `/audit` + `/indie-review`.
6. ROADMAP FIBR-0009 → ✅ with a resolution note; CHANGELOG `[Unreleased]`
   updated; `docs/journal/FIBR-0009.md` written; tag `FIBR-0009-complete`.

## Dependencies

- **Roadmap:** depends on **FIBR-0007** (✅ — the CSV pipeline this phase reuses
  wholesale). FIBR-0008 (OFX, ✅) is not a prerequisite but the D10 rename touches
  its `read_file_bytes`.
- **New runtime dependency:** `pdfplumber==0.11.10` (latest; dependencies.md
  latest-version policy). Its transitive tree (verified, § Verified API basis):
  `pdfminer.six==20260107` → `cryptography>=36` (**Rust native**) +
  `charset-normalizer` (native speedups); `Pillow>=12.2` (**native**);
  `pypdfium2>=5.9` → `pypdfium2_raw` (**PDFium native binary**). **Bundle
  obligation:** these must travel into the FIBR-0003 bundle (DoD #2); the build
  script gains `--collect-all pypdfium2` / `--collect-all PIL` (D12). `pip-audit`
  covers the new tree.
- **Already pinned:** `pikepdf==10.9.1` (ADR-0004) — the in-memory decryptor
  (D2), no new dependency for the locked-PDF path.

## Verified API basis

Empirically verified in the project venv before drafting (global rule § 13; the
probe built a synthetic ruled-table PDF with `reportlab`, extracted it, encrypted
it with `pikepdf` AES-256/R6, and round-tripped the decrypt):

- **Versions:** `pdfplumber 0.11.10`, `pikepdf 10.9.1`.
- **`pdfplumber.open(io.BytesIO(bytes))`** works (no path needed); `.pages` is the
  page list; **`page.extract_tables()`** returns `list[table]` where a table is
  `list[row]` and a row is `list[cell]` — cells are `str`, an empty cell is `""`
  (and can be `None` under other table settings — the adapter treats both as
  empty, D1). The synthetic header + 3 data rows extracted exactly, including a
  bank's separate **"Money Out" / "Money In"** columns (→ the CSV debit/credit
  mapping path). `.metadata` is a dict (`Author`/`Creator`/`CreationDate`/…).
- **`pikepdf.open(io.BytesIO(raw))` with no password on an encrypted file → raises
  `pikepdf.PasswordError`** (module `pikepdf._core`, exposed as
  `pikepdf.PasswordError`); **with `password="wrong"` → the same
  `pikepdf.PasswordError`** (INV-3's signal).
- **In-memory decrypt:** `pikepdf.open(io.BytesIO(enc), password="secret")` opens
  (`.is_encrypted` True); **`pdf.save(io.BytesIO())`** yields decrypted bytes that
  `pdfplumber` re-extracts — **no filesystem path involved** (INV-2/D4).
- **`pikepdf.open(...).is_encrypted`** is `False` for a plaintext PDF (D3).
- Note (informational, not the chosen path): `pdfplumber.open(io.BytesIO(enc),
  password="secret")` **also** decrypts AES-256/R6 via `pdfminer.six`'s
  `cryptography` backend — confirming both routes work; D2 still selects `pikepdf`
  for robustness.

## Cold-eyes loop log

_(To be filled as the `/cold-eyes` loops run — global rule § 14. Loop 2+ runs
cold, no briefing on prior findings. Cleared for code only when a pass returns
zero verified findings across all lanes.)_

| Loop | Date | Lanes | CRIT | HIGH | MED | LOW | Notes |
|------|------|-------|------|------|-----|-----|-------|
| — | — | — | — | — | — | — | not yet run |
