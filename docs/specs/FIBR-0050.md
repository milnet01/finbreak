# FIBR-0050 — Standard Bank (SA) statement text-parser (one reader, all account types)

> **Status:** 🚧 **DRAFT — `/cold-eyes` in progress** (global rule § 14: loop until
> a pass returns zero verified findings before any code). Not cleared for code.
> **Phase:** enhancement of P07 (PDF import). **Kind:** feature.
> **Source:** user-request-2026-07-05 (the user supplied **six** real Standard Bank
> statement types + confirmed the design; see § Verified basis).
> **Security: NO new surface.** This phase adds **no** dependency, **no** schema
> change, and **no** new credential path. It **reuses** FIBR-0009's in-memory
> `pikepdf` decrypt (security-model INV-4, decrypted bytes never on disk), the
> `pikepdf.PasswordError` re-prompt, the opt-in remembered password
> (`accounts.statement_pdf_password`, v5), and the byte/page/row caps — all
> unchanged. The one security-relevant obligation is inherited, not new: the
> statement password is **never** logged (INV-12 re-asserts it for the new path).
> **Test fixtures are 100 % synthetic** — no real statement, account number, or ID
> number is committed (§ Fixtures).
> **Roadmap:** [ROADMAP.md → FIBR-0050](../../ROADMAP.md).
> **Governed by:** `docs/standards/{coding,naming,testing,commits,dependencies}.md`.
> **Relation to [ADR-0005](../decisions/0005-csv-mapping-profiles.md) — a
> deliberate, justified deviation.** ADR-0005 *rejects* "hard-coded per-bank
> parsers" as unbounded maintenance for the next user's unknown bank, and reserves
> "skips mapping" for genuinely **self-describing machine formats** (OFX). A
> Standard Bank **PDF is not self-describing** — FIBR-0009 itself calls a PDF
> "table-with-columns, so it does not skip mapping the way OFX does"
> ([FIBR-0009.md](FIBR-0009.md) INV-7). This phase **knowingly deviates**: it adds
> one hard-coded per-bank reader that skips mapping because it *hard-codes* SB's
> column grammar — accepted here for two specific reasons ADR-0005's blanket
> rejection does not weigh: (1) this is the user's **own** bank, the only one they
> hold, so the "next user's unknown bank" cost does not apply; (2) the FIBR-0009
> **generic table path remains the total fallback** for every unrecognised layout
> (INV-2), so the reader adds capability without removing generality. Builds on
> [FIBR-0009](FIBR-0009.md) (the decrypt + PDF-extraction machinery reused) and
> [FIBR-0008](FIBR-0008.md) (the OFX `parse(bytes) -> ParseResult` shape mirrored,
> and its `preview_ofx` seam renamed — D5).

**Theme.** FIBR-0009 shipped a **generic** PDF path: lift a **ruled table** off
the page (`pdfplumber.extract_tables()`), serialise it to CSV, feed the CSV
pipeline. That path is right for a bank whose statement is a clean ruled grid —
but it is **fragile** on real Standard Bank statements, empirically:

| Real SB statement | Generic table path (FIBR-0009) |
|---|---|
| Home Loan | ✅ extracts row-aligned |
| Savings (Puresave) | ✅ extracts row-aligned |
| Revolving-Credit (RCP) loan | ✅ extracts row-aligned |
| Money Market (investment) | ~ (not re-probed via tables; text is clean) |
| **Current account** | ❌ **collapses** — all transactions into one cell |
| **Credit card** | ❌ two-columns-per-line, no grid lines — unreadable |

Same bank, same visual language, but the **table geometry** is unreliable (the
Current account has the same column shape as Savings yet collapses; a denser
statement could regress the same way). The **printed text**, however, is stable
across every layout — it is what a human reads. So this phase adds **one Standard
Bank reader** that parses the **text lines** (not the grid) and feeds the existing
preview → dedup → commit pipeline. It is **one reader for all account types**, not
one-per-type (the user's explicit constraint): the account families differ only in
a small, enumerated set of line-grammar knobs handled inside the single module.

**Contents:** At a glance (INV-1..INV-14) · Context · The four layout families
(empirical grammar) · Design decisions (D1–D13) · Invariants (sub-invariants) ·
Deliverables · Fixtures (synthetic) · Out of scope · Exit criteria · Dependencies ·
Verified basis · Cold-eyes loop log.

## At a glance — invariants

Families: **A** = transactional (Current / Savings / Revolving-Credit); **B** =
Home Loan; **D** = Money Market / investment; **C** = credit card. A, B and D are
single-column **balance-bearing** families (a running balance per row); C has no
per-row balance.

| INV | Guarantee | Upholds |
|-----|-----------|---------|
| INV-1 | `StandardBankImporter.parse(pdf_bytes, exponent, password) -> ParseResult \| None` produces the **same** `ParseResult` (drafts + period; SB import is all-or-nothing, so `errors` is always empty on success — INV-11) an OFX statement does, and the wizard feeds it through the **public** `preview_result` (the renamed `preview_ofx`, D5) → the unchanged `_preview_from_result` → `_dedup` → `commit_import` tail — so an SB amount obeys the **identical** money + dedup contract (`parse_transaction`, reused verbatim) as CSV/OFX/manual. Like OFX, a recognised SB statement **skips column-mapping** (the reader knows the columns; D1). `parse` returns `None` when the PDF is **not** a recognised SB statement (INV-2) | FIBR-0008 D2 (`preview_ofx`); FIBR-0007 D5 (`parse_transaction`); coding.md § 1.3 |
| INV-2 | **Detection is a gate, never a hijack.** A PDF is parsed by the SB reader **iff** `detect_standard_bank(full_text)` returns a recognised family (the SB legal marker **and** a family's column-header signature both present, matched over the **whole concatenated text**, D4); otherwise `parse` returns `None` and the wizard **falls through to the unchanged FIBR-0009 generic table path** — so no non-SB bank and no un-characterised SB layout regresses; the generic path is a total fallback | coding.md § 2 (Error handling — surface, don't swallow); § Out of scope |
| INV-3 | **Family A** (transactional) parses each transaction line **right-anchored**: `…description… [amount][-] MM DD balance[-]` → (date, description, amount), robust when a description contains digits ("INTEREST ON OVERDRAFT UP TO 08 24") because only the fixed tail (balance, the `MM DD` date, an optional amount) is peeled from the right | § The four layout families |
| INV-4 | **Family B** (Home Loan) parses `PostingDate[ EffectiveDate] description amount balance` with **full ISO dates** and **no printed per-amount sign** (the amount magnitude *is* printed, e.g. `69.00`; only the sign is absent). The **posting** date is `occurred_on` (D7) | § The four layout families |
| INV-5 | **Family D** (Money Market / investment) parses `YYYY MM DD description [±R amount] R balance` — **full space-separated dates**, amounts and balance **R-prefixed** with a **leading** sign (`-R4,200.00` withdrawal, `R4,282.14` deposit/interest). Page 2's "Interest payment details" table is **not** transactions and is region-excluded (INV-10) | § The four layout families |
| INV-6 | **Family C** (Credit card) **de-interleaves** the two-columns-per-line layout (split each line at each line-position `D[D] Mon YY` date; each segment is `date description amount`, amount = the last numeric token so a price inside a description is kept), and takes the sign from the **printed** figure (credits already print negative, purchases positive) **flipped** to the budget view (D3) | § The four layout families |
| INV-7 | **Sign.** For the balance families (A, B, D): **imported signed amount = the printed amount's magnitude carrying the sign of the running-balance delta** `balance[i] − balance[i−1]` (both balance signs honoured, incl. an overdraft `730.55-`). The delta sign is authoritative (it is the only sign source for Home Loan); where the amount also prints a sign (A trailing `-`, D leading `-`), it must **agree** with the delta sign. For credit card (no per-row balance): **`−(printed signed)`** (D3). **Documented loan consequence:** on a **loan** a fee raises the balance and so imports **positive** (debt up); a loan-sign toggle is deferred (§ Out of scope) | § Verified basis (delta reproduced on real data); coding.md § 2 |
| INV-8 | **Both South-African number formats parse**, detected **per statement** and required **consistent**: US `1,427.41` (`,` thousands, `.` decimal) and European `239.206,04` (`.` thousands, `,` decimal — the Revolving-Credit statement). Detection reads, over the statement's **money tokens only** (amount/balance figures — after stripping any leading `-` and `R` prefix; **excluding** interest-rate / reference tokens like `@09,250%` or `7.050%`), the decimal separator as the punctuation preceding the final two digits; a statement mixing both separators-as-decimal is refused with a friendly `ValueError` (D9) | § Verified basis (the RCP European sample); coding.md § 2 |
| INV-9 | **Dates resolve to a full ISO `YYYY-MM-DD`.** Family A's bare `MM DD` gets its **year inferred** from the "Statement from `<D Month YYYY>` to `<D Month YYYY>`" period: transactions are walked in statement order from the start year, and the year **increments once** at a strict **Dec→Jan** boundary (previous month `12` → current month `1`), assuming the within-year chronological order SB prints. Families B (`YYYY-MM-DD`), C (`D[D] Mon YY`, 2-digit year) and D (`YYYY MM DD`) carry explicit years — no inference | § The four layout families |
| INV-10 | **Only transaction rows become drafts.** A "Balance Brought Forward" / "BROUGHT FORWARD" row is a **balance anchor**, not a transaction (it seeds the delta chain, incl. the per-page repeat on a continuation page, D12); "Closing balance" / "Balance as at …", section headers ("Credits"/"Debit"), the masked "Account …" line, and the whole post-table region (VAT Summary / Interest Calculation / Interest payment details / Limit-or-Fee Structure / Account Summary / legal footer / "End of Statement") are **skipped**. A line with no date+amount tail (a `VAS…`/`*****…` reference, a second-line payee) is **folded into the preceding transaction's description**. The table region is bounded (column-header line → first terminator line, recomputed per page) so footer prose can never fold into a description | § The four layout families; coding.md § 2 |
| INV-11 | **A parsed statement is checksum-verified; SB import is all-or-nothing.** Balance families (A/B/D): `opening_balance + Σ(signed amounts) == printed closing balance` — **genuine**, because each summand is the row's **printed magnitude** (independent of the balance chain, INV-7), plus a per-row cross-check `\|delta\| == printed magnitude` (INV-7b). Credit card (C): `brought_forward + Σ(printed amounts) == closing balance`. Any reconciliation failure, or any row `parse_transaction` rejects, raises **one** friendly `ValueError` ("this statement didn't add up — try the generic table import or your bank's CSV/OFX") — **no partial import** (a running-balance ledger that doesn't reconcile is untrustworthy; safer to defer to the generic path than import a wrong figure) | coding.md § 2; global rule § 1 (no silent wrong result) |
| INV-12 | **Reused security spine, no new surface.** The wizard decrypts **once** (the shared `_decrypt_pdf` password loop, D6) yielding plaintext bytes that both the SB reader and the generic extractor consume (their internal `_normalise_to_plaintext` is a cheap idempotent re-save on already-plaintext bytes); decrypted bytes are **never** on disk (security-model INV-4); `pikepdf.PasswordError` re-prompts (FIBR-0009 INV-3); the byte/page/row caps hold; and **no secret is logged** — a sentinel password appears in **no** log record (`caplog`) and **no** exception message (`str(exc)`). No new network import; the module is `eval`/shell-free (`bandit`) | FIBR-0009 INV-2/INV-3/INV-9; security-model INV-4/INV-8/INV-9 |
| INV-13 | Import-wizard SB round-trip (`qtbot`): picking a recognised SB `.pdf` → **if encrypted**, the existing password dialog (a wrong password re-prompts, FIBR-0009 INV-3; a remembered password auto-applies) → **straight to preview** with **no** mapping step and **no** table chooser (unlike the generic PDF path) → **Import** inserts exactly the new rows + records the period; a second import of the same statement previews all-duplicate and adds zero (reused multiset-delta dedup) | FIBR-0008 INV-7 (OFX skips mapping); FIBR-0009 INV-7; FIBR-0007 INV-5/6/7 |
| INV-14 | PDF is **untrusted, bounded input** (reused): the SB path is subject to the same `_MAX_IMPORT_BYTES` / `_MAX_PDF_PAGES` caps, plus a whole-statement transaction cap reusing FIBR-0009's `_MAX_PDF_ROWS` (100 000); extracted text is **inert** — a cell that looks like a formula/path round-trips as literal `str` through `parse_transaction`; extraction is synchronous inline (FIBR-0009 D14), bounded, so a pathological input is refused, not left to hang the UI | FIBR-0009 INV-9/D14; security-model INV-5a/INV-5b |

## Context

FIBR-0009 (P07) reused, unchanged, the FIBR-0007 CSV pipeline and added a generic
PDF **table** extractor + the in-memory decrypt. This phase reuses **both** — the
FIBR-0007 write pipeline **and** FIBR-0009's decrypt/caps — and adds a Standard
Bank **text** reader beside the generic table extractor. It reuses, unchanged:

- **`_normalise_to_plaintext(raw, password)`** (`importers/pdf_importer.py:165`) —
  the in-memory `pikepdf` decrypt + save-strip (FIBR-0009 INV-2), exposed to the
  wizard via a thin public `PdfImporter.decrypt_to_plaintext` (D6). The SB reader
  opens the plaintext with `pdfplumber` to read **text** (`page.extract_text()`).
- **`parse_transaction(occurred_on: str, raw_amount: str | Decimal, description:
  str, exponent: int)`** + **`read_minor_unit_exponent`**
  (`services/transactions.py:35`) — the money contract. The SB reader builds a
  signed `Decimal` and calls it **exactly** as `OfxImporter._parse_statement` does
  (`ofx_importer.py:91`).
- **`ParseResult` / `RowError` / `TransactionDraft`** (`importers/base.py`,
  `models.py`) — the uniform importer output.
- **`ImportService.preview_result` (renamed from `preview_ofx`, D5) /
  `_preview_from_result` / `commit_import` / `_dedup`** — the dedup-analysis +
  atomic write tail. The wizard calls `preview_result` so the SB path reaches the
  **identical** `commit_import` as CSV/OFX.
- **The import wizard's PDF branch + password loop** (`ui/import_wizard.py`,
  `ui/password_dialog.py`) — the `pikepdf.PasswordError` catch, stored-password
  auto-try, and re-prompt are reused (refactored into `_decrypt_pdf`, D6); the SB
  branch is a **pre-check** inside the existing `_select_pdf`, not a new screen.
- **The `accounts.statement_pdf_password` column + accessors** (v5, FIBR-0009) —
  the remembered-password path is reused wholesale; **no schema change** here.

So the genuinely new code is: the `StandardBankImporter` (text reader + the pure
line-grammar helpers), the `detect_standard_bank` sniff, the `_decrypt_pdf`
refactor + the small `decrypt_to_plaintext` accessor, the wizard SB pre-check, and
the format-neutral rename of `preview_ofx` (D5). **No new runtime dependency**
(`reportlab`, used to author synthetic fixtures, is a probe/test tool, never
shipped — as in FIBR-0009) and **no migration**.

## The four layout families (empirical grammar)

All grammar below is transcribed from the user's **real** statements via
`pdfplumber.page.extract_text()` (§ Verified basis). Every worked run is a
**contiguous** set of real extracted lines that chains exactly, so a fixture built
from it reconciles.

### Family A — transactional (Current, Savings, Revolving-Credit "RCP")

Column header (may **wrap across 2–3 text lines**): `Details │ Service Fee │ Debits
│ Credits │ Date │ Balance` (Savings shows it on one line; Current and the
Revolving-Credit (RCP) statement wrap; RCP orders the middle pair `Credits /
Debits`). Transactions are **right-anchored**; a contiguous run:

```
BALANCE BROUGHT FORWARD 05 13 1,427.41          ← anchor: MM DD balance, NO amount
LOTTERY WINNINGS 12.10 05 15 1,439.51           ← +12.10   (1,439.51 − 1,427.41)
VAS00216222570                                  ← continuation → fold into prior desc
PAYSHAP PAYMENT TO 300.00- 05 16 1,139.51       ← −300.00  (1,139.51 − 1,439.51)
FEE: PAYSHAP PAYMENT ## 7.00- 05 16 1,132.51    ← −7.00    (1,132.51 − 1,139.51)
```

Overdraft (balance crosses to negative) — a contiguous run showing the sign-flip:

```
LOTTERY WINNINGS 8.40 05 18 1,140.91            ← +8.40    (1,140.91 − 1,132.51)
BILL PAYMENT 1,871.46- 05 20 730.55-            ← −1,871.46 (−730.55 − 1,140.91)
FEE BILL PAYMENT ## 1.50- 05 20 732.05-         ← −1.50    (−732.05 − −730.55)
```

Parse rule (per line, from the **right**): `balance` = last token (number, optional
trailing `-`); `DD`,`MM` = the two integer tokens before it; `amount` = the token
before **that** **iff** it is a number (optional trailing `-`) — **absent** ⇒ the
line is the balance anchor (brought-forward), not a transaction; `description` =
everything before. Sign per INV-7. RCP is identical but **European numbers**
(`239.206,04-`) and a bi-annual span crossing a year boundary (INV-9 rollover); its
fee lines carry a `*##` marker and reference continuation lines
(`"302393463 @09,250%"`, `302393463 - RCC`) — folded/stripped by INV-10.

### Family B — Home Loan

Column header (wraps): `Posting Date │ Effective Date │ [Description] │ Cash Fee │
Debit │ Credit │ Balance`. Dates are **full ISO**; the amount magnitude prints but
carries **no sign**; a contiguous run:

```
2025-03-01 BROUGHT FORWARD 58,175.07                        ← anchor: 1 date, no amount
2025-03-01 2025-03-01 Service Fee HL * 69.00 58,244.07      ← +69.00   (delta, debt up)
2025-03-03 2025-03-02 Insurance Premium 743.73 58,987.80    ← +743.73
2025-03-05 2025-03-05 Autobank Transfer 25,000.00 33,987.80 ← −25,000.00 (delta, payment)
```

Parse rule: from the **left**, take 1–2 ISO dates (`YYYY-MM-DD`); the **posting**
(first) date is `occurred_on` (D7). From the **right**, `balance` = last number,
`amount` = second-to-last number (absent on the anchor row); `description` = the
middle. **Sign is the balance delta** (INV-7) — the only family with no printed
sign to cross-check, so the magnitude cross-check `|delta| == printed amount`
(INV-7b) is the mis-parse gate.

### Family D — Money Market / investment

Column header: `Date │ Transaction description │ Withdrawals │ Deposits │ Interest
rate │ Balance`, with a `ccyy mm dd` sub-header. Dates are **full space-separated
`YYYY MM DD`**; amounts and the running balance are **`R`-prefixed** and carry a
**leading** sign; a contiguous run:

```
2026 03 26 Balance brought forward R709,297.74              ← anchor: full date, no amount
2026 03 28 Ib transfer to -R4,200.00 R705,097.74            ← −4,200.00 (withdrawal)
*****9000740 06H54 *****5235                                ← continuation → fold
2026 03 28 Ib transfer to -R500.00 R704,597.74              ← −500.00
2026 03 30 Ib transfer to -R15,000.00 R689,597.74           ← −15,000.00
2026 03 31 Credit interest R4,282.14 R693,879.88            ← +4,282.14 (interest)
Balance as at 26 June 2026 R623,457.48                      ← closing (skip; terminator)
```

Parse rule: from the **left**, `YYYY MM DD` (three integer tokens) → `occurred_on`;
from the **right**, `balance` = last money token, `amount` = the token before it
**iff** present (absent on the anchor). Money tokens are `[-]R<number>`: strip the
`R` (and the leading `-`) before number parsing (INV-8). Sign is the printed
leading sign, cross-checked against the balance delta (INV-7/7b). **Page 2's
"Interest payment details" table is not transactions** — its header is not the
transaction header and "Balance as at …" / "Interest payment details" / "VAT
Summary" terminate the region (INV-10), so its rows are never parsed as
transactions.

### Family C — credit card

Page 2 carries the transactions in **two columns per line**; header `Date
Description Amount Date Description Amount`. There is **no per-transaction running
balance** — only a brought-forward and a closing figure:

```
20 Apr 26 Balance Brought Forward 2,704.14                     ← opening (skip as txn)
Credits Credits                                                ← section header (skip)
28 Apr 26 Fund Transfers -5,000.00 20 May 26 Fund Transfers -4,504.51  ← 2 credits
11 May 26 Fund Transfers -1,400.00                             ← 1 txn (odd row)
Debit Debit                                                    ← section header (skip)
21 Apr 26 Chinacashandcarry 350.00 9 May 26 Checkers Sixty60 415.94    ← 2 debits
24 Apr 26 Patreon* Membership 5.75 95.41 ...                   ← desc keeps "5.75"; amt 95.41
Closing balance 1,983.25                                       ← closing (skip)
```

Parse rule: within the bounded table region, **split each line at each
line-position `D[D] Mon YY` date** (a 1–2-digit day, so `9 May 26` matches) to
recover 1–2 transaction segments; a segment is `date description amount`, `amount`
= the **last** numeric token (so a price inside the description, "Patreon*
Membership 5.75", is kept in the description). **Sign = the printed figure,
flipped** (D3): a purchase `350.00` → `−350.00` (spent), a credit `-5,000.00` →
`+5,000.00`. Order-independent (no balance chain). A segment whose description is
"Balance Brought Forward" / "Closing balance", a section-header line, the masked
"Account …" line, or a region line with **zero** dates (folds into the prior
segment's description) is handled by INV-10. **De-interleave risk (counter-example):**
if a *merchant description* itself contained a `D[D] Mon YY` substring, the split
would over-segment — mitigated by (a) anchoring the split only at line-leading /
post-amount date positions and (b) the genuine Family-C checksum (INV-11) failing
loudly on any mis-split rather than importing a wrong split. Checksum:
`brought_forward + Σ(printed) == closing` (INV-11).

## Design decisions

- **D1 — One text reader, family-dispatched; not one importer per statement
  type.** `StandardBankImporter.parse` detects the family
  (`detect_standard_bank`) and dispatches to one of four **pure** line-grammar
  functions (`_parse_family_a/b/c/d`) in the **same** module. Rejected: a module
  per account type (6+ files) — the families share the number-format,
  year-inference, continuation-fold, checksum, and `parse_transaction` machinery,
  so per-type files would duplicate it (coding.md § 1.3; the user's explicit "no
  per-bank-per-type importers"). A future *other* bank would be a sibling module
  implementing the same `parse(bytes) -> ParseResult` — **not built now** (YAGNI;
  the user banks only at Standard Bank).
- **D2 — Read the text layer, not the table geometry.** The reader uses
  `page.extract_text()` (stable across all six layouts) rather than
  `extract_tables()` (empirically collapses the Current account, unreadable on the
  credit card). This is the core reason the phase exists; the generic table path
  stays as the **fallback** for unrecognised layouts (INV-2).
- **D3 — Budget-view signs: printed-magnitude × balance-delta-sign for A/B/D,
  flipped-print for C.** The user chose "money leaving = negative everywhere". For
  the balance families the imported figure is the bank's **printed** magnitude
  carrying the **balance-delta** sign (INV-7) — so the number the user sees matches
  the statement, and summing the printed magnitudes is a genuine checksum (D10). For
  the credit card (no per-row balance) it is `−(printed)`, because the card prints a
  purchase positive ("you owe more"); flipping yields spent-money-negative. The
  **loan caveat** (a loan fee reads positive under the balance-delta sign) is
  documented (INV-7) and a loan-sign toggle deferred, not silently resolved (global
  rule § 8 — surfaced to the user at design time; § Out of scope).
- **D4 — Detection is conservative: legal marker AND a family header signature,
  matched over the whole concatenated text.** `detect_standard_bank(full_text)`
  requires the SB legal string ("Standard Bank of South Africa", present in
  **every** statement's footer — the masthead is an **image**, not text, so the
  footer legal line is the reliable marker) **and** one family's column-header
  token signature, tested by **token-presence over the whole concatenated page
  text** (NOT "a single line contains all tokens" — the Family-A header wraps
  across 2–3 lines, so a per-line test would never fire on the Current account, the
  very statement this feature exists to fix). Signatures (checked **most-specific
  first**): **C** = `Date Description Amount Date Description Amount` (+ "Credit
  Card"/"Titanium"); **D** = `Withdrawals` + `Deposits` + `Balance` (+
  "MoneyMarket" / "Transaction details" with the `ccyy mm dd` sub-header); **B** =
  `Posting` + `Effective` + `Debit` + `Credit` + `Balance`; **A** = `Debits` +
  `Credits` + `Date` + `Balance`. Missing the legal marker **or** every family
  signature ⇒ `None` ⇒ generic fallback (INV-2) — an un-characterised SB layout is
  safer on the generic path than mis-parsed.
- **D5 — Rename `preview_ofx` → `preview_result` (format-neutral seam).**
  `preview_ofx(result, account_id)` is already a one-line delegate to
  `_preview_from_result` (`import_.py:145-150`). Rename it `preview_result` so
  **OFX and SB** share one wizard-facing entry for a pre-built `ParseResult`
  (coding.md § 1.3 clause 2 — the OFX call-site benefits, behaviour unchanged).
  Sites to update (verified by grep): the single **production** call-site
  (`import_wizard.py:331`), the **four** OFX-test call-sites
  (`tests/features/ofx_import/test_ofx_import.py:332,340,358,565`), the OFX-test
  docstring (`:5`), and the method's own docstring reference (`import_.py:156`).
  `_preview_from_result` stays private. The OFX import suite must stay green.
- **D6 — Decrypt once in the wizard; both paths consume the plaintext.** The
  current `_extract_pdf_tables` runs the password loop (stored auto-try / prompt /
  re-prompt / store-on-success) **wrapping `candidate_tables`**, and
  `_normalise_to_plaintext` is called *inside* `candidate_tables`
  (`pdf_importer.py:139`) — so the wizard never decrypts directly. Refactor: add a
  thin public **`PdfImporter.decrypt_to_plaintext(data, password=None) -> bytes`**
  (wrapping the module `_normalise_to_plaintext`, re-raising `pikepdf.PasswordError`),
  and factor the wizard's loop into **`_decrypt_pdf(data) -> bytes | None`** that
  wraps `decrypt_to_plaintext` (same stored/prompt/re-prompt/store-on-success
  logic, unchanged). Then `_select_pdf`: `plaintext = self._decrypt_pdf(data)` (one
  password loop, one decrypt-**with-password**); `result =
  StandardBankImporter().parse(plaintext, exponent)`; **if `result is not None`** →
  `preview_result` → preview step (skip map + table chooser, like the OFX branch);
  **else** the existing generic path runs on the **same plaintext**
  (`candidate_tables(plaintext)`). Both downstream calls pass **already-plaintext**
  bytes with no password, so their internal `_normalise_to_plaintext` is a cheap
  idempotent re-save (FIBR-0009 D3 — plaintext opens + re-saves) — **no second
  password prompt, no second decrypt-with-password**. `parse` staying self-contained
  on `bytes` keeps it unit-testable on a raw encrypted fixture (INV-12).
- **D7 — `occurred_on` is the posting date (Family B); the effective date is not
  imported.** The Home Loan prints a Posting and an Effective date (usually equal
  or ±1 day). The **posting** date is always present (the anchor row has only it)
  and is the ledger date, so it is `occurred_on`; the effective date is dropped (no
  field for it; it does not affect dedup). Surfacing both is out of scope.
- **D8 — Year inference is period-anchored with a strict Dec→Jan rollover
  (Family A only).** The bare `MM DD` gets its year from the "Statement from `<D
  Month YYYY>` to `<D Month YYYY>`" line: seed the year at the start endpoint's
  year; walking transactions in statement (chronological) order, increment the year
  **only** on a `previous month == 12 and current month == 1` transition (one span
  never covers >12 months, so a single rollover suffices; a same-month reordering
  never trips it). The brought-forward anchor's `MM DD` seeds the initial "previous
  month". The period line also yields `period_start`/`period_end`; families B/C/D
  carry explicit years and need no inference (D's dates are full `YYYY MM DD`, B's
  are ISO, C's carry a 2-digit year). When the period line is absent (B has none),
  `period_start`/`period_end` default to the min/max parsed date (as the CSV path
  does for a span-less file).
- **D9 — Number format is detected once per statement and required consistent.**
  Over the statement's **money tokens** (amount/balance figures only — after
  stripping a leading `-` and any `R` prefix, and **excluding** interest-rate /
  reference tokens such as `@09,250%`, `7.050%`, `SBSARCP STD …`), the decimal
  separator is the punctuation before the final two digits (`.` ⇒ US, `,` ⇒
  European). All money tokens must agree; a statement with **both** `1,234.56` and
  `1.234,56` shapes is refused with a friendly `ValueError`. (SB always prints
  amounts to exactly 2 decimals, so "punctuation before the final two digits" is
  well-defined.) Parsing strips the thousands separator and normalises the decimal
  to `.` before `Decimal(...)`.
- **D10 — The checksum is a hard, all-or-nothing parse gate (INV-11).** After
  parsing, the reader asserts the balance identity to **exact** 2-decimal equality
  (A/B/D: `opening + Σ signed == closing`, genuine because the summands are the
  printed magnitudes; C: `brought_forward + Σ printed == closing`), and for A/B/D
  additionally the per-row `|delta| == printed magnitude` cross-check (INV-7b). A
  mismatch — or any `parse_transaction` rejection — means a row was mis-parsed (a
  continuation misread as a transaction, a dropped row, a number-format slip); the
  reader raises **one** friendly `ValueError` and imports **nothing** (no partial),
  directing the user to the generic table import or CSV/OFX (global rule § 1;
  coding.md § 2). Opening/closing come from the anchor row + last transaction's
  balance (A/B/D) or the "Balance Brought Forward"/"Closing balance" lines (C).
- **D11 — Continuation folding is bounded to the table region (INV-10).** The table
  region is `[column-header line, first terminator line)` where a terminator is any
  enumerated post-table marker ("Please verify", "The Standard Bank of South
  Africa", "VAT Summary", "Interest Calculation", "Interest payment details",
  "Balance as at", "Limit Structure", "Fee Structure", "Account Summary", "End of
  Statement", "***End of Statement***", "*## These fees", "## These fees", the
  credit-card advisory "* * * * *"). Only lines **inside** the region are parsed; a
  non-transaction line inside it folds into the preceding transaction's
  description. Footer/interest-schedule prose can therefore never fold. The region
  is recomputed per page (the header repeats).
- **D12 — Multi-page continuation via the repeated brought-forward (INV-10).**
  Transactions flow across pages; each continuation page repeats "BALANCE BROUGHT
  FORWARD" whose balance equals the prior page's closing. The reader concatenates
  pages in order and treats each brought-forward as a **balance re-anchor** (not a
  transaction), so the delta chain is seamless and no row is double-counted — a
  stronger guarantee than FIBR-0009 D8 (which stitched a repeated **header**; here
  the repeated **balance** stitches the ledger).
- **D13 — Degenerate inputs fail friendly, never crash.** A recognised-family PDF
  with **zero** transaction rows (empty statement / a page with only the anchor),
  a row whose amount is unparseable, or a description empty after folding (which
  `parse_transaction` would reject) folds into D10's single friendly `ValueError`
  (all-or-nothing); the wizard shows the message and the import is abandoned
  cleanly. A **non-SB** PDF returns `None` from `parse` (INV-2), not an error.

## Invariants (sub-invariants)

The *At a glance* table is canonical (INV-1..INV-14). Sub-invariants:

- **INV-3a** — a Family-A description containing an embedded `MM DD`-shaped number
  ("INTEREST ON OVERDRAFT UP TO 08 24") parses with that text **kept in the
  description**, because only the fixed right tail is peeled (INV-3).
- **INV-6a** — a Family-C description containing an embedded price ("Patreon*
  Membership 5.75") keeps the price in the description; the **amount** is the last
  numeric token (`95.41`) (INV-6).
- **INV-7a** — a Home-Loan row (no printed sign) gets the correct sign purely from
  the balance delta: a fee is `+`, a payment is `−` (INV-4/7).
- **INV-7b** — a balance-family row's `|delta|` equals its printed amount magnitude
  (and, where a sign prints, the signs agree); a deliberately corrupted fixture
  (printed amount ≠ delta) trips the all-or-nothing checksum `ValueError` (INV-11),
  never a silent wrong draft (INV-7).
- **INV-8a** — a European-number fixture (`1.910,76-`) parses to the same minor
  units a US-number fixture (`1,910.76-`) of the same value does (INV-8).
- **INV-9a** — a Family-A fixture whose transactions cross a Dec→Jan boundary
  assigns the correct years via the single strict rollover (INV-9).
- **INV-11a** — a fixture whose transactions do **not** reconcile to the printed
  closing balance raises the friendly checksum `ValueError` and imports nothing
  (INV-11). Balance-family variant: a corrupted **printed amount** (trips both the
  per-row cross-check and the additive sum) and a corrupted **endpoint balance**.
- **INV-13a** — `detect_standard_bank` returns `None` for a non-SB PDF (a plain
  gridded CSV-style PDF) **and** for an SB-legal-marker PDF with no recognised
  family header; the wizard then uses the generic path (INV-2).

The security invariant INV-12 (in-memory decrypt reuse, no secret logged) and the
resource-bound INV-14 each carry a headless test on a `pikepdf`-encrypted synthetic
fixture; the pure line-grammar helpers are unit-tested on **synthetic text
strings** (no PDF needed).

## Deliverables

1. **`src/finbreak/importers/standard_bank.py`** (naming.md) —
   - `StandardBankImporter.parse(pdf_bytes: bytes, exponent: int, password: str |
     None = None) -> ParseResult | None` — `_normalise_to_plaintext` (idempotent on
     already-plaintext bytes) → `pdfplumber` text per page → `detect_standard_bank`;
     **`None` when not a recognised SB statement** (INV-2); else family-dispatch →
     `parse_transaction` per row → `ParseResult`. Raises `pikepdf.PasswordError`
     (reused signal) on a bad/absent password; the all-or-nothing friendly
     `ValueError` on a checksum failure / a `parse_transaction` rejection (INV-11),
     a mixed number format (INV-8/D9), or an over-large input (INV-14).
   - `detect_standard_bank(full_text: str) -> Family | None` — the D4 sniff
     (`Family` a small `StrEnum` of `A`/`B`/`C`/`D`), token-presence over the whole
     text; unit-testable without a PDF (INV-13a).
   - Pure, PDF-free helpers (unit-tested on text), each taking `exponent` explicitly
     and each **owning** its `parse_transaction` calls + the checksum:
     `_parse_family_a/b/c/d(lines: list[str], exponent: int, …) -> ParseResult`
     (raising the INV-11 `ValueError` on non-reconciliation); `_parse_amount(token:
     str, fmt: Literal["us","eu"]) -> Decimal` (strips `R`/leading-sign/thousands);
     `_detect_number_format(money_tokens) -> Literal["us","eu"]`; `_infer_years(rows,
     period)`; `_table_region(lines) -> slice`; `_split_credit_card_line(line) ->
     list[str]`; `_verify_checksum(...)`.
   - Constants: reuses FIBR-0009's `_MAX_PDF_ROWS` (100 000) as the whole-statement
     transaction cap (INV-14). Reuses `_normalise_to_plaintext`,
     `parse_transaction`, `read_minor_unit_exponent`,
     `ParseResult`/`RowError`/`TransactionDraft`. **No** disk-write token (INV-12);
     no new network import; imports only `pdfplumber`/`pikepdf`/stdlib (already
     bundled — DoD #4).
2. **`src/finbreak/importers/pdf_importer.py`** — add the thin public
   `PdfImporter.decrypt_to_plaintext(data: bytes, password: str | None = None) ->
   bytes` wrapping `_normalise_to_plaintext` (D6); `candidate_tables` unchanged.
3. **`src/finbreak/services/import_.py`** — rename `preview_ofx` → `preview_result`
   (D5); update the OFX call-site + the four OFX-test calls + the two docstrings.
   `_preview_from_result` / `commit_import` / `_dedup` unchanged.
4. **`src/finbreak/ui/import_wizard.py`** — factor the password loop into
   `_decrypt_pdf(data) -> bytes | None` (D6); in `_select_pdf`, decrypt once, try
   `StandardBankImporter().parse(plaintext, exponent)`; on a non-`None` result
   route to preview (skip map + table chooser, like OFX); on `None` run the existing
   generic `candidate_tables(plaintext)` path unchanged.
5. **`tests/features/standard_bank_pdf/{spec.md, test_standard_bank.py}`** —
   INV-1..INV-14 (+ sub-invariants), unit tests on synthetic text for the pure
   helpers, and `qtbot` wizard round-trips (INV-13). Fixtures per § Fixtures.
6. **No** `pyproject.toml` change (no new runtime dep), **no** migration, **no**
   `_selftest.py`/build change (pdfplumber/pikepdf already covered by FIBR-0009).

## Fixtures (synthetic — no real data)

**Hard rule: not one byte of the user's real statements, account numbers, or ID
number is committed.** All fixtures are **synthetic** SB-shaped PDFs generated with
`reportlab` (a probe/authoring tool, **not** a project or test dependency —
identical to FIBR-0009's `reportlab` usage), committed as small binary blobs:

- **Family A** blob (US numbers, single page) + a **European-number** Family-A blob
  (INV-8a) + a **year-rollover** Family-A blob (INV-9a).
- **Family B** (Home Loan, ISO dates, no printed sign) blob (INV-7a).
- **Family D** (Money Market, `YYYY MM DD`, `R`-prefixed signed amounts, a page-2
  interest-schedule that must be region-excluded) blob (INV-5/INV-10).
- **Family C** (credit card, two-column, sections) blob (INV-6/6a).
- A **multi-page** Family-A blob with a repeated brought-forward (D12/INV-10).
- A **checksum-fail** blob (a corrupted printed amount) and an **endpoint-balance-
  corrupted** blob (INV-11a).
- A **non-SB gridded** blob + an **SB-legal-marker, no-family-header** blob (INV-13a
  → generic fallback).
- An **encrypted** variant produced in-test by `pikepdf`-encrypting a plaintext
  blob (INV-12/INV-13 password path).

The pure line-grammar functions are additionally unit-tested on **synthetic text
strings** built inline (no PDF). Every fixture uses invented merchants, amounts,
and a fake account number (never `5239 **** **** 9556` / `1023 224 679 1`, never
the real ID).

## Out of scope (deferred)

- **A loan-sign toggle** (present a loan fee as negative). INV-7's balance-delta
  reads a loan fee positive (debt up); flipping loan accounts to a "cost =
  negative" view is a **user preference**, deferred to a follow-up once the user has
  seen real loan data and decided (global rule § 8 — surfaced, not silently chosen).
- **Non-Standard-Bank statements.** Other banks use CSV/OFX or the FIBR-0009 generic
  table path; no other bank profile is built (YAGNI).
- **Un-characterised SB layouts** (a statement type the user does not hold, or a
  future SB redesign) — falls through to the generic path (INV-2), not force-parsed.
- **The effective date** (Family B imports the posting date only, D7).
- **The Money-Market interest schedule** (page 2): the per-period interest
  breakdown is region-excluded (INV-10), not imported as transactions — the
  month-end "Credit interest" lines on page 1 already capture the interest as
  transactions.
- **Scanned / image-only PDFs (OCR)** — no text layer (inherited from FIBR-0009).

## Exit criteria (Definition of Done)

1. `./scripts/ci-local.sh` green — ruff + format-check + bandit + pip-audit +
   gitleaks + `pytest -m "not perf"`; **and `mypy` 0** (run separately — `mypy` is
   not one of `ci-local.sh`'s stages). Any bandit hit **root-caused**, not
   suppressed (global rule § 1).
2. Every INV (INV-1..INV-14 + sub-invariants) has a passing test;
   `tests/features/standard_bank_pdf/` added. The pure grammar helpers are unit-
   tested on synthetic text; the wizard round-trips use `qtbot`.
3. **All existing suites stay green** after the D5 `preview_ofx` → `preview_result`
   rename (the OFX suite especially) and the D6 wizard `_decrypt_pdf` refactor (the
   generic PDF path must still pass FIBR-0009's `pdf_import` suite unchanged).
4. **No** new runtime dependency, **no** migration, **no** `_selftest`/build change
   (the SB reader imports only `pdfplumber`/`pikepdf`/stdlib, already bundled).
5. Security: INV-12 (password never logged / never in an exception message; decrypt
   reused in memory) has a passing test; `docs/audit-allowlist.md` consulted before
   `/audit` + `/indie-review`.
6. **No real PII committed** — a reviewer can confirm every fixture is synthetic;
   the real statements live only outside the repo.
7. ROADMAP FIBR-0050 → ✅ with a resolution note; CHANGELOG `[Unreleased]` updated;
   `docs/journal/FIBR-0050.md` written; tag `FIBR-0050-complete`.

## Dependencies

- **Roadmap:** depends on **FIBR-0009** (✅ — the decrypt + PDF-extraction machinery
  and the wizard PDF branch reused) and, transitively, FIBR-0007 (the CSV/write
  pipeline) + FIBR-0008 (the OFX `ParseResult` seam mirrored, `preview_ofx`
  renamed).
- **New runtime dependency:** **none.** `pdfplumber==0.11.10` + `pikepdf==10.9.1`
  are already pinned (FIBR-0009 / ADR-0004). `reportlab` is a probe/fixture-
  authoring tool, **not** added to any dependency group.
- **Schema:** **no migration** — reuses the v5 `accounts.statement_pdf_password`.

## Verified basis

Empirically verified against the user's **real** Standard Bank statements in the
project venv before drafting (global rule § 13), password supplied by the user and
passed via an environment variable, **never** logged/written/committed:

- **Versions:** `pdfplumber 0.11.10`, `pikepdf 10.9.1` (the pinned stack).
- **All six statements are user-password-encrypted** with the same ID; the reused
  `_normalise_to_plaintext` decrypts each in memory and `pdfplumber` extracts text.
- **Family A right-anchored grammar** reproduced on Current, Savings, RCP; the
  **balance-delta** equals the printed amount magnitude on every sampled row,
  including across a balance sign-flip into overdraft (`… 1,140.91` → `BILL PAYMENT
  1,871.46- … 730.55-`, delta `−1,871.46`; `→ FEE BILL PAYMENT ## 1.50- … 732.05-`,
  delta `−1.50`).
- **Family B** (Home Loan) prints the amount magnitude with **no sign**; the balance
  delta is the only sign source (`… 58,175.07` → `Service Fee … 69.00 … 58,244.07`
  ⇒ `+69.00`; `… 58,987.80` → `Autobank Transfer 25,000.00 … 33,987.80` ⇒
  `−25,000.00`).
- **Family D** (Money Market) uses `YYYY MM DD` dates and `R`-prefixed **signed**
  money (`… R709,297.74` → `Ib transfer to -R4,200.00 R705,097.74` ⇒ `−4,200.00`;
  `Credit interest R4,282.14 …` ⇒ `+4,282.14`); page 2 is a separate "Interest
  payment details" table (region-excluded).
- **Family C** (credit card) is two-columns-per-line with `Credits`/`Debit`
  sections; credits print negative (`Fund Transfers -5,000.00`), purchases positive
  (`Chinacashandcarry 350.00`); a price can sit inside a description (`Patreon*
  Membership 5.75 95.41`, amount `95.41`); brought-forward `2,704.14` and closing
  `1,983.25` bound the checksum.
- **European numbers** confirmed on the Revolving-Credit statement (`239.206,04-`,
  `1.910,76-`), US on all others (`1,427.41`, `R709,297.74`).
- **Period line** "Statement from `<D Month YYYY>` to `<D Month YYYY>`" present on A
  and C (incl. RCP's year-crossing "19 August 2023 to 17 February 2024"); Home Loan
  and Money Market have none (their dates are explicit).
- **The masthead is an image** (no "Standard Bank" text on page 1 of Current); the
  reliable text marker is the footer "The Standard Bank of South Africa" (present on
  every statement, incl. Money Market's "The Standard Bank of South Africa (Reg. No.
  1962/000738/06)") — the basis for D4's legal-marker sniff.
- **The generic FIBR-0009 table path** was re-run on the encrypted set: Home Loan /
  Savings / RCP extract row-aligned; the **Current account collapses** into one cell
  and the **credit card** yields two mega-cells — the empirical motivation for this
  phase (§ Theme table).

## Cold-eyes loop log

_(global rule § 14. Loop 2+ runs cold, no briefing on prior findings. Cleared for
code only when a pass returns zero verified findings across all lanes.)_

| Loop | Date | Lanes | CRIT | HIGH | MED | LOW | Notes |
|------|------|-------|------|------|-----|-----|-------|
| 1 | 2026-07-05 | 3 — accuracy vs live libs+reused code · implementability/testability · cross-doc | 2 | 4 | 8 | ~10 | See notes ↓ |

**Loop 1 notes.** Two lanes independently caught a **CRITICAL**: the A/B(/D)
running-balance checksum `opening + Σ(balance-delta) == closing` is a telescoping
**tautology** (Σ of deltas ≡ closing − opening for any input), so it could never
fail — and Home Loan, having no printed sign, had no cross-check either, risking a
silently-wrong import (global rule § 1). **Fixed** by importing the **printed
magnitude** carrying the delta sign (INV-7): the additive checksum then sums printed
magnitudes (genuine), plus a per-row `|delta| == printed` cross-check (INV-7b), and
the whole reader is **all-or-nothing** (INV-11). Second CRITICAL: D4's Family-A
detection ("a line with Debits+Credits+Date+Balance") could not match the **wrapped**
Current-account header — the exact target statement — **fixed** to token-presence
over the whole concatenated text. HIGHs: the two worked examples didn't chain
arithmetically (**fixed** to contiguous exact runs); the `_select_pdf` seam was
fictional (the password loop wraps `candidate_tables`, not a standalone normalise)
and forced a double-decrypt — **fixed** by the `_decrypt_pdf` refactor (D6, decrypt
once → plaintext to both paths). MED/LOW: `parse_transaction` real signature
(`raw_amount: str | Decimal`); credit-card date-split anchoring + counter-example;
strict Dec→Jan rollover; the full `preview_ofx` rename site list; the named INV-14
transaction cap; explicit `exponent` in helper signatures; ADR-0005 reframed as a
**deliberate deviation** (it *rejects* hard-coded per-bank parsers; a PDF is not
self-describing); "RCP" expanded; number-format token exclusion + sign/`R` strip;
`D[D] Mon YY` day notation; mypy listed as a separate gate; degenerate-row and
date-less-line handling (D13); INV-1 seam named `preview_result`; the `coding.md
§ 2` gloss aligned. **Plus** the user supplied a **sixth** statement (Money Market /
investment) mid-loop → added as **Family D** (INV-5) and the "investment → use CSV"
out-of-scope line removed.
