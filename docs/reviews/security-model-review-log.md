# docs/security-model.md — review loop log

**Why this log is not in `security-model.md` itself.** `review-contract`
Phase 4d keeps the log outside a document that has never carried one and whose
governing standard does not require one — a review table is not what a security
model's readers came for, and appending one changes what the document is. Same
remedy as [`CLAUDE-md-review-log.md`](CLAUDE-md-review-log.md): a dated record
here, a one-line pointer in the document.

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-02 | 3, cold — genre pinned `standard`; packet carried executed argon2 results and windows on the AAD, the validators and ADR-0011 | 5 | 4 | 0 | n/a | **Nine verified, nine fixed; one unverified. First gate on this document.** Trigger: FIBR-0313 Q3 amended INV-3d to enumerate the AAD fields and say why `salt_len` is excluded. **All three lanes confirmed the gated edit** — INV-3d's field list matches `keywrap.slot_aad` exactly, and `salt_len` is genuinely absent. The change was sound; the document around it was not. **Three defects were found independently by all three lanes.** The enforcement clause named `/cold-eyes` and `/audit`, both deleted skills, so a conformer amending this document invoked nothing and either stalled or recorded a gate that never ran. INV-2 gave **one number for two constants the code deliberately keeps apart** — the creation pin and the validation floor — directly under a parenthetical promising the pin could be raised "without locking out existing vaults"; following it locks out every existing vault, the exact outcome the sentence promises cannot happen. And § 5 and § 6 carried **two different test schedules**, disagreeing over INV-11 and omitting INV-3b/3c/3d and INV-13 from both, though each declares itself falsifiable by test. **The worst finding came from a single lane: T11 predated FIBR-0019 and never mentioned the recovery code**, routing a forgotten master password to backup-restore and then to the destructive reset — so a user holding a working recovery code would have had their vault deleted, the precise failure FIBR-0019 exists to prevent. **A second single-lane finding sat inside the invariant being amended**: INV-2's closing sentence still asserted the Argon2id output reaches SQLCipher's raw-key pragma, below its own "Amended by FIBR-0019" clause saying that output is a KEK and the raw key a DEK — a builder following it reinstates the keying ADR-0011 exists to eliminate. Also fixed: INV-3 claimed an unqualified wipe while `derive_key`, `vault._connect` and `AESGCM` each keep a copy the code admits it cannot zero, so a test written to it asserts what the code deliberately does not do; INV-2 undercounted the loader's refusals, omitting `format_version`; and the header claimed to name the most-recent material amendment while the body carried later ones — deleted rather than updated, since a header history goes stale by construction. **INV-12's footprint list was stale three ways, one of them this session's own collateral**: it missed the migration artefacts, the `window.ini` keys, and the `*.old` sets FIBR-0313 H3 had taught `reset_vault` to remove hours earlier. **One unverified, recorded:** a lane read INV-12 as leaving the old vault's hint and throttle behind — the shell clears both on success, so only the document under-specified it. **Two lanes raised the same open question, and it was a defect in my packet, not the document**: the `crypto.py` window ended mid-docstring and omitted `validate_slot`'s body, so neither lane could confirm it calls `validate_params` — the support INV-3d's own argument rests on. Both filed it as an open question rather than a finding, which is the lanes working; the cost was a round-trip, and Phase 1b is where it was avoidable. |
| 2 | 2026-09-02 | 3, cold — identical brief, packet rebuilt from disk with the `validate_slot` window's bounds corrected | 5 | 2 | 1 | n/a | **Eight verified, eight fixed; none dismissed. Nearly half landed on text this run wrote** — 4a-min's pattern, and the reason a second loop exists. **The best finding is against the gated edit itself.** INV-3d said `validate_params` "runs before every unwrap", which states a per-route obligation as a structural guarantee: `read_sidecar_v2` hard-fails on `master` alone, so every other slot's route must call `validate_slot` itself — the defect FIBR-0313 M8 had just fixed in one such route hours earlier. `slots` is an open map by design and FIBR-0020 adds the next one, so an implementer reading the old sentence would have inherited M8's bug. The obligation is now stated in INV-3d rather than assumed. **Two more were loop 1's own repairs.** Its pointer to "§ 5's third bullet" named the INV-8 bullet, not the enumeration — routing a phase author to no list, which is the second list the pointer exists to prevent; it now cites the bullet by its own lead-in, since an ordinal goes stale the same way. And its replacement for the deleted skill names substituted live passes that do not do the job: `review-contract` asks four questions, none of which is "does this spec state how it upholds § 5", and the other two read code. That obligation is self-assessed, and the document now says so — with the scope a lane found missing, each invariant the spec's own code path touches. **Loop 1's T11 fix also inherited a pre-existing error:** A8 and T11 both tied the recovery code to vault creation, and a converted vault gets its code at conversion (D7), so the very users T11 now sends to the recovery code were told they never had one. **Three pre-existing, one of them corroborating an open item from a cold read:** T5 claimed one documented residual while the KDF floor is one-sided, so a crafted `.fbk` can force an arbitrarily large Argon2 allocation pre-login — the code half is already tracked as FIBR-0327, and an independent lane reaching it is evidence the item is real. The § 6 tool table said `gitleaks` catches "secrets staged for commit" when the gate runs `gitleaks dir .`, which reads neither the index nor history. And T14's bare `(INV-2)` resolved inside this document to the KDF pin rather than to FIBR-0198's re-mask bound. |
| 3 | 2026-09-02 | 3, cold — identical brief, packet rebuilt from disk | 3 | 2 | 0 | n/a | **Five verified, five fixed. Cap reached (3 for a standard); the run files its tail and exits — the tail is empty.** **A calm cap, not a violent one:** most of this loop's findings were pre-existing rather than repairs of the last, so the document held more defects than the cap held loops and shipping is right. **The best finding was corroborated by two lanes and is the one a reader would most rely on.** INV-6 credited `gitleaks` with enforcing "no real financial record is ever committed" — but `gitleaks` matches credential-shaped strings and a bank account number is not one. The guard that does is `tests/features/account_detect/` INV-8, which binds prose as well as fixtures, skips when no local corpus is supplied, and therefore never runs in CI. An implementer trusting the CI stage on that leg has nothing. Each leg now names its own enforcer, T6 says the same, and § 6's table gained the missing row with its blind spots stated. **Two lanes also caught my loop-2 residual naming one cost axis of the several that are unbounded**: `validate_params` does not check `time_cost` or `parallelism` in either direction, so a crafted `.fbk` can force an arbitrarily long pre-login derivation needing no memory at all — closing FIBR-0327 on `memory_kib` alone would leave it open, and the item's scope is now stated as every axis. **One lane found a clause that would have reproduced a fixed defect:** "the unwrap failure never distinguishes wrong credential from tampered slot" is true of `unwrap_dek` and false of `validate_slot`, whose distinct refusal is exactly what stops a damaged record being reported as a wrong password and charged to the throttle (FIBR-0307 finding 9) — an implementer of the next slot would have collapsed the two. **Also fixed:** INV-3d's roster of routes owing `validate_slot` named only slots not yet built, while INV-11's hint scan already unwraps one — the code keeps the obligation as of M8, and the document now says so; and swap was called out of scope "see § 4" by three clauses while § 4 listed no such exclusion, so an implementer enumerating exclusions there would have built the buffer pinning INV-4 disclaims. **Run-level: of everything verified across the three loops, almost none fell inside the span that armed the gate.** The Q3 edit was confirmed correct by every lane on every loop. This run was overwhelmingly an audit rather than a gate, and it paid for itself twice over. |
