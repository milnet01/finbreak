# CLAUDE.md — review loop log

**Why this log is not in `CLAUDE.md` itself.** That file is loaded in full by
every session on every turn, so a table appended to it forever is a permanent
context cost paid by every future session against an audit trail almost none of
them will read. `review-contract` Phase 4d requires an always-loaded config
document to keep its log in a dated record under `docs/`, with at most a
one-line pointer in the document. This is that record.

**Rows start at 1 here, and the 2026-08-17 run is deliberately absent.** That
run (ROADMAP FIBR-0275, `review-contract`, 2 loops, 22 verified findings) wrote
no log anywhere; its history survives only in commits `559d96c` and `6ef163d`.
Reconstructing rows for it now would be back-filling, which destroys the thing
a loop log is kept for. Read those two commit messages instead.

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-08-18 | 3, cold — genre pinned `standard`, scrubbed copy, packet carrying 10 verified source facts | 4 | 2 | 1 | n/a | **Seven verified, seven fixed; one dismissed.** Trigger: the user settled the doc-only push rule (ROADMAP FIBR-0244), replacing a "does this add digits?" judgement with an unconditional prose-check pair. **All three lanes independently found the same two defects**, which is the strongest signal this run produced. **The worst was the new section's own central claim.** It said "Three stages read prose, and those two commands are all three of them" — false: `tests/features/release_integrity/test_release_integrity.py:40` reads `docs/security-model.md` and asserts an `INV-13` definition plus a `SHA256SUMS` / `sign\|Ed25519` match inside the 800 characters after it, so merely *reflowing* that section turns the suite red. A conformer would have run both prescribed commands, seen green, pushed with `--no-verify` and landed a red CI — the exact failure the rewrite was written to close. That claim was also load-bearing for retiring the old `docs/specs/FIBR-0001.md` carve-out. **Second, all three lanes again:** § Pre-push's "`git push --no-verify` for that one transient case only" contradicted the new section outright, so one conformer runs the full 1m45s gate on every ROADMAP annotation and another never does. Both readings follow the document. Also fixed: the prescribed `gitleaks dir .` had been shortened from the gate's own `gitleaks dir . --no-banner --redact --config .gitleaks.toml` (the `--redact` loss is the one that bites — without it a hit prints the secret in clear, against § Build and test's own "never print the values"); § Pre-push's "before every `git push`" was unqualified; § Cutting a release's `release-linux.sh` comment listed four assets where the script uploads five, omitting `SHA256SUMS.sig` and contradicting the eight-asset completeness rule 54 lines below; and "a push that touches **only** documentation" never defined documentation [Q3]. **One finding was my own fix, caught by 4a step 3 before the loop closed** — the replacement derivation sentence claimed "seven files match, five read", where the grep I had actually written into the document returns five, and it named `tests/features/gitignore/` as existence-sensitive when that suite tests ignore *patterns* inside a fresh tmp repo holding only a copy of `.gitignore`, so deleting `docs/design.md` turns nothing red. Executing the claim is what found it; reading it would not have. The list is four suites, not five. **One dismissed:** a lane predicted the shortened `gitleaks` would red-scan the local Ed25519 signing key under `release/`; run both ways, bare and full, both return *no leaks found* — the config is auto-discovered on this machine, so only the `--redact` half of that finding survives. **Four lane open questions resolved clean:** no test in the prescribed directories is `perf`-marked (63 deselected), `--config` is auto-discovered, all four suites together cost 1.56s, and `review-contract` does document `--max-loops`. **Filed rather than fixed:** nothing binds the enumerated suite list to the tree, which is what let it go stale in the first place (**FIBR-0278**), and `docs/standards/commits.md` § 277 still lists `--no-verify` as an anti-pattern with no pointer to the route this section now makes normal (**FIBR-0279**, out of scope — editing that standard trips its own rule-14 gate). |
| 2 | 2026-08-18 | 3, cold — identical brief, packet rebuilt from disk | 3 | 3 | 0 | n/a | **Six verified, six fixed; none dismissed. One surfaced to the user, unfixed.** **Five of the six were loop 1's own collateral, and every one landed in text a fix ADDED** — 4a-min's measured pattern exactly. **All three lanes found the same two.** The worst: loop 1 defined *only documentation* as a deny-list of five directories, and `.githooks/pre-push` is under none of them — so a commit touching only the pre-push hook passed the definition word for word, took the `--no-verify` route, and skipped the one gate stage that reads it (`ci-local.sh:54` is `shellcheck "${SH_FILES[@]}" `.githooks/pre-push`). `.gitleaks.toml`, `.gitignore` and any stray `.sh` failed it the same way. **A closed list of directories cannot express "not code"; a suffix can** — the rule is now positive, every changed path ends in `.md`. Loop 1 also wrote *the five suites above* two lines after saying **four**, the fifth item in its own list being `gitleaks`, which is a tool. **Lane B disputed a fact in my own packet and was right**: loop 1's derivation recipe — *grep for a `.md` string literal or a `"docs"` path segment, 5 files match* — reproduces nothing. Measured, the loose reading returns **64** files and the strict one **4**; only the exact unstated pattern returns 5. **Deleted rather than patched** (4a-min): no grep can re-derive this list at all, because `account_detect` walks `git ls-files` and names no file. And loop 1's *the four cost less than the two they replace* was a warm run compared against a cold one — re-measured back to back, 1.20s against 1.47s, so they are 0.27s **slower**. **The one pre-existing finding is the run's best.** Lane A found § Push policy's `--follow-tags` carve-out licensing the exact command the next sentence forbids — *unless you are cutting a release* — where a release is precisely when someone reaches for it. Verifying it turned up something worse: **all 54 `<ID>-complete` phase tags are ALREADY on the remote** (`git tag -l '*-complete'` = 54; the remote carries the same 54), so *a phase tag stays local until the user says otherwise* describes an intention rather than this repository. The carve-out is gone and the factual state is now stated in one home with a pointer from § Commit conventions. **Whether to delete those tags from the remote or retire the rule is a policy decision, SURFACED to the user and deliberately unfixed** — a 4a stop condition, and it does not count against convergence. |
| 3 | 2026-08-18 | 3, cold — identical brief, packet rebuilt from disk | 3 | 2 | 1 | n/a | **Six verified, six fixed. Cap reached (3 for a standard); the run files its tail and exits.** **All three lanes independently found the same defect, and it answers the question loop 2 left open.** Loop 2's own fix asserted *nothing here ever needs the flag* — false. `~/.claude/skills/cut-release/SKILL.md` § Phase 5 on a public repo is `git push --follow-tags origin <branch>`, and `/close-phase` Step 6 offers the same command in a prompt that NAMES the `<ID>-complete` tag it is about to publish. **So the mechanism behind loop 2's 54 published phase tags is the project's own two prescribed procedures, running every time** — the rule was never enforceable, not merely unenforced. That reframes the surfaced policy question rather than answering it, and it is the second wrong version of this sentence in a day (loop 2's predecessor carved out *unless you are cutting a release*). **The best pre-existing finding is a unit error nobody had noticed**: the doc-only test said *every changed path ends in `.md`* without saying changed since WHEN. Judge it by the last commit and an ungated code commit already queued behind a ROADMAP line rides through on its coat-tails, breaching *a code change never skips the full gate* with nothing to notice. Pinned to `git diff --name-only @{u}..HEAD`, executed to confirm. **A lane re-raised the `.corpus-numbers` printf recipe, and that dissolved its own deferral**: ROADMAP FIBR-0276 was filed rather than fixed *because* amending it trips rule 14's gate with three cold lanes — which was running. Fixed inside the gate already paid for and closed: an editor rather than a command line, spacing stated as irrelevant (the guard normalises), the step named as the user's and not an agent's, and *type them onto a shell command line* added to the never-list. Also fixed: *skipped on every run before FIBR-0248* was wrong by four days — that item wired the file route on 2026-08-14 and did not create the file, so the first real scan of the tree was 2026-08-18; **wiring a source is not supplying one**. And a leftover *the list below is now five* against *Four suites* four lines up. **Surfaced, unfixed:** the § Cold-eyes `--max-loops 7` override says *for this project* while every word of its rationale is spec-only, so this run had to guess its own cap — it applied the skill's default of 3 and reached it. Marked open in the document; the user's to settle. **Cap note:** three loops, 19 verified findings, and the collateral share fell 5/6 → 3/6 as the run went on. |
