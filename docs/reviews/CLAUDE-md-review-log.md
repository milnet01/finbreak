# CLAUDE.md — review loop log

**Why this log is not in `CLAUDE.md` itself.** That file is loaded in full by
every session on every turn, so a table appended to it forever is a permanent
context cost paid by every future session against an audit trail almost none of
them will read. `review-contract` Phase 4d requires an always-loaded config
document to keep its log in a dated record under `docs/`, with at most a
one-line pointer in the document. This is that record.

**Rows start at 1 here, and the 2026-08-17 run is deliberately absent.** That
run (ROADMAP FIBR-0275, `review-contract`, 2 loops, 22 verified findings) wrote
no log anywhere; its history survives only in commits `559d96c` and `6ef163d`.
Reconstructing rows for it now would be back-filling, which destroys the thing
a loop log is kept for. Read those two commit messages instead.

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-08-18 | 3, cold — genre pinned `standard`, scrubbed copy, packet carrying 10 verified source facts | 4 | 2 | 1 | n/a | **Seven verified, seven fixed; one dismissed.** Trigger: the user settled the doc-only push rule (ROADMAP FIBR-0244), replacing a "does this add digits?" judgement with an unconditional prose-check pair. **All three lanes independently found the same two defects**, which is the strongest signal this run produced. **The worst was the new section's own central claim.** It said "Three stages read prose, and those two commands are all three of them" — false: `tests/features/release_integrity/test_release_integrity.py:40` reads `docs/security-model.md` and asserts an `INV-13` definition plus a `SHA256SUMS` / `sign\|Ed25519` match inside the 800 characters after it, so merely *reflowing* that section turns the suite red. A conformer would have run both prescribed commands, seen green, pushed with `--no-verify` and landed a red CI — the exact failure the rewrite was written to close. That claim was also load-bearing for retiring the old `docs/specs/FIBR-0001.md` carve-out. **Second, all three lanes again:** § Pre-push's "`git push --no-verify` for that one transient case only" contradicted the new section outright, so one conformer runs the full 1m45s gate on every ROADMAP annotation and another never does. Both readings follow the document. Also fixed: the prescribed `gitleaks dir .` had been shortened from the gate's own `gitleaks dir . --no-banner --redact --config .gitleaks.toml` (the `--redact` loss is the one that bites — without it a hit prints the secret in clear, against § Build and test's own "never print the values"); § Pre-push's "before every `git push`" was unqualified; § Cutting a release's `release-linux.sh` comment listed four assets where the script uploads five, omitting `SHA256SUMS.sig` and contradicting the eight-asset completeness rule 54 lines below; and "a push that touches **only** documentation" never defined documentation [Q3]. **One finding was my own fix, caught by 4a step 3 before the loop closed** — the replacement derivation sentence claimed "seven files match, five read", where the grep I had actually written into the document returns five, and it named `tests/features/gitignore/` as existence-sensitive when that suite tests ignore *patterns* inside a fresh tmp repo holding only a copy of `.gitignore`, so deleting `docs/design.md` turns nothing red. Executing the claim is what found it; reading it would not have. The list is four suites, not five. **One dismissed:** a lane predicted the shortened `gitleaks` would red-scan the local Ed25519 signing key under `release/`; run both ways, bare and full, both return *no leaks found* — the config is auto-discovered on this machine, so only the `--redact` half of that finding survives. **Four lane open questions resolved clean:** no test in the prescribed directories is `perf`-marked (63 deselected), `--config` is auto-discovered, all four suites together cost 1.56s, and `review-contract` does document `--max-loops`. **Filed rather than fixed:** nothing binds the enumerated suite list to the tree, which is what let it go stale in the first place (**FIBR-0278**), and `docs/standards/commits.md` § 277 still lists `--no-verify` as an anti-pattern with no pointer to the route this section now makes normal (**FIBR-0279**, out of scope — editing that standard trips its own rule-14 gate). |
| 2 | 2026-08-18 | 3, cold — identical brief, packet rebuilt from disk | 3 | 3 | 0 | n/a | **Six verified, six fixed; none dismissed. One surfaced to the user, unfixed.** **Five of the six were loop 1's own collateral, and every one landed in text a fix ADDED** — 4a-min's measured pattern exactly. **All three lanes found the same two.** The worst: loop 1 defined *only documentation* as a deny-list of five directories, and `.githooks/pre-push` is under none of them — so a commit touching only the pre-push hook passed the definition word for word, took the `--no-verify` route, and skipped the one gate stage that reads it (`ci-local.sh:54` is `shellcheck "${SH_FILES[@]}" `.githooks/pre-push`). `.gitleaks.toml`, `.gitignore` and any stray `.sh` failed it the same way. **A closed list of directories cannot express "not code"; a suffix can** — the rule is now positive, every changed path ends in `.md`. Loop 1 also wrote *the five suites above* two lines after saying **four**, the fifth item in its own list being `gitleaks`, which is a tool. **Lane B disputed a fact in my own packet and was right**: loop 1's derivation recipe — *grep for a `.md` string literal or a `"docs"` path segment, 5 files match* — reproduces nothing. Measured, the loose reading returns **64** files and the strict one **4**; only the exact unstated pattern returns 5. **Deleted rather than patched** (4a-min): no grep can re-derive this list at all, because `account_detect` walks `git ls-files` and names no file. And loop 1's *the four cost less than the two they replace* was a warm run compared against a cold one — re-measured back to back, 1.20s against 1.47s, so they are 0.27s **slower**. **The one pre-existing finding is the run's best.** Lane A found § Push policy's `--follow-tags` carve-out licensing the exact command the next sentence forbids — *unless you are cutting a release* — where a release is precisely when someone reaches for it. Verifying it turned up something worse: **all 54 `<ID>-complete` phase tags are ALREADY on the remote** (`git tag -l '*-complete'` = 54; the remote carries the same 54), so *a phase tag stays local until the user says otherwise* describes an intention rather than this repository. The carve-out is gone and the factual state is now stated in one home with a pointer from § Commit conventions. **Whether to delete those tags from the remote or retire the rule is a policy decision, SURFACED to the user and deliberately unfixed** — a 4a stop condition, and it does not count against convergence. |
| 3 | 2026-08-18 | 3, cold — identical brief, packet rebuilt from disk | 3 | 2 | 1 | n/a | **Six verified, six fixed. Cap reached (3 for a standard); the run files its tail and exits.** **All three lanes independently found the same defect, and it answers the question loop 2 left open.** Loop 2's own fix asserted *nothing here ever needs the flag* — false. `~/.claude/skills/cut-release/SKILL.md` § Phase 5 on a public repo is `git push --follow-tags origin <branch>`, and `/close-phase` Step 6 offers the same command in a prompt that NAMES the `<ID>-complete` tag it is about to publish. **So the mechanism behind loop 2's 54 published phase tags is the project's own two prescribed procedures, running every time** — the rule was never enforceable, not merely unenforced. That reframes the surfaced policy question rather than answering it, and it is the second wrong version of this sentence in a day (loop 2's predecessor carved out *unless you are cutting a release*). **The best pre-existing finding is a unit error nobody had noticed**: the doc-only test said *every changed path ends in `.md`* without saying changed since WHEN. Judge it by the last commit and an ungated code commit already queued behind a ROADMAP line rides through on its coat-tails, breaching *a code change never skips the full gate* with nothing to notice. Pinned to `git diff --name-only @{u}..HEAD`, executed to confirm. **A lane re-raised the `.corpus-numbers` printf recipe, and that dissolved its own deferral**: ROADMAP FIBR-0276 was filed rather than fixed *because* amending it trips rule 14's gate with three cold lanes — which was running. Fixed inside the gate already paid for and closed: an editor rather than a command line, spacing stated as irrelevant (the guard normalises), the step named as the user's and not an agent's, and *type them onto a shell command line* added to the never-list. Also fixed: *skipped on every run before FIBR-0248* was wrong by four days — that item wired the file route on 2026-08-14 and did not create the file, so the first real scan of the tree was 2026-08-18; **wiring a source is not supplying one**. And a leftover *the list below is now five* against *Four suites* four lines up. **Surfaced, unfixed:** the § Cold-eyes `--max-loops 7` override says *for this project* while every word of its rationale is spec-only, so this run had to guess its own cap — it applied the skill's default of 3 and reached it. Marked open in the document; the user's to settle. **Cap note:** three loops, 19 verified findings, and the collateral share fell 5/6 → 3/6 as the run went on. |
| 3-close | 2026-08-18 | **none — no reviewer dispatched** | — | — | — | — | Not a review loop. The user's answers to loop 2's and loop 3's two SURFACED stop conditions, applied. **The phase-tag rule is RETIRED**: phase tags are public build markers on a public repo and carry nothing private, so the ban is gone rather than enforced. The reasoning is kept in § Push policy because it is the useful part — the rule was never enforceable, `cut-release` Phase 5 and `/close-phase` Step 6 both running `--follow-tags` on every public-repo push, and three successive drafts of that paragraph each contradicted themselves trying to hold a line the tooling broke. A note now says not to reinstate the ban without changing the tooling first. `--tags` stays forbidden (global § 6). **The `--max-loops 7` override is settled to specs and plans only**; a standard or ADR keeps the skill's default of 3, which is the cap this run applied. One collateral caught by the sweep: § Commit conventions' *a release tag is NOT covered by that* pointed at the rule just retired. **No re-arm of rule 14's gate**: these are Phase 4 fixes to findings this run itself raised and deferred only because they needed a decision, so the run that surfaced them was the review. Rows 1–3 above are frozen and left as written. |
| 4 | 2026-08-19 | 3, cold — genre pinned `standard`, scrubbed copy, packet carrying 7 verified source facts + a live `roadmap_migrate` dry-run envelope | 3 | 3 | 1 | n/a | **Seven verified, seven fixed; one dismissed. First loop of a NEW run** (the 2026-08-18 run reached its cap at row 3, and 3-close applied the user's answers). Trigger: FIBR-0281, naming the roadmap DB as the source of truth in § Where state lives. **All three lanes independently found the same two defects.** The first is the one that matters: the new text warned against trusting **`items_rendered`**, and `roadmap_migrate`'s envelope has no such field — it is `roadmap_log`'s (measured: `items_rendered: 277` on a one-item annotate). So a session was told to distrust a counter the verb never returns, and the counters it *does* return — `items_unchanged: 267`, `items_orphaned: 0` — carried no warning at all, which is the exact all-clear the paragraph existed to deny. **Deleted rather than repointed** (4a-min). The second: `.claude/workflow.md` § 1 still read *"for 'where are we', read `ROADMAP.md`, not this"* with a table row to match, and § 1 is item 3 — read at every session start — so both instructions reached the same session and the two disagree on ten items' bodies. **Fixed at its home in `workflow.md`, not by adding text here.** **The most consequential is lane A's [Q3], found by one lane only**: the migrate rule had a precondition (*"when the FILE is genuinely ahead"*) whose only detector was the counters the next sentence disqualified — so a conformer seeing `items_updated: 10` on a clean tree concludes the file is ahead and applies a **non-dry** migrate, whose own `updated_items[]` is the plan to write re-parsed bodies over correct ones in a store shared by every project on this machine. The rule is now a positive test (run it only when you KNOW something other than `roadmap_log` wrote the file) and says outright that a non-dry run on a clean tree is destructive. **A contradiction inside my own new text**, found by lane C: *"never hand-edit that file"* against the migrate paragraph listing *"a hand edit"* as the state it exists to ingest — two sessions handed the same file do opposite things. Fixed by deletion. Also fixed: `source` was missing from the round-tripped field list (four field sets in the measurement, not three); *"updates items in place"* misdescribed a whole-file re-render, which is also what *"the next render overwrites you"* rests on and nothing named; and a pre-existing *"The four suites"* at :478 against **Five suites** at :391 — collateral from FIBR-0278, and a session trimming the fenced command to four turns `prose_checks` red. **One finding caught by 4a step 3, in the loop that created it**: my own fix claimed a non-dry migrate would make *"the next render emit their nested lines at column 0"*. The file renders that line at a 4-space indent while the store body reads at column 0, so the renderer re-indents and I could not verify the claim without reading it — **deleted rather than hedged**. **One dismissed, and the fault was the packet's**: lane A found `roadmap_query` has no open/status filter, resting on my fact 2, which omitted `status: all\|active\|shipped\|planned\|in-progress\|considered`. The verb has one. **Two lane open questions resolved clean** — a wrapped bold headline really does render its continuation at column 0 (ANTS-4484's worked example), and `roadmap_log` really does locate against the file while `roadmap_query` reads the store (confirmed in source by the MCP maintainer, ANTS-4485). |
| 5 | 2026-08-19 | 2, cold — identical brief, packet rebuilt from disk and its fact 2 repaired | 1 | 1 | 3 | n/a | **Five verified, five fixed; two dismissed. Lane count cut 3 → 2** because loop 4's three agreed so heavily that the third bought little; recorded here because it is a deviation from the default. **Three of the five landed on text loop 4 wrote** — 4a-min's pattern again. **The sharpest is lane D's [Q3], and it is a hazard attached to the wrong condition.** Loop 4 wrote *"a non-dry run on a **clean tree** is destructive"*; the lossiness is a property of the render → parse round trip, not of the tree, so the *sanctioned* case — an external merge, a restore from git — clobbers the same ~10 items and the document said nothing about it. A conformer doing exactly what the rule permits corrupts a machine-global store. Now scoped to every run, with `dry_run` + an item-by-item read of `updated_items[]` required before any real one. **Lane D's second is the same shape one paragraph up:** the freshness check prescribed a `roadmap_query`, and the paragraph two lines below says `roadmap_log` locates against the **file** while `roadmap_query` reads the **store** — so the prescribed check read the one source the write does not use and could not detect the divergence it warned about. It now has both halves, with a write `dry_run` named as the cheapest way to test them at once (it resolves the locator and echoes `from_status` — executed). **Both lanes independently found line 14**, *"a question the ROADMAP already answers"*, which survived the FIBR-0281 rewrite pointing at the very file item 2 was rewritten to stop sessions opening. **Lane E's [Q3] closed a route the document never named:** § Resumption flow says *"`roadmap_query` for the open items"* while item 2 enumerated only `id`/`ids` and `headline_only`, so the cheapest literal reading dumps all 277 headlines — the cost the section exists to control. `status:"active"` is now named and was executed (95 open items, filter echoed). **One pre-existing [Q1]:** item 6 still routed the audit-allowlist read at `/code-quality-review`, replaced by `review-code` on 2026-08-18 and absent from the tree — so the false-positive memory had no trigger for the skill actually in use. **Two dismissed, both on verification.** Lane D read item 2's *"open, in progress and next"* against item 3's *"active item"* as one fact with two owners; `:717` states the split outright, the DB and § 1 agree today (both FIBR-0159), and a *status* is not *which item we are on*. Lane E held that none of item 2's routes yields `Kind`, which lives in the body — but a targeted `id`/`ids` fetch returns bodies without `include_body` (ANTS-4362), so that route does yield it. **The sweep filed rather than fixed:** `docs/audit-allowlist.md` carries 7 more `/code-quality-review` occurrences including its own **entry-key format**, and `.claude/workflow.md:61` carries it in a copy of the app-workflow nine-step list — a data migration and a divergence risk, neither a sentence (**FIBR-0285**). |
| 6 | 2026-08-19 | 2, cold — identical brief, packet rebuilt from disk | 1 | 2 | 0 | n/a | **Three verified, three fixed; one dismissed. Cap reached (3 for a standard); the run files its tail and exits.** **Both lanes independently found the same [Q1], and it was loop 5's own fix**: that a write `dry_run` is *"the cheapest way to test both at once"*. The measured envelope carries no body, so it covers the file-side half only — and a session substituting one dry run for both would write without ever reading the bullet it is annotating, which is precisely the mid-bullet-note failure the same paragraph warns about two lines above. Now scoped to that half, with the read-back stated as still owed. **Both lanes also found the active-item ownership question, which loop 5 DISMISSED — the dismissal is reversed here.** It was defensible against loop 4's text and stopped being so once loop 5's own sibling edit made `.claude/workflow.md` § 1 say *"for 'where are we', query the roadmap DB, **not this**"*: the DB can carry several 🚧 at once and does not record which one is being worked, so a session taking the DB route at resumption finds `FIBR-0133` **and** `FIBR-0159` and has no rule to choose. Three lanes across two loops raised it. Both documents now state the split — the DB owns which items are in progress, § 1 owns which ONE is active and the step within it. **One pre-existing [Q2] from lane F:** the header and item 4 both put the standard read *after* the summarise-back while § Resumption flow — which the header itself says **governs** — reads it at step 2, before step 3. The ordering claim was **deleted rather than reordered**, because the nine-step list is a copy of the app-workflow skill's and editing it here alone diverges from its source. **The dismissal is the interesting one, and it was caught by 4a step 3 running the REFUTING case.** Both lanes reported that `status:"active"` cannot yield `Kind` because `Kind` lives in the body, so § Resumption flow step 2 needs an unnamed second call; I wrote that into the document, then ran a filtered call to check. The envelope returns `bodies_omitted: true` **and `"kind":"package"` as a structured field** — so the survey answers step 2 on its own and the second call is not owed. The lanes reasoned correctly about the rendered markdown and wrongly about the verb; my fix inherited the error and the run would have shipped a rule costing every future session a needless call. The document now states the true behaviour, quoting the envelope. **Cap character: OSCILLATING, not calm.** Three of this loop's four raised findings landed on text this run itself wrote (two squarely, one mixed with a pre-existing claim); only lane F's step-order finding was wholly pre-existing. That is the signal to stop rather than a reason to loop again — the same shape loops 4 and 5 showed, where each loop's largest class was the previous loop's own fixes. **Size:** 767 lines, within `review-contract`'s ~800-line design point, so the cap is not a size problem. **Tail filed, not fixed:** FIBR-0285 (the `/code-quality-review` copies in `docs/audit-allowlist.md` and `.claude/workflow.md`). |
| 7 | 2026-08-19 | 3, cold — genre pinned `standard`, `--max-loops 3` (project override), packet carrying 11 verified source facts including `ci.yml` in full; no scrubbed copy needed, this doc's log lives here | 2 | 2 | 2 | n/a | **Six verified, six fixed; none dismissed. First loop of a NEW run** (the 2026-08-18 run reached its cap at loop 3). Trigger: the user settled FIBR-0295 — adopt `ci-docker.sh` as this project's `cut-release` Phase 2b rather than configure `act` — and recording that override changes what a conformer does, so rule 14's gate is owed. **All three lanes independently found the same defect, and it is the run's most consequential.** The new section says *"**Three things it does NOT cover.** State them; do not report a full pipeline run"*, while three older passages call the same script an exact reproduction: § Build and test's heading *"Reproduce GitHub CI EXACTLY"*, the fenced `# identical to the GitHub run`, and § Module map's *"reproduce the GitHub CI run exactly"*. A conformer reading the older half runs Phase 2b green and reports a full pipeline run — precisely what the new half forbids. All three reworded to *CI's own image and CI's own two scripts*, each pointing at the exclusions. **The second Q1 was in the text this run had just written**, found by two lanes: item 1 of the uncovered list said `actions/checkout`'s pin and `persist-credentials: false` are *"caught by the gate's own `zizmor` stage, **not by this run**"* — but `zizmor .github/workflows/` IS a `ci-local.sh` stage and `ci-docker.sh` calls `ci-local.sh`, so this run does check them. Executed rather than reasoned: `zizmor .github/workflows/` exits **0** on the real tree and **14** with the pin reverted to `actions/checkout@v7`. What is actually uncovered is the step *executing*, and the section now says so — otherwise the coverage statement it mandates would itself have been false, which is the one failure the section exists to prevent. **A lane open question became the run's third Q1, and nothing else would have caught it.** The fenced block documented `./scripts/ci-docker.sh --build  # ...plus the FIBR-0003 build smoke-test`. The flag does reach `ci-local.sh` and does set `FINBREAK_BUILD_SMOKE=1` — but `ci-setup.sh` installs no container runtime, so inside the container the test hits `pytest.skip("no container runtime (podman/docker) on PATH")` (`tests/features/bundling/test_bundling.py:299`) and the smoke-test **silently does not run**. A skip that reads as coverage, in the same file that already names that failure mode for `.corpus-numbers`. The line is deleted and the host route named instead. **Both Q3s are about a rule whose breach a conformer could not see.** *"the release notes say which steps it left uncovered, in those words"* named neither artefact nor words, and *"the release notes"* has two live referents — the published GitHub release body (`release-linux.sh` passes `--notes-file`, built from the CHANGELOG section and read by end users on the download page) and the session's own report. Now pinned to the session report, with the published notes ruled out by name. And *"If `act` is ever configured on this machine this override lapses"* gave no test for *configured*; `act --version` succeeds on an unconfigured install and settles nothing. Now: `~/.config/act/actrc` exists **and** `act push -W .github/workflows/ci.yml -n </dev/null` exits 0 — measured 2026-08-19, `actrc` absent and the dry run exits **1** on `level=fatal msg=EOF`, so the override stands. **The one pre-existing finding came from a single lane and is the best of them.** `FINBREAK_CORPUS_NUMBERS` *"(comma-separated) overrides the file for a one-off run"* sits three sentences below **"Never … type them onto a shell command line"** and above the paragraph recording FIBR-0276, which was filed over exactly that. The sanctioned override reintroduced the defect the never-list removed. Fixed by naming the safe form — `FINBREAK_CORPUS_NUMBERS="$(paste -sd, .corpus-numbers)"` puts no value on the line or in `~/.bash_history` — verified against a dummy file, never against the real values. **Two lane open questions resolved clean and are therefore not in the tally:** § Build and test's `zizmor` claim is true (the mutation above), and § Doc-only pushes' `(FIBR-0001 INV-1)` citation is correct — INV-1 is *"One command, all gates, locally"*, which is what the harness stage-table check tests. **4c green:** 108 passed across the five prose suites, `gitleaks` clean, `doc_integrity` 0 findings. |
